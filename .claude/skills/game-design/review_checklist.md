# 셀프 리뷰 체크리스트

> `game-design` 절차 5 에서 위에서 아래로 돌린다. 절차 4(전파)의 폐기 어휘 grep 도 여기 §6.
> 하나라도 걸리면 고친 뒤 다시 돌린다.

목차 — 1 컨셉 락 · 2 설계 검증 기준 · 3 방치형 계약 · 4 구조 결함 · 5 SSOT 위반(수치 박힘) · 6 폐기 어휘 · 7 문서 위생 · 8 남긴 것·넘긴 것

## 1. 컨셉 락 대조

- [ ] 이 결정이 [CLAUDE.md](CLAUDE.md) 컨셉 락의 **따름정리 셋** 중 어느 것과도 부딪히지 않는가
- [ ] 부딪히면 **결정을 버렸는가** — 락을 고쳐서 통과시키지 않았는가
- [ ] 락을 정말 고쳐야 한다면 사용자에게 그 사실을 먼저 물었는가

## 2. 설계 검증 기준 — [GAME_DESIGN.md](docs/game_design/GAME_DESIGN.md) §2

다섯 항목을 하나씩 대본다. 항목의 정의는 §2 에서 읽는다 — 여기 옮겨 적지 않는다.

- [ ] 드롭의 유의미함
- [ ] 숫자로 읽히는 최적화
- [ ] 병렬 가동
- [ ] 긴 체이스
- [ ] 순환 구조

## 3. 방치형 계약 — [base_expedition_design.md](docs/game_design/base_expedition_design.md) §4

**불변 원칙이다.** 세 조항을 그 절에서 읽고 대조한다.

- [ ] 자리를 비운 동안 잃는 것이 생기지 않는가
- [ ] 오프라인 진행이 막히지 않는가
- [ ] 실시간 개입을 요구하지 않는가 (모든 사건은 리포트로)

## 4. 구조 결함 — 순서대로

- [ ] **모순** — 다른 문서의 확정과 정면으로 부딪히는 문장이 있는가
- [ ] **빈 구멍** — 이 결정이 열어 놓고 안 닫은 경우가 있는가. 닫지 못했으면 §10 에 올렸는가
- [ ] **이중 처벌** — 같은 실패에 벌이 두 번 붙는가
- [ ] **축 중복** — 새 게이지 · 수치 · 분기 없이 **기존 축으로 표현할 수 있는가** (CLAUDE.md 철학 3). 없앨 수 있으면 없앤다
- [ ] **인과가 읽히는가** — 플레이어가 원인과 결과를 눈으로 이을 수 있는가 (통제성 우선)

## 5. SSOT 위반 — 수치가 문서에 박혔나

기획서에서 숫자가 보이면 그건 값이 아니라 위반이다. 고친 문서에 대해 돌린다:

```bash
grep -nE "\b[0-9]+%|\b[0-9]+초|\b[0-9]+분|HP [0-9]+|\+[0-9]+" docs/game_design/<고친 문서>.md
```

허용(화이트리스트) — 걸려도 위반이 아닌 것:

- 날짜 · 절 번호 · 표의 행 번호
- **구조 카운트** — `7대 죄악` · `9라운드` 처럼 §9 가 확정한 구조값
- `[<파일>.csv:<key>]` 키 참조 안의 문자열
- 계승 원본 · 참고작 인용임이 그 자리에서 명시된 값

그 밖은 전부 CSV 로 내리고 문서에는 **키 이름만** 남긴다.

- [ ] grep 결과를 한 줄씩 판정했다
- [ ] 새로 필요한 수치는 **키 이름만 발행**하고 값은 `⚠제안` 으로 표시했다

## 6. 폐기 어휘 — 죽은 말이 다른 문서에 살아 있나

**절차** (목록을 외우지 말고 매번 뽑는다):

1. §9 에서 폐기 행을 뽑는다
   ```bash
   grep -n "~~\|폐기\|폐지\|→ 대체\|대체됨" docs/game_design/GAME_DESIGN.md
   ```
2. 각 행에서 **버려진 말**을 골라낸다 (취소선 안쪽 · `「…」 어휘 폐기` 의 앞말)
3. 그 말로 대상 문서를 grep 한다
   ```bash
   grep -rn "<버려진 말>" CLAUDE.md docs/game_design/ docs/client/
   ```
4. 걸린 자리를 판정한다 — **새 어휘로 교체**하거나, 남겨야 할 기록이면 취소선·"기록" 표기를 붙인다

**2026-08-27 기준 예시 — 출처 [GAME_DESIGN.md](docs/game_design/GAME_DESIGN.md) §9.** 스냅샷이므로 그대로 믿지 말고 위 절차로 다시 뽑는다.

| 버려진 말 | 대체 |
|---|---|
| 「시설」 | 「파견처」 |
| 「슬롯」(액티브 슬롯) | 「액티브」 |
| 세트효과 · 세트포인트 | 전술카드 |
| 명중 · 회피 | 레벨 차 적중 |
| 고유 패시브 | 고유 스킬 |

- [ ] 절차 1~4 를 돌렸다
- [ ] 이번에 내가 폐기한 어휘도 §9 에 **한 줄로 남겼다** (다음 사람이 grep 할 근거)

## 7. 문서 위생

- [ ] **날짜** — 고친 문서마다 꼬리의 `마지막 업데이트` 를 갱신했다 (최신이 앞)
- [ ] **절 태그** — 새로 확정·개정한 절에 상태 태그를 붙였다
- [ ] **교차 링크 양방향** — GAME_DESIGN 쪽(세부 문서 표 · `상세:` 줄)과 세부 문서 쪽(`상위:` 줄)이 둘 다 맞다
- [ ] **낡은 참조** — 이 결정을 가리키던 다른 문서의 줄이 아직 옛말을 하고 있지 않다
- [ ] **분량** — GAME_DESIGN 에 근거를 길게 쓰지 않았다. 길어진 부분은 세부 문서로 내렸다
- [ ] **계승 폴더** — `src/data/inherited/` · `src/assets/inherited/` 를 건드리지 않았다

## 8. 남긴 것 · 넘긴 것

- [ ] **§10 에 남긴 것을 보고에 명시**했다 — 이번에 못 닫은 구멍이 §10 어느 분류에 어떤 문장으로 들어갔는지
- [ ] **확정된 과제는 §10 에서 지웠다**
- [ ] 코드 영향이 있으면 [DEV_PLAN.md](docs/client/DEV_PLAN.md) §3-3 에 R 행을 올렸다
- [ ] 화면 영향은 문서를 직접 고치지 않고 `/ui` 로 넘겼다
- [ ] 마무리 질문("반영할까요?")을 쓰지 않았다

---
*마지막 업데이트: 2026-08-27 (최초 작성)*
