# pitfalls — 실행 · 판독 함정

> `qa` 1단계 브리핑에서 읽는다. **처음 밟으면 아래 표에 한 줄**, 두 번째로 밟으면 흡수(스크립트 · README 명령 · dev 주소)를 **제안**하고, 흡수되면 지운다.

## 이미 다른 곳에 적힌 것 — 가리키기만

- 헤드리스는 PowerShell 도구로만 · `--headless=new` · 덤프 0바이트 = 단정이 멈춘 것 → [src/dev/README.md](src/dev/README.md) 「개발용 URL」 아래
- CSV 가 캐시돼 바꾼 값이 안 먹는다 → 실행마다 **새 프로필 이름** · `--disk-cache-size=1` ([client/verify.md](.claude/skills/client/verify.md) §2)
- PowerShell `Out-File -Encoding utf8` 은 BOM 을 붙인다 → 파일 쓰기는 Edit 도구나 BOM 없는 UTF-8
- Playwright — 이 PC 는 anaconda python · `channel='msedge'` · **1.41 이라 `page.clock` 이 없다**(`Date.now` 스큐로 시계를 민다) · 헤드리스 기본 인자가 스크롤바를 숨긴다 · 스크립트에 한국어 리터럴을 넣지 않는다
- 시드 20 표로 결론 내기 → `sim.html` 요약이 시드 앞 절반 / 뒤 절반을 따로 낸다 (SKILL.md 기획 4단계)
- 대량 실행기를 고친 뒤 결과를 그대로 믿기 → 시드 1~20 을 캘리브레이션 표와 대조 ([src/dev/README.md](src/dev/README.md) 「대량 실행」)

## QA 에서 밟은 것

| 날짜 | 함정 | 증상 | 피하는 법 | 횟수 |
|---|---|---|---|---|
| 2026-09-15 | `?dev=battle&runs=n` 을 대량 실행으로 착각 | 리포트 목록 상한에 막히고 첫 스테이지만 돈다 | 대량 실행은 `dev/sim.html` | 1 |
| 2026-09-15 | 한 서버에 헤드리스를 여럿 동시에 붙임 | Playwright 쪽이 `ERR_CONNECTION_REFUSED` · `Failed to fetch` → 「세이브 없음」 거짓 양성 | 대량 실행(Edge)과 Playwright 는 번갈아 돌린다 · 결과에 연결 오류가 있으면 그 시나리오는 무효로 치고 단독 재실행 | 1 |
| 2026-09-15 | `?dev=` 주소로 두 탭 · 새로고침 시나리오 | dev 주소는 열 때마다 새 게임을 코드로 조립해 서로의 세이브를 덮어쓴다 — 거짓 충돌 | 세이브를 한 번 만든 뒤 두 탭 · 새로고침은 **일반 주소**(`index.html`)로 연다 | 1 |
| 2026-09-15 | 기획 3단계의 근거 재독을 건너뛰고 대량 실행 | 09-10 에 폐기된 약속(「장비 0 이면 0」)을 재고 있었다 — GAME_DESIGN §1 요약이 옛말을 인용하고 있었다(09-17 정리 — 요약이 원문보다 늦는 일은 또 생긴다) | 근거 § 와 DECISION_LOG 의 폐기를 **실행 전에** 확인 · 요약 문서의 인용만 믿지 않는다 | 1 |
| 2026-09-15 | 세이브 · 화면 앞부분만으로 판독 | 세이브는 이벤트 때만 써져 「진행 0」인지 「안 썼을 뿐」인지 못 가른다 · 화면 글 앞 300자에는 멈춤 창이 안 잡혔다 | 세이브와 **화면 글 전체**를 같이 본다 — 창 · 알림은 글 끝에 붙는다 | 1 |
| 2026-09-15 | 콘솔 404 를 결함으로 봄 | `favicon.ico` 가 없어 브라우저 첫 페이지마다 404 한 줄 — 응답 이벤트에는 안 잡힌다 | `favicon.ico` 404 는 잡음 · 다른 404 는 응답 이벤트로 주소를 확인 | 1 |

---
*마지막 업데이트: 2026-09-17*
