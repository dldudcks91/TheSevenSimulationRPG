# PLAN — 전투 준비를 「전술」 탭 하나로 모은다

> **상태: 초안 — D1~D4 가 안 닫혔다.** 닫히기 전에는 어떤 문서도 안 고친다.
> 요청(사용자) — ① 원정에서 스테이지를 누르면 **정해진 전술만 골라** 출발 ② 전투 준비는 전술 탭에서 구성 · **프리셋 제작** ③ 전술 탭이 캐릭터 선택 · 전투 높낮이 · 물약 · 전술카드를 전부 든다

---

## 1. 조사에서 나온 사실

| | |
|---|---|
| **「전투 높낮이」** | 저장소 전체에 **전투 개념으로 0건** — 새 축이다. 지금 있는 자리 축은 진형 **2랭크(전열·후열) 하드 게이트** 하나 (battle_design §3-1) |
| **「프리셋」** | `docs/game_design/` 에 **0건** — 새 축이다 |
| **물약** | 칸 4개는 서 있으나 **고르는 결정이 없다**(얻은 순서대로 자동 · battle_design §7-1). **「칸을 직접 고르나」가 GAME_DESIGN §10 에 미확정으로 등재** — 이번 작업이 그 빈칸을 닫는다 |
| **전술카드** | 이미 **파티 단위**. `tacticState(state, party)` 가 **이미 party 를 인자로 받는다** — 프리셋의 파티를 넘기면 조건 카운터가 그대로 다시 세어진다 |
| ⚠ **문서↔코드 어긋남** | tactic_card_design §2-1 은 「전술 판정은 출발 시 1회 고정」인데, `state.js:1462` 가 라운드마다 `partyUnits` → `tacticBonus` 를 재계산한다 — **도중 리롤이 도는 원정에 다음 라운드부터 먹는다.** 프리셋이 들어오면 같은 자리에서 「도는 원정은 어느 프리셋으로 싸우나」가 또 갈린다 |

---

## 2. 결정 목록 (D1~D4) — 여기가 먼저다

| # | 갈림길 | 후보 | 추천 | 근거 |
|---|---|---|---|---|
| **D1** | 「전투 높낮이」의 정체 | ① 지금의 진형(전열·후열)을 부른 이름 ② 위험도(던전 레벨) ③ 랭크를 3단으로 ④ 지형 고저(새 시스템) | **① — 이번엔 진형 이사까지만.** ③④면 별도 과제로 뗀다 | ③④는 battle_design §3-1(전열 우선 하드 게이트) 재작성 + §10 「거리 깎임」·「진형 템플릿을 늘릴지」가 같이 열린다. 화면 개편과 전투 규칙 개정을 한 덩어리로 묶으면 회귀 검증이 안 선다 |
| **D2** | 전술 칸 7의 소유자 | ① 전역 하나 ② 프리셋이 칸 on/off 를 고름 ③ 프리셋마다 칸 7개 | **① 전역.** 단 전술 탭은 **고른 프리셋의 파티로** 조건 카운터를 센다 | ⚠ **GAME_DESIGN §10 의 소멸한 과제 「전술 칸의 소유자 — 파티가 둘이 됐다」가 되살아난다** (09-07 에 「전투 파티는 원정 하나」로 소멸했는데, 프리셋이 파티를 시간차로 여럿으로 만든다). ①이면 새 축 없이 기존 축으로 표현된다(원칙 3) · ③은 리롤 비용 · 잠금 · 합산 레벨 해금이 프리셋 수만큼 곱해져 tactic_card_design §5 재작성이 따라온다 |
| **D3** | 물약 칸 규칙 | 같은 물약을 여러 칸에 넣나 · 재고보다 칸이 많으면 | 미정 — 기획 결정 | battle_design §7-1 「무엇으로 차나」가 폐기되고 「플레이어가 넣은 것」으로 바뀐다 |
| **D4** | 탭의 자리와 이름 | ① 독립 탭(10→11) ② 원정 탭의 4번째 세그먼트 ③ 연구 탭을 키운다 | **① 독립 탭**, 원정 바로 뒤 · 이름 「전술 / Tactics」 | 사용자가 「하나의 탭」이라 했고, 연구에서 전술이 빠지면 ADR-0145 의 위쪽 탭 근거가 사라져 연구는 어차피 단일 화면이 된다. ⚠ 탭 11개의 내비 폭은 `src/ui/README.md` 크기 정책으로 실측 |

---

## 3. 이사표

| 지금 있는 것 | 지금 자리 | 개편 후 |
|---|---|---|
| 캐릭터 선택 (영웅 띠) | 출정 창 위 줄 왼쪽 (§4-1) | **→ 전술 탭** |
| 진형 (템플릿 3 + 보드) | 출정 창 아래 줄 왼쪽 (§4-1) | **→ 전술 탭** |
| 전술 칸 7 | 연구 탭 위쪽 탭 (§13-2) | **→ 전술 탭** · 연구는 「연구」 하나로 줄고 위쪽 탭이 사라진다 |
| 물약 칸 4 | 제련소 · 관전에 **표시만** (§8-2 · §4-2) | **→ 전술 탭에서 편집** · 제련소 · 관전은 읽기 그대로 |
| 높낮이 | — | **D1 에 달렸다** |
| 위험도 (던전 레벨) | 출정 창 「출정 방식」 | **남는다** — 스테이지에 붙는 값이라 프리셋에 넣으면 SSOT 가 갈린다 |
| 반복 원정 · 보내기 | 출정 창 「출정 방식」 | **남는다** — 출발의 성격이지 편성이 아니다 |
| 적 구성 · 이야기 | 출정 창 오른쪽 열 | **남는다** — 스테이지가 정하는 것 |

### 개편 후 출정 창 — **2×2 격자는 유지**

```
┌─────────────────────────┬──────────────────────┐
│ 프리셋 고르개           │ 적 구성              │
│  [돌파] [버티기] [보스] │  ▣ ▣ ▣ ▣            │
├─────────────────────────┼──────────────────────┤
│ 미리보기(읽기) │ 출정   │ 이야기               │
│  ▣▣ / ▣        │ 위험도 │                      │
│  물약 ♦♦··     │ 반복   │                      │
│                │ 보내기 │                      │
└─────────────────────────┴──────────────────────┘
```

왼쪽 열만 갈아 끼운다 — ADR-0088 · 0105 가 이 격자를 세운 근거(「창 크기가 상태에 안 흔들린다」 · 2026-08-28 사용자 지시)가 그대로 산다. **미리보기는 읽기 전용이고 클릭이 없다** — 고치려면 전술 탭으로 간다.

---

## 4. 구조 쟁점 (D 와 별개로 반드시 처리)

- **R1 — 프리셋 0개면 못 나간다.** 새 게임에 기본 프리셋 1개가 서 있어야 한다
- **R2 — 프리셋 개수 상한은 CSV.** `balance.csv:battle_preset_max` 신설 (코드 하드코딩 금지 · 규칙 2)
- **R3 — 세이브 v31 → v32.** `party: [uid]` · `formation: {tpl, ranks}` 가 각각 하나씩인 것이 배열이 된다. `upgradeV31` 이 기존 값을 프리셋 1로 접는다
- **R4 — 파견 나간 영웅이 든 프리셋**으로는 출발을 거절한다 (새 오류 코드)
- **R5 — 위 §1 의 문서↔코드 어긋남**을 개편 전에 닫는다

---

## 5. 단계

### 0단계 — 기획 (`/game-design`)
D1 · D2 · D3 · R5 를 닫는다. **GAME_DESIGN §10 「물약의 남은 설계」에서 「칸을 직접 고르나」를 빼 §9 로 옮긴다.** 여기가 안 닫히면 화면이 죽은 축을 그린다.

### 1단계 — 화면 문서 (**코드보다 먼저** · 예외 없음)
- `SCREEN_DESIGN.md` — §1 화면 지도 · **새 절 「전술 탭」**(보여준다 / 결정 표) · §4-1 출정 창 축소 · §5 영웅 띠가 붙는 자리 · §13 연구 탭 축소(§13-2 삭제) · §8-2 물약 칸이 읽기 전용임을 명시
- **ADR 신규 3장** — 다음 번호는 **0184** (현재 최신 0183)
  - `0184` 전투 준비는 전술 탭 하나로 모인다
  - `0185` 원정 스테이지는 프리셋을 골라 출발한다
  - `0186` 프리셋은 편성 · 물약을 품고 전술 칸은 전역이다 *(D2 결정에 따름)*
- **「대체됨」 한 줄씩** — 0084 · 0085 · 0088 · 0105(출정 창 구성) · 0145(연구 탭 위쪽 탭) · 0148(물약 칸이 표시 전용이던 근거)
- `python docs/client/adr/_build_index.py` · `SCREEN_CHANGELOG.md` 한 줄

### 2단계 — 계약 (`/client` · **INTERFACE.md 먼저**)
```
game.presetState(state)                        → {presets, activeId, max}
game.addPreset / renamePreset / deletePreset / selectPreset
game.toggleParty(state, presetId, uid, now)    ← 시그니처 개정
game.setFormation / placeFormation             ← presetId 를 받는다
game.setPotionSlot(state, presetId, slot, potionId)   ← 신설
game.departRun(state, stageId, presetId, now)  ← 시그니처 개정
game.tacticState(state, party)                 ← 그대로 (프리셋의 파티를 넘긴다)
```
세이브 **v32** + `upgradeV31`.

### 3단계 — 구현 (`/ui`)
- `app.js:241` `TABS` 에 `'tactics'` 추가(원정 뒤) + `renderTactics`
- `app.js:1483` `departBody` 축소 — `heroStrip` · `formBox` 를 전술 탭으로 옮기고 프리셋 고르개 + 읽기 전용 미리보기로 교체
- `app.js:2691` `renderResearch` 위쪽 탭 제거
- `i18n.js` — `tc.` 접두 신설 · `rs.*` 중 전술 키 이사 · 오류는 `tc.err.<코드>` 로 결과 코드와 짝 맞춤
- 라우트 `?tab=tactics&tc=<presetId>` — **`?dev=` 뒤에 건다**(앞에 두면 `startGame()` 이 탭을 되돌린다)

### 4단계 — 검증
- 한국어 리터럴 0 · ko/en 스크린샷 · 렌더 예외 0
- **`dev/golden.json` 무변동** — 프리셋은 rng 를 안 쓰므로 난수 수열이 안 바뀌어야 한다. **이것이 이번 개편의 회귀 안전판이다**
- `dev/test.js` 단정 추가 — v31→v32 업그레이드 · 활성 프리셋 삭제 · 파견 나간 영웅이 든 프리셋의 출발 거절 · 프리셋 상한

---

*마지막 업데이트: 2026-09-21*
