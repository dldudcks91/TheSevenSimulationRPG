# PLAN — 전투 준비를 「편성」 탭 하나로 모은다

> **상태: 0 ~ 5단계 끝 [09-21] — 사용자의 화면 수정 요청을 받는 중.** 규격은 SCREEN_DESIGN **§15 · §4-1** · 계약은 INTERFACE **§2-7 · §4** 가 SSOT 다 — 아래 §3 스케치는 계획 당시의 것. §6 넷 **전부 승인** [09-21]
> 요청(사용자) — ① 원정에서 스테이지를 누르면 **정해진 편성만 골라** 출발 ② 전투 준비는 편성 탭에서 구성 · **편성 여럿을 만들어 둔다** ③ 편성 탭이 캐릭터 선택 · **전투 진형** · 물약 · 전술칸을 전부 든다 ④ 출정 창의 편성 고르개는 **고른 편성의 영웅 셋을 초상으로** 보여 준다 ⑤ 탭 이름은 **「편성」** · 탭 속 화면은 실행자가 만들고 사용자가 수정 요청한다

---

## 1. 조사에서 나온 사실

| | |
|---|---|
| **「전투 진형」** | ~~「전투 높낮이」— 저장소에 0건, 새 축인가~~ → **09-21 사용자 확정: 지금의 진형을 부른 말이다.** 새 축이 아니다 — 진형 **2랭크(전열·후열) 하드 게이트**(battle_design §3-1)를 편성 탭으로 옮기는 것뿐 |
| **「편성」이 이미 쓰이는 말** | ⚠ **원정 탭의 첫 세그먼트 이름이 「편성」이다**(SCREEN_DESIGN §4-1 — 스테이지 목록 + 출정 창). 새 탭이 「편성」이면 같은 말이 두 자리에 선다 → **D5** |
| **「편성 여럿」** | `docs/game_design/` 에 **0건** — 새 축이다. 지금 세이브는 파티 하나 · 진형 하나만 든다 |
| **물약** | 칸 4개는 서 있으나 고르는 결정이 없다(얻은 순서대로 자동 · battle_design §7-1). 게다가 코드상 **물약에 개수가 없다** — `state.potions` 는 가진 **종류 목록**이고 제작은 「이미 있음」으로 거절된다(`state.js:1121`) → **D3 이 둘 다 뒤집는다** |
| **전술 칸** | 이미 **파티 단위**. `tacticState(state, party)` 가 **이미 party 를 인자로 받는다** — 고른 편성의 파티를 넘기면 조건 카운터가 그대로 다시 세어진다. 옵션 66개 중 **48개가 파티·장비를 읽는다**(죄종 접사 21 · 스킬 태그 12 · 같은 죄종 6 · 같은 직업 6 · 죄종 가짓수 3 · 무조건 18) |
| **탭 11개** | 내비는 **세로 열**이고 폭 330px 고정 — 탭이 늘어도 **가로는 안 변한다**. 버튼 ≈39px + 간격 2px 이라 10개 ≈410px · 11개 ≈451px 이고 쓸 수 있는 세로는 ≈764px. **여유 300px 이상** (`style.css:314` · `ui/README` 크기 정책) |
| ⚠ **문서↔코드 어긋남 — 실측하니 좁다** | 코드는 **이미 나갈 때의 인원을 든다**(`state.js:1477` 이 `run.party` 를 넘기고, `heroCombat` 주석이 R92 로 그 이유를 적어 뒀다) — 편성을 바꿔도 도는 원정은 안 흔들린다. 라운드 경계에서 **지금 것으로 다시 세는 것은 장비·레벨**이고 §4-1 이 그걸 명시적으로 허용한다. 진짜로 부딪히는 것은 **도중 리롤 하나** — tactic_card_design §2-1 의 「판정은 **출발 시 1회 고정**」과 갈린다 → **R5** |

---

## 2. 결정

### 2-1. 닫힌 것 (D1~D4)

| # | 갈림길 | 결정 | 근거 |
|---|---|---|---|
| ~~**D1**~~ | 「전투 높낮이」의 정체 | **✅ ① 지금의 진형이다 [09-21 사용자]** — 이번엔 진형 이사까지만 | 새 축이 아니다. 랭크 3단·지형 고저는 battle_design §3-1(전열 우선 하드 게이트) 재작성 + §10 「사거리 축」을 같이 여는 **전투 규칙 개정**이라 별도 과제로 뗀다 — 화면 개편과 한 덩어리로 묶으면 회귀 검증이 안 선다 |
| ~~**D2**~~ | 전술 칸 7의 소유자 | **✅ ① 전역 하나 [09-21 사용자]** — 칸 해금이 **연구**로 가면서 자동으로 닫혔다(§2-3) | 연구는 계정 하나·영웅 파견도 없으므로 칸은 계정 소유다 — 「편성마다 7칸」은 성립하지 않는다. 「편성이 칸을 끈다」는 죽은 결정(안 맞는 옵션은 어차피 안 켜진다). 전역이어도 **옵션 48/66 이 파티를 읽어** 편성마다 결과가 갈린다 — 새 축 없이 기존 축으로 표현된다(원칙 3). ⚠ 09-07 에 소멸한 §10 과제 「전술 칸의 소유자」는 이것으로 다시 닫힌다 |
| ~~**D3**~~ | 물약 칸 규칙 | **✅ 중복 허용(플레이어 재량) + 물약은 개수를 가진 소모품 [09-21 사용자]** — 규칙 전문은 §2-4 | 둘이 맞물려 「최고 등급을 전 칸에」가 **재고 비용으로** 막힌다 — 새 제한 규칙이 필요 없다(원칙 3). 물약 종류가 힐링 말고도 늘어날 예정이라 조합은 재량으로 연다(사용자) |
| ~~**D4**~~ | 탭의 자리와 이름 | **✅ 독립 탭 · 원정 바로 뒤 · 이름 「편성」 [09-21 사용자]** | 셋의 역할이 갈린다 — **연구**=영구히 여는 것 / **편성**=이번에 나갈 파티 / **원정**=어디로 보내나. 전술 칸 하나만 봐도 「여는 건 연구, 굴리는 건 편성」으로 갈린다. 원정 탭 세그먼트로 넣으면 출정 창을 비우려는 이번 개편과 정반대(ADR-0088 「창 크기가 상태에 안 흔들린다」)이고, 연구 탭에 넣으면 없애려던 위쪽 탭(ADR-0145)이 남는다. 탭 11개는 위 §1 실측대로 여유가 있다 |

### 2-2. D5 — 원정 탭 첫 세그먼트의 새 이름

**✅ 「스테이지」 확정 [09-21 사용자]** — 새 탭이 「편성」이 되면 원정 세그먼트의 첫 칸과 이름이 겹치고, 그 화면이 실제로 하는 일은 **챕터를 넘기며 스테이지를 고르는 것**이라 내용과도 안 맞았다.

**가운데 칸은 안 바꾼다** — ADR-0147 로 이미 원정 상태를 들고 글자가 바뀐다(「전투 관전」 흐린 회색 / 「전투 중」 파랑 / 「전투 종료」 빨강). 사용자가 말한 「전투 관련」이 이미 서 있는 상태다.

```
세그먼트 셋   스테이지 / 전투 관전·전투 중·전투 종료 / 리포트
```

### 2-3. 같이 실린 기획 결정 A — 전술 칸 해금이 연구로 간다 [09-21 사용자]

- **방향만 확정이고 로직은 아직 안 만든다**(사용자). 「연구에 넣는다」까지가 이번 결정이다
- **2단 문** — 로스터 합산 레벨이 문턱을 넘으면 **그 연구가 열리고**, 연구를 해야 **칸이 열린다.** 레벨 압력(tactic_card_design §5 「로스터 전체를 굴린다」)은 살아 있고 그 위에 재료 문이 하나 얹힌다
- **앞 1~2칸은 그냥 열어 둘 수 있다**(사용자 · 미정) — 바닥 보장이 필요하면 여기서 준다
- **근거 — 연구가 이 게임의 핵심이 되어야 한다**(사용자). ⚠ 08-31 「파티전술은 그대로 합산 레벨로 칸을 연다」를 **대체**한다. 그때는 연구가 핵심이 아니었다
- ⚠ 이 결정으로 **D4 의 옛 근거 한 줄이 죽는다** — 「연구에서 전술이 빠지면 연구는 어차피 단일 화면이 된다」. 연구는 오히려 커질 화면이고, 그래서 편성과 섞지 않는 편이 더 맞다(§2-1 D4 근거로 교체됨)

### 2-4. 같이 실린 기획 결정 B — 물약이 개수를 가진 소모품이 된다 [09-21 사용자]

1. **물약마다 개수를 가진다** — 제작하면 는다(「이미 있음」 거절 폐기)
2. **칸 구성은 플레이어 재량** — 같은 물약을 여러 칸에 넣어도 된다
3. **마시면 그 물약이 1 줄어든다**
4. 스테이지 시작에 구성대로 채우되 **개수가 모자란 칸은 빈 채로 시작**한다 — 같은 물약이 여러 칸이면 **앞 칸부터** 채운다
5. 「**앞 칸부터 마신다**」는 그대로 · 빈 칸은 건너뛴다

- ⚠ battle_design §7-1 의 **「무엇으로 차나」(얻은 순서대로 자동)** 와 **「마신 칸이 돌아온다」** 가 폐기된다
- ⚠ **반복 원정이 물약을 말린다** — 방치형 계약(base_expedition_design §4)과 만나는 자리. **추천: 그냥 마르게 둔다**(물약은 보험이지 필수가 아니다). 「떨어지면 반복을 멈춘다」는 09-07 「추가 브레이크를 안 만든다」와 부딪힌다 — **미결로 §10 에 올린다**
- 따라오는 빈칸(미정 · §10) — 재고 상한 · 오프라인 전투(보호)에서도 닳나 · 등급별 제작 가격(지금 5종 동일 · 임시)

---

## 3. 화면 설계

### 3-1. 편성 탭 (신설) — 탭 속은 박스 하나 (§2 「박스」 규격)

```
┌ 편성 ─────────────────────────────────────────────────────────────┐
│ [편성 1] [편성 2] [편성 3]                                        │  ← 고르개 줄
├───────────────────────────────┬───────────────────────────────────┤
│ 파티                          │ 진형                              │
│  로스터 전원 띠 (§5 규격)     │  [2·1] [1·2] [3]                  │
│  ▣ ▣ ▣ ▣ ▣ ▣ ▣ ▣ ▣ ▣       │   전열  ▣ ▣                       │
│  (파티에 든 셋은 반투명)      │   후열   ▣                        │
├───────────────────────────────┴───────────────────────────────────┤
│ 물약          [칸1] [칸2] [칸3] [칸4]      가진 물약 ▸ 힐링×3 …   │
├────────────────────────────────────────────────────────────────────┤
│ 파티 전술 — 모든 편성이 같이 쓴다             [전체 굴리기 1200G] │
│  ▣ ▣ ▣ 🔒 🔒 🔒 🔒                                               │
└────────────────────────────────────────────────────────────────────┘
```

| 영역 | 보여준다 | 결정 |
|---|---|---|
| **고르개 줄** | 편성 카드가 가로로. 고른 것은 테두리 강조 | 클릭 = 이 편성을 편집한다(+ 원정의 기본 선택이 된다) |
| **파티** | 로스터 전원 띠 — **§5 공통 영웅 띠 규격 그대로** · 파티에 든 셋은 반투명 · 위칸 글씨는 「원정 중 / 대기」 | 클릭 = 넣고 뺀다 · 드래그 = 진형 칸으로 (§4-1 이 쓰던 동사 그대로) |
| **진형** | 템플릿 아이콘 셋(2·1 / 1·2 / 3) + 보드(전열·후열) — **ADR-0057 의 3칸 축 가운데 정렬 유지** | 아이콘 클릭 = 전환 · 칸 클릭 = 파티에서 빼기 · 드래그 = 자리 (ADR-0096 그대로) |
| **물약** | 칸 4개 + 가진 물약 목록(개수 함께) | 칸 클릭 = 무엇을 넣을지 고른다 · **같은 물약 중복 허용** · 개수보다 많이 넣어도 막지 않고 **부족한 칸은 런에서 빈다**(§2-4) |
| **파티 전술** | 칸 7 + 전체 굴리기 + 잠금 — **「모든 편성이 같이 쓴다」를 칸 이름 옆 잔글씨로 못박는다**(D2) · 안 열린 칸은 **연구로 연다**고 적는다(§2-3) | 잠금 토글 · 전체 굴리기 |

**왜 전술 칸이 여기 있나** — 굴린 결과가 어느 편성에서 켜지는지는 **파티를 봐야** 알 수 있다(옵션 48/66 이 파티를 읽는다). 연구 탭에 두면 「굴렸는데 뭐가 켜지는지 모르는」 화면이 된다. **여는 건 연구, 굴리는 건 편성**이 그래서 갈린다.

### 3-2. 출정 창 개편 — **2×2 격자는 유지** (ADR-0088 · 0105 의 근거가 그대로 산다)

```
┌─────────────────────────────┬──────────────────────┐
│ 편성                        │ 적 구성              │
│  [편성 1]                   │  ▣ ▣ ▣ ▣            │
│  [편성 2]     ▣  ▣  ▣      │                      │
│  [편성 3]     (고른 편성의  │                      │
│                영웅 셋)     │                      │
├─────────────────────────────┼──────────────────────┤
│ 출정 방식                   │ 이야기               │
│  위험도  « ‹ 3 › »          │                      │
│  [반복 원정]                │                      │
│  [보내기]                   │                      │
└─────────────────────────────┴──────────────────────┘
```

- **위 줄 왼쪽 = 편성 고르개** — 편성 버튼이 **세로로** 쌓이고 **그 오른쪽에 고른 편성의 영웅 셋이 초상으로** 선다(사용자 요청 ④). **읽기 전용이고 클릭이 없다** — 고치려면 편성 탭으로 간다
- **초상은 셋 자리를 늘 잡는다** — 파티가 덜 찼어도 빈 칸을 그려 **창이 편성마다 넓어졌다 좁아졌다 하지 않는다**(ADR-0098 이 적 구성에 쓴 규칙과 같다)
- **아래 줄 왼쪽 = 출정 방식 단독** — 진형이 편성 탭으로 빠져 칸이 하나가 된다. 위험도 · 반복 · 보내기는 그대로
- **오른쪽 열은 안 건드린다** — 적 구성 · 이야기 그대로
- **「어디를 갈지 먼저, 누구를 보낼지 나중」은 그대로다**(2026-08-27) — 스테이지를 눌러야 창이 뜨는 인과가 안 바뀐다. 바뀌는 것은 **창 안에서 편성을 짜던 것 → 만들어 둔 편성을 고르는 것**뿐이다

### 3-3. 연구 탭

위쪽 탭 둘(연구 · 파티전술)이 **하나로 줄고**, 대신 **전술 칸 해금**이 연구 가지에 들어간다(§2-3 · 형태는 로직 미정이라 이번엔 **자리만** 잡는다).

---

## 4. 구조 쟁점 (D 와 별개로 반드시 처리)

- ~~**R1 — 편성 0개면 못 나간다**~~ → **소멸 [1단계 설계]** — 편성은 **늘 그 개수만큼 서 있고** 만들고 지우는 동작이 없다(ADR-0192). 0개가 될 수 없다
- **R2 — 편성 개수는 CSV.** `balance.csv:party_preset_count` 신설 — **값 3**(사용자가 편성1·2·3 이라 했다) · 「상한」이 아니라 **늘 서 있는 개수**라 이름이 `_count` · 코드 하드코딩 금지(규칙 2)
- **R3 — 세이브 v31 → v32.** ① `party: [uid]` · `formation: {tpl, ranks}` 가 각각 하나씩인 것이 **편성 배열**이 된다 ② `potions: [id]`(가진 종류)가 **`{id: 개수}`** 가 된다 ③ 편성마다 **물약 칸 구성**이 붙는다. `upgradeV31` 이 기존 값을 **편성 1**로 접고, 가진 물약은 **각 1개**로 옮긴다
- **R4 — 파견 나간 영웅이 든 편성**으로는 출발을 거절한다 (새 오류 코드)
- **R5 — 보류한다 [09-21 사용자]** — 더 큰 물음(**「런 단위 스냅샷」** · §7)에 흡수됐다. 이번 개편에서는 **닫지 않고 지나간다**: 기획서 문구는 그대로 두고 ⚠ 미결만 달며, **전술 계산 경로를 안 건드린다**(`tacticState(state, party)` 를 그대로 쓰고 「어느 편성으로 나가나」만 바꾼다). 편성이 여럿이 되어도 **「도는 원정은 어느 편성으로 싸우나」는 안 갈린다** — 코드가 이미 나갈 때의 인원을 들고 있다
- **R6 — 물약 차감 자리.** 전투는 순수 시뮬레이터라 재고를 못 건드린다. **계약은 안 바꿔도 된다** — `battle.js` 의 `potion` 이벤트가 이미 `s`(물약 id)를 싣고 결과에 `used` 가 있다. 런을 정산하는 쪽(`advanceRun` · `closeRun`)이 그것을 보고 깎는다
- **R7 — 물약 제작이 반복 가능해진다.** `potionState` 의 `owned` 거절이 사라지고 개수만 는다 — 골드 소모처가 하나 열린다

---

## 5. 단계

> ⚠ **병렬 세션 경보 [09-21 실측]** — 세션 둘이 동시에 돌며 **`SCREEN_DESIGN.md` · `INTERFACE.md` · `app.js` · `i18n.js` · `style.css` · `state.js` · `test.js`** 를 고치는 중이고 **ADR 0184~0188 을 이미 썼다**(분해 · 가방 쪽 — 내용은 안 겹친다). **`docs/game_design/` 만 비어 있다.**
> - **0단계는 지금 해도 된다**
> - ~~1단계부터는 그 세션들이 커밋할 때까지 기다린다~~ → **1단계는 병렬 세션이 도는 채로 끝냈다** — 프로젝트 규칙은 「기다려라」가 아니라 **「정확한 old 문자열 매칭으로만 패치해 남의 변경을 덮어쓰지 마라」** 다. 그쪽이 고치는 절(§1 제련소 줄 · §2 · §4-2 로그 · §6 · §8-2 제련소)과 **줄 단위로 안 겹치게** 패치했고, 편집 중 파일이 두 번 바뀌었지만 매칭이 깨진 곳은 없었다. 큰 블록(§13 · §15)은 단정 붙은 스크립트로 한 번에 넣었다
> - ⚠ **2 · 3단계는 겹침이 크다** — `INTERFACE` §2-7 · §3 · §4(세이브) · `state.js` · `app.js` · `i18n.js` 를 그쪽도 고치는 중이다. 착수 전 `git status` · `git diff --stat` 으로 다시 재고 같은 방식으로 간다

### 0단계 — 기획 (`/game-design`)
고칠 곳 — **전부 `docs/game_design/` 안이라 지금 안전하다**:
- `tactic_card_design.md` §5 · §5-1 — 해금이 **2단 문**으로(§2-3). §5-1 이유 2(「못 얻어서 뒤처지지 않는다」)가 어떻게 유지되는지 다시 적는다
- `battle_design.md` §7-1 — 물약이 **개수 소모품**으로(§2-4). 「무엇으로 차나」·「마신 칸이 돌아온다」 취소선 대체
- `GAME_DESIGN.md` §9 — 결정 세 줄(편성 탭 · 전술 칸 해금이 연구로 · 물약 소모품) · §10 — 「물약의 남은 설계」에서 **「칸을 직접 고르나」를 빼고** 새 빈칸(재고 상한 · 오프라인 소모 · 반복 원정이 마를 때) 등재 · 「기능 해금 사다리」에 전술 칸 이관 반영
- **R5 는 안 닫는다** — §7 로 넘긴다. 대신 tactic_card_design §2-1 에 ⚠ 미결 한 줄을 단다

### 1단계 — 화면 문서 ✅ [09-21 끝]
- **`SCREEN_DESIGN.md`** — **§15 편성 탭 신설**(고르개 · 파티 · 진형 · 물약 · 파티 전술의 보여준다/결정 표 + 규칙 + 호출 · 오류) · §1 화면 지도(탭 11 · 편성 줄 · 원정 세그먼트 · 연구 줄) · §2 상단바(탭 세그먼트를 쓰는 탭) · §2 창 배율 · §2 유닛 툴팁이 서는 자리 · §4 세그먼트 「스테이지」 · 원정 중 편성 바꾸기(런은 나간 편성 번호를 든다) · **§4-1 출정 창**(캐릭터 선택 → 편성 고르개 + 초상 셋 · 진형 행 삭제 · 출정 방식 단독 · 이야기의 리더 · 호출 · 오류) · §4-2 아레나 물약 칸이 무엇으로 차나 · §5 띠의 두 자리 · §8-2 제련소 물약 = 재고 · §9 도감 한 줄 · §10 라우트(`?tab=party` · `&ps=n` · `?dev=tactics` · `?dev=form&party=full`) · **§13 위쪽 탭 걷음 · §13-2 삭제**(규칙째 §15 로)
- **ADR 신규 넷 — 0192 ~ 0195** (0184 ~ 0191 은 병렬 세션이 썼다) — 0192 편성 탭 · 탭 11 · 「스테이지」(D5 를 여기 합쳤다) / 0193 출정 창 / 0194 전술 칸 / 0195 물약 칸 · 제련소 재고
- **「대체됨」 칸** — 0084 · 0085 · 0088 · 0105 · 0145 · 0148 (0094 는 세그먼트의 **자리**를 정한 것이라 이름이 바뀌어도 안 대체된다)
- `_build_index.py` → **195 건** · `SCREEN_CHANGELOG.md` 한 줄 · 줄바꿈 13 파일 검사(섞임 0)
- **설계를 다듬은 셋** — ① **편성은 늘 셋이 서 있다**(만들기 · 지우기 · 이름 바꾸기 없음 — R1 소멸 · 계약이 준다) ② **고르개는 상단바**(원정 · 도감 · 제련소와 같은 자리) ③ **제련소는 칸 대신 재고**(칸이 편성마다라 제련소가 「그 칸」을 못 보인다 — 만드는 곳 = 재고 · 넣는 곳 = 편성 · 마시는 곳 = 관전)
- **물약 칸 손** — 가진 물약 클릭 = 앞 빈 칸에 · 칸 클릭 = 비우기 · 드래그 = 넣기 / 맞바꾸기(진형과 같은 「클릭은 소속 · 드래그는 자리」) · 재고보다 많이 넣어도 막지 않고 「모자람」으로 보인다
- ⚠ **잠긴 전술 칸은 지금 규칙(필요 합산 레벨)을 적는다** — 연구 로직이 없어서(R123). 그래서 **`tactic_slot.csv` 는 이번 개편에서 안 바뀐다**

### 2단계 — 계약 · 로직 ✅ [09-21 끝]
- **INTERFACE** — §2-6(물약 칸에 빈 칸 `null` · 라운드 요약 `potions`) · §2-7(편성 함수 · 신설 다섯 · `canDepart`/`departRun` 번호 · 수색 · 해고) · §3(`slotsFull` 신설 · `owned` 삭제 · `searching` 출발) · §4(스키마 v32 · v31 → v32 이관 절)
- **계약에서 다듬은 셋 — 1단계 때 보여 드린 모양과 다르다**
  1. **파티 · 진형 · 물약 칸 함수는 편성 번호를 안 받고 「고른 편성」에 작용한다** — 편성 탭은 늘 고른 편성을 펴므로 번호가 늘 같은 값이다. 옛 함수 이름 · 인자가 그대로라 부르는 곳(화면 · 단정 · 골든)이 거의 안 바뀐다. **번호를 받는 것은 출발 하나**(`departRun(state, stageId, now, no?)`) — 반복이 **도는 원정의 편성**으로 다시 나가야 해서다(고른 편성이 그새 바뀌었을 수 있다)
  2. **신설 다섯** — `presetState` · `selectPreset` · `setPotionSlot` · `swapPotionSlot` + **`partyOf`**(편성의 파티 — 옛 `state.party` 를 읽던 곳이 쓴다)
  3. **수색은 편성을 안 막는다 · 출발이 막는다(R4)** — 편성은 계획이라 편성에 든 영웅도 수색을 보낼 수 있고, 그 편성으로 나가려 하면 `searching` 으로 거절한다. 옛 규칙(파티원은 수색 못 보냄)은 파티가 하나일 때의 것이다
- **세이브 v32** — `presets[{party, formation, potionSlots}]` · `preset` · `potions{id: n}` · `run.preset`. 이관은 이미 든 것을 안 건드린다(옛 번호를 붙인 새 모양 세이브 — 단정 25곳이 그렇게 만든다)
- **물약** — 칸 채우기 · 모자람이 **같은 함수**(`fillSlots`) · 마신 양은 라운드 요약이 싣고 `advanceRun` 이 뺀다(이겼든 졌든) · 철수 · 끊긴 라운드의 물약은 안 준다 · 0 이면 키를 지운다
- **과도기 화면** — `G.party` 를 읽던 여섯 곳 → `partyOf` · 반복 · 다음 스테이지 · 다시 도전이 도는 원정의 편성 번호를 넘긴다 · 출발 거절 `searching` 플래시 · **제련소 물약 = 재고**(칸 줄 → 그림 + `×n` · 가진 뒤에도 [만들기]) · 아레나 물약 칸이 빈 칸 `null` 을 받는다
- **검증** — 작업 폴더는 다른 세션의 도감 카드 → 처치 수 개편이 도중이라(13:29 ~ `codex_card_drop_pct` 삭제 · `codexBonus` 오류) 거기서 못 쟀다. **커밋된 HEAD 두 벌**(그대로 / 내 패치만)을 풀어 대조했다:
  - `test.html` — HEAD **358/363** → 내 패치 **363/368**(신설 5 전부 PASS · 실패 5 는 둘이 같다: 툴팁 Alt 식 1 + 골든 4 — HEAD 커밋 때부터 이미 빨갛다)
  - **골든 출력 무변동** — 시작 파티 10 · 50런 지문이 한 글자도 안 다르다. 다른 것은 입력 메타 둘(`party_preset_count` 키 · `balance.csv` 해시)뿐
  - ⚠ `golden.json` 은 **다시 안 찍었다** — 지금 찍으면 다른 세션의 밸런스 · CSV 변경(무기 고정 옵션 값 · monster · skill · item_base)이 같이 굳는다. 그쪽 작업과 함께 찍어야 한다

### 3단계 — 구현 ✅ [09-21 끝]
- **편성 탭**(`renderParty`) — 상단바 고르개(편성 n · 도는 원정의 편성에 「원정 중」) · ① 파티 띠 | 진형 보드 ② 물약 칸 + 가진 물약(`×n` · 0 개는 흐리게 · 잠긴 단계는 안 선다) ③ 파티 전술(「모든 편성이 같이 쓴다」 · 카운터는 고른 편성의 파티)
- **물약 손** — 가진 물약 클릭 = 앞 빈 칸 · 칸 클릭 = 비우기 · 드래그(물약 → 칸 · 칸 → 칸 맞바꾸기) · 모자란 칸은 흐린 점선 · 다 찬 뒤 클릭은 `pt.err.slotsFull`
- **출정 창** — 위 왼쪽 = 편성 버튼 세로 + 초상 셋(읽기 전용 · 빈 자리 점선 · 리더 · 툴팁) · 아래 왼쪽 = 출정 방식 단독(제 폭 그대로) · 경고 줄은 고른 편성의 판정(빈 편성 · 수색)
- **연구 탭** — 위쪽 탭을 걷고 연구 하나 · **도움말** — 편성 섹션 신설(파티 전술 묶음 이사) · SCREEN_DESIGN §12 도 같이
- **i18n** — `nav.party` · `pt.*` 14 · `exp.seg.idle` 「스테이지」 · `exp.noParty` 문구(편성 탭으로 안내) · 지운 키 — `exp.heroes.h` · `exp.partyFull` · `exp.searching`(→ `pt.err.*`) · `rs.err.*` 셋 · `fg.err.owned` · `fg.potion.loaded/empty/slotTitle/owned`
- **라우트** — `?tab=party` · `&ps=n`(`&party=full` 과 겹치면 편성 n 을 채운다) · `?dev=tactics` → 편성 탭 · ~~`&rs=`~~ 삭제 · `src/dev/README.md` 표도 같이
- ⚠ **1단계 계획과 다른 것 하나** — 「`rs.*` 전술 키를 새 접두로 이사」는 **안 했다**: 전술 칸과 연구 노드가 같은 칸 문법(`rs-` 클래스 · `rs.slot` 등)을 같이 쓰고 있어(§13-1 「노드는 파티 전술 칸과 같은 생김새」) 가르면 한 문법이 둘로 갈린다. 오류 키만 `pt.err.*` 로 옮겼다

### 4단계 — 검증 ✅ [09-21 끝]
- 한국어 리터럴 0(app · battle · tip) · test **371/376**(실패 5 는 이 작업 전부터 — 툴팁 Alt 식 1 · 골든 4: 다른 세션의 밸런스 · CSV 변경) · 골든 출력 무변동(2단계 · HEAD 두 벌 대조)
- **Playwright 22/22** — 띠 클릭 · 물약 넣기 / 맞바꾸기 / 비우기 / 모자람 / 다 찬 뒤 거절 · 편성 2 로 넘기기 · 띠 → 진형 드래그 · 한 영웅 두 편성 · 편성 1 안 흔들림 · 출정 창 고르개 · 편성 2 로 출발 → 고르개 「원정 중」 · 페이지 예외 0
- 스크린샷(ko/en) — 편성 탭 · 출정 창(찬 편성 · 빈 편성) · 전술 전부 열림 · 연구 · 제련소 · 도움말 — 세션 scratchpad `shots/`
- ⚠ `golden.json` 은 다시 안 찍었다 — 다른 세션의 밸런스 · CSV 변경과 함께 찍어야 한다 *(09-21 늦게 다른 세션이 다시 찍었다 — 지금 남은 골든 실패는 그 뒤의 `monster.csv` 변경 하나)*

### 5단계 — 사용자 화면 수정 요청 ✅ [09-21]
- **새 편성의 기본 진형 = 일자(모두 앞)** [사용자 지시 — 「나중에 편성에서 고칠 수 있으니까」 · 셋 다 처음부터 열림 · 나중에 다시 본다] — `formation_template.csv` 의 행 순서(첫 행 = 기본값)를 일자 / 2·1 / 1·2 로 · 이미 저장된 편성은 안 건드린다 · 단정 하나 개정(기본 = 첫 행 = 일자)
- **출정 창 2×2 자리 바꿈** [사용자 지시 — 「적 이미지 - 스토리 / 편성 관련 내용 - 원정시작버튼」 · ADR-0214] — 위 줄 = 적 구성 ↔ 이야기(읽는 자리) · 아래 줄 = 편성 ↔ 출정 방식(누르는 자리) · 열 폭이 뒤집혀 왼쪽 = 초상 넉 장 · 오른쪽 = 남는 폭 · 이야기 칸은 가로만 크기 격리(`contain: inline-size`)로 바꿔 긴 글이 안 잘린다
- **출정 창 — 보내기는 칸 오른쪽 끝 · 그 왼쪽에 전술 효과 · 물약 칸(읽기 전용)** [사용자 지시 · ADR-0216] — 값은 고른 편성의 것(`tacticState` · `presetState`) · 조건 문장은 툴팁 · 못 채운 칸은 흐리게 · 모자란 물약 칸은 흐린 점선 · **새 i18n 키 없음**
- **편성 버튼 셋의 위아래를 초상 높이에 맞췄다** [사용자 지시] — 버튼 셋이 초상 한 변(`--fm-slot`)을 나눠 갖는다 · 실측으로 위아래가 초상과 같은 좌표
- **새 게임은 편성 1 에 시작 영웅 셋이 들어 있다** [사용자 지시 · ADR-0227 — 09-09 의 「빈 파티」(ADR-0047)를 뒤집었다] — `newGame` 이 로스터 순서로 채우고 진형도 같이 선다 · 편성 2 · 3 은 빈다 · **골든 · 단정 · 시뮬이 직접 채우던 자리를 걷었다**(또 부르면 `toggleParty` 가 빼기로 뒤집힌다) · 전투 결과 무변동(런 지문 단정 통과)
- **진형 보드가 관전과 같은 한 줄이 된다** [사용자 지시 — 「실제 인게임처럼 조금만 차이나게」 · ADR-0234] — 랭크는 줄이 아니라 **한 단**(`--fm-step` = 칸의 0.22배 · 관전 `--form-step` 과 같은 비율) · 가로 차례는 관전과 같은 식(`x = (i+0.5)/k` — 2·1 은 앞 · 뒤 · 앞) · 라벨은 제 단 높이 · 보드 높이 243 → 144 · 드래그 · 클릭 계약 불변
- 검증 — Playwright **24/24**(페이지 예외 0) · 한국어 리터럴 0 · 단정 **382/385**(실패 2 는 이 작업과 무관 — 툴팁 Alt 식 1 · 다른 세션의 `monster.csv` 1) · ko/en 스크린샷(긴 이야기 · 챕터보스 단독 포함)

---

## 6. ⚠ 진행 전 사용자 확인이 필요한 것

글로벌 규칙(임의 확장 금지)에 걸리는 항목들이다. **2단계를 시작하기 전에 한 번에 확인받는다.**

| # | 무엇 | 왜 확인이 필요한가 |
|---|---|---|
| ~~1~~ | ~~`tactic_slot.csv` 컬럼 교체~~ | **이번 개편에서 빠졌다** — 잠긴 칸이 지금 규칙을 적기로 해 컬럼이 안 바뀐다. 연구 설계(R123) 때 다시 묻는다 |
| 2 ⏳ | **세이브 v31 → v32** — 사용자가 뜻을 물음 → 쉬운 말로 설명하고 확인 대기 [09-21] | 파티 · 진형이 **편성 배열**로, 가진 물약이 **물약별 개수**로, 편성마다 **물약 칸 구성**이 붙는다 — 되돌릴 수 없는 마이그레이션 |
| 3 △ | **`potion.csv:start_owned` 의 뜻 변경** — 답이 「한 스테이지에 물약은 칸마다 한 번 · 마시면 총 개수에서 −1」로 왔다(개수 전제). 같은 물약 두 칸 = 두 번인지 재확인 중 [09-21] | 0/1 플래그 → **시작 개수** (컬럼 뜻 변경 · 값은 그대로 1) |
| 4 ✅ | **계약 함수 개정** — 승인 「새 함수 니가 만들어서 진행」 [09-21] | `toggleParty` · `formationState` · `setFormation` · `placeFormation` · `departRun` 이 **편성 번호**를 받는다 + 신설 넷(`presetState` · `selectPreset` · `setPotionSlot` · `swapPotionSlot`) — public API 변경 |
| 5 ✅ | **`balance.csv:party_preset_count` 신설** (값 3) — 승인 [09-21] · 2단계에서 읽는 코드와 같이 넣는다(§5 4단계 골든 메모) | 새 키 — 컬럼이 아니라 행 추가지만 CSV 스키마에 닿는다 |

---

## 7. 다음 과제 — 「런 단위 스냅샷」 [09-21 사용자 제기 · 이번 개편 뒤]

> **물음** — 원정 중에 바꾼 것이 「다음 라운드부터」 먹는 지금 규칙을, **「런이 끝난 뒤부터」** 로 통일해야 하나?

**왜 무거운가 — 방치형 계약이 걸려 있다.** 지금은 라운드 경계마다 장비·레벨을 다시 읽으므로 **보스 라운드 직전에 갈아 끼우면 이득**이다. 화면을 보고 있는 사람이 유리해져 「자리 비워도 안전」(CLAUDE.md 철학 2 · base_expedition_design §4)과 부딪힌다.

**이미 절반은 런 단위다** — 물약은 「스테이지를 시작할 때마다 찬다」(battle_design §7-1) · 파티·자리는 나갈 때 고정. **장비 · 레벨 · 전술 칸만** 라운드 단위로 남아 있다. 통일하면 규칙이 한 줄이 된다:

> **런이 시작될 때 모든 것이 굳는다. 런이 끝나면 다음 런이 지금 상태로 다시 굳힌다.**

**왜 이번 개편에 안 섞나** — 편성 개편의 회귀 안전판이 **「골든 무변동」**인데(§5 4단계), 스냅샷 개편은 **전투 입력을 바꿔 골든이 반드시 바뀐다.** 같이 하면 골든이 바뀐 이유를 못 가린다 — D1 에서 지형 고저를 떼어 낸 것과 같은 논리.

**그때 정할 것**
- **무엇을 굳히나** — 장비 · 레벨 · 스킬트리 · 전술 칸 · 도감 보너스 · 마스터리
- ⚠ **레벨업** — 경험치는 라운드마다 들어온다. 런 중에 레벨이 올라도 그 런엔 안 먹는 게 맞나
- **반복 원정에서 「런」의 경계** — 런마다 새로 굳히나, 반복 전체가 한 덩어리인가
- **예외를 둘 것인가** — 소생 · 회복처럼 즉시 먹어야 할 것이 있나
- **R5(도중 리롤)** 가 여기서 같이 닫힌다

**따라오는 것** — 골든 재촬영 · SCREEN_DESIGN §4 의 규칙 일곱 재작성 · ADR-0106 · 0111 대체 · battle_design 개정

---

## 8. 0단계 결과 · 뒤 단계로 넘긴 옛 주장 [09-21 전수 조사]

### 8-1. 0단계 — 끝났다

기획서 7 · 대장 1 — `tactic_card_design` · `battle_design` · `base_expedition_design`(**09-06 부분 개정**) · `item_design` · `skill_design` · `GAME_DESIGN`(§9 결정 3줄 · §10 네 항목 개정 + 「런 단위 스냅샷」 신설 · 문서 목록 · 아이템 요약 · 골드 소모처) · `DECISION_LOG`(§9 에서 넘친 09-16 12행) · `DEV_PLAN` §3-3(**R122 · R123 · R124**). 전부 CRLF 유지 · 단독 LF 0.

⚠ R 번호는 병렬 세션도 붙일 수 있다 — 그쪽 커밋과 겹치면 뒤에 붙는 쪽이 번호를 민다.

### 8-2. 뒤 단계에서 고칠 옛 주장 — 조사가 찾은 33건

**지금 고치지 않는 이유** — 코드가 아직 옛 규칙대로 돌아서 이 줄들은 **지금 코드의 정확한 설명**이다. 코드를 바꿀 때 같이 바꾼다. 그리고 `SCREEN_DESIGN` · `INTERFACE` · `i18n.js` 는 병렬 세션이 고치는 중이다.

**1단계 — 화면 문서**
- [ ] `SCREEN_DESIGN.md` §1 화면 지도(탭 트리의 원정 세그먼트 · 연구 탭 줄) · §4 세그먼트 이름 · **§4-1 전체**(출정 창 2×2 의 캐릭터 선택 · 진형) · §4-2 아레나 물약 칸(「얻은 순서대로 앞 칸부터」) · §5 공통 영웅 띠(출정 창 전제) · §8-2 제련소 물약 칸 · 툴팁(「스테이지마다 다시 찬다」) · §10 라우트 `?dev=tactics`(「합산 레벨로만 열려」) · **§13 전체**(진척줄 · 잠김 칸의 「필요 합산 레벨」)
- [ ] ADR — **0148**(물약 칸 표시 전용 · 「칸을 직접 고르는 화면은 없다 — 기획 미정」) · **0006**(연구 위 · 파티전술 아래) → 새 ADR 이 대체 (안 고친다 · 대체 표기만)

**2단계 — 계약 · 데이터 설명**
- [x] `INTERFACE.md` — 물약: `owned` · 세이브 주석 · 「앞 칸부터」 자동 채움 계약 [2단계] / ⏸️ 전술 칸 「로스터 합산 레벨이 칸을 연다」 두 곳 — **코드가 아직 그렇게 돈다**(연구 로직 없음) → R123 때
- ⏸️ `DEV_PLAN.md` §1 현황의 「칸은 로스터 합산 레벨로 열리고」 — 지금 코드의 정확한 설명이다 → R123 때
- [x] `src/data/README.md` — `potion.csv:start_owned`(0/1 → 시작 개수) [2단계] / ⏸️ `tactic_slot.csv:unlock_total_level` 설명(2단 문) → R123 때
- [x] CSV 설명 칸 — `balance.csv:potion_slot_max`(「이만큼 다시 찬다」) [2단계] / ⏸️ `tactic_slot.csv` 1행(「첫 칸은 새 게임 첫 화면부터 열려 있다」 — ⚠ 앞 1~2칸 무료 여부가 미정이라 **결정 뒤에** 쓴다)

**3단계 — 구현 (코드와 같이)**
- [x] `i18n.js` **화면에 보이는 문구** — ~~`fg.err.owned`~~ · ~~`fg.potion.slotTitle`~~ [2단계] · `exp.seg.idle` [3단계] / ⏸️ `rs.note` — 코드가 아직 합산 레벨로 연다 → R123 때 · 원래 적혀 있던 것:(「편성 · 지역」 → 「스테이지」) · `rs.note`(연구 탭 도움말 — 「레벨 합이 문턱을 넘을 때마다 열리고」)
- [x] 주석 — `state.js` 물약 · `battle.js` 물약 칸 [2단계] / ⏸️ 전술 칸 해금 쪽은 R123 때 — 원래 적혀 있던 것: `state.js`(물약 · 전술 칸 해금) · `battle.js`(물약 칸 · 「스테이지마다 다시 차는 것」) · `tactic.js` 모듈 머리(「칸은 로스터 합산 레벨로 하나씩 열리고」)
- [x] `src/dev/README.md`(`?dev=tactics` → 편성 탭) [3단계] / ⏸️ `src/game_logic/README.md`(「칸 해금(로스터 합산 레벨)」) — R123 때

**§7 「런 단위 스냅샷」 으로 넘긴 것**
- `INTERFACE.md` 「라운드 경계만이 전투에 끼어드는 자리다」 두 곳
- `tactic.js` 머리 주석 — 「판정은 출발 시 1회 고정이 아니라 편성이 바뀔 때마다 다시 참이 된다(**같은 뜻이다 — 전투 중엔 편성이 안 바뀐다**)」 → **이 「같은 뜻」 전제가 깨졌다.** 원정 중에도 편성을 바꾸고(ADR-0111) 리롤이 스며든다

**그대로 두는 것 — 이력**
- `state.js` v5→v6 세이브 마이그레이션 주석(08-30 시점의 설명) · `DEV_PLAN` R103 · R104 완료 행 · `INTERFACE` 재촬영 기록 — 한 번 쓰고 안 고치는 기록이다
- `monster_design.md` 「물약은 원정 파티의 칸이라 몬스터에게 안 준다」 — 여전히 맞다

---

*마지막 업데이트: 2026-09-21*
