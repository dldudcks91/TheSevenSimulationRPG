# 무기 피해 범위 전환 — 실행 계획

> **인계용 임시 문서** — 2026-09-14 기획 세션에서 작성. 실행 세션이 이 파일을 읽고 돌린다. 기획서 · DEV_PLAN 에 옮겨지고 코드가 끝나면 **지운다**.
> **결정(§1)은 바꾸지 않는다.** §2 확인 항목이 풀리기 전에는 CSV · 코드 단계로 가지 않는다(글로벌 CLAUDE.md — 스키마 변경 · 의미 변화 · 다운스트림 패치는 별도 확인).
> 결정 전 기획안(비공개 페이지): https://claude.ai/code/artifact/adabf574-6f1e-4289-b023-0bd57e278348 — D1~D8 의 선택지와 이유가 거기 있다. **결정은 아래 §1 이 최종이다**(기획안의 추천과 다른 답이 있다 — D3 · D8).

---

## 0. 실행 세션이 먼저 할 것

- [CLAUDE.md](CLAUDE.md) 작업 규칙 — 편집 직전 `ls -la --time-style=long-iso` 로 mtime 확인 · **정확한 old 문자열 매칭으로만 패치** · 커밋은 사용자가 요청할 때만
- 아래 줄 번호는 **2026-09-14 19:30 기준**이다. 다른 세션이 같은 날 여러 번 고쳤으므로 **줄 번호 말고 인용한 앵커 문구로 찾는다**
- 단계 순서 — **1 기획서(`/game-design`) → [§2 사용자 확인] → 2 이식 계약 INTERFACE(`/client`) → 3 CSV · 코드 · 검증(`/client`) → 4 화면(`/ui`)**. 2 → 3 → 4 는 앞 단계가 계약을 정해야 뒤가 선다(DEV_PLAN §7 「경계를 바꾸면 INTERFACE 먼저 · 화면을 바꾸면 SCREEN_DESIGN 먼저」)
- 기획서에는 **절대 수치를 쓰지 않는다** — 키 참조 `[balance.csv:key]` 만. 이 계획서의 숫자는 설명용 예시다

---

## 1. 사용자 결정 (2026-09-14)

| # | 사용자 답 (원문 요지) | 확정 내용 | 주의 |
|---|---|---|---|
| **D1** | 가 — 무기 기본 피해량에만 적용 · 디아블로 스타일 | **무기 기본 피해만** 최소 ~ 최대 범위로 쓴다. % 옵션(공격력 +% · vs 종족 · 원소별 % …)은 % 그대로 | 최소/최대 피해 **옵션** · 원소 피해 추가는 이번 범위 밖(D6) |
| **D2** | 타격마다 굴려야지 당연히 | **직격마다 한 번**, 최소 ~ 최대 사이 균등 굴림 | — |
| **D3** | 무기마다 아이템 레벨에 따라 고정해 놓고 거기에 공격력%가 영향 | **개체 편차 폐지.** 범위는 **아이템 레벨이 고정**하고, 고정 옵션 「공격력 +%」가 양끝에 곱한다. 같은 조건의 무기는 같은 범위다 | ⚠ 「무기마다」의 단위 — §2 ① |
| **D4** | 레벨별 데미지 구간이 너무 작아 조금 늘리면 될 듯 · 1레벨은 이 느낌 · 20레벨은 너무 적음 · 세부 수치는 나중 | **방향만 기록한다** — 레벨 1 대역은 지금 느낌 유지 · 레벨이 오를수록(레벨 20 쪽) 구간을 키운다 · **수치 작업 없음** | 「구간」이 범위의 **폭**인지 피해 **크기**인지 원문이 둘 다로 읽힌다 — 기획서에는 원문 뜻 그대로 적고 해석하지 않는다 |
| **D5** | 당연 — 몬스터는 영웅과 같은 구조 | **몬스터가 낀 무기도 같은 규칙** | 09-11 「몬스터는 영웅과 같은 모양」 재확인 |
| **D6** | 최소/최대 피해 옵션은 나중에 추가 · 지금은 이 수정에 집중 | **문서에만 적는다** — 「나중에 추가(사용자)」 | 풀 · CSV · 코드 손대지 않는다 |
| **D7** | 최소 데미지 증가 마스터리도 나중에 · 6번이랑 같이 문서에만 | **문서에만 적는다** — 「효과의 형태는 사용자가 나중에」 | 스탯 발행 · 코드 없음 |
| **D8** | 회복과 배리어도 일단은 똑같이 모두 적용 | **회복도 굴린다** | ⚠ 배리어는 구조가 달라 확인 필요 — §2 ② |

### 규칙 본문 (기획서에 옮길 내용)

```
기본 공격력 = [ 무기 최소 피해 ~ 최대 피해 사이 균등 굴림 ] × ( 1 + Σ 상시 피해 % )
최소 · 최대 = 가운데 × ( 1 ∓ 타격 범위 폭 )
가운데      = [balance.csv:weapon_atk_base] × 성장 곡선(아이템 레벨)     ← 지금 무기 공격력의 평균과 같다
타격 피해   = ( 기본 공격력 × 스킬 배율 + Σ 능력치 항 ) × 치명 배수 × ( 1 + Σ 조건부 % )   ← 그대로
```

- **개체 굴림 없음** — 같은 무기군 · 같은 아이템 레벨이면 같은 범위다. 무기끼리의 차이는 고정 옵션 「공격력 +%」 굴림과 나머지 옵션이 든다
- **평균 세기는 지금과 같다** — 가운데를 지금 무기 공격력의 평균에 두므로 밸런스 중립으로 시작한다. D4 로 구간을 키울 때는 캘리브레이션에서 본다
- **`+`강화** — 양끝에 같은 배율 · 골드 · 상한 · 실패 없음 그대로
- **그대로 가는 것** — 스킬 배율 · 능력치 항(굴리지 않는다) · 치명 · 추가 피해 배수 · 조건부 괄호 · 강타(대상 현재 체력 기준) · 반사 · 도트(비직격) · 흡혈(굴린 뒤 최종 피해 기준)
- **방어구 고유 방어력의 개체 굴림은 유지** — 이번 결정은 무기만이다
- 몬스터 · 회복도 같은 굴림을 탄다(D5 · D8)

### 참고 — 조사에서 나온 사실 (근거로 인용 가능)

- **이 결정이 대체하는 것** — 08-26 「편차는 무기 개체 굴림 — 타격당 편차 폐기」(DECISION_LOG §1) · battle_design §9-1 「무기 개체 편차」 · item_design §2 「무기 개체 편차」 행 · DEV_PLAN R2
- **이 결정이 부딪히는 원칙** — battle_design §9-4 「조작이 없는 자동전투에서 분산은 자산이 아니라 부채다」(명중·회피 폐지 근거 1) · hero_design §2 「자동전투에서 변동이 큰 것은 약점이다」. 사용자 확정으로 받아들인 것이다 — 근거 문장은 명중·회피(맞냐 안 맞냐)의 근거로 남기고, 타격 피해 굴림은 **한 라운드 수십 타격에서 평균으로 모인다**는 차이를 적는다
- **이 결정이 푸는 것** — ① 소수점 표기(지금 무기 공격력은 소수 2자리 — `item.js` 의 `r2`) ② skill_design §3-5 T2-1 「최소 데미지 증가」(09-07 확정)가 밀 하한이 없던 모순(DEV_PLAN §3-2 · R46) ③ item_design §1 「무기 옵션」 보류 이유 「전투에 타격 편차가 없어 올릴 대상이 없다」 소멸 ④ `weapon_group.csv` 설명(「창 = 최대 피해가 낮다」 · 「도끼 = 운에 휘둘린다」)이 화면에서 읽힌다 ⑤ battle_design §2-2 가 「§9-1 원칙의 첫 구멍」이라 적은 전기의 분산이 예외가 아니게 된다 — 전기는 **범위를 더 넓히는 원소**(D2 라이트닝 선례 — `docs/reference/diablo2/skills/07_sorceress.md`)
- **화면은 이미 예고돼 있다** — [ADR-0081](docs/client/adr/0081-아이템-툴팁-메인-옵션이-크고-담은-스킬은-바닥이다.md) 「범위 데미지(「물리공격력 10-20」)는 안 들어갔다 … 그 확정이 서면 이 카드에서 바뀌는 것은 공격력 행의 값 하나」
- **사용자 이력** — 08-26 「편차는 공격력 계산할 때 들어가는 거 아니야?」(→ 개체 굴림 결정) · 09-07 T2-1 「둔기 · 창 · 오브 · 십자가 · 활 사용 시 최소 데미지 증가」 확정 · 09-11 「최소/최대 피해도 일단 보류」
- **D4 예시(현행 폭을 그대로 타격 폭으로 쓸 때)** — 아이템 레벨 1: 도끼 2~4 · 둔기 3~4 · 양손검 3~4 · 창 3~4 / 레벨 20: 도끼 7~13 · 둔기 8~12 · 양손검 9~12 · 창 9~11

---

## 2. 코드 단계 전에 사용자에게 확인할 것

글로벌 규칙 대상(스키마 · 의미 변화 · public 이름 · 다운스트림)이다. **1단계(기획서)는 이것 없이 진행해도 된다** — 기획서에는 결정만 적고, 아래는 「⚠ 구현 시 확인」으로 둔다.

| # | 무엇 | 추천 | 왜 확인이 필요한가 |
|---|---|---|---|
| **①** | **범위의 단위** — 「무기마다」가 **무기군(도끼 · 창 …) × 아이템 레벨**인가, **베이스(배틀 액스 · 할버드 … 무기군당 7종) × 아이템 레벨**인가 | 무기군 × 아이템 레벨 — 베이스는 아직 이름만 있고 성격 축(A/B/C)이 미결(item_design §1 무기 베이스 「⚠ 미결」) | 기획 세션의 해석이고 사용자가 확인하지 않았다 |
| **②** | **배리어** — 지금 배리어 양은 **대상 최대 HP × %** 다(`skill_effects.js` `barrier_pct`). 무기 피해를 안 쓰므로 범위를 탈 자리가 없다 | 무기 피해를 밑수로 쓰는 것(공격 · 회복)만 굴리고 배리어는 그대로 | D8 「배리어도 똑같이」의 뜻 — 배리어 양 자체를 흔들라는 것이면 굴림 방식을 새로 정해야 한다 |
| **③** | **세이브 형태** — 옛 무기의 개체값 `watk` 을 버리고 무기군 · 아이템 레벨에서 파생한다 · `SAVE_VERSION` 25 → 26 이관 | 버린다 — 옛 무기는 같은 조건의 평균값으로 돌아간다 | 스키마 변경 · 플레이어 보유 무기의 세기가 바뀐다 |
| **④** | **CSV 칸의 뜻 이동** — `weapon_group.csv:variance_pct` 와 `balance.csv:dmg_variance_pct` 가 「드롭 때 개체 편차 폭」→「타격 범위 폭」. 이름을 유지할지 리네임할지 | 이름 유지 · 설명만 갱신(리네임은 INTERFACE · 로더 · 테스트 전부가 따라온다) | 같은 칸이 다른 뜻을 갖는다 |
| **⑤** | **전투 유닛 필드 이름** — `atk` · `matk` · `atkBase` · `matkBase` 단일값이 범위 두 값이 된다(예: `atkMin`/`atkMax`) · 툴팁 표시값(`battle.js` result 의 `atk`/`matk`)도 | 실행 세션이 INTERFACE 초안을 먼저 보여 주고 이름을 받는다 | public 필드 이름 · 이식 계약 |
| **⑥** | **전투 난수 순서** — 드롭: 무기 개체 굴림 **1회 삭제** · 직격: 피해 굴림 **1회 추가** · 회복: 1회 추가 → **골든 재촬영** | 직격 순서 「적중 → (적중 시) 피해 → 치명 → 추가 피해」 · 회복은 양 계산 직전 1회 | INTERFACE §5-2 계약 · 같은 시드가 다른 결과를 낸다 |
| **⑦** | **정수 여부** — 범위 양끝을 정수로 계산할지(표기 = 계산) · 실수로 굴리고 표기만 반올림할지 | 표기 = 계산(양끝 정수 · 반올림은 강화 배율까지 곱한 뒤 한 번 · 최소 ≥ 1 · 최대 ≥ 최소) | 레벨 1 대역에서 반올림이 평균을 흔든다(가운데 3.34 → 도끼 2~4 = 평균 3). D4 구간 확대가 흡수하는지 캘리브레이션에서 본다 |

---

## 3. 1단계 — 기획서 (`/game-design`)

형식은 [.claude/skills/game-design/doc_conventions.md](.claude/skills/game-design/doc_conventions.md) — 대체는 취소선 + 「→ **대체**」 · 절 제목 태그 · 꼬리 날짜 한 줄 · §9 한 줄(대략 300바이트). 끝나면 [review_checklist.md](.claude/skills/game-design/review_checklist.md).

### 3-1. GAME_DESIGN.md

| 자리 | 앵커 | 할 일 |
|---|---|---|
| **§9 끝** (현재 마지막 행) | `**모든 스킬은 전투 시작 때 쿨부터 돈다**` 행 뒤 | 한 줄 추가 — §9 가 28 → 29 행이라 30 행 상한 안이다(넘치면 가장 오래된 날짜째 DECISION_LOG §1 로). 안: `\| 09-14 \| **무기 피해 = 최소 ~ 최대 범위 · 타격마다 굴린다** [사용자 확정] — 범위는 아이템 레벨이 고정 · 개체 편차 폐지 · 공격력 % 가 양끝에 곱한다 · 몬스터 · 회복도 같다 · 08-26 「편차는 무기 개체 굴림」 대체 · ⚠ 구간 확대 · 최소/최대 피해 옵션 · 최소 데미지 마스터리는 §10 (battle_design.md §9-1 · item_design.md §1 · §2 · DEV_PLAN R90) \|` |
| §10 시스템 | `**전기의 분산을 몬스터에도 줄 것인가**` | 상태 칸 끝에 — 09-14 로 **모든 직격이 범위를 굴린다(몬스터 포함)** · 남은 질문은 전기의 **더 넓은 폭**을 몬스터에도 주는가 · ⚠ 「몬스터는 영웅과 같은 구조」(09-11 · 09-14 재확인)가 후보 ①(영웅만)과 부딪힌다 |
| §10 시스템 (새 행) | 위 행 근처 | `\| **최소/최대 피해 옵션 · 최소 데미지 마스터리** [신설 09-14 — 사용자가 나중에] \| 09-14 무기 피해에 하한 · 상한이 생겨 두 자리가 설 곳이 생겼다. 정할 것 — 옵션의 형태(고정 수치 / %) · 무기 통합옵션 풀 등재 · 마스터리 T2-1 이 올리는 것(하한을 범위 폭의 % / 최소 +n) · 전투 스탯 발행. **사용자가 정한다** (item_design.md §1 「무기 옵션」 보류 · skill_design.md §3-5) \|` |
| §10 시스템 | `**아이템 점수의 구간 폭**` | 끝에 — 09-14 로 무기 피해가 개체 굴림이 아니게 되어 **점수에 넣을지** 다시 본다 (item_design §6-4) |
| §10 수치 · 밸런스 | `**무기 공격력 곡선 재설정** [신설 09-14]` | 끝에 — 09-14 로 무기 피해가 최소 ~ 최대 범위가 되어 이 곡선이 **범위 양끝**을 민다 · **사용자: 레벨별 피해 구간이 작다 — 레벨 1 은 지금 느낌 유지 · 레벨 20 쪽이 너무 적다 · 세부 수치는 나중** (DEV_PLAN R90) |

### 3-2. DECISION_LOG.md §1

| 앵커 | 할 일 |
|---|---|
| `\| 08-26 \| **편차는 무기 개체 굴림** — 타격당 편차 폐기. 드롭 시 한 번 굴려 개체에 박는다 \|` | → `\| 08-26 \| ~~**편차는 무기 개체 굴림** — 타격당 편차 폐기. 드롭 시 한 번 굴려 개체에 박는다~~ — **09-14 「무기 피해 = 최소 ~ 최대 범위 · 타격마다 굴린다」로 대체**(개체 굴림 없음 · 방어구 고유값 굴림은 유지) \|` |

### 3-3. battle_design.md

| 자리 | 앵커 | 할 일 |
|---|---|---|
| §2-2 원소 | `**⚠ 전기의 분산은 §9-4 와 부딪힌다**` 단락의 `편차도 타격마다 굴리지 않고 무기 개체에 한 번 박는 것으로 정해져 있다(§9-1). 전기를 타격 단위 분산으로 두면 그 원칙에 처음으로 구멍이 난다.` | 이 두 문장을 취소선 → **09-14 로 모든 직격이 무기 피해 범위를 굴린다(§9-1) — 전기는 예외가 아니라 범위를 더 넓히는 원소다.** 아래 몬스터 쪽 질문은 남는다(GAME_DESIGN §10) |
| §8 | `**회복량은 스탯이 아니다**` 불릿의 `회복량은 \`마법 무기 공격력 × 배율 + 능력치 항\`이고` | → 회복량은 `마법 무기 피해 범위를 굴린 값 × 배율 + 능력치 항`(09-14 — 회복도 굴린다) |
| §9 머리 | `- **편차** — 타격마다 굴리지 않는다. 무기 **개체**가 드롭될 때 한 번 굴려 \`무기 공격력\`에 박힌다 (§9-1)` | 취소선 → **09-14 개정** — 무기 피해는 최소 ~ 최대 범위이고 **직격마다 굴린다** (§9-1) |
| §9 머리 코드블록 | `[1] 기본 공격력    능력치 계수 × 무기 공격력 × ( 1 + Σ 상시 피해 % )` | ⚠ 09-10 능력치 분리 전 표기라 **이미 낡았다**. 고칠 때 `[ 무기 피해 굴림 ] × ( 1 + Σ 상시 피해 % )` 로 맞추고 보고한다 |
| §9-0 코드블록 | `무기 공격력 · 공격력/HP flat 접사 · 몬스터 공격력(낀 무기)` | `무기 피해 범위(양끝) · …` 로 용어만 · 아래 `무기 공격력을 체력 구간에 맞춰 다시 정한다` 도 `무기 피해 범위를` |
| **§9-1** 제목 | `### 9-1. 기본 공격력 — 무기가 바탕값이다 [개정 2026-08-26 · 능력치 분리 2026-09-10]` | 태그에 ` · 범위 2026-09-14` |
| §9-1 식 | `기본 공격력 = 무기 공격력 × ( 1 + Σ 상시 피해 % )` | `기본 공격력 = [ 무기 최소 피해 ~ 최대 피해 굴림 ] × ( 1 + Σ 상시 피해 % )` |
| §9-1 불릿 | `- **무기 개체 편차** — 드롭 시 \`weapon_group.csv:variance_pct\` 폭으로 한 번 굴려 개체에 박는다.` 로 시작하는 불릿 | 취소선 → 새 불릿 셋 — ① **무기 피해는 최소 ~ 최대 범위다** [2026-09-14 · 사용자 확정] — 범위는 아이템 레벨이 고정하고(가운데 = `[balance.csv:weapon_atk_base]` × 성장 곡선 · 폭 = 무기군) **개체 굴림이 없다**. 무기끼리의 차이는 고정 옵션 「공격력 +%」와 나머지 옵션이 든다. 방어구 고유값의 개체 굴림은 유지 ② **직격마다 한 번 균등하게 굴린다** — 몬스터가 낀 무기 · 회복도 같다(§8-1 몬스터 = 영웅 같은 구조) · 배리어는 ⚠ 구현 시 확인(최대 HP 비율이라 무기 피해를 안 쓴다) ③ **분산 원칙과의 관계** — §9-4 「분산은 부채」는 명중·회피(0/1 게이트)의 근거로 남는다. 타격 피해 굴림은 기대값이 같고 한 라운드 수십 타격에서 평균으로 모인다 — 사용자 확정으로 받아들인다. 한 방이 영웅 체력의 큰 몫인 경우(보스 · 느린 무기)는 폭 캘리브레이션에서 본다 · ⚠ **레벨별 구간 크기는 사용자가 나중에 키운다**(GAME_DESIGN §10 「무기 공격력 곡선 재설정」) |
| §9-1 불릿 | `- 몬스터: \`monster.csv:attack\` × 등급 배율 × 전역 스케일이 곧 기본 공격력` | ⚠ 09-11 몬스터 장비 개정(§8-1 표) 이후 **이미 낡았다** — 「몬스터: 낀 무기의 피해 범위 × 몬스터 전용 전역 배율」로 맞추고 보고한다 |
| §9-4 | `1. **기대값이 같은데 분산만 늘어난다**` 항목 | 끝에 한 마디 — ⚠ 09-14 무기 피해 범위(§9-1)는 이 근거의 적용 밖이다(사용자 확정) — 0/1 이 아니라 타격마다 평균으로 모이는 분산 |
| §9-7 표 | `\| \`min_damage\` \`max_damage\` \| 9-1 무기 공격력 개체 굴림으로 흡수 \|` | → `~~9-1 무기 공격력 개체 굴림으로 흡수~~ → **9-1 무기 피해 범위**(09-14) · 옵션으로 되살리는 것은 사용자가 나중에(GAME_DESIGN §10)` |
| 꼬리 | `*마지막 업데이트:` | 날짜 |

### 3-4. item_design.md

| 자리 | 앵커 | 할 일 |
|---|---|---|
| §1 슬롯 | `ilvl 이 미는 것은 **성장 축**(무기 바탕값 · 공격력/HP flat 접사)` | `무기 바탕값` → `무기 피해 범위` |
| §1 고정 옵션 | `- **바탕값(개체 굴림)와는 다른 층이다** — 무기 공격력·방어구 고유값(§2 개체 굴림, 위 드롭 파이프라인 6단계)이 이미 존재하는 베이스이고` | 무기는 **아이템 레벨이 고정한 피해 범위**(09-14 — 굴림 없음), 방어구는 고유값(개체 굴림) — 고정 옵션은 그 위에 얹는 배율 한 겹이라는 뜻은 유지 |
| §1 무기 옵션 | `- **보류 셋 — 풀에 넣지 않는다** [사용자 확정 2026-09-11] — 통합옵션의 **최소/최대 피해**(전투에 타격 편차가 없어 올릴 대상이 없다 — 편차는 드롭 때 무기 개체에 박힌다 · DEV_PLAN R46 과 같은 모순)` | 괄호 안 이유를 취소선 → **09-14 무기 피해가 범위가 되어 올릴 대상이 생겼다 — 사용자가 나중에 추가**(GAME_DESIGN §10) · 나머지 보류 둘은 그대로 |
| §1 무기 베이스 「⚠ 미결」 | `**⚠ 축이 하나뿐이다**` 행의 `편차 폭(⚠ [hero_design.md](hero_design.md) §2 가 「자동전투에서 변동은 약점」으로 막아 뒀다)` · `**편차를 되살릴지는 실측으로 정한다**` | 09-14 **타격 범위 폭**으로 되살아났다 — 폭은 무기군이 정한다 · 베이스마다 범위 모양을 가를지는 A/B/C 축과 함께 미결 |
| §1 드롭 파이프라인 표 6단계 | `\| 6 \| **개체 굴림** \| 무기 공격력 · 방어구 고유값 (§2)` | `~~무기 공격력~~`(09-14 — 무기 피해는 굴리지 않는다 · 파생) · 방어구 고유값. ⚠ 난수 순서는 INTERFACE §5-2 먼저(2단계) |
| §1 강화 불릿 | `**\`+\`강화** = 무기 공격력·방어구 방어력 같은 **베이스 능력치**(§2 개체 굴림)를` | `무기 피해 범위 · 방어구 방어력` · `(§2)` |
| §2 변경점 표 | `\| **무기 개체 편차** \| \`min_damage\`~\`max_damage\` \| **드롭 시 한 번 굴려 개체에 박는다** (무기군 편차 폭) \|` 행 | 행 제목 → `**무기 피해** [개정 2026-09-14]` · 현행 칸 → `~~드롭 시 한 번 굴려 개체에 박는다~~ → **최소 ~ 최대 범위 · 직격마다 굴린다 · 범위는 아이템 레벨이 고정**`(사용자 확정 — D2 방식) · 이유 칸 → 옛 이유(`타격당 편차는 타격 수가 늘면 평균으로 사라져 축이 못 되지만 …`) 취소선 + 개체차는 고정 옵션 공격력 % 굴림이 든다 · 방어구 고유값 굴림은 유지 |
| §2-1 | `- **무기 공격력·방어구 고유값은 접사가 아니라 개체 굴림**` | 무기는 **아이템 레벨이 고정한 범위**(09-14) · 방어구 고유값만 개체 굴림 |
| §6-2 | `- **접사가 아닌 값도 점수 대상이다.** 무기 공격력과 방어구 고유값은 접사가 아니라 개체 굴림이지만(§2)` | 끝에 ⚠ 09-14 — 무기 피해는 굴림이 아니게 됐다 · 점수에 넣을지는 §6-4 |
| §6-4 표 (새 행) | `\| **자동 분해 자체** \|` 행 뒤 | `\| **무기 피해를 점수에 넣나** [신설 09-14] \| 같은 무기군 · 같은 아이템 레벨이면 범위가 같아 **운이 없다**. 점수가 「굴림의 운」을 잰다(§6-2)면 빠지고, 「절대값 — ilvl 로 커진다」면 들어간다. 미정 \|` |
| 꼬리 | `*마지막 업데이트:` | 날짜 |

### 3-5. skill_design.md

| 자리 | 앵커 | 할 일 |
|---|---|---|
| §3-5 | `- ⚠ **「최소 데미지」는 신규 전투 스탯이다**` | 끝에 → **09-14 무기 피해가 최소 ~ 최대 범위가 되어 올릴 하한이 생겼다**(battle_design §9-1). 효과의 형태(하한을 범위 폭의 % / 최소 +n) · 스탯 발행은 **사용자가 나중에 정한다**(GAME_DESIGN §10) |
| §7 표 | `\| **「최소 데미지」 스탯 신설** \|` | 같은 한 마디 |
| §7 표 | `\| **번개의 분산이 몬스터에도 걸리나** \|` | 전제 한 마디 — 09-14 모든 직격이 범위를 굴린다 · 남은 것은 번개의 더 넓은 폭(GAME_DESIGN §10 과 같은 문장) |
| §3 코드블록 | `[1] 기본 공격력 = 능력치계수 × 무기 공격력 × ( 1 + Σ 상시 % )      ← 직업` | ⚠ 09-10 전 표기라 이미 낡음 — 고치면 보고 |
| 꼬리 | `*마지막 업데이트:` | 날짜 |

### 3-6. hero_design.md §2

| 앵커 | 할 일 |
|---|---|
| `- ⚠ **자동전투에서 변동이 큰 것은 장점이 아니라 약점이다** [09-01]` 불릿 끝 | 09-14 — 변동은 무기 개체가 아니라 **직격마다 굴리는 범위의 폭**이 든다(battle_design §9-1) · 최소 데미지 노드의 형태는 사용자가 나중에 |
| 꼬리 | 날짜 |

monster_design.md — **변경 없음**(몬스터는 낀 무기를 쓰므로 규칙이 따라온다 · 「개체 굴림」 서술 없음 확인).

### 3-7. DEV_PLAN.md

| 자리 | 앵커 | 할 일 |
|---|---|---|
| §3-3 표 끝 | 마지막 행(지금 `\| R89 \|`) 뒤 | `\| R90 \| 무기 피해 = 최소 ~ 최대 범위 · 직격마다 굴림 · 개체 편차 폐지 · 몬스터 · 회복 동일 \| 09-14 \| ⏸️ 확인 대기 — PLAN_weapon_damage_range.md §2 \| GAME_DESIGN §9 09-14 · battle_design §9-1 · item_design §1 · §2 \|` — 다른 세션이 R90 을 먼저 썼으면 다음 번호 |
| §3-3 | `\| R2 \| 편차 = 무기 개체 굴림 \| 08-26 \| ✅ 08-26 \|` | 상태 칸 → `✅ 08-26 **→ R90**` |
| §3-2 | `**T2 무기 특성 2노드**(「최소 데미지」가 기획 모순 — R46)` | → `**T2 무기 특성 2노드**(09-14 무기 피해 범위로 모순 해소 — 효과 형태는 사용자 대기 · R46)` |

---

## 4. 2단계 — 이식 계약 INTERFACE.md (`/client`, §2 확인 뒤)

| 자리 | 할 일 |
|---|---|
| §2 `formula.strike` 표 행(`직격 1회. **rng 소비 순서 = 적중 → (적중 시) 치명 → …`) · 의사코드(`← 타격 편차 없음 (무기 개체에 박혀 있다)`) · §5-2 소비 표(`\| \`formula.strike\` \| 적중 → (적중 시) 치명 →`) | 피해 굴림 삽입(⑥) · 공격자 `a` 필드를 범위로(⑤) · 「옛 `strike` 의 편차 굴림도 없다」 문장 정리 |
| `**삭제된 출력** — \`variance_pct\`(편차는 무기 개체에 박혔다)` · `weaponGroups` 행의 `variance` | 뜻 이동(④) |
| `**개체 굴림** — 편차는 타격마다가 아니라 **드롭 시 한 번**` 표의 `무기 \`watk\`` 행 | 삭제 → 무기 피해 범위 **파생** 공식(가운데 · 폭 · 강화 · 반올림 ⑦) |
| `- **랜덤한 것은 박고, 결정적인 것은 파생한다.**` | 원칙은 그대로 — 무기 피해가 파생 쪽으로 옮겼다는 예만 추가 |
| `regroupWeapon` 행 · 세이브 이관 표의 `무기 \`watk\`` · `개체 굴림(\`watk\` · \`element\` · …)` | `watk` 제거 반영 · §4 에 v25 → v26 이관 행(③) |
| §5-2 드롭 순서 | 무기 개체 굴림 1회 삭제(⑥) |
| 회복 | 회복량 계산 직전 굴림 1회 — 소비 순서에 명시 |
| 골든 드롭 지문 `개체 굴림은 무기 \`w<watk>\`` | 무기는 `-` 또는 범위 표기로 |

---

## 5. 3단계 — CSV · 코드 · 검증 (`/client`)

**CSV** (값은 건드리지 않는다 — 사용자 「세부 수치는 나중」)
- `weapon_group.csv:variance_pct` · `balance.csv:dmg_variance_pct` — 설명(description)을 「타격 범위 폭」으로(④)
- `balance.csv:weapon_atk_base` — 설명의 `× 개체 편차` 제거 · 「범위의 가운데」
- `armor_def_variance_pct` — 그대로(방어구 개체 굴림 유지)

**코드** (위치는 2026-09-14 기준 — 재확인)

| 파일 | 자리 | 할 일 |
|---|---|---|
| `src/game_logic/item.js` | `build()` 의 `const eps = (rng() * 2 - 1) * (base.variance ?? B.dmg_variance_pct) / 100;` · `item.watk = r2(...)` | 개체 굴림 삭제 · `watk` 저장 중단 · 범위 파생 함수 신설(무기군 · ilvl · `up`) |
| 〃 | `function effective(item)` 의 `out.watk = r2(out.watk * m)` | 범위 양끝 × `upMult` |
| 〃 | `hasBase` · 파일 머리 주석(`growth — … 소수 1자리`) | `watk` 기준 판정 정리 |
| `src/game_logic/hero.js` | `weapon.watk + (weapon.affixes ?? []).reduce(... 'atk_flat' ...)` · `[magic ? 'atk_magic' : 'atk_physical']: atk` · 주석 `무기 개체 공격력(watk)에는 드롭 시 굴린 편차가 이미 박혀 있다` | 전투 능력치를 범위 두 값으로(⑤) |
| `src/game_logic/battle.js` | 유닛 생성 `const atk = c.atk_physical ?? …` · `matk` · `matkBase` · `REFIT_FIELDS` · 몬스터 `c.atk_magic *= B.monster_atk_scale` · result/timeline 표시값 `atk: p.atk, matk: p.matk` (세 곳) | 범위 필드 · 몬스터 스케일은 양끝에 |
| `src/game_logic/formula.js` | 머리 주석 `**타격 편차 없음**` · `strike` | 피해 굴림(⑥) |
| `src/game_logic/skill_effects.js` | `atk_pct` 의 `u.atk = u.atkBase * mult; u.matk = u.matkBase * mult;` | 양끝 모두 |
| 〃 | `barrier_pct` 의 `tgt.hpMax * def.value / 100` | ② 답에 따라(추천: 그대로) |
| `src/game_logic/skill_runtime.js` | `const amt = Math.round(u.matk * def.mult / 100 + (def.flat ?? 0));` | 회복 굴림 |
| `src/game_logic/skill.js` | `AMOUNT_BASIS = { attack: 'atk', heal: 'matk', … }` · `amount` 표시 | 스킬 문장의 피해 · 회복량을 범위로 |
| `src/game_logic/state.js` | `SAVE_VERSION = 25` · 이관 주석 `무기 \`watk\` 는 **재굴림하지 않는다**` · `regroup` 주석 | v26 이관 — 옛 `watk` 제거(③) |
| `src/dev/golden.js` · `golden.json` | `it.watk != null ? \`w${it.watk}\`` | 지문 갱신 · **재촬영**(사유: R90 난수 순서 변경) |
| `src/dev/test.js` | `watk` 단정 16곳 | 갱신 + 신규 단정 — ① 같은 무기군 · 같은 ilvl = 같은 범위 ② 직격 피해가 범위 × 배율 안 ③ 많이 굴린 평균 ≈ 가운데 ④ 몬스터 · 회복도 굴림 ⑤ 난수 소비 순서 ⑥ v25 → v26 이관 |

**검증** — [src/dev/README.md](src/dev/README.md) 절차 · test.html 단정 전부 통과 · 캘리브레이션으로 **기대값 중립** 확인(라운드 목표 시간 대비 전후 비교) · 골든 재촬영 · DEV_PLAN §3 구현 현황의 `개체 굴림 — 무기 \`watk\` 편차` 행 갱신 · R90 상태 ✅

---

## 6. 4단계 — 화면 (`/ui`)

- **SCREEN_DESIGN.md 먼저** — 아이템 툴팁 · 캐릭터 시트의 「공격력」 줄 → 「피해 x ~ y」. ADR-0081 이 이 전환을 예고해 뒀다(행의 자리 · 크기는 그대로, 값만)
- `src/ui/app.js` — `eff.watk` 표시 두 곳(툴팁 밑수 행 · 비교/시트) · 비교 델타를 무엇으로 낼지(평균 / 양끝) · 스킬 문장의 피해 · 회복량(battle 표시값)
- `src/ui/i18n.js` — ko/en 나란히 · 렌더러에 한국어 리터럴 금지
- ADR — 「피해 범위 표기」가 새 결정이면 **새 ADR**(ADR-0051 · 0081 은 고치지 않는다) · SCREEN_CHANGELOG 한 줄

---

## 7. 끝나면

- 이 파일을 지운다(루트 임시 문서)
- DEV_PLAN R90 상태 갱신 · 기획서 · INTERFACE · SCREEN_DESIGN 이 결정을 들고 있는지 마지막 grep — `watk` · `개체 편차` · `타격마다 굴리지 않는다`
- 커밋은 사용자가 요청할 때만

---
*작성: 2026-09-14*
