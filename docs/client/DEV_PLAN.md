# DEV_PLAN — 클라이언트 개발 계획서

> **계획과 현황만.** 구조는 [ARCHITECTURE.md](ARCHITECTURE.md), 계약은 [INTERFACE.md](INTERFACE.md), 화면은 [SCREEN_DESIGN.md](SCREEN_DESIGN.md), 기획 백로그는 [GAME_DESIGN.md §10](../game_design/GAME_DESIGN.md).
> 이 문서에 이력을 쌓지 않는다 — 결정 이력은 GAME_DESIGN §9, 코드 이력은 git.

표기: ✅ 완료 · 🔧 진행 · ⏸️ 보류(기획 대기) · ☐ 예정

---

## 1. 현황 한 줄

**Phase 1 웹 프로토타입 — 초반 루프(새 게임 → 원정 → 리포트 → 장비 → 고용 → 반복)가 CSV 실값으로 돈다.** 규칙은 임시(⚠제안 계수), 파견·유니크·크래프트 없음 · 스킬은 **액티브(프로토타입 14행) + 마스터리 수치층(T1·T2)** — 전직·반응형(T3)은 없다. 세이브 **v10**(v2~v9 는 `deserialize` 안에서 연쇄 이관).
**기획↔프로토타입 대조 반영 (2026-08-30)** — 확정 기획과 **어긋나 있던** 다섯을 코드가 따라갔다: 귀환 룰(부채 #14) · 선술집 명단 규칙(R9 절반) · 레벨 상한(R12 절반) · 관전이 실제 스킬을 그린다(#13·#22·#25) · 실효 쿨 공식 사본 제거(#3). **캘리브레이션은 귀환 룰 위에서 다시 찍었다** (§3-1).
**battle_design §9 개정(08-26)이 코드에 들어갔다** — 성장 곡선 SSOT · 무기=밑수 · 개체 굴림 · K 상수 · 피해 감소 원천별 곱 · 적중 = 레벨 차 · 저항 상한형 · 몬스터=영웅 공통 체계 (§3-3 R1~R8·R10·R11). 남은 기획 선행 항목은 R9·R12·R13.
**M7 mock→CSV 이관 완료 (2026-08-31)** — 이식 차단 항목이 **9 → 3** 으로 줄었다(`affix`·`item_base`·`equip_slot`·`class`·`hero_name`·`hero_trait` 6종 발행 · `ELEMENT_IDS` 삭제 · `nm`·`eliteName` → `game_logic/naming.js`). 남은 셋(`SINS`·`SIN_TRAITS`·`COMMON_TRAITS`)은 전부 **죄종 매핑 미확정** 하나에 걸려 있다. **값은 하나도 안 바꿨다** — 캘리브레이션 4행이 비트 단위로 같은 것으로 확인.
**밸런스 재보정 (2026-08-31)** — `monster_atk_scale` 0.1 → 0.2 **하나만** 돌려 목표 대역에 들어왔다: 101 100% · 102 55% · 103 5% · 104 0% (§3-1 아래 주).
**골든 시드 스냅샷 (2026-08-31)** — `dev/golden.json` 40런(시드 10 × 스테이지 101~104)을 `test.html` 이 매번 대조한다. **Phase 2 이식 검증의 실제 도구**이자 rng 소비 순서를 잡는 그물이다(부채 #7 해소 · 계약은 INTERFACE §5-5). 단정 **137**.
**스킬 구조 재편 (2026-09-01 — R31)** — 사용자 지시(구조 리뷰 권장 3건 전부). ①배정 단위 = **인스턴스** `{id, source}`(우선순위 저장 · 전직 1택 · 변형 노드의 자리) + 런타임 **사건 훅** 5종(`cast`·`hit`·`hitTaken`·`kill`·`down` — 등록 소비자 0) ②**`skill_effects.js`**(종류 등록표 — 어휘 = 표의 키) · **`skill_runtime.js`**(액티브 실행을 `battle.simulate` 밖으로) · `makeUnit` 하나(몬스터도 같은 생성자) · 검증 강화(종류↔대상 정합) — **결과 불변**을 골든 `tl`(타임라인 해시)로 잠근 채 진행 ③CSV 이관 — `skill.csv` 에 `icon`·`desc_kr`·`desc_en`·`innate_pool`·`note` · **`skill_tag.csv` 신설**(태그 어휘·이름 ko/en) · `balance.csv:skill_cd_floor_mult` · `mock.js` 의 `SKILL_DISPLAY`·`SKILL_TAGS` 삭제. 행동 변경 둘 — **동률 = 칸 순서**(CSV `priority` 는 기본 정렬만) · **회복 밑수도 버프 괄호**. 발견한 버그 1 — `partyMembers` 가 id 를 넘겨 `skill_tag` 전술 조건이 영원히 0 을 셌다(수정 · 회귀 단정). 단정 160 → **175** · 골든 재촬영 2회(기준선 · B). 캘리브레이션: 101 50% 그대로 · 102 10% → 15% · 103 0% → 10% (손잡이 무변동 · §3-1).
**전술 옵션 등급 축 반영 (2026-09-02 — R33)** — 09-01 에 `tactic_option.csv` 가 **`(option_id, grade)` 복합키 66행**(22가족 × 3등급)으로 나갔는데 코드가 안 따라와 **프로토타입이 빨간불이었다**(FAIL 171/175): 로더가 `option_id` 를 유일 키로 봐서 ①가족당 마지막 행 = **레어 값만** 남고 ②같은 가족이 두 칸에 들어갔다(09-01 확정 「중복 방지는 가족 기준」 위반). `tactic.js` 를 가족/등급 두 축으로 재작성하고 **첫 배정은 언제나 일반** · **리롤은 가족과 등급을 같이 굴린다**(가중치 3키 ⚠제안 발행) · 폐기 확정 어휘 2종(`party_size`·`damage_kind`) 삭제 · 세이브 **v10**. 단정 **178** · 골든 재촬영. ⚠ **캘리브레이션이 내려갔다** — 101 50% → **45%**(레어 값으로 돌던 것이 일반 값으로 정정된 결과 · §3-1). ⚠ 화면에 등급이 아직 안 보인다(`/ui` 몫).

**레어 고유 스킬 프로토타입 배정 (2026-09-01 — R30)** — 사용자 지시. 영웅이 생성될 때 **현재 `skill.csv` 전 행에서 균등 1개**를 고유 스킬로 굴려 액티브 1번 칸에 싣는다(직업 비종속 · 생성 후 불변 · 세이브 `innate`). 나머지 칸은 직업 행이 `priority` 순으로 채운다 — 종전 「직업 셋 전부」(§9-0) 폐기. 세이브 **v9**(옛 영웅은 시드에서 소급) · 단정 **160** · 골든 재촬영(시작 파티 30명 · 40런 전부 — rng 소비 1회 증가 + 액티브 구성 변화). ⚠ **캘리브레이션이 움직였다** — 101 70% → **50%** · 102 0% → **10%**, 손잡이는 안 돌렸다(§3-1). ⚠ 임시 풀의 구멍 — 물리 직업 + 회복 = 0 (부채 #28).
**한손 개념 폐지 · 보조 슬롯 폐지 (2026-09-01 — R27)** — 사용자 확정. **전 무기가 양손**이 되고 **보조(offhand) 부위가 사라졌다**(위치 9 → 8 · 부위 8 → 7). 무기군도 재편 — 한손검 삭제 · 창이 전사 → 기사 · 완드 → 오브라서 **본편 5직업이 전부 무기 2개씩**이고, 같은 직업의 두 무기는 행동 주기(1.3 ↔ 1.6)로 갈린다. `two_hand_atk_mult`(1.45)는 전역 상수가 되어 `weapon_atk_base` 에 흡수(2.3 → 3.34). 세이브 **v8** · 단정 **157** · 골든 재촬영. ⚠ **캘리브레이션이 움직였다** — 101 30% → 70% (무기 밑수가 올라 시작 파티가 세졌다). 102 는 0% 그대로라 목표 대역(102 30~70%)은 여전히 아래로 벗어나 있다 (§3-1).
**강화 구현 (2026-08-31 — R25)** — 기획 08-31 개정분 중 **코드가 닫을 수 있는 하나**를 닫았다. 골드로 `up` 을 +9 까지 올리고 **베이스는 파생 · 3강마다 오르는 접사 값만 박는다**(랜덤한 것은 저장하고 결정적인 것은 다시 계산한다 — INTERFACE §2-5). 세이브 **v7** · `balance.csv` 6키 · 가방의 **강화 모드** · 단정 **155**. **캘리브레이션 4행은 한 칸도 안 움직였다** — 강화는 플레이어가 누르는 행동이라 레벨 1 시작 파티가 밟지 않고, 골든 재촬영에서 `runs` 40런·`meta.parties` 가 그대로인 것으로 확인했다.
**08-31 개정분의 나머지 둘 (R24 의뢰 · R26 파견처)** — 코드로 닫지 못한다. 의뢰는 `battle.js` 가 전투 인스턴스를 하나만 가정하는 데다 쿨다운·드랍·해금이 미정이고, 파견처 개편은 거점 화면 자체가 없다. **닫은 것은 잔재와 안내뿐이다** — `hero_attribute.csv` 의 지능 행에서 연구소를 지웠고(파견처 배정 상실), 도움말이 의뢰·연구·파견처 5를 새 기획대로 말한다.
**골든 사각지대 보강 (2026-08-31 2차)** — 1차 스냅샷이 **실측으로 못 잡던 것들**을 메웠다: `grantXp` 성장 전체 · 영웅 이름/특성 풀 순서 · 시작 무기 · 정예 편성 · `balance.csv` 의 15+ 키 · 어느 CSV 가 바뀌었는지. 단정은 입력(csvHash · balance 전 키 · 시작 파티) → 출력(40런) **네 개**가 되고 지문 생성이 `check()` 안으로 들어갔다(던져도 나머지가 산다). 부채 **#7-a**. 단정 **142** · **캘리브레이션 4행 불변**(관찰 도구만 고쳤다).
**밸런스 전멸 해소 (08-27)** — `monster_hp_scale`·`monster_atk_scale`·`hero_hp_base` 조정으로 101 승률 100%·102 50%·103/104 는 정상 패배 대역에 들어왔다 (§3-1 아래 주).

---

## 2. Phase 1 마일스톤

| # | 날짜 | 마일스톤 | 상태 |
|---|---|---|---|
| M1 | 08-21 | 기획 골격 확정 · TheSevenRPG 데이터 포크 25종 · 화면 UI 목업 | ✅ |
| M2 | 08-23 | 캐릭터 탭 개편 · 한/영 다국어 레이어 · 신규 SSOT 데이터 8종 | ✅ |
| M3 | 08-25 | **초반 루프 실동작** — `game_logic/` 개봉 · 새 게임 · 세이브 | ✅ |
| M4 | 08-26 | 피해 공식(`formula.js`) · 프로토타입 ↔ 기획서 동기화 · 세이브 v2 · 공통 영웅 띠 | ✅ |
| M5 | 08-26 | 문서 체계 — 큰 틀 정리 · CLAUDE.md 재구성 · `docs/client/` 4종 · 폴더 README | ✅ |
| M6 | 08-31 | 경계 위반 정리 + 골든 시드 스냅샷 (§5-A) | 🔧 — **스냅샷은 찍었다**(`dev/golden.json` 40런 · 부채 #7 해소). 남은 것은 부채 #4(예상 소요 집계) · #6(재생기 경고) · #10(코드 상수 단정) |
| M7 | 08-31 | mock → CSV 이관 (§5-B) — Phase 2 착수 조건 | 🔧 — **9 → 3 항목**. 남은 셋은 전부 죄종 매핑(`sin_mapping.md`) 대기라 **기획이 열어 줘야 닫힌다** |
| M8 | 08-31 | 기획 확정분 구현 (§5-C) — 순서는 기획이 정한다 | 🔧 — **R25 강화 반영**(첫 칸). R24·R26 은 기획·구조가 막고 있다(§3-3) |

---

## 3. 구현 현황

### 3-1. 돌아가는 것

| 항목 | 상태 | 비고 |
|---|---|---|
| **스킬 실행 — 등록표 + 런타임 분리 + 사건 훅** | ✅ | 2026-09-01 (R31, 사용자 지시). `skill_effects.js`(공격 대상 4 · 버프 효과 4 · 발동 조건 2 — 종류 하나 = 등록 한 번) · `skill_runtime.js`(시전·창·배리어·회복·훅 — 전투마다 생성) · `battle.js` 는 `makeUnit` 하나 + 직격·도발·정산. 배정은 인스턴스 `{id, source}`(`skill.resolve` 로 정의). **훅 `reactions` 은 자리만** — 마스터리 T3 가 첫 소비자. 단위 단정 11(`runtime:`) · 골든 `tl` 해시 |
| **레어 고유 스킬 — 생성 시 랜덤 1** | ✅ | 2026-09-01 (R30, 사용자 지시). `rollHero` 가 `skill.csv` 전 행에서 1개를 굴려 `innate` 에 박고 `activesFor` 가 1번 칸에 싣는다 · 나머지 칸 = 직업 행(priority 순 · 중복 제거 · `active_slots` 상한) · 화면은 번호 옆 「고유」. **정식 고유 풀은 없다** — 전직 임시분 14행을 빌려 쓴다(skill_design §9-0). ⚠ 물리 직업 + 회복 = 0(부채 #28) · 자기 직업 행을 뽑은 전사는 2칸(전사 ③ 미정과 겹친다) |
| **장비 8칸 · 전 무기 양손 · 무기군 재편** | ✅ | 2026-09-01 (R27, 사용자 확정). 보조(offhand) 부위·`twoHanded` 플래그·양손↔보조 배타·`two_hand_atk_mult` 가 **전부** 사라졌다. 무기군 10종(본편 8) — 전사 둔기(1.3/22)·도끼(1.6/28) / 기사 창(1.3/10)·양손검(1.6/15) / 마법사·사제 오브(1.3/10)·스태프(1.7/15) / 궁수 활(1.4/14)·석궁(1.8/20). **전사는 변동이 크고 기사는 작다** — 직업 성격이 무기에서부터 갈린다. 세이브 v8 이관(보조 아이템 삭제 · `sword1h`→`sword2h` · `wand`→`orb` · 직업 불일치 무기는 가방으로) |
| 새 게임 — 후보 3명 굴림 · 리롤 · 시작 무기 | ✅ | 후보 시드는 UI 고정 상수 |
| **프롤로그 — 새 게임 직후 5씬** | ✅ | 2026-09-03 (사용자 지시). 파티를 확정하면 `startGame({ prologue: true })` 가 **세이브를 먼저 쓰고** 화면만 프롤로그에 세운다 — 읽다 닫아도 이어하기로 돌아온다. 5씬 넘김 · 건너뛰기(모든 씬) · **뒤로 없음** · `pregame` 셸(탭·자원 띠 없음). 글은 [prologue.md](../game_design/story/prologue.md) 에서 옮겨 `i18n:pro.*` 가 들고, 본문 칸이 남는 높이를 먹고 스스로 넘쳐 **버튼이 어느 씬에서도 화면 안에 선다**. ⚠ **본문은 한국어뿐**(부채 #30) · **다시 보기 자리 없음**(나중에) · 개발용 경로 `?dev=prologue&s=n` (SCREEN_DESIGN §3-1) |
| 편성 — 파티 3 / 로스터 7 / 부상자 제외 | ✅ | |
| 스테이지 해금 — 직전 클리어 | ✅ | 28 스테이지 · 9 라운드 |
| 전투 — 헤드리스 시뮬 · 타임라인 · 관전 재생 | ✅ | 액티브(**출처 고정 3칸 — 고유 · 무기군 · 전직** — 프로토타입 §9-0 재개정 2026-09-03 · R35 · 실행은 `skill_runtime.js` R31) 실동작 · 타겟팅 랜덤(진형 미확정) · **귀환 룰**(전투불능 1명 = `reason:'retreat'`, 2026-08-30) |
| **의뢰 탭 — 게시판 7장** | 🔧 | 2026-09-03 (사용자 지시). 종류 4종 확정(약탈 · 보호 · 사냥 · 파견 의뢰 — GAME_DESIGN §9 09-03)으로 미착수 스텁을 걷고 **게시판 한 줄**이 섰다. 일곱이 `flex: 1 1 0` 으로 **줄을 나눠 가져** 하한 폭에서도 스크롤 없이 다 보인다. **데이터는 CSV 두 표**(`commission_kind.csv` 종류 4종 = 확정 기획 / `commission.csv` 게시판 행 = ⚠임시 · 칸 수 = 행 수) — 상단(TRADE)·연구(RESEARCH) 목업과 **갈리는 지점**이다(「mock 과 CSV 가 겹치면 CSV 만」, 사용자 지시 2026-09-03). **기능은 아직 없다** — `SYS.*` 를 하나도 안 부르고 누르면 미착수 안내. 부채 등재는 §4 #31. **수락 상한은 숫자를 안 찍는다**(상한은 확정이지만 개수 CSV 키가 없다). 남은 것 넷은 `game_logic` 몫 — 의뢰 굴림 · 수락/진행 상태 · 처치 카운터 · 명성 정산 (SCREEN_DESIGN §14) |
| **연구 탭 — 두 섹션 한 화면** | 🔧 | 2026-09-01 (사용자 지시). 세그먼트 2 대신 세로 2단(위 연구 · 아래 파티전술) — 파티전술이 전부 열려도 362px 만 써서 자리가 이미 있고, 미착수인 연구가 빈 세그먼트가 되면 「고장」으로 읽힌다. **연구 섹션은 통째로 목업**(`ui/mock.js:RESEARCH` · 노드 4 · `SYS.*` 를 하나도 안 부른다 · 누르면 미착수 안내) — 부채 등재는 §4. ⚠ **뒤집는 문턱**: 연구가 세로 300px 을 넘으면 세그먼트 2로 (SCREEN_DESIGN §13) |
| **탭 7 — 구현 7 (의뢰는 목업)** | ✅ | 2026-09-01 (사용자 지시). 탭이 코어 루프의 시간축과 맞물린다 — 실시간 전투 = 원정·의뢰 / 오프라인 = **마을** 하나 / 아이템 정리 = 캐릭터. **스킬**은 탭을 잃고 캐릭터 안의 **창**(창 레이어 신설 · 닫는 길 셋) · **거점·탐험·선술집**은 **마을** 탭의 칸이 됐다(파견처 목록 6 → 고른 것의 속 · 들어오면 선술집이 골라져 있다 · 순서 선술집·제련소·광산·채집·탐험·상단). ~~미착수는 의뢰 탭 하나 + 마을 칸 다섯~~ → **2026-09-03 의뢰가 게시판 목업을 받아 미착수 탭이 0이 됐다**(위 행). 남은 미착수는 마을 칸뿐이고, **문구는 도움말이 쓰던 키를 그대로 부른다** (SCREEN_DESIGN §1 · §8 · §11 · §14) |
| ~~탭 10 — 구현 7 + 미착수 3~~ | — | 2026-08-31. 위 행으로 대체됐다 — 의뢰·거점·탐험에 탭 자리를 준 결정 자체는 살아 있고(기획이 화면까지 확정한 것을 지도 밖에 두지 않는다), **거점·탐험이 마을 탭 안으로 접힌 것만** 바뀌었다 |
| **관전 — 창 뱃지 줄 · 빈 스킬 칸** | ✅ | 2026-08-31 (사용자 지시). 걸린 창을 카드 아래 칩으로 그린다(이로운 창 초록·해로운 창 빨강 — 가르는 건 `buff` 이벤트의 `v` 부호이고 **지금 도는 창 4종은 전부 이롭다**). 스킬 칸은 언제나 `active_slots` 개 — 액티브가 둘인 영웅은 셋째가 **빈 칸**으로 남는다(옛 「빈 칸을 그리지 않는다」 결정 폐기). 둘 다 줄 높이를 고정해 **카드 크기 고정**을 안 깬다 |
| **관전 — 실제 스킬 이벤트** | ✅ | 2026-08-30. 쿨 칸이 `skill` 이벤트의 `ready` 로 걷히고 `heal`·`buff`·`buffEnd`·`regen` 도 그린다. `MOCK_ACTIVES` 폐기 — 아이콘·설명만 표시 사전(`SKILL_DISPLAY`) |
| **마스터리 — 죄종 T1 공통 3 · T2 16 · 전사 직업 T1 3** | ✅ | `mastery_node.csv` 22행 · 랭크는 세이브 · 포인트는 레벨업 지급 · 롤백 무료. **T3(반응형)는 없다** |
| **스킬 탭 — 마스터리 화면** | ✅ | 단계가 **위→아래**로 쌓인 3줄 × 3칸 · 칸을 눌러 1랭크 · 초기화(전액 환급) · 잠긴 칸은 필요 레벨 표시. **「T1·T2·T3」는 화면·도움말에 안 쓴다**(내부 어휘) · 가지×깊이 트리 목업 삭제 |
| **파티 전술 — 연구 탭** | ✅ | 2026-08-30 (**등급 축 2026-09-02 · R33**). 칸은 **로스터 합산 레벨**로 열리고(`tactic_slot.csv`) 칸의 옵션은 **골드 리롤**(`tactic_option.csv` — **`(option_id, grade)` 복합키** 22가족 × 3등급). 조건 **6종**은 편성에서 확정되는 값만 세고, 켜진 효과는 접사·마스터리와 **같은 채널**로 `heroCombat` 에 합류한다(파티원 한정). **첫 배정은 언제나 일반 · 리롤은 가족과 등급을 같이 굴린다 · 중복 방지는 가족 기준** · 세이브 v10. ⚠ 화면에 등급 표시 없음(`/ui`) · ⚠ 리롤은 아직 **칸별**이다(R28 미반영) |
| 피해 공식 — 적중(레벨 차) · K 상수 감쇠 · 저항 상한형 · 피해 감소 곱 · 직격/비직격 | ✅ | battle_design §9 전면 반영(§3-3). 계수는 ⚠제안 키 |
| 성장 곡선 — `power_growth_per_level` 하나(무기 ilvl · 영웅 HP · growth 접사) | ✅ | 방어·저항·% 접사는 곡선 밖 |
| 개체 굴림 — 무기 `watk` 편차 · 방어구 implicit 편차 (드롭 시 1회) | ✅ | 같은 ilvl 이라도 개체차가 영구히 남는다 |
| 빗나감 집계 — `result.strikes` → 리포트 | ✅ | 레벨 부족의 전용 신호 (§9-8) |
| 정산 — XP(전원 동일) · 골드 · 가루 · 드롭 · 부상 타이머 · 클리어 | ✅ | XP 분배 규칙 ⚠제안 · **레벨 상한 `hero_level_cap`**(2026-08-30). 「50 이후 느린 곡선」은 미반영 |
| 장착 / 해제 / 분해 — 위치 8 · 직업 전속 무기군 · 가방 상한 | ✅ | 능력치 게이트 없음. ~~양손 배타~~ 는 2026-09-01 보조 슬롯 폐지로 사라졌다 (R27) |
| **강화 — 골드로 +9 · 3강마다 옵션 값 상승** | ✅ | 2026-08-31 (R25). 베이스(`watk`·implicit)는 `up` 으로 **파생**하고 오른 접사 값만 **박는다** — 개체 굴림값이 원본으로 남는다. **재굴림 없음**(종류·개수·순서 불변) · 실패 없음 · 가방·착용 안 가림 · 전용 rng 스트림(`^ 0xF0C3`)이라 전투 수열과 안 섞인다. 비용·계수는 ⚠제안 6키 |
| 선술집 — 명단 `tavern_candidates` · 고용 · **리롤 쿨다운** | ✅ | 2026-08-30 — 쿨다운 끝나면 무료 · 중이면 골드 · **고용한 칸만 빈다**. 유니크 등장은 없음(§3-2) |
| 선술집 — **수색 칸(화면만)** | 🔧 | 2026-09-01 (사용자 지시) — 명단 오른쪽 셋째 칸에 후보 카드와 **같은 크기**의 칸 + 수색 버튼 · 확정 수치 둘(`tavern_search_slots` · `tavern_search_hours`)을 찍는다. **동작은 없다** — `game_logic` 에 수색이 없어 누르면 미착수 안내만 뜬다(R9 참조). 옛 규칙 「칸을 미리 그리지 않는다」 폐기 (SCREEN_DESIGN §8-1) |
| 도감 — 몬스터 카드 · 레벨 · 스테이지 보정 | ✅ | 레벨별 % 도 계열 배정도 CSV(`codex_level.bonus_pct` · `codex_series`) · 공격·체력·피해 보정은 실제로 걸린다 · **스테이지 3 계열만 갈 곳 없음**(§3-2) |
| 반복 원정 — 켜져 있는 동안만 · 재접속 시 `closeRun` 알림 | ✅ | |
| 세이브 v9 — 엔진 중립 JSON · 어댑터 1곳 · **v2~v8 자동 이관** | ✅ | v1 은 throw(스키마 단절). 이관 규칙 INTERFACE §4. v8→v9 는 고유 스킬 소급(R30 · 2026-09-01) |
| 다국어 ko/en · 렌더러 리터럴 0 | ✅ | |
| 스킬 — `skill.csv` 22행 로드 · **출처 고정 배정**(고유 R30 → 무기군·전직 R35) · 발동 선택(동률 = 칸 순서 · R31) · 쿨 · 버프 창 · 배리어 · 회복 · 다단타 · 광역 · 도발 | ✅ | 08-27 판 기본 액티브 14행(전사 ③ 미정). 마스터리·전직·패시브·상태이상은 없음(§3-2) |
| 검증 — test.html 단정 + 캘리브레이션 + **골든 지문** | ✅ | **175 단정 통과** (2026-09-01 — R31 런타임 단위 11 + 회귀 1 + CSV/태그 4 · 고유 스킬 3(생성 굴림 · v8→v9 소급 이관 · 1번 칸 적재, R30) · 골든 4(csvHash · balance 전 키 · 시작 파티 · 40런 지문) · CSV 정합 3(이관 6종 행 수 · **부위 7 유일** · 접사/베이스 부위 실재) · v7→v8 이관 1) |
| **골든 시드 스냅샷 — `dev/golden.json` 40런 + 입력 지문** | ✅ | 2026-08-31 (**08-31 2차로 사각지대 보강**). 시드 1~10 × 스테이지 101~104 의 요약 지문(승패·라운드·소요·골드·XP·이벤트 수·타격/빗나감·카드·**처치 구성**·**스킬 시전 수**·**정예 편성**·**정산 후 성장**·전술 칸 전체·**드롭의 접사 stat·값·순서**) + **입력 지문**(CSV 22종 원문 해시 · `balance.csv` 전 키 · 시드 10개의 시작 파티)을 박아 두고 `test.html` 이 매번 대조한다. **입력을 출력보다 먼저** 찍는다 — 입력이 달라졌으면 런 불일치는 증상일 뿐이다. **Phase 2 이식 검증의 실제 도구** — 계약은 INTERFACE §5-5, 다시 찍는 법과 **보장하지 않는 것**은 [src/dev/README.md](../../src/dev/README.md) |

> ⚠ **아래 넷은 오래된 것부터 쌓여 있다 — 현재값은 맨 아래(08-31) 블록이다.** 위에서부터 읽고 08-27 판을 현재로 오해하지 말 것. (`src/dev/README.md` 는 반대로 최신이 위다)

> **캘리브레이션 (2026-08-27, 전멸 해소 손잡이 조정 후 · 시드 20 · Lv1 시작 파티 · 방어구 없음)** — 101 승률 100% / 9.0 라운드 / 283 s / 전투불능 0.00 / 1298 골드 · 102 50% / 8.3 / 408 s / 1.55 / 984 · 103 5% / 7.7 / 378 s / 2.85 / 550 · 104 0% / 5.8 / 234 s / 3.00 / 431. 시간 초과 0.
> 목표(101 ≥70% · 102 30~70% · 103/104 는 지는 게 정상)를 충족한다. 조정한 손잡이는 `monster_hp_scale`(2.4→1.0 — 킬 속도가 라운드 목표 20s 대비 한참 느렸다) · `monster_atk_scale`(0.25→0.1 — 더 내리면 `dmg_min` 바닥에 깔려 등급별 공격력 차이가 무의미해진다) · `hero_hp_base`(100→230 — 9라운드 무회복 구조에서 누적 피해를 버틸 체력 총량, 101/102 목표 승률로 역산). `weapon_atk_base`·`power_growth_per_level`(28스테이지 역산값)은 건드리지 않았다 — 손잡이와 측정 갱신은 [src/dev/README.md](../../src/dev/README.md).

> **캘리브레이션 재측정 (2026-08-28 두 번째, CSV 형태 최적화 후 · 손잡이 미조정 · 같은 조건 시드 20)** — 101 100% / 9.0 라운드 / 164 s / 전투불능 0.00 / 1292 골드 · 102 **100%** / 9.0 / 289 s / 0.00 / 1593 · 103 **100%** / 9.0 / 270 s / 0.00 / 1935 · 104 **100%** / 9.0 / 229 s / 0.25 / 7525. 시간 초과 0.
> 앞선 08-28 측정(스킬 도입 직후)은 101 100% / 102 100% / 103 100% / 104 15% 였다. **수치를 움직인 것은 손잡이가 아니라 데이터 결함 수정 셋이다** — 부채 #18(보스 raw 이중 elevation) 해소로 보스 hp·attack 이 그 스테이지 일반몹 평균으로 내려갔고(예: 사탄 hp 183→60), 부채 #17 해소로 정예 `hp_mult` 3.0→2.0 이 됐다. 드롭 판정도 등급별 굴림 N회에서 **처치당 1회 × 확률 배율**로 바뀌었다(item_design §1). **104 골드 7525 는 챕터보스를 이제 실제로 잡기 때문**이다(`exp_reward × exp_mult × gold_mult × gold_rate`).
> **이제 네 스테이지가 전부 100% 로, 목표 대역(102 는 30~70% · 103/104 는 지는 게 정상)을 위로 벗어난다.** 손잡이는 하나도 돌리지 않았다 — 재보정은 스킬 예산(`skill_dps_budget_pct`)·몬스터 축·귀환 룰(부채 #14)을 함께 보는 별도 결정이다.

> **캘리브레이션 재측정 (2026-08-30, 귀환 룰 반영 후 · 손잡이 미조정 · 같은 조건 시드 20)** — 101 100% / 9.0 라운드 / 164 s / 전투불능 0.00 / 1292 골드 · 102 100% / 9.0 / 289 s / 0.00 / 1593 · 103 100% / 9.0 / 270 s / 0.00 / 1935 · 104 **75%** / 8.8 / 223 s / 0.25 / 5785. 시간 초과 0. **PASS 126/126.**
> **움직인 것은 104 하나뿐이다**(100% → 75%). 101~103 은 전투불능이 아예 안 나서 귀환 룰이 걸릴 자리가 없다 — 표가 안 바뀐 것이 정상이고, 바뀌었다면 그게 회귀였다. 104 의 `avg downed` 는 **0.25 로 직전 측정과 같다** — 전에도 같은 빈도로 쓰러졌고 그 뒤에도 계속 싸워서 이겼을 뿐이다. 즉 이 25%p 는 밸런스가 나빠진 것이 아니라 **없던 안전장치가 생긴 값**이다.
> 목표 대역(102 30~70% · 103/104 는 지는 게 정상) 은 여전히 위로 벗어나 있다 — 손잡이 재보정은 그대로 남은 별도 결정이다.

> **캘리브레이션 재측정 (2026-08-31 3차, `hero_hp_base` 230 → 100 — 사용자 지시 · 같은 조건 시드 20)** — 101 **30%** / 7.6 라운드 / 133 s / 전투불능 0.70 / 590 골드 · 102 **0%** / 4.8 / 132 s / 1.00 / 230 · 103 **0%** / 3.0 / 86 s · 104 **0%** / 1.6 / 43 s. 시간 초과 0. **PASS 155/155** · 골든 재촬영. ⚠ **목표 대역을 아래로 벗어난다**(101 ≥70% · 102 30~70%) — 230 은 `monster_atk_scale` 0.1 시절 101·102 목표 승률에서 역산한 값인데 그 손잡이가 08-31 에 0.2 로 올라갔다. 생존 축이 두 번 깎인 상태라 되돌리려면 두 손잡이를 함께 본다 (src/dev/README.md 「마지막 측정」).
> **직전 측정 (2026-08-31, 손잡이 재보정 — `monster_atk_scale` 0.1 → 0.2 · `hero_hp_base` 230 · 같은 조건 시드 20)** — 101 100% / 9.0 라운드 / 166 s / 전투불능 0.00 / 1296 골드 · 102 **55%** / 8.5 / 243 s / 0.45 / 1043 · 103 **5%** / 7.3 / 211 s / 0.95 / 516 · 104 **0%** / 4.8 / 124 s / 1.00 / 351. 시간 초과 0. **PASS 142/142** (골든 사각지대 보강으로 140 → 142. **표는 한 칸도 안 움직였다** — 관찰 도구만 고쳤으니 안 움직인 것이 정상이고, 움직였다면 그게 회귀였다).
> **목표 대역을 충족한다** — 101 ≥ 70% · 102 30~70% · 103·104 는 102 보다 낮다. 08-28 이후 세 번의 측정이 계속 위로 벗어나 있던 것이 여기서 닫혔다.
> **돌린 손잡이는 하나다.** 승률 100% 의 원인은 킬 속도가 아니라 **파티가 안 죽는 것**이었으므로(101~103 의 `avg downed` 가 0.00 · `avg sec` 은 이미 목표 안) 몬스터 공격력 축으로 잡았다. `monster_hp_scale` 을 올렸으면 승률과 함께 라운드 시간이 같이 늘어 `round_budget.time_target_sec` 을 깨뜨린다. 탐색은 0.15 → **0.2(채택)** → 0.25(102 가 20%) → 0.3(102 가 0%) — 0.25 부터는 대역 아래로 지나친다.
> **안 건드린 것**: `hero_hp_base`(같은 축인 생존을 두 손잡이로 동시에 돌리면 어느 쪽이 효과를 냈는지 못 읽는다) · `skill_dps_budget_pct`(`skill.csv` 14행 전체가 이 값에서 역산됐다 — 돌리면 기획 작업이 된다) · `weapon_atk_base`·`power_growth_per_level`(28스테이지 역산값).
> **이 표가 골든 스냅샷(§3-1)이 찍힌 지반이다** — 손잡이를 다시 돌리면 스냅샷도 다시 찍는다.

> **캘리브레이션 재측정 (2026-09-01 — R27 한손 폐지 · `weapon_atk_base` 2.3 → 3.34 · 같은 조건 시드 20)** — 101 **70%** / 8.3 라운드 / 122 s / 전투불능 0.30 / 972 골드 · 102 **0%** / 5.8 / 136 s / 1.00 / 273 · 103 0% / 4.2 / 101 s · 104 0% / 2.0 / 47 s. **PASS 157/157** · 골든 재촬영. (§1 이 가리키던 값인데 이 절에 빠져 있던 것을 R30 때 채웠다)
> **캘리브레이션 재측정 (2026-09-01 — R30 레어 고유 스킬 프로토타입 배정 · 손잡이 미조정 · 같은 조건 시드 20 · 액티브 = 고유 1 + 직업 채움)** — 101 **50%** / 7.7 라운드 / 128 s / 전투불능 0.50 / 779 골드 · 102 **10%** / 5.5 / 146 s / 0.90 / 389 · 103 0% / 3.4 / 91 s · 104 0% / 2.0 / 46 s. 시간 초과 0. **PASS 160/160** · 골든 재촬영(시작 파티 30명 · 40런 전부).
> **손잡이는 하나도 안 돌렸다 — 배정 규칙이 표를 움직였다.** 자기 직업 행을 뽑은 전사는 액티브 2칸(중복 제거 · 전사 ③ 미정), 남의 직업 행을 뽑은 영웅은 `priority` 3 의 단쿨기가 상한 `active_slots` 에서 잘린다 → 파티 화력의 **분산**이 커졌다(101 은 내려가고 102 는 강한 고유를 받은 시드가 올라왔다). 같은 시드라도 시작 파티가 달라졌으므로(rng 소비 1회 증가) 시드 단위 비교는 되지 않는다. 목표 대역(101 ≥70% · 102 30~70%)은 여전히 아래 — 재보정 선택지 셋([src/dev/README.md](../../src/dev/README.md) 「마지막 측정」)은 그대로. **이 표가 골든 스냅샷이 찍힌 새 지반이다.**

> **캘리브레이션 재측정 (2026-09-01 — R31 스킬 구조 재편 · 손잡이 미조정 · 같은 조건 시드 20)** — 101 **50%** / 7.8 라운드 / 132 s / 전투불능 0.50 / 779 골드 · 102 **15%** / 5.7 / 145 s / 0.85 / 455 · 103 **10%** / 3.9 / 106 s / 0.90 / 350 · 104 0% / 2.3 / 50 s / 1.05 / 135. 시간 초과 0. **PASS 175/175** · 골든 재촬영(40런 · `parties` 불변).
> **손잡이는 안 돌렸다 — 행동 변경 둘이 표를 움직였다.** ①동률 = 칸 순서 → 1번 칸(고유)이 첫 차례에 확정으로 나간다 ②함성 창이 치유의 빛 밑수까지 올린다. 둘 다 위쪽 스테이지를 조금 올리는 방향이고 101 은 그대로다. Phase A(구조)는 40런 지문(`tl` 포함)이 비트 단위로 같았다 — 표가 안 움직인 것이 정상이고, 움직였다면 그게 회귀였다. 목표 대역(101 ≥70% · 102 30~70%)은 여전히 아래 — 재보정은 별도 결정. **이 표가 골든 스냅샷이 찍힌 새 지반이다.**

> **캘리브레이션 재측정 (2026-09-02 — R33 전술 옵션 등급 축 · 손잡이 미조정 · 같은 조건 시드 20)** — 101 **45%** / 7.8 라운드 / 134 s / 전투불능 0.55 / 733 골드 · 102 **15%** / 5.0 / 142 s / 0.85 / 430 · 103 **10%** / 3.5 / 97 s / 0.90 / 333 · 104 0% / 2.5 / 54 s / 1.05 / 146. 시간 초과 0. **PASS 178/178** · 골든 재촬영(`csvHash` 2종 · `balance` 3키 신규 · 40런 전부).
> **손잡이는 안 돌렸다 — 버그가 고쳐지면서 표가 내려갔다.** 09-01 부터 코드가 가족당 마지막 행(= `rare`)만 들고 있어서 **모든 전술 칸이 레어 값으로 돌고 있었다.** 등급 축을 반영하면서 첫 배정이 확정대로 **일반**이 됐고, 일반 값은 옛 단일 값보다 낮다(`opt_march_atk` 3 → 2 · `opt_sin_pair` 7 → 5). 101 이 50% → 45% 로 내려간 것이 그 차이다. 목표 대역(101 ≥70% · 102 30~70%)은 여전히 아래 — 재보정은 별도 결정. ⚠ **이 지반은 임시 풀이다**(tactic_card_design §5-7 — 통째로 교체 예정)이므로 여기서 손잡이를 맞추면 풀 교체 때 다시 어긋난다. **이 표가 골든 스냅샷이 찍힌 새 지반이다.**

> **캘리브레이션 재측정 (2026-09-03 — R35 액티브 출처 고정 · 손잡이 미조정 · 같은 조건 시드 20)** — 101 **50%** / 7.8 라운드 / 119 s / 전투불능 0.50 / 787 골드 · 102 **10%** / 4.8 / 133 s / 0.90 / 352 · 103 **10%** / 4.0 / 108 s / 0.95 / 361 · 104 0% / 2.6 / 58 s / 1.00 / 162. 시간 초과 0. **PASS 179/179** · 골든 재촬영.
> **손잡이는 안 돌렸다 — 액티브 구성이 통째로 바뀌었다.** 종전 「고유 + 직업 행 2」가 「고유 + 무기군 + 전직 1」이 됐다: 직업 행 하나가 빠진 자리에 **무기군 액티브(⚠임시 값)**가 들어왔고, 무기군은 든 무기가 정하므로 같은 직업이라도 칸이 갈린다. 101 이 45% → 50% 로 오른 것은 무기군 액티브가 직업 `priority` 2번 행보다 세게 잡힌 결과로 보인다 — **⚠임시 값이라 이 수치를 근거로 손잡이를 돌리면 안 된다.** 목표 대역(101 ≥70% · 102 30~70%)은 여전히 아래.

### 3-2. 없는 것 (기획 미작성 → 구현 불가)

| 항목 | 기획 의존 |
|---|---|
| **마스터리 T3(반응형)** · 죄종 T2 빈 칸 5 · **전직 3갈래** · ~~무기군 액티브~~(2026-09-03 **⚠임시 8행 발행** — 이름·수치는 여전히 기획 미발행 · R34) · 고유 스킬 풀 · 전직 액티브 45 · **전사 ③** · 무기군/방어구 숙련 노드 | skill_design.md — T1·T2 수치 노드는 08-28 구현(R15 절반). **T3 는 값이 전부 미정이고 발동 빈도에 파워가 종속돼 예산 자가 없다**(§7) · 전직·무기군 테이블 미발행 |
| **전술 리롤의 잠금 비용 곡선** [신설 2026-09-01] | tactic_card_design.md §5-6 — 형태(곱셈/덧셈)도 값도 미정이라 `balance.csv` 키 둘을 발행할 수 없다. **R28 이 여기서 막혀 있다** — 계약과 화면 규칙은 이미 적혔다 |
| **전술 옵션 등급 출현 가중치** [신설 2026-09-02] | tactic_card_design §5-3 — 일반/매직/레어가 각각 얼마나 뜨는지가 미정이다. R33 이 코드를 열려면 값이 있어야 해서 **`tactic_grade_weight_*` 3키를 ⚠제안(70/25/5)으로 발행**했다 — 형태는 상대 가중치로 잡혔고 **값만** 열려 있다 |
| **마스터리 포인트 지급 곡선** | skill_design.md §7 — 「레벨당 몇 점」이 미정이라 `mastery_point_per_level`(⚠제안 1)은 **형태부터 임시**다. 전직 전용 포인트는 총량도 미정 (2026-08-28 등재) |
| 몬스터의 치명·반사·피해 감소·방어 무시 (전부 0 — 영웅 체계의 부분집합) | 정예 특성(elite_trait.csv) 데이터 연결 |
| **도감 스테이지 3 계열 스탯** — 명중 폐지로 `acc_pct` 가 갈 곳을 잃었다. `codexBonus` 는 계속 계산하지만 `computeCombat` 이 읽지 않는다. 도감 탭은 아직 "명중률 +x%"(계열)·"회피율 +2%"(완성 보상)를 **그리고 있고 효과는 없다** | GAME_DESIGN §10 재배정 (1 공격 / 2 체력 / 3 **?** / 4 피해) |
| 건강 계수 (상태이상 회복 속도) | 상태이상 7종 구현 |
| 유니크 영웅 · 유니크 아이템 | hero_design §1 / item_design |
| 크래프트 · 낙인 | item_design |
| **거점 · 파견 · 탐험** (코어 루프의 오프라인 절반) | base_expedition_design §2·§3 — 탐험 상세 미정 |
| **의뢰** (활동이 아니라 **그릇** — 개정 2026-09-01) | base_expedition_design §1-3 — **기존 활동의 여분 회차**로 뜨고 **파티/개인 × 실시간/위임** 두 축으로 갈린다. 종류는 보스/던전 지목 · **약탈** · **보호** · 탐험/파견/수색 의뢰. 등급 사다리는 **명성**(전체 하나 · 패배 시 하락 + 파티 전원 부상). 의뢰 파티는 원정에 안 나간 영웅이라 **원정과 무관**. 못 가는 이유 — **종류 목록 미마감**(개인×실시간 칸이 빈다) · **명성 수치 미발행** · **적 영웅 파티의 생성·장비·드롭 미정**. **보호는 09-03 확정** — 전투가 오프라인에 돌고 리포트에서 재생한다(GAME_DESIGN §9 09-03) (GAME_DESIGN §10 「의뢰 세부」·「의뢰 — 적 영웅 파티」) |
| **도박장** (선술집 안) [신설 2026-09-03] — 화면에 **이름 줄만** 서 있다(SCREEN_DESIGN §8-1). 수치도 버튼도 동작도 없다 | **기획 미확정** — GAME_DESIGN §9 에 결정이 없고 §10 에도 아직 안 올랐다. 논의된 범위는 「선술집 안 · 슬롯머신/뽑기 · **일일 리셋이 아니라 쿨다운**(자동 갱신 폐지와 같은 이유) · 골드를 걸어 골드·재료(장비는 아님)」까지다. 발행된 CSV 키가 **없어서** 화면이 수치를 못 찍는다 |
| 진형 · 타겟팅 · 무기군 Implicit | GAME_DESIGN §10 |
| 죄종 매핑 | sin_mapping.md (첫 SSOT 과제) |
| **지역(챕터) 죄종 드롭 편향** — item_design §1 이 "계승하는 구조"로 규정한 타겟 파밍의 축. 코드는 스테이지 무관 균등 추첨(`item.js build`) | item_design §1 — 규정은 있고 구현이 없다 (2026-08-27 등재) |
| **능력치 성장률 개체차** — hero_design §4-3 은 성장률·상한 **둘 다** 히든이라 하는데 코드는 상한만 개체별이고 성장 확률은 전역 상수 하나 | hero_design §4-3 · 리롤 루프 논의(§4-1-1)의 전제가 절반 비어 있다 (2026-08-27 등재) |
| **HP 재생(`hp_regen`)** — battle_design §8 의 sustain 두 축 중 하나. `combat_stat.csv` 와 캐릭터 시트엔 있는데 접사 풀·계산·적용이 전부 없어 항상 빈 줄이다 (흡혈은 정상 동작) | battle_design §8 — 획득 경로 미정 (2026-08-27 등재) |

### 3-3. 기획 개정 반영 대장 — 2026-08-26 · 08-27

기획서(battle_design §9 · GAME_DESIGN §9)가 확정한 개정과 코드의 대조표. **INTERFACE.md 는 반영된 코드를 기술한다** — 남은 R 을 반영할 때 해당 절을 같이 고친다. 반영 순서는 §5-C — 기획이 정한다.

| # | 기획 | 코드 | 위치 |
|---|---|---|---|
| R1 | **무기 = 밑수** — `+피해%` 는 무기만 곱함, 다른 슬롯 고정 공격력은 더하지 않음 | **반영 완료 2026-08-26** — `atk_flat` 은 무기 전속 접사 | `hero.js computeCombat` |
| R2 | **편차 = 무기 개체 굴림** (드롭 시 1회, 개체에 박힘) · 방어구 고유값도 굴림 | **반영 완료 2026-08-26** — `watk` 소수 2자리 · 타격 편차 삭제 | `item.js build` · `formula.js strike` |
| R3 | **`def_curve_k` 상수** — 감쇠 50% 방어값. 공격자 레벨 무관 | **반영 완료 2026-08-26** — `mitigation(D)` 인자 하나 | `formula.js mitigation` |
| R4 | **피해 감소 = 원천별 곱** | **반영 완료 2026-08-26** — `reductionMult` · 출력은 실효 % 한 숫자 | `formula.js` · `hero.js` |
| R5 | **명중·회피 폐지 → 적중 = f(레벨 차)** · 하한 · 오버레벨 초과 이득 없음 | **반영 완료 2026-08-26** — `hitChance(aLvl, dLvl)` · 두 스탯 전 층 삭제 | `formula.js` · `hero.js` · `ui/mock.js` |
| R6 | **원소 저항 = 상한형 %** (`res_cap_base` + 최대 저항 증가 · `res_cap_absolute` · 하한 없음) | **반영 완료 2026-08-26** — `appliedResist`/`resCap` · `defenseAgainst` 삭제 | `formula.js` |
| R7 | **감각 → 운** — 전투 계수 제거, 드랍률·골드 계수 | **반영 완료 2026-08-26** — 세이브 v2→v3 이관 포함 | `hero.js` · `ui/mock.js STATS` · `state.js` |
| R8 | **몬스터 = 영웅 공통 전투 능력치 체계** | **반영 완료 2026-08-26** — `res` 4원소 객체 통일(INTERFACE §8 항목 11 해소) | `battle.js makeEnemy` |
| R9 | **게임 정의 개정** — 원정 독점 삭제 → 크래프트 제작 · 영웅 획득 창구 = 선술집 하나(명단 + 수색, 수색은 파견 아님 — base_expedition §2-4) · 해고 | **절반 반영 2026-08-30** — **명단은 다 들어갔다**: 리롤 쿨다운(`tavern_refresh_hours`, 오프라인에도 진행) · 쿨다운 중 즉시 리롤은 골드 · **고용한 칸만 비고 나머지 명단은 안 갈린다** · 세이브 v5 에 `tavern {rerolledAt, hired}`. **수색 · 해고 · 크래프트는 미반영** — 수색은 **화면 자리만 잡혔다**(2026-09-01 · §8-1 수색 칸 · 버튼은 안내만 낸다). `tavern_search_hours`·`tavern_search_slots` 는 이제 **화면이 표시용으로 읽지만** `game_logic` 은 여전히 안 읽는다 | `state.js tavernState/tavernReroll/hire` · `ui/app.js renderTavern` · `INTERFACE §2-7·§4` · SCREEN_DESIGN §8 |
| R10 | **성장 SSOT `power_growth_per_level`** — 무기 공격력·영웅 HP·공격/HP flat 접사가 기하 곡선을 탄다 | **반영 완료 2026-08-26** — `formula.growthMult` 하나가 세 곳을 먹인다 | `formula.js` · `item.js` · `hero.js` |
| R11 | **접사 ilvl 스케일링 3분류** (item_design §2-1) — `perIlvl` 은 band 에만 · 저항은 직접 % | **반영 완료 2026-08-26** — `scale: growth\|band\|flat` · 원소 저항 4종·피해 감소 접사 신설 | `data/affix.csv`(08-31 이관 — 발행 당시엔 `ui/mock.js AFFIX_DEFS`) · `item.js rollAffixes` |
| R12 | **레벨 상한 99 · 50 이후 느린 곡선 · 전직 30** (GAME_DESIGN §9 08-26) | **절반 반영 2026-08-30** — **상한은 들어갔다**: `balance.csv:hero_level_cap` 발행(99·fixed) · `grantXp` 가 상한에서 멈추고 `xp=0` 으로 끊는다(상한 뒤 지급은 `null`). **「50 이후 느린 곡선」은 미반영** — 무릎 키는 여전히 미발행이고 곡선 숫자는 캘리브레이션 뒤다 | `hero.js grantXp` · `balance.csv:hero_level_cap` · `INTERFACE §2-4` |
| R13 | ~~**엔드게임 = 티어 재순환 + 무한모드**~~ · 레이드 5인 · ~~챕터 클리어 해금~~ — **09-03 개정** (GAME_DESIGN §9 09-03): 엔드게임 형태는 **미확정으로 되돌아갔고**(가드 = 무한 수직 스케일링 금지 · 첫 데모 이후 판단) 해금 기준도 **챕터 클리어 고정이 아니다**(기본값으로 낮춤) | **미반영 · 착수 불가로 격하** — 스테이지 28개 단일 티어, 해금 없음, 레이드 없음. 기획이 형태를 안 정했으므로 `stage.csv` 티어 축을 **지금 굳히지 않는다** | `stage.csv` 티어 축(보류) · `balance.csv` `raid_party_size` 등 키 미발행 |
| R14 | **죄종 세트효과 폐기 → 전술카드 이관** (GAME_DESIGN §9 08-26) | **반영 완료 2026-08-30** — 이관처가 실동작한다(R23). `item.js` 의 `sins` 는 죄종 **태그 목록** 그대로이고(INTERFACE §2-5), 그것을 **세는 쪽**이 `tactic.js:contextOf` 로 생겼다 | `game_logic/tactic.js` · 캐릭터 탭의 접사 죄종 카운터가 조건 가시성의 전제 (SCREEN_DESIGN §6) |
| R15 | **화면까지 반영 2026-08-28** — 스킬 탭이 `masteryState` 를 그린다(SCREEN_DESIGN §7 개정 · 단계가 위→아래로 쌓인다 · 단계 번호는 화면에 안 쓴다). **액티브 / 패시브 2층 분리** · 마스터리 **T1 공통 3 / T2 죄종 정체성 / T3 반응형** · 직업 마스터리도 티어 구조(전사 T1) (skill_design §1 · §3-1~§3-4) | **절반 반영 2026-08-28** — **수치 패시브(T1·T2)는 실동작**한다: `mastery_node.csv` 22행 · `hero.computeCombat` 이 접사와 같은 채널로 합산 · 세이브 v4 에 랭크·포인트 · 찍기/롤백 전이. **반응 패시브(T3)는 미반영** — 값이 전부 미정이고 발동 빈도에 파워가 종속돼 예산 자가 없다(§7) | `data/mastery_node.csv`(신설) · `hero.js masteryBonus` · `state.js learnMastery/resetMastery` · `INTERFACE §2-4·§2-7·§4`. ⚠ 포인트 지급 곡선이 미확정이라 `mastery_point_per_level` 은 형태부터 임시다 |
| R16 | **전직 = 직업마다 3갈래 택1** · 고른 전직이 **액티브 3** 을 주고 **그중 1개를 찍는다** — 찍은 그 스킬만 칸에 오르고 그 스킬만 트리가 열린다 (skill_design §4 · §4-2 **B안 확정 08-28**) · **마법사 3전직 확정**(§10-1) | **미반영** — `advance_unlock_level` 키만 있고 전직 개념이 코드에 없다 | **액티브 런타임은 있다** — 전직 층만 없음. `balance.csv:advance_unlock_level` · 전직 테이블 **미발행** · 세이브에 전직 필드 신설 필요 |
| R17 | **원소 4종의 성격** — 화염=치유 감소 · 냉기=상태이상 순환 · 전기=큰 분산 · 독 미배정 (battle_design §2-2) | **미반영** — `attack_type` 은 저항 선택에만 쓰인다(`formula.js`). 치유 감소·상태이상·분산 어느 것도 없음 | `formula.js` · `battle.js` · `status_effect.csv` **미발행**. `skill.csv:status` 컬럼은 발행됐고 **코드가 읽지 않는다**(부채 #21). ⚠ 분산·도트는 **rng 소비 수열**을 바꾼다 (INTERFACE rng 순서 계약) |
| R18 | **진형(위치) 폐기 · 타겟팅은 확정 대상** (skill_design §9-1 규칙 5) | **미반영** — 대상 선택 규칙이 문서에 없다. 코드는 `battle.js` 가 자체 규칙으로 고른다 | `battle.js` 대상 선택 — 무작위(임시) **위에 도발(taunt) 창이 얹혀 있다**(INTERFACE §2-6). 확정 시 둘을 함께 정리. 무기군 Implicit·다중타격 분배·`crushing_blow`·`counter_dmg` 보류가 함께 풀린다 |
| R19 | **`skill.csv` 15행이 08-26 판이라 기획과 어긋난다** (skill_design §9-2 · §9-5) | **반영 완료 2026-08-28** — 08-27 판 14행으로 재작성(전사 ③ 만 미정) · 로더(`FILES`) 등록 · `skill.js` 신설 · `battle.js` 가 실제로 시전한다 | `src/data/skill.csv` · `game_logic/skill.js` · `battle.js`. 관전 화면의 목업(부채 #13)은 아직 남아 있다 |

| R20 | **드롭 파이프라인** (item_design §1 확정 08-27) — 처치당 최대 1개 · ① 드롭 여부(등급 + 드랍률) → ② 부위 → ③ ilvl(dlvl + **등급**) → ④ 희귀도(등급 + **매직찬스**) → ⑤ 접사 → ⑥ 개체 굴림 | **부분 반영 2026-08-28** — ①만 반영(판정 1회 · `spawn_grade.drop_chance_mult` 가 확률 배율). ③의 등급 반영과 ④의 매직찬스는 **미반영** — ilvl 은 `dlvl + [0..drop_ilvl_spread)` · 희귀도는 ilvl·등급 무관 고정 가중치 | `battle.js onKill` · `item.js rollDrop/rollRarity` · `balance.csv` 매직찬스 키 **미발행** |
| R21 | **스킬 태그 13종** (skill_design §11 확정 08-28) — 4 대분류 · `tags` 최대 2 · `광역`·`단일`·`다단히트`는 `target`·`hits` 에서 파생 | **반영 완료 2026-08-28** — `skill.js` 가 `tags` 를 정규화·검증하고(`TAGS` 10종 사전 · 상한 2 · 중복 금지 · **파생 태그를 적으면 throw**) 파생 3종을 `def.derived` 로 계산한다. `tagsOf(def)` 가 세는 쪽의 유일한 입구. **전투 로직은 여전히 안 읽는다** — 소비자는 전술카드·변형 노드·화면 | `game_logic/skill.js` · `INTERFACE §2-8`. 화면 쪽(필터·툴팁)은 아직 없다 → `/ui` |

| R22 | **스킬 3층 확정 — [죄종][직업] = 패시브 / [전직] = 액티브** (skill_design §1-4 · §4 확정 08-28). 액티브를 주는 층은 전직 하나이고, `skill.csv` 의 14행은 「직업 액티브」가 아니라 **08-26 판 전직 액티브의 프로토타입 임시분**이다(§9 명칭 교정) | **문서만 반영 · 코드/CSV 미반영** — `skill.csv:owner_kind` 값이 전부 `job` 이고 `skill.js:activesFor` 가 `ownerKind === 'job' && ownerId === hero.cls` 로 배정한다(INTERFACE §330·§342). **이름은 전직인데 배정은 직업**인 상태 | `data/skill.csv` · `game_logic/skill.js` · `INTERFACE.md`. ⚠ `job` → `advance` 로 바꾸려면 `owner_id` 가 **직업 id(`warrior`)에서 갈래 id(`berserker`)로** 바뀌어야 하는데 **갈래 테이블이 미발행**이다. 전직 층 구현(R16)과 **한 묶음** — 그전엔 `job` 을 유지한다 |

| R23 | **파티 전술 — 획득이 아니라 「칸 해금 + 리롤」** (GAME_DESIGN §9 08-30 · tactic_card_design §5) — 칸은 로스터 합산 레벨 · 내용은 재화 리롤 · 소유자는 파티 · 조건은 편성 확정 어휘만 · 효과는 기존 채널 | **반영 완료 2026-08-30** — `tactic.js` 신설(무상태 · 조건 어휘 9종 · 첫 배정은 섞기라 리롤이 다른 칸을 안 흔든다) · `tactic_slot.csv`·`tactic_option.csv` 발행 · `state.tacticState/tacticBonus/rerollTactic` · `computeCombat` 4번째 인자 · **세이브 v6** · **연구 탭**(SCREEN_DESIGN §13) | `game_logic/tactic.js` · `data/tactic_*.csv` · `state.js` · `hero.js` · `ui/app.js renderResearch` · `INTERFACE §2-9·§4`. ⚠ **리롤 재화는 골드다** — item_design §5-1 은 낙인으로 잡아 뒀지만 낙인 공급 경로가 코드에 없다(부채 #26) |

| R24 | **의뢰 신설** (GAME_DESIGN §9 08-31 · base_expedition_design §1-3) — 특정 보스/던전 지정 → 즉시 전장 오픈 → 파티 편성. 원정과 **동시에** 실시간 진행, 관전은 유저가 보고 있는 탭이 정함. 원정 파티와 완전히 분리된 인원 | **문서·안내만 반영 2026-08-31** — 기능은 여전히 없다. 바뀐 것은 ①SCREEN_DESIGN §1 의 「전투 창구는 하나」 폐기(원정·의뢰 둘 · 관전은 보고 있는 탭이 정한다) ②§11 에 의뢰 탭 행 신설 ③도움말(원정 절)이 「의뢰 미착수 + 파티 완전 분리」를 말한다. **코드는 못 연다** — `battle.js` 가 전투 하나를 가정하고 `state.run` 도 하나뿐이라, 둘째 채널은 **계약 변경이 먼저**다(전투 인스턴스 복수화). 쿨다운·드랍 테이블 재사용·첫 해금은 기획 미정. **[2026-09-01 범위 확대]** 의뢰가 전투 채널이 아니라 **그릇**이 됐다(전투형 · 비전투형(오프라인) · 약탈 — GAME_DESIGN §9 09-01). 이 행의 계약 변경(전투 인스턴스 복수화)은 **전투형에만** 걸리고 비전투형은 파견 쪽 계약을 탄다. 형태 목록·공통 불변식이 미정이라 코드는 여전히 못 연다 | 새 탭(의뢰) · `battle.js`/`state.js` 의 전투 인스턴스 복수화 · `balance.csv` 의뢰 쿨다운 키 미발행 |
| R25 | **강화 재정의** (GAME_DESIGN §9 08-31 · item_design §1) — 강화가 **접사 증폭**에서 **베이스 능력치(+9강) 성장 + 3강마다 옵션 값 상승(재굴림 없음)** 으로 개정 | **반영 완료 2026-08-31** — `item.js` 에 `upgradeMax`·`upgradeCost`·`upgrade`·`effective` · `state.js` 에 `upgradeState`·`upgradeItem`(결과 코드 `maxUp` 신설) · 세이브 **v7**(v6→v7 이관: `up=0`) · 가방의 **강화 모드**. **두 갈래가 서로 다르게 남는다** — 베이스는 `up` 으로 파생(개체 굴림 보존 · 단계마다 반올림이 안 쌓인다), 3강마다 오르는 접사 값은 박는다(랜덤이라 다시 못 만든다). ⚠ **부위 제한이 기획에 없어** 목걸이·반지도 강화된다(옵션 갈래만 받는다) — 확정 필요 | `data/balance.csv` 6키(`equip_upgrade_max`·`_option_interval`·`_base_pct`·`_option_pct`·`_gold_base`·`_gold_growth` — 뒤 4개는 손잡이) · 단정 13 · INTERFACE §2-5·§2-7·§3·§4·§5-1·§5-2 |
| R26 | **파견처 개편 — 연구소 삭제 · 채집 신설** (GAME_DESIGN §9 08-31 · base_expedition_design §2-2) — 파견처 5(선술집·상단·제련소·광산·채집). 지능은 파견처 배정을 잃음. 연구 탭이 "연구"(스킬·레시피 해금, 채집 재료 소모, 영웅 파견 없음) + "파티전술"(기존, 이름 정정) 두 섹션으로 분리 | **잔재 정리만 2026-08-31** — 거점·파견 화면 자체가 없어 개편할 화면이 없다(§3-2). 닫은 것은 **연구소가 남아 있던 자리 셋**이다: ①`hero_attribute.csv` 의 `int` 행 — `dispatch` 를 `lab` → `-` 로 내리고 「연구소(기술·레시피 해금)」 설명을 「2026-08-31 삭제로 배정 상실」로 교체 ②도움말의 파견처 목록 — 영입·교역·**제련·채광·채집** + 「연구는 파견이 아니다」 + 중도 회수(§3-2) ③SCREEN_DESIGN §1 지도·§11 표 · 연구 탭이 「연구」+「파티전술」 두 섹션임을 도움말이 말한다. **연구 섹션의 화면은 없다** — 비용 곡선·해금 순서가 미정이라 그릴 것이 없다 | 거점 화면 신설 시 파견처 5종 반영 · 채집 산출 재료 CSV 키 미발행 · 연구 섹션은 기획 확정 후 |
| R27 | **한손 개념 폐지 · 보조 슬롯 폐지 · 무기군 재편** (2026-09-01 사용자 확정) — 전 무기 양손 · 보조(offhand) 부위 삭제(슬롯 9 → 8 · 부위 8 → 7) · 한손검 삭제 · 창이 전사 → 기사 · 완드 → 오브. **본편 5직업 × 무기 2개**로 통일되고, 같은 직업의 두 무기는 행동 주기로 갈린다 | **반영 완료 2026-09-01** — `weapon_group.csv` 재작성(11 → 10행 · `hands` 컬럼 삭제) · `equip_slot.csv` 8행 · `item_base.csv` 24행(보조 베이스 4 삭제) · `affix.csv` 9행에서 `offhand` 제거 · `balance.csv` `two_hand_atk_mult` 폐기 + `weapon_atk_base` 2.3 → **3.34**(1.45 흡수) · `tactic_option.csv` `opt_two_hand` 삭제(23 → 22) · `item.js`(보조 ×1.5 · `twoHanded` · 양손 배율 · `canEquip` 인자 3 → 2) · `state.js`(양손↔보조 배타 삭제 · **세이브 v8** + v7→v8 이관) · `tactic.js`(조건 어휘 9 → 8) · `ui/data.js`·`mock.js`(페이퍼돌 3×4 → 3×3)·`app.js`·`i18n.js`(죽은 키 3 삭제 · `eq.twoHand` → `eq.slots`)·`style.css`(`.pd-cell.locked`). **PASS 157/157** · 골든 재촬영 | ⚠ **SCREEN_DESIGN §6 페이퍼돌 갱신은 `/ui` 몫**(칸 배치가 3행이 됐다) · **전술카드 `opt_two_hand` 삭제는 새 공백이 아니다** — tactic_card_design §5-4(08-31)가 이미 「무기 종류 조건 폐기」를 확정해 뒀고, 조건이 항상 참이 되면서 집행이 앞당겨졌을 뿐이다(§5-3 해소 행) · ⚠ **보조 슬롯 자리에 무엇을 넣을지 미정**(부적·문장 등 — 사용자: 「나중에 필요하면 추가」) · ⚠ 캘리브레이션 재보정은 별도 결정 |
| R28 | **전술 리롤 = 전체 굴리기 + 잠금** (tactic_card_design §5-6 개정 2026-09-01 사용자 지시) — 칸별 리롤 폐기 · 잠근 칸은 안 굴러가고 **잠금 개수가 리롤 비용을 정한다** · 잠그기는 무료 · 전부 잠그면 거절 | **미반영 — 문서만 2026-09-01.** 계약(INTERFACE §2-7·§3·§4·§5-2)과 화면 규칙(SCREEN_DESIGN §13-2)은 적혔고 **코드는 아직 옛 칸별 리롤**이다. ⚠ **비용 곡선이 기획 미정**이라 착수 못 한다(§5-6 — 곱셈/덧셈 형태부터) | `game_logic/tactic.js`(비용 파서 제거 + 곡선) · `state.js`(`tacticState`·`rerollTactic` 재작성 · `toggleTacticLock` 신설 · **세이브 v10 → v11** — 09-01 고유 스킬 소급 이관(R30)이 v9 를, 09-02 등급 축(R33)이 v10 을 먼저 썼다) · `data/tactic_slot.csv`(`reroll_cost_gold` 컬럼 삭제) · `data/balance.csv`(키 2 발행) · `dev/test.js` · `ui/app.js`+`i18n`+`style` · **골든 재촬영**(CSV 해시 + rng 순서 둘 다 변동) |
| R29 | **무기 통합옵션 물리/마법 분리 · 시기→강타** (GAME_DESIGN §9 2026-09-01 사용자 확정 · item_design §1) — 무기 통합옵션이 `weapon_group.csv:damage_kind` 로 갈리고 신규 4종(방어력 감소·원소 저항 감소·원소 피해 추가 ×4·원소별 피해 증가 ×4) + vs 등급(정예·보스) 추가 피해 · 방어구 통합옵션에서 회피율 삭제 · 죄종 「시기」 정체성이 「강타」로 교체(5칸 전부 공백) | **미반영 — 문서만 2026-09-01.** 지금 착수 가능한 것은 **한 줄뿐**이다 — `affix.csv` 의 `gold_find`·`item_find` `slots` 에 `weapon` 추가(현재 무기가 빠져 있는데 통합옵션 표는 무기에 있다고 적는다 — 문서↔CSV 불일치). 나머지는 전부 **stat_id 미발행**이라 못 간다: 방어력 감소(파티 디버프) · 원소 피해 추가 · 원소별 피해 증가 · vs 등급 추가피해가 `combat_stat.csv` 에 없다. ⚠ **원소 피해 추가는 stat 발행 전에 공식 개정이 선행**한다(별개 채널 + 성장 축 — GAME_DESIGN §10) · ⚠ `vs_status_damage` 는 발행돼 있으나 자기 설명이 「부여 수단이 생기기 전까지 접사 풀에 넣지 않는다」고 막아 뒀다 | `data/affix.csv`(즉시 2행) · `data/combat_stat.csv`(신규 stat 4~11) · `game_logic/item.js`(무기 접사 풀을 `damage_kind` 로 가르는 분기 — **지금 `affix.csv:slots` 는 부위만 알고 무기군을 모른다**. 스키마 확장 필요) · `battle.js`(지속 디버프 = **새 상태 축**) · `INTERFACE §2-5` |
| R30 | **레어 고유 스킬 — 생성 시 현재 풀에서 랜덤 1** (GAME_DESIGN §9 2026-09-01 사용자 지시 · hero_design §1 · skill_design §9-0 개정) — 영웅이 만들어질 때 `skill.csv` 전 행에서 균등 1개를 고유 스킬로 굴린다 · 직업 비종속 · 생성 후 불변 · 액티브 1번 칸 · 나머지 칸은 직업 행 채움(중복 제거 · `active_slots` 상한) | **반영 완료 2026-09-01** — `hero.rollInnate` 신설 · `rollHero` 가 `rollCaps` 뒤 1회 굴려 `innate` 에 박는다 · `skill.activesFor` 재작성 · `buildSystems` 가 skill 을 hero 보다 먼저 만들어 `skillPool`(id 목록) 주입 · **세이브 v9**(v8→v9: 옛 영웅은 `seed ^ 0x5C11` 스트림으로 소급) · 캐릭터 탭 카드·스킬 창에 「고유」 라벨(`sk.innate`) · 단정 157 → **160** · **골든 재촬영**(`balance` 해시 · `meta.parties` 30명 전원 · 40런 전부) · **PASS 160/160**. ⚠ **캘리브레이션이 움직였다** — 101 70% → **50%** · 102 0% → **10%**(손잡이 무변동 · §3-1). ⚠ **임시 풀의 구멍** — 물리 직업이 `pri_heal` 을 고유로 뽑으면 회복량 0 인데 차례는 먹는다(부채 #28) · 자기 직업 행을 뽑은 전사는 액티브 2칸(전사 ③ 미정과 겹침) · `simulate: 다단타(arc_rapid)` 단정은 파티 궁수의 연사가 상한에서 잘리게 되어 `clsUnits('archer')` 로 직업 액티브를 손으로 싣는다 | `game_logic/hero.js` · `skill.js` · `state.js` · `ui/data.js` · `ui/app.js`+`i18n`+`style` · `data/balance.csv`(`active_slots` 설명만) · `dev/golden.js`·`golden.json` · `INTERFACE §1·§2-4·§2-7·§2-8·§4·§5-1~§5-3·§5-5` · SCREEN_DESIGN §6·§7. ⚠ R28 의 계획 세이브 버전은 v9 → **v10** 으로 밀렸다 |
| R31 | **스킬 구조 재편** (2026-09-01 사용자 지시 — 구조 리뷰 권장 1·2·3) — 배정 단위 인스턴스 · 사건 훅 · 종류 등록표 · 런타임 분리 · `makeUnit` · CSV 이관(아이콘·설명·고유 풀·태그 어휘·쿨 바닥) · 동률 = 칸 순서(battle_design §5) · 회복 밑수 = 버프 괄호(battle_design §9-2) | **반영 완료 2026-09-01** — Phase 0(`partyMembers` 정의 전달 버그 수정 · 골든 `tl`) → **Phase A 결과 불변**(골든 4단정 재촬영 없이 초록) → Phase B(행동 2 + CSV · 재촬영 1). `skill_effects.js`·`skill_runtime.js` 신설 · `activesFor → [{id, source}]` · `resolve` · `reactions`/훅 5종 · 검증 강화 · `skill.csv` 21 → 25컬럼 · `skill_tag.csv` 13행 · `balance.csv` 107키(`skill_cd_floor_mult`) · `mock.js` `SKILL_DISPLAY`·`SKILL_TAGS` 삭제 · `data.js:skillTagName` · 단정 160 → **175** · **PASS 175/175**. ⚠ `hero.skillOrder` 는 선택 필드 자리만(UI 없음) · `reactions` 소비자 0 | `game_logic/skill.js`·`skill_effects.js`·`skill_runtime.js`·`battle.js`·`state.js` · `ui/data.js`·`app.js`·`mock.js` · `data/skill.csv`·`skill_tag.csv`·`balance.csv` · `dev/golden.js`·`golden.json`·`test.js` · INTERFACE §1·§2-4·§2-6·§2-7·§2-8·§2-9·§2-11·§2-12·§5-2·§5-3·§5-5·§7·§8 · skill_design §9-5·§11 · SCREEN_DESIGN §4-2 · data/README · game_logic/README · `.claude/skills/client/layer_map.md` |
| R32 | **의뢰 확정 4건** (GAME_DESIGN §9 2026-09-01 사용자 확정 · base_expedition_design §1-3) — ①의뢰 = **기존 활동의 여분 회차**, 분류는 **파티/개인 × 실시간/위임** ②**약탈**(파티×실시간)·**보호**(파티×위임, 표국) 신설 — **적이 몬스터가 아니라 진짜 영웅 파티** ③**명성** 신설(전체 하나 · 오르면 난이도·보상↑ · 패배 시 하락 + 파티 전원 부상) ④의뢰는 **원정에 안 나간 영웅**이 가므로 원정과 무관 | **문서만 반영 2026-09-01** — 코드는 못 연다. R24 가 막힌 이유(전투 인스턴스 하나 가정)에 더해 ①**적 영웅 파티**가 새 계약이다 — 지금 전투는 영웅 파티 vs `monster.csv` 행이고, 적으로 영웅을 세우려면 생성·장비 굴림·전술 사용 여부가 먼저 정해져야 한다 ②**명성**은 세이브 스키마 신규 필드 + `balance.csv` 키 미발행 ③위임형 전투(보호)의 계약 — **09-03 확정**으로 방향은 정해졌다(오프라인에 돌고 리포트에서 재생 · GAME_DESIGN §9 09-03). 오프라인 전투 시뮬을 언제 어떻게 굴릴지가 새 계약 | `battle.js` 의 적 진영을 몬스터 전용에서 일반화 · 영웅 생성기(`hero.js`) 를 적 파티 생성에 재사용 · `state.js` 명성 필드 + 세이브 버전 · `balance.csv` 명성 키 미발행 · 새 탭(의뢰) |

| R33 | **전술 옵션 등급 축 — `(option_id, grade)` 복합키** (tactic_card_design §5-5 확정 2026-09-01) — 등급 3단(일반·매직·레어)은 **값만** 가른다 · **리롤은 옵션과 등급을 같이 굴린다** · **첫 배정은 일반 고정** · **중복 방지는 가족(`option_id`) 기준** | **반영 완료 2026-09-02** — ⚠ **착수 시점에 프로토타입이 빨간불이었다**(FAIL 171/175): 09-01 에 CSV 만 66행으로 나가고 코드가 `option_id` 를 유일 키로 봐서 ①`byId` 가 가족당 **마지막 행 = 레어**만 남기고 ②`list` 66개의 중복 id 때문에 **같은 가족이 두 칸**에 들어갔다(`opt_march_def` ×2). `tactic.js` 로더를 가족/등급 두 축으로 재작성(3등급 완비 · 가족 안 조건·stat 동일 · 값 오름차순을 **로드 시 검증**) · `optionOf`·`families`·`familyIds`·`GRADES` export · `initialAssign` 이 `{id, grade:'common'}` · `pick` 이 **rng 2회**(가족 → 등급 가중 추첨) · `state.js` 슬롯이 `{id, grade}` + **세이브 v10**(v9 문자열 → `common`) · `balance.csv` **가중치 3키 ⚠제안 발행**(70/25/5) · `dev/golden.js` 지문에 등급 · 단정 175 → **178** · **PASS 178/178**. **폐기 확정 어휘 2종도 함께 정리** — `party_size`·`damage_kind` 는 §5-4 가 폐기했고 09-01 에 CSV 에서 먼저 빠져 어휘만 코드에 남아 있었다(어휘 8 → 6 · `contextOf` 의 무기 피해 종류 집계와 `weaponGroups` 주입 삭제 · `ui/app.js` 죽은 분기 · `i18n` 죽은 키 2 · `mock.js:DAMAGE_KINDS` 삭제) | `game_logic/tactic.js`·`state.js` · `ui/data.js`·`app.js`·`i18n.js`·`mock.js` · `data/balance.csv` · `dev/test.js`·`golden.js`·`golden.json` · INTERFACE §2-7·§2-9·§4·§5-2·§5-3·§5-5 · tactic_card_design §5-3·§5-7 · src/dev/README. ⚠ **화면에 등급이 아직 안 보인다** — SCREEN_DESIGN §13-2 를 먼저 고쳐야 하는 자리라 `/ui` 몫이다. 지금은 같은 조건·같은 축의 칸이 값만 다르게 보인다(§4 「안 보이면 함정」) · ⚠ **R28(전체 리롤 + 잠금)의 계획 세이브 버전은 v10 → v11 로 밀렸다** |
| R34 | **강화 두 갈래 — `+`강화 / 옵션강화** (GAME_DESIGN §9 2026-09-03 사용자 확정 · item_design §1 · base_expedition_design §2-5) — 지금 코드(R25)는 `+`강화 한 갈래이고 **3·6·9강에서 옵션이 자동으로 오른다**. 이 자동 상승을 떼어 **별개 행위**로 만든다 · **재굴림 금지 불변**(값만) · **실패·파괴·하락 없음**(현행과 같다) · 자리는 **제련소**(지금은 캐릭터 탭 가방의 강화 모드 — SCREEN_DESIGN §6) | **부분 반영 2026-09-03** — 화면(SCREEN_DESIGN §8-2)이 서고 **`+`강화가 제련소에서 실제로 돈다**(`game.upgradeState`/`upgradeItem` 재사용 · 새 로직 0). 캐릭터 탭의 강화 모드 토글은 같은 작업에서 **삭제**했다. **로직은 아직 한 줄도 안 바뀌었다** — 3·6·9강 자동 상승은 그대로 살아 있고 옵션강화는 버튼만 있다. 기획 미확정 넷(비용 자원 · 대상 · 상한 · 비용 곡선)이 선행이라 CSV 를 앞질러 굳히지 않는다 | 착수 시 ①`balance.csv` 키 발행 + 골든 `meta.balance` 재촬영 ②`item.js` 의 3·6·9 계단 분리 ③세이브 스키마 — 옵션강화 횟수를 아이템에 기록해야 한다(현행은 `+n` 하나) ④화면은 **구현됨**(§8-2) — 남은 화면 일은 **옵션강화 · 제작 · 배치**이고 셋 다 로직이 선행이다. 화면이 지금 못 그리는 둘도 여기 걸려 있다: **`upgradeState` 가 다음 베이스 값을 안 준다**(렌더러는 공식을 못 갖는다 — ui 원칙 2) · **옵션의 「올린 횟수」를 세이브가 안 든다** |
| R36 | **치료 타이머 폐기 · 귀환 룰 개정** (GAME_DESIGN §9 2026-09-03 사용자 확정 · base_expedition_design §1-1) — 전투불능은 **그 출정 동안 아웃**(남은 라운드 + 이어지는 반복 런)이고 **마을로 돌아오면 전원 회복**한다 · 귀환 룰은 **전멸일 때만** | **반영 완료 2026-09-03** — `battle.js` 에서 `retreat` 삭제(전멸만) · `state.js` `isOut`/`returnToTown`/`activeParty` 신설, `isInjured`/`tickInjuries` 삭제 · `partyUnits(state, uids?)` · 리포트에 `party`·`outTotal` · **세이브 v11**(hero `injuredUntil` 삭제 · `run.downed` 신설) · `balance.csv:injury_minutes` 폐기 · UI 표시 교체(§8 아래 치료 표시 절) · 단정 **PASS 182/182**(178 → 182 · 출정 아웃 3 · 귀환 룰 재작성 1) · **골든 40런 재촬영**(귀환 룰이 결과를 바꾼다 — 회귀 아님) | ⚠ 남은 것은 기획 쪽이다 — 「출정의 브레이크가 전멸 하나뿐인가」(GAME_DESIGN §10). 파견이 붙으면 「부상 중 파견 불가」가 없어진 것이 로테이션에 어떻게 작용하는지 재확인 |
| R37 | **상단 = 기본상단 + 특수상단 · 파견처 순서 개정** (GAME_DESIGN §9 2026-09-03 사용자 확정 · base_expedition_design §2-6) — 상단이 상점을 겸하되 **장비는 안 판다** · 파견처 순서에서 상단을 **제련소 오른쪽**으로 | **화면만 반영 2026-09-03** — `POSTS` 순서 변경(선술집·제련소·상단·광산·채집·탐험) · `tradePanel` 신설(세로 2단) · `mock.js:TRADE` · i18n `td.*` 7키 · `?dev=trade` · SCREEN_DESIGN §8-3. **`game_logic` 은 한 줄도 안 바꿨다** — 방문 시계·재고 굴림·구매 정산이 전부 미구현이다 (부채 #29) | 기획 수치가 확정되면 ①재고·가격을 CSV 로 ②방문 상태를 `game_logic` 상태 함수로 ③구매 정산(골드 → 자원)까지. ⚠ **되팔기는 분해와 자리가 겹친다**(§10) — 열기 전에 기획이 먼저 답해야 한다 |

| R35 | **액티브 3칸 = 출처 고정** (skill_design §2 확정 · 사용자 지시 2026-09-03) — 배운 것 중 셋을 고르는 게 아니라 **출처가 셋이고 각각 하나씩** 준다: 고유(영웅에 박힘) · 무기군(**든 무기가 정한다**) · 전직(찍은 하나) | **구조 반영 완료 2026-09-03 · 데이터는 절반 ⚠임시** — `activesFor(hero, ctx)` 재작성(칸을 출처가 정하고 그 순서가 칸 번호 · 빈 출처는 자리를 안 남긴다) · `state.weaponGroupOf` 신설·export(장비를 아는 층이 무기군을 넘긴다 — `skill.js` 는 아이템을 모른다) · `skill.csv` **14 → 22행**(`owner_kind` job → **advance** 명칭 교정 + **무기군 8행 ⚠임시 발행** · 고유 풀은 `innate_pool=1` 인 전직 임시분 14 그대로라 굴림 결과 불변) · 화면은 칸을 **출처로** 배치하고 빈 사유를 찍는다(`ACTIVE_SOURCES` · `sk.src.*`·`sk.empty*` 4키) · 단정 178 → **179** · **PASS 179/179** · 골든 재촬영. ⚠ **캘리브레이션이 움직였다** — 101 45% → **50%**(§3-1). ⚠ **전직 칸이 직업당 하나로 굳었다** — `priority` 2·3 스킬(치유의 빛·연사·도발)은 배정으로 안 나가고 검증만 킷을 손으로 싣는다(`clsUnits`) | `game_logic/skill.js`·`state.js` · `ui/app.js`·`i18n.js` · `data/skill.csv` · `dev/test.js`·`golden.json` · INTERFACE §2-4·§2-7·§2-8 · skill_design §9-0 · SCREEN_DESIGN §6·§7. **남은 것 둘** — ①무기군 8종의 이름·수치가 기획 미발행(§6 이 수량만 센다)이라 전부 자리채움이다 ②**전직 시스템은 여전히 없다**(R16) — 갈래 택1·찍기·롤백이 오면 `activesFor` 의 전직 한 줄이 바뀐다 |
| R38 | **크래프트 경제 백지화 — 가루 · 광석 삭제** (GAME_DESIGN §9 2026-09-03 사용자 확정 · item_design §5-3 · base_expedition_design §2-5) — 제작·강화가 먹는 재료와 처치·분해가 뱉는 재료를 **정한 적이 없다**고 판정하고 문서에서 전부 걷어냈다. 남은 확정은 골드(`+`강화) · 낙인(죄종 지정) · 몬스터 카드(도감) 셋뿐 | **미반영 — 코드가 기획을 앞서 있다.** 가루가 지급만 되고 **소모처가 0** 인 채로 돈다: 정예·보스 처치 드롭 · 분해 산출 · 초기 지급. 기획이 백지라 **지금 지울지 남길지도 기획 결정**이다 — 자원 표(DESIGN_ROADMAP 2단계)가 서기 전에는 손대지 않는다. 손댈 때 함께 볼 것 — 세이브 스키마(`state.resources.dust`) · 단정 · 분해 UI | `game_logic/battle.js`(처치 지급) · `item.js:salvageDust` · `state.js`(`resources.dust`) · `data/balance.csv`(`start_dust`·`dust_elite`·`dust_boss`·`salvage_dust_magic`·`salvage_dust_rare`) · `dev/test.js` · INTERFACE §2-6 `salvage` · item_design §5-3 |

> **CSV·코드 반영 완료** — CSV 는 08-26 저녁(`balance.csv` 신설 7·삭제 3 · `monster.csv` 21컬럼 · `combat_stat.csv` · `hero_attribute.csv` sen→luck · `spawn_grade.csv` res_add · `equipment_option_override.csv`), 코드는 같은 날 R1~R8·R10·R11 로 따라붙었다. 삭제된 키·컬럼을 참조하는 지점은 남아 있지 않다.

R1~R8·R10·R11 은 한 묶음이었다 — **골든 스냅샷(§5-A #4)은 이제 찍을 수 있다.**

---

## 4. 기술 부채 · 결함

2026-08-26 코드 인벤토리에서 확인. 표시 없는 항목은 **아직 고치지 않았다** — 수정은 별도 작업으로.

| # | 종류 | 내용 | 위치 | 영향 | 조치 |
|---|---|---|---|---|---|
| 1 | ~~버그~~ **수정됨 2026-08-26** | 공격자 필드 `dmgBonus` ↔ `strike` 가 읽는 `bonusPct` 불일치 → 도감 피해 보정(`dmg_pct`) 미적용 | `battle.js` · `formula.js` | — | 필드명을 `bonusPct` 로 통일. **회귀 단정**: 같은 시드로 `bonusPct` 0 vs 50 의 피해가 달라야 한다 |
| 2 | ~~버그~~ → **기획 공백 2026-08-26** | `codexBonus` 가 내는 `acc_pct` 를 읽는 곳이 없다 | `state.js` · `hero.js` | 도감 스테이지 3 계열 보정 무효 (도감 탭에는 계속 표시된다) | 코드 쪽은 끝 — `codexBonus` 누적 키를 `statByNum` 에서 일반화해 하드코딩을 없앴다. 남은 것은 **계열 재배정**(§3-2 · GAME_DESIGN §10) |
| 3 | ~~경계 위반~~ **해소됨 2026-08-30** | 실효 쿨 공식(battle_design §6)이 렌더러에만 있었다 | `ui/app.js` · `ui/tip.js` | — | 두 사본을 `SYS.formula.effectiveCd` 로 갈아끼웠다. 공식은 이제 `game_logic/formula.js` 한 곳뿐이다 (INTERFACE §2-3 · §9) |
| 4 | 경계 위반 (경미) | 예상 소요 시간 집계가 렌더러에 | `ui/app.js:77` | 표시 전용이라 낮음 | `battle.js` 에 `stageTimeTarget(stage)` |
| 5 | **이식 차단** (축소 08-31) | game_logic 주입 데이터가 `ui/mock.js` 에 잔류 — **3항목**(`SINS`·`SIN_TRAITS`·`COMMON_TRAITS`). 2026-08-28 12→9 · **2026-08-31 M7 이관으로 9→3** (INTERFACE §7) | `ui/mock.js` | UI 폐기 = 로직 굶음 | **남은 셋은 코드가 못 닫는다** — 전부 죄종 매핑(`sin_mapping.md`, GAME_DESIGN §10 첫 SSOT 과제) 하나에 걸려 있고 `SIN_TRAITS`/`COMMON_TRAITS` 는 계승 `elite_trait.csv` 연결과 한 묶음이다. 기획이 열어 줘야 §5-B 가 닫힌다 |
| 6 | 무음 실패 | 재생기가 모르는 유닛 키·이벤트를 조용히 무시 | `ui/battle.js:apply` | 타임라인 계약 위반을 못 잡음 | 개발 모드에서 `console.warn` |
| 7 | ~~계약 취약~~ **해소됨 2026-08-31** (**같은 날 2차 보강 — 사각지대를 메웠다**) | rng 소비 순서를 강제하는 테스트가 없어 리팩터링이 곧 회귀였다 | `dev/test.js` · `dev/golden.js` | — | **골든 시드 스냅샷** — `dev/golden.js`(생성·대조) · `dev/golden.json`(40런 + 입력 지문). 단정 **4개**가 **입력 → 출력 순서로** 대조한다: ① CSV 22종 원문 해시(어느 **파일**이 바뀌었는지) ② `balance.csv` **전 키**(`키: 옛값 → 새값`) ③ 시드 10개의 **시작 파티**(영웅 생성 + 시작 무기) ④ 40런 지문. 불일치는 `40/40 런 불일치` 요약 뒤에 `[seed 2 stage 101] events: 1168 → 836` 처럼 **런·필드 단위로** 찍힌다. 계약은 INTERFACE §5-5. ⚠ 스냅샷은 **관찰 도구**다 — 의도한 변경이 아닌데 깨졌으면 지문을 갈아 끼우지 말고 회귀를 찾는다 |
| 7-a | ~~그물 구멍~~ **해소됨 2026-08-31 (2차)** | 08-31 1차 스냅샷이 **실측으로 못 잡던 것들** — `grantXp` 성장 전체(40런 레벨업 120회·rng 1,454회. `hero_xp_base` 80→10 도 지문 무변화) · 영웅 이름/특성 풀 행 순서 · 정예 죄종·공통 특성 · 시작 무기(`item.build` 110회 중 30회) · `balance.csv` 의 15+ 키(지문은 깨지는데 손잡이 대조가 "같다"고 통과시켜 **회귀로 오진**시켰다) · 어느 CSV 가 바뀌었는지. 게다가 지문 생성이 `check()` **밖**이라 던지면 **나머지 140 단정 결과가 통째로 사라졌다** | `dev/golden.js` · `dev/test.js` · `ui/data.js` | — | 지문 생성을 `check()` 안으로(④가 던져도 ①②③과 나머지가 산다) · 런 지문에 `grew`(정산 **후** 레벨/능력치/포인트 **정수 합**) · `kills` · `casts` · `elites` 추가 · `tactics` 를 **열린 칸 전체**로 · `meta.parties`/`meta.balance`/`meta.csvHash` 신설(`D.csvText` 로 원문 보관) · 요약(`n/40 런 불일치`)을 맨 앞에 · **키 합집합** 순회(필드를 추가하고 재촬영을 잊어도 미검증으로 안 남는다). **네 가지를 일부러 깨뜨려 전부 잡히는 것까지 확인**했다(src/dev/README.md 표). 캘리브레이션 4행 불변 |
| 8 | ~~타입 이원성~~ **해소됨 2026-08-26** | `res` 가 몬스터는 숫자·영웅은 객체였다 | `battle.js makeEnemy` | — | 몬스터도 4원소 객체를 든다(R8). 이식 시 인터페이스 하나 — INTERFACE §8 항목 11 |
| 9 | 설계 선택 | `grantXp` in-place mutate | `hero.js:110` | 계약으로 명문화됨 | 유지 (순수화는 선택) |
| 10 | 코드 상수 | 결정론에 걸리는 상수 13개가 CSV 밖 (INTERFACE §5-3 — 반올림 자릿수 3종 추가) | 여러 곳 | 이식 시 누락 위험 | 목록 유지. 보조 ×1.5 는 CSV 후보 |
| 11 | 외부 의존 | Galmuri·Pretendard CDN 폰트 (2026-08-27 하이브리드 폰트 도입으로 CDN 2개) | `index.html` | 오프라인 폴백만 | 스팀 빌드 전 로컬 동봉 |
| 12 | ~~미사용 SSOT~~ **해소됨 2026-08-28** | `hero_attribute` · `combat_stat` 을 코드가 안 읽었다 (`equipment_option_override.csv` 는 폐기 — #16) | `ui/data.js` | — | 둘 다 로더에 등록하고 mock 사본(`STATS`·`COMBAT_STATS`)을 삭제했다. `combat_stat.csv:impl` 이 `computeCombat` 출력과 같은지 단정이 대조한다. **`reflect_damage` 표기 불일치는 2026-08-31 해소** — `combat_stat.csv` 를 `n` → `pct` 로 고쳤다(`battle.js` 가 `dmg × reflect / 100` 이므로 % 가 맞고, 그 행의 설명문도 "이 비율"이라 CSV 가 자기모순이었다). 같은 탭에서 마스터리 칸은 `+5%`·옵션 시트는 `5` 로 갈려 보이던 증상이 사라졌다. ⚠ **더 큰 뿌리는 남아 있다** — `AFFIX_LABELS` 19개 중 **12개가 `combat_stat.csv` 와 이름·단위가 중복**이고 이번이 그중 하나가 갈라진 사례다. 통합은 /ui |
| 13 | ~~표시 목업~~ **해소됨 2026-08-30** | 관전 스킬 쿨 게이지 · 로그/누적 데미지의 스킬명이 목업이었다 — 재생기가 영웅 행동마다 슬롯 순으로 제 마음대로 리셋했다 | `ui/battle.js` · `ui/mock.js` | — | `mockUseSkill` 삭제 · `skill` 이벤트가 칸을 켜고 `ready` 로 끈다(INTERFACE §2-6) · 로그·누적의 라벨은 이벤트의 `s` 에서 온다 · `MOCK_ACTIVES` → `SKILL_DISPLAY`(아이콘·설명만). ~~아이콘 세트가 흑백 글리프와 컬러 이모지로 섞여 있는 문제는 남았다~~ → **2026-09-03 화면이 그림으로 갈아탔다**(`mock.js:skillIcon` · `assets/art/icons/skills/`) — 이모지 컬럼(`skill.csv:icon`)은 데이터로 남고 화면이 안 읽는다. ⚠ **자산이 전사류 4종뿐**이라 나머지 스킬은 id 해시로 그 넷 중 하나를 빌려 쓴다(제 그림이 아니다 — 자산이 차면 `SKILL_ICON_FILES` 에 더하는 것으로 닫힌다) |
| 14 | ~~기획 미반영~~ **해소됨 2026-08-30** | **귀환 룰이 없었다** — base_expedition_design §1-1 은 "전투불능자 발생 시 파티 귀환(연쇄 전멸 방지)"인데 코드는 전멸까지 싸웠다 | `game_logic/battle.js` 메인 루프 | — | 종료 조건에 「전투불능자 ≥1」 → `reason: 'retreat'`. 판정 순서는 전멸 → 라운드 정리 → 귀환 → 제한시간이라 **마지막 타격과 같은 틱에 쓰러져도 클리어는 클리어다**. 리포트는 철수(루팅 보존)로 그린다(SCREEN_DESIGN §4-3) · 단정 2 · **캘리브레이션 재측정**(§3-1) |
| 15 | ~~미사용 SSOT~~ **해소됨 2026-08-28** | `skill.csv` 가 로더 목록에 없었다 | `ui/data.js` | — | `FILES` 에 `skill` 추가 · `D.skillRows` → `createSkillSystem` (INTERFACE §7) |
| 16 | ~~콘텐츠 미연결~~ → **판정 정정 2026-08-31: 연결 대상이 아니다** | 계승 장비 테이블 6종 — `equipment_base`(39) · `prefix`(35) · `suffix`(35) · `common_option`(70) · `equip_drop_rate`(11) · `monster_drop_equipment`(48). **"연결하면 최대 단일 이득"이라는 08-28 판정은 틀렸다** — 전수 조사 결과 전제 셋이 전부 무너졌다 (아래 정정표) | `src/data/inherited/` (읽기 전용) | 없음 — **부족한 것은 계승 테이블이 아니라 본작 기획**이다 | **연결하지 않는다.** 실제 결핍(접사 가짓수 · 지역 드롭 편향 · 유니크)은 각각 다른 항목으로 서 있다 — §3-2 「지역 죄종 드롭 편향」 · GAME_DESIGN §10 · §5-B #6·#8. **`equipment_option_override.csv` 는 08-28 폐기**(34행 → inherited_data_gaps 부록 A) |
| 17 | ~~데이터 오류~~ **해소됨 2026-08-28** | `spawn_grade.csv` hp_mult 만 등급 역행 (elite 3.0 > stage_boss 2.5) | `data/spawn_grade.csv` | — | elite `hp_mult` 3.0 → **2.0** (1.0/2.0/2.5/3.6). 7축(hp·atk·def·res_add·exp·gold·**drop_chance_mult**) 단조를 단정이 잠근다 |
| 18 | ~~데이터 오류~~ **해소됨 2026-08-28** | 보스 hp·attack 이중 elevation — 보스 행 raw 가 부풀려진 채 `spawn_grade` 배율을 또 받았다 | `data/monster.csv` 보스행 | — | 28개 보스 행의 `hp`·`attack` 을 **그 스테이지 일반몹 3종 평균(반올림)** 으로 되돌렸다. 비율 0.8~1.25 를 단정이 잠근다. **캘리브레이션은 재측정했고 대역을 위로 벗어난다** (§3-1) |
| 19 | 죽은 키 (그물 추가 08-30) | `concurrent_expedition_parties` 를 코드가 안 읽는다 — `state.party`/`state.run` 이 단수 필드라 "동시 원정 1"이 **구조로 우연히** 지켜질 뿐 | `data/balance.csv` · `game_logic/state.js` | 값을 2로 올려도 아무 일도 안 일어난다 | 동시 원정 영구 여부 확정(GAME_DESIGN §10) 뒤 정리. **08-30 로 단정이 값을 잠갔다** — 1 이 아닌 값이 들어오면 test.html 이 빨간불을 켠다(코드가 표현할 수 없는 값이라고 말한다). 가짜로 읽는 척하지 않는다 |
| 20 | ~~계약 위반~~ **해소됨 2026-08-30** | 리포트 빗나감이 `n===0` 과 `strikes` 부재를 구분 못 했다 — `ms?.n` 이 falsy 라 둘 다 "없음" | `ui/app.js` | — | `ms?.n` → `ms` · 비율은 `n===0` 에서 0 으로 끊는다. 귀환 룰로 짧은 런이 흔해져 **도달 가능한 자리가 됐다** |
| 21 | 미사용 컬럼 (`tags` 는 해소 08-28) | **`skill.csv:status` 를 코드가 읽지 않는다** — 아이스 블라스트의 `freeze`. `status_effect.csv` 가 미발행이라 결빙이 구현될 자리가 없다. **같은 줄에 있던 `tags` 는 R21 로 해소**(검증까지 들어갔다) (2026-08-28 등재) | `data/skill.csv` · `game_logic/skill.js` | 데이터에는 있고 전투에는 없다 — 기획서와 대조할 때 오해 소지 | `status_effect.csv` 발행 + R17(원소 성격)과 한 묶음 |
| 22 | ~~표시 어긋남~~ **해소됨 2026-08-30** | **관전 화면이 `heal`·`buff`·`buffEnd` 를 몰랐다** — 조용히 무시돼(#6) 회복으로 오른 HP·창이 화면에 안 나타났다 | `ui/battle.js apply` | — | 셋 다 소비한다 — 회복은 초록 팝업 + HP 바, 창은 카드 테두리(`.unit.buffed`)와 로그. #13·#25 와 한 묶음으로 처리 |
| 24 | ~~계약 위반~~ **수정됨 2026-08-28** | 시작 화면이 `saved.version !== SAVE_VERSION` 하나로 「이어하기」를 막아서, `deserialize` 가 올릴 수 있는 v2·v3 세이브까지 거부했다. v2 때부터 있던 결함 | `ui/app.js` · `game_logic/state.js` | — | **`state.canLoad(obj)` 신설**(INTERFACE §2-7) — 받아들이는 버전 목록을 두 곳에 두지 않으려고 `deserialize` 를 실제로 한 번 돌려 답한다. `app.js` 는 그것만 묻고 `SAVE_VERSION` 임포트를 버렸다. **회귀 단정**: v2·v3·v4 는 `canLoad` 참 · v1·v99 는 거짓 |
| 25 | ~~표시 어긋남~~ **해소됨 2026-08-30** | **관전 화면이 `regen` 이벤트를 몰랐다** — #22 와 같은 자리 | `ui/battle.js apply` | — | 팝업 없이 HP 만 올린다(조용한 회복이라는 성질을 그대로 그린다) |
| 27 | 목업 | **연구 섹션이 통째로 목업이다** [2026-09-01 사용자 지시로 신설] — 노드 4개의 이름·해금 내용·비용·보유 재료가 전부 지어낸 값이고, 화면은 `SYS.*` 를 하나도 안 부른다. 누르면 안내(`todo.lead`)만 뜬다. **CSV 로 안 간 것은 의도**다: 기획이 이름도 비용 곡선도 해금 순서도 안 정해서(GAME_DESIGN §10 「스킬의 자리」 · 채집 재료는 이름조차 미정) SSOT 를 먼저 만들면 CSV 가 기획을 앞질러 굳는다 | `ui/mock.js:RESEARCH` · `ui/app.js researchPanel` · SCREEN_DESIGN §13-1 | 자리와 읽는 법은 확정, **수치는 전부 거짓**. 스킬 트리 목업(`SKILL_TREES`)이 걸었던 길과 같다 | 기획 확정 시 **상수와 함수를 함께 삭제**하고 `game_logic` 의 상태 함수로 갈아탄다. 그때 채집 재료가 `G.resources` 에 자원 칸을 얻어야 한다 |
| 26 | 기획↔구현 어긋남 | **파티 전술의 리롤 재화가 골드다** — item_design §5-1 은 낙인의 소모처를 「크래프트 · 전술」 둘로 확정해 뒀는데, **낙인을 주는 코드가 없다**(챕터 보스 미연결 · `start_stigma` 만 존재). 낙인으로 두면 기능이 영원히 안 열려 골드로 발행했다 (2026-08-30 등재) | `data/tactic_slot.csv:reroll_cost_gold` · `game_logic/state.js rerollTactic` | 낙인의 소모처가 하나(크래프트)뿐인 상태가 이어진다 — 공급이 붙어도 예산 배분이 안 잡힌다 | 낙인 공급(챕터 보스 드롭)이 붙을 때 컬럼 하나를 바꾼다. 그전엔 골드가 맞다 |
| 29 | 목업 | **상단이 통째로 목업이다** [2026-09-03 사용자 지시로 신설] — 기본상단 3품목 · 특수상단(방문 상태 · 재고 2)의 **이름 · 수량 · 가격 · 체류 시간이 전부 지어낸 값**이고 화면은 `SYS.*` 를 하나도 안 부른다. 누르면 안내(`todo.lead`)만 뜬다. **CSV 로 안 간 것은 의도**다 — 기획이 방문 주기 · 체류 · 오프라인 진행 · 재고 테이블 · 가격 곡선을 하나도 안 정했다(GAME_DESIGN §10 「상단의 수치 전부」). #27(연구)이 걸어 둔 길과 같다 | `ui/mock.js:TRADE` · `ui/app.js tradePanel`·`tradeRows` · SCREEN_DESIGN §8-3 | **자리와 읽는 법만 확정**이고 수치는 전부 거짓. ⚠ 화면이 기획의 방치형 계약 규칙 셋(base_expedition §2-6)을 지고 있다 — 「놓친 방문」을 세거나 재촉 연출을 붙이면 그 계약이 화면에서 깨진다 | 기획 확정 시 **상수와 두 함수를 함께 삭제**하고 `game_logic` 의 상태 함수로 갈아탄다. 방문 시계는 오프라인에도 흘러야 하는지가 선행(§10) |
| 31 | 임시 데이터 | **의뢰 게시판의 목표·보상이 임시값이다** [2026-09-03 사용자 지시로 신설 · 같은 날 mock → CSV 이관] — 카드 7장의 **목표 · 보상 골드 · 명성 폭이 전부 지어낸 값**이고 화면은 `SYS.*` 를 하나도 안 부른다. 누르면 안내(`todo.lead`)만 뜬다. **처음엔 `mock.js:COMMISSIONS` 였는데 사용자 지시로 CSV 로 옮겼다** — #27(연구) · #29(상단)와 갈리는 지점이고, 근거는 「mock 과 CSV 가 겹치면 CSV 만 둔다」(§5-B). **두 표의 지위가 다르다** — `commission_kind.csv`(종류 4종)는 **확정 기획**이고 임시가 아니다. 임시인 것은 `commission.csv` 뿐이다(`mine_node.csv` 와 같은 처지) | `data/commission.csv` · `ui/data.js:D.commissionList` · `ui/app.js renderCommission`·`commissionCard` · SCREEN_DESIGN §14 | **자리와 읽는 법만 확정.** ⚠ 화면이 「수락 상한을 숫자로 안 찍는다」를 지고 있다 — 개수 CSV 키가 없는데 지어내면 확정으로 읽힌다. **진행 중 목록도 아직 없다**(상한·유한성이 선행) | 기획 확정 시 `commission.csv` 의 값을 갈아 끼우고(표는 남는다 — 굴림이 오면 **템플릿 표**가 된다) `game_logic` 을 붙인다. 필요한 것 넷 — 의뢰 굴림 · 수락/진행 상태(세이브) · 처치 카운터(세는 형) · 명성 정산. **약탈의 실시간 전투는 R24**(전투 인스턴스가 하나뿐이다) |
| 28 | 기획 공백 | **물리 직업이 회복 스킬을 고유로 뽑으면 회복량 0** [2026-09-01 R30] — 고유 풀이 `skill.csv` 전 행(전직 임시분)이라 `pri_heal` 이 전사·기사·궁수에게도 배정되는데, 회복 밑수는 `u.matk`(마법 공격력 — battle_design §9-2)라 물리 무기를 든 영웅은 0 을 회복하고 **차례는 먹는다**(발동 조건 `ally_hp_below` 가 참이면 시전) | `game_logic/battle.js castHeal` · `hero.js rollInnate` | 영웅 1명당 ≈ 1/14 × 물리 직업 3/5 — 드물지만 그 영웅은 액티브 한 칸이 죽는다 | **코드로 막지 않았다**(풀 규칙은 기획). **손잡이는 생겼다**(R31) — `skill.csv:innate_pool` 을 0 으로 두면 그 행은 풀에서 빠진다. 「회복을 빼는가」는 기획이 CSV 한 칸으로 정한다(skill_design §7) |
| 30 | 다국어 미완 | **프롤로그 본문에 `en` 이 없다** [2026-09-03 사용자 지시로 신설] — 5씬 본문·씬 제목(`pro.s1~s5.h`·`.b` · `pro.s5.q` · `pro.end`)은 `ko` 만 넣었다. 원문이 한국어 전용이고 **번역은 나중에** 채우기로 했다. 조작 문구(`pro.h`·`pro.step`·`pro.next`·`pro.skip`·`pro.begin`)는 ko/en 둘 다 있다 | `ui/i18n.js` | 영어로 켜도 프롤로그 본문은 한국어가 나온다 — `t()` 의 ko 폴백이라 **화면은 안 깨지고, 그래서 자동 검증도 못 잡는다**(verify.md §2) | 번역을 채우면 닫힌다. ⚠ **챕터 본문이 들어오면 같은 자리에 쌓인다** — 그때 문구의 집을 다시 본다. 지금 CSV 로 못 가는 이유는 파서가 따옴표·줄바꿈·쉼표를 못 읽어서다(`game_logic/csv.js` — `line.split(',')`) |
| 23 | 기획 공백 | **다단타·순환 중 대상이 쓰러지면 남은 타수를 버린다** — 재지정 규칙이 기획에 없어 코드가 임의로 정했다(INTERFACE §2-6 ⚠) | `game_logic/battle.js castAttack` | 다단타 스킬의 실효 DPS 가 설계값보다 낮다 | skill_design 에서 재지정 규칙 확정 후 반영 (2026-08-28 등재) |

#### 부채 #16 정정 — 계승 장비 6종은 「아직 안 붙인 것」이 아니라 「붙일 것이 아닌 것」이다 (2026-08-31)

08-28 판은 이 여섯을 **미연결 콘텐츠**로 적고 "죄종 매핑과 무관하게 착수 가능한 최대 단일 이득"이라고 했다. 그 판정은 **세 전제 위에 서 있었다** — ① 계승 축이 본작에도 산다 ② 계승 커브가 본작 확정 기획과 안 부딪힌다 ③ 계승 컬럼이 본작 스키마를 덮는다. 전수 조사 결과 **셋 다 무너졌다.** 테이블마다 왜인지:

| 계승 테이블 | 왜 연결 대상이 아닌가 |
|---|---|
| `equipment_common_option.csv` (70행) | 14종 × 5부위지만 본작에서 **살아남는 축은 4개뿐**이다 — `defense`·`hp`·`gold_find`·`item_find`. 나머지는 폐지됐거나(`accuracy`·`evasion` = R5 명중·회피 폐지 · `str`/`dex` = 접사가 기본 능력치를 주지 않는다(hero_design §4-2) · `cost_reduction`), 다른 층이 대체했거나(`min_damage`/`max_damage` → R2 개체 굴림이 드롭 시 한 번 굴려 개체에 박는다), `combat_stat.csv` 에서 `impl=0` 이다(`vs_*_dmg`·`vs_*_dr`). **살아남는 4개는 이미 `affix.csv` 에 있다 — 연결해서 얻는 것이 0** |
| `equip_drop_rate.csv` (11행) | mlvl 구간별 magic/rare 커브. **item_design §1 이 "희귀도는 ilvl 을 보지 않는다"로 확정**했고 "레벨과 희귀도를 뗀 것이 핵심"이라고 명시했다. 연결하면 확정 기획을 되돌린다 — 미연결이 아니라 **폐기 대상**이다 (GAME_DESIGN §10 등재) |
| `equipment_prefix.csv` · `equipment_suffix.csv` (35 / 35행) | 죄종 × 부위별 접사 매트릭스. 연결하면 지금 **순수 태그**인 `sins`(INTERFACE §2-5)가 **접사를 결정하는 축**이 된다 = 같은 필드의 의미 변화. 죄종이 무엇을 뜻하는지가 먼저다 — `sin_mapping.md`(GAME_DESIGN §10 첫 SSOT 과제) 선행 |
| `equipment_base.csv` (39행) | 무기 15행이 `min_damage`/`max_damage`/`speed` 를 들어 **R1(무기 = 밑수)·R2(개체 굴림)와 정면으로 충돌**한다. 방어구 24행은 **영문 이름만 있고 `_kr` 이 없어** CSV 규약(`_kr`/`_en` 쌍)을 못 지킨다. 쓸 수 있는 건 이름뿐인데 그건 08-31 M7 이 `item_base.csv` 로 신규 발행했다 |
| `monster_drop_equipment.csv` (48행) | 몬스터별 드롭 부위 편향. **편향의 단위가 미확정**이다(`monster_base` 인가 `role` 인가 — GAME_DESIGN §10) + 08-26 에 신설한 3부위(보조·장갑·신발) 컬럼이 없다. 단위가 정해지기 전에 붙이면 그 결정을 계승 표가 대신 내린다 |
| `equipment_unique.csv` | **0행**(헤더만) — 연결할 내용 자체가 없다 (inherited_data_gaps G1) |

**그래서 "접사 다양성 14%" 라는 옛 서술도 틀렸다** — 분모가 계승 70행이 아니다. 접사 가짓수가 부족한 것은 맞지만 그 해법은 **본작 접사 CSV 를 늘리는 것**이지 계승 표를 붙이는 것이 아니다. 계승 6종은 `src/data/inherited/`(읽기 전용)에 **참고 자료로** 남는다.

---

## 5. 다음 작업 — 기획 무관하게 할 수 있는 것

### 5-A. 경계 정리 + 골든 스냅샷 (M6)

1. ~~부채 #1 · #2 수정 + 회귀 단정~~ — ✅ 2026-08-26 (#2 의 남은 절반은 기획 결정)
2. ~~부채 #3~~ ✅ 2026-08-30 (실효 쿨 사본 제거) · 부채 #4 — 예상 소요 시간 집계를 game_logic 으로
3. 부채 #6 — 재생기 경고
4. ~~**골든 시드 스냅샷**~~ — ✅ **2026-08-31** (같은 날 **2차로 사각지대 보강** — 부채 #7-a). 시드 1~10 × 스테이지 101~104 = 40런의 **요약 지문** + **입력 지문**을 `dev/golden.json` 에 고정하고 `test.html` 이 매번 대조한다(**단정 4** — csvHash → balance 전 키 → 시작 파티 → 40런). **해시가 아니라 요약을 골랐다** — 해시는 "달라졌다"만 알려 주고 어디가 깨졌는지는 못 읽는다. 지금은 `40/40 런 불일치` 요약 뒤에 런·필드 단위로 찍힌다. 드롭은 **접사 stat·값·순서까지** 들어가 `rollAffixes` 의 수열 변화가 잡힌다. `uid` 는 넣지 않았다(발급은 `state.js` 축). 계약 = INTERFACE §5-5 · 다시 찍는 법과 **보장하지 않는 것** = [src/dev/README.md](../../src/dev/README.md). 남은 R(R13 등)은 rng 소비 순서를 건드리지 않는 범위에서 진행하고, **건드리면 다시 찍는다**
5. 부채 #10 — 상수 목록을 test.js 단정으로 고정

> ⚠ **파티 전술이 전투 수치에 들어간다** (2026-08-30, R23) — 켜진 칸의 효과가 파티원의 전투 능력치에 합류하므로, **캘리브레이션 표와 골든 스냅샷은 「칸이 무엇을 들고 있느냐」에 따라 달라진다.** rng 소비 순서는 안 바뀐다(전술은 자기 스트림을 쓴다 — INTERFACE §5-2). 스냅샷을 찍을 때 전술 칸 상태를 함께 고정한다.

### 5-B. mock → CSV 이관 (M7)

INTERFACE §7 표의 **3항목**(08-28 이관으로 12→9 · **08-31 M7 이관으로 9→3**). 우선순위 = game_logic 이 실제로 읽는 것부터.
**남은 셋(#6·#8)은 코드가 못 닫는다** — 전부 죄종 매핑(`sin_mapping.md`) 대기다.

| 순서 | 항목 | 대상 |
|---|---|---|
| 1 | ✅ **2026-08-28** `STATS` → `hero_attribute.csv` 읽기 전환 (부채 #12) · `COMBAT_STATS` → `combat_stat.csv` · 챕터·스테이지·몬스터 이름 → `chapter.csv`(신설) · `stage.csv:stage_name_en` · `monster.csv:monster_name_en`/`face` | 기존 CSV + 신설 |
| 2 | ✅ **2026-08-31** `EQUIP_SLOTS` / `SLOTS` → `equip_slot.csv` — **한 표가 둘을 먹인다**(`part_order` = 부위 · `slot_order` = 위치. 당시 8/9 · **2026-09-01 부터 7/8**). 두 파일로 쪼개면 `part` 참조가 파일을 건너간다 | 신규 |
| 3 | ✅ **2026-08-31** `CLASSES` → `class.csv` (id · keyAttr · **CSV 컬럼은 `release`**(스테이지와 이름 충돌 회피 — `weapon_group.csv` 와 같은 축) · 주입 필드는 `stage` 그대로 · 무기군은 weapon_group 이 이미 듦) | 신규 |
| 4 | ✅ **2026-08-28** `CODEX_LEVEL_BONUS` → `codex_level.csv:bonus_pct` · `CODEX_STAT_BY_NUM` → `codex_series.csv`(신설) | 기존 CSV + 신설 |
| 5 | ✅ **2026-08-31** `HERO_NAME_POOL` · `HERO_TRAIT_POOL` → `hero_name.csv`(24행) · `hero_trait.csv`(12행) — **배열 순서 유지**(`pick(rng, pool)` 이 인덱스를 굴린다) | 신규 |
| 6 | ⏸️ `SIN_TRAITS` · `COMMON_TRAITS` → 계승 `elite_trait.csv` 연결 — 죄종 하나당 정예 특성 하나라 #8 과 같이 간다 | 계승 |
| 7 | ✅ **2026-08-31** `ITEM_BASES` → `item_base.csv`(7부위 × 4 = 28행 · 무기는 없다 — 무기의 베이스는 무기군) · `AFFIX_DEFS` → `affix.csv`(19행 · `scale` 3분류가 그대로 컬럼) — **계승 테이블은 붙이지 않았다**(부채 #16 정정) · 3부위는 이미 반영돼 있다 | 신규 |
| 8 | ⏸️ `SINS` 키 → 죄종 테이블 | sin_mapping.md 확정 후 |

~~`nm()`(이름 조립)은 CSV 가 아니라 **이식 대상 코드** — `game_logic/` 로 옮긴다~~ → ✅ **2026-08-31 `game_logic/naming.js` 신설** (INTERFACE §2-10). `nm` 은 `composeName` 으로 개명해 옮겼고 `eliteName` 도 같은 자리로 왔다. 죄종 표시명(`SINS`)은 아직 CSV 가 아니라 **주입**받는다(`createNaming({sins})`) — CSV 가 생기면 주입원만 갈아 끼운다. 함께 `ELEMENT_IDS` 중복을 없앴다(SSOT 는 `hero.js:ELEMENTS` 하나 — INTERFACE §7 등재분 해소).

**M7 의 절대 규칙은 「값을 바꾸지 않는다」였다** — 배열 순서 하나가 `pick(rng, arr)` 의 인덱스를 바꿔 같은 시드가 다른 게임이 되기 때문이다(INTERFACE §5-2). 이관 전후로 캘리브레이션 4행과 **그 시점의 단정 135개가 전부** 통과하는 것으로 증명했다(이후 골든 2 · CSV 정합 3 이 더해져 지금은 140).

**2026-08-28 CSV 형태 최적화** — 위 #1·#4 외에 공통 규약을 확정하고(첫 컬럼 = id · `_kr`/`_en` 쌍 · `description_kr` 하나 · 불리언 `0/1` · 없음 `-` · 리스트 `|` · 셀에 쉼표 금지), `chapter.csv`·`codex_series.csv` 를 신설하고, `equipment_option_override.csv` 를 폐기했다. 컬럼 개명 — `weapon_group.attack_type→damage_kind` · `weapon_group.stage→release` · `skill.class→owner_kind,owner_id` · `codex_level.cards_required→cards_to_next` · `spawn_grade.drop_roll→drop_chance_mult`. 규약 본문은 [src/data/README.md](../../src/data/README.md).

### 5-C. 기획 확정분 (M8 — 순서는 기획이 정한다)

§3-2 표. 각 항목은 기획서 확정 → INTERFACE 갱신 → 구현 → 단정 순.

---

## 6. Phase 2 — 엔진 이식 계획

### 6-1. 착수 조건 (Phase 1 종료 기준)

- [ ] M6 · M7 완료 — 경계 위반 0, mock 잔류 0, 골든 스냅샷 존재
- [ ] 코어 기획 확정 — 최소 스킬 · 진형/타겟팅 · 파견/탐험 (이게 없으면 이식 후 로직을 다시 짠다)
- [ ] INTERFACE.md 가 코드와 일치 (경계 변경마다 갱신했는가)
- [ ] 엔진 결정 (Godot / Unity) — 기획 검증 완료 후

### 6-2. 이식 전략 — 선택지

| | A. 1:1 재작성 (GDScript / C#) | B. JS 런타임 임베드 |
|---|---|---|
| 방법 | game_logic 7 파일을 엔진 언어로 옮김 | 엔진 안에서 JS 엔진(QuickJS 등)으로 기존 코드 실행 |
| 장점 | 엔진 네이티브 · 디버깅 · 성능 · 플랫폼 제약 없음 | 이식 작업 0 · 결정론 자동 보장 |
| 단점 | 결정론 재현 부담 (rng · double · 호출 순서) | 브리지 층 · 콘솔/모바일 제약 · 스택 이중화 |
| 검증 | **골든 스냅샷 대조**로 해결 | 불필요 |

**권장 A.** 이유: 로직이 작고(7 파일) 순수하며, 계약(INTERFACE)과 골든 스냅샷이 있으면 재현 부담이 검증 가능한 문제로 바뀐다. B 는 "이식 안 함"이지 이식이 아니다.

### 6-3. 이식 순서 (A 기준)

| 단계 | 대상 | 완료 기준 |
|---|---|---|
| P2-1 | `csv` · `rng` · `formula` | 같은 CSV → 같은 파싱 결과 · 같은 시드 → 같은 난수열 · 같은 입력 → 같은 `strike` 결과 (골든) |
| P2-2 | `hero` · `item` | 같은 시드 → 같은 영웅/아이템 (JSON 비교) |
| P2-3 | `battle` | 같은 시드 → **같은 타임라인 해시** |
| P2-4 | `state` | 세이브 JSON 왕복 · 정산 결과 동일 |
| P2-5 | 저장 어댑터 · 데이터 로더 | `user://` / 파일 |
| P2-6 | UI 재작성 | SCREEN_DESIGN.md 화면 지도 전부 · `i18n.js:STRINGS` 계승 |
| P2-7 | 웹 프로토타입과 동시 실행 대조 | 시드 N개 전 스테이지 해시 일치 |

### 6-4. 이식에서 버리는 것

`ui/app.js` · `ui/battle.js` · `style.css` · `index.html` · `start.bat` · `mock.js` **거의 전부**(08-31 이관 뒤 남은 게임 데이터는 죄종 3항목뿐이고 나머지는 표시 사전이다). **버리기 전에 SCREEN_DESIGN.md 가 그 내용을 다 담고 있어야 한다.**

---

## 7. 문서 유지 규칙

| 바꾸는 것 | 먼저 고치는 문서 |
|---|---|
| game_logic 의 export · 입출력 · rng 순서 · 세이브 스키마 | **INTERFACE.md** |
| 화면 · 탭 · 플레이어가 내리는 결정 | **SCREEN_DESIGN.md** |
| 레이어 · 전역 · 조립 · 부팅 | ARCHITECTURE.md |
| 마일스톤 · 부채 · 현황 | 이 문서 |
| 기획 결정 | GAME_DESIGN.md §9 (확정) · §10 (미확정) |

폴더 README(`src/*/README.md`)는 "이 폴더에 뭐가 있나"만 — 현황·이력은 여기로.

---

*마지막 업데이트: 2026-09-03 (**의뢰 데이터를 mock → CSV 로 이관** [사용자 지시] — `COMMISSIONS` 를 지우고 **CSV 두 표**를 발행했다: `commission_kind.csv`(종류 4종 · **확정 기획**이라 임시가 아니다 — `form`=count/go · `channel`=realtime/offline/expedition/dispatch 로 「세는 형 / 가는 형」을 데이터가 든다) · `commission.csv`(게시판 행 · **⚠임시**, `mine_node.csv` 와 같은 처지). **칸 수 = 행 수**라 화면이 7을 안 박는다(`tactic_slot` 문법). 로더 `D.commissionKinds`·`D.commissionList` · FILES 24 → **26** · i18n `cm.*` 12키 → **2키**(종류 이름·「어떻게 도는가」가 CSV 의 `_kr`/`_en` 으로 갔다 — mock 과 CSV 가 겹치면 CSV 만). **`game_logic` 무변경** · 골든 재촬영했고 **차이는 csvHash 2줄 + 촬영일뿐**(40런·parties·balance 전부 불변 — CSV 추가가 로직을 안 건드린다는 증거). 한국어 리터럴 0 · ko/en 렌더 · **PASS 182/182**) · 2026-09-03 (**의뢰 탭 게시판 구현(목업)** — 의뢰 종류 4종 확정(GAME_DESIGN §9 09-03)으로 미착수 스텁을 걷고 카드 7장이 한 줄에 선다. **`flex: 1 1 0` 으로 줄을 나눠 가져** 하한 폭(1280)에서도 일곱이 스크롤 없이 다 보인다 — 폭을 고정했더니 마지막 장이 스크롤 뒤로 밀려 「7개가 쭉」이 깨졌다. §3-1 「의뢰 탭 — 게시판 7장」 행 신설 · 「탭 7」 행을 **구현 7(의뢰는 목업)** 으로 · §4 부채 **#31** 등재(#27 연구 · #29 상단과 같은 처지). 도움말의 `exp.commission.note` 도 4종 기준으로 갈아 썼다(옛 「보스/던전 지목」 서술 폐기). `game_logic`·CSV 무변경 · 한국어 리터럴 0 · ko/en 렌더 · **PASS 182/182**. 사용자 지시) · 2026-09-03 (**관전 이름 띠 재도입 · 영웅 죄종 색 폐지** — 유닛 카드가 **양 진영** 이름 띠를 되찾고(몬스터도 카드에서 이름을 다시 든다) 카드 윗변의 색 선이 띠의 아랫변으로 옮겨간다. 색은 **등급**(영웅 등급 / 스폰 등급)이고, 영웅에게 죄종 색을 주던 자리 다섯이 전부 등급 색으로 바뀐다(SCREEN_DESIGN §4-2 · §5). ⚠ **몬스터 스킬 칸은 여전히 빈 칸**이다 — `skill.csv` 는 `owner_kind` 가 `advance`/`weapon_group` 뿐이라 **몬스터 액티브가 데이터에 없다**. 화면은 자리를 그릴 뿐이고 채우는 것은 기획·로직 몫이다(§3-2). `game_logic`·CSV 무변경 · PASS 182/182) · 2026-09-03 (**§3-3 R38 추가 — 크래프트 경제 백지화**(GAME_DESIGN §9 09-03 사용자 확정): 가루·광석 서술이 기획서에서 걷혔는데 가루는 코드에 남아 돈다 — 지울지 남길지가 기획 결정이라 자원 표가 설 때까지 미반영으로 둔다 (item_design §5-3)) · 2026-09-03 (**스킬 아이콘 = 그림** — 화면이 `skill.csv:icon` 이모지 대신 `assets/art/icons/skills/<skill_id>.png` 를 그린다(SCREEN_DESIGN §2 · `mock.js:skillIcon`). 부채 #13 의 「아이콘 세트가 글리프·이모지로 섞여 있다」가 화면에서는 닫혔고, ⚠ **자산이 전사류 4종뿐이라 나머지 스킬은 해시로 빌려 쓴다**는 새 ⚠ 가 그 자리에 남는다. `game_logic`·CSV 무변경 · PASS 182/182) · 2026-09-03 (**R37 화면 반영 — 상단(기본/특수) 목업 · 파견처 순서 개정** · 부채 #29 등재(상단 목업 — 수치 전부 거짓 · CSV 로 안 간 것은 의도). `game_logic` 무변경 · PASS 182/182) · 2026-09-03 (**R36 반영 완료 — 치료 타이머 폐기 · 귀환 룰 개정** — `retreat` 사유 삭제 · 출정 누적 아웃(`run.downed`) · 세이브 v11 · `injury_minutes` 폐기(balance 110 → 109) · PASS 182/182 · 골든 재촬영) · 2026-09-03 (**R34 부분 반영 — 제련소 화면 + `+`강화** — SCREEN_DESIGN §8-2 구현 · 캐릭터 탭 강화 모드 토글 삭제 · `?dev=forge` 신설. `game_logic` 은 한 줄도 안 바꿨다(기존 `upgradeState`/`upgradeItem` 재사용) — 단정 PASS 178/178 · 골든 무변경) · 2026-09-03 (**§3-3 R34 등재 — 강화 두 갈래** (미착수). 기획 미확정 4건이 선행이라 CSV 를 먼저 굳히지 않는다. ⚠ INTERFACE §5-6 「실패는 없다」의 제련소 성공률 주석이 09-03 확정으로 낡았다 — 정정은 `/client`) · 2026-09-02 (**전술 옵션 등급 축 반영 — R33** — §1 현황(세이브 v10) · §3-1 전술 행·캘리브레이션 재측정(101 45% · **PASS 178/178**) · §3-2 「등급 출현 가중치」 등재(⚠제안 발행으로 형태만 닫힘) · §3-3 **R33 행 신설** + R28 의 계획 세이브 버전 v10 → v11) · *마지막 업데이트: 2026-09-03 (**R35 액티브 3칸 = 출처 고정** — §1·§3-1 현황(전투·스킬 행) · §3-1 캘리브레이션 재측정(101 **50%** · PASS 179/179) · §3-2 무기군 액티브에 ⚠임시 발행 표기 · §3-3 R35 행 신설) · *마지막 업데이트: 2026-09-03 (**프롤로그 화면**(사용자 지시) — §3-1 현황 1행 · §4 부채 **#30**(프롤로그 본문 `en` 없음 — ko 폴백이라 자동 검증이 못 잡는다). 화면은 SCREEN_DESIGN §3-1 · 개발용 `?dev=prologue&s=n`. 단정 **PASS 182/182** · `?dev=battle` 회귀 없음) · 2026-09-01 (**R32 등재 — 의뢰 확정 4건**(여분 회차 · 두 축 분류 · 약탈/보호(적 = 영웅 파티) · 명성) — §3-2 의뢰 행 갱신 · §3-3 R32 신설. **문서만 반영** — 적 영웅 파티가 새 전투 계약이고 명성은 세이브 스키마 신규라 코드는 못 연다 (GAME_DESIGN §9 09-01)) · 2026-09-01 (**R24 범위 확대 — 의뢰는 「그릇」** — 아르바이트 폐기로 비전투형 의뢰가 생겨 전투 인스턴스 복수화 계약이 **전투형에만** 걸리게 됐다. §3-2 의뢰 행 · §3-3 R24 주석. 코드 변경 없음 — 기획 미정이라 여전히 못 연다 (GAME_DESIGN §9 09-01)) · 2026-09-01 (**R31 반영 완료 — 스킬 구조 재편** — §1 현황 · §3-1 행 신설 + 검증 175 + 전투/스킬 행 · §3-3 R31 · §4 부채 #28 조치(`innate_pool` 손잡이) · 캘리브레이션 주(102 10% → 15% · 103 0% → 10% · 손잡이 무변동)) · 2026-09-01 (**R30 반영 완료 — 레어 고유 스킬 프로토타입 배정** — §1 현황 · §3-1 행 신설 + 전투 행 액티브 문구 · §3-3 R30 · R28 계획 버전 v9 → v10 · §4 부채 #28(물리 직업 + 회복 = 0) · 캘리브레이션 주 갱신(101 70% → 50% · 102 0% → 10% · 손잡이 무변동) · 단정 160 · 골든 재촬영) · 2026-09-01 (**⚠ R29 등재 — 무기 통합옵션 물리/마법 분리, 문서만** — 착수 가능한 것은 `affix.csv` 의 `gold_find`·`item_find` 에 `weapon` 추가 2행뿐이고 나머지는 stat_id 미발행 · `affix.csv:slots` 가 부위만 알고 무기군을 몰라 **스키마 확장이 선행** · 지속 디버프는 `battle.js` 의 새 상태 축) · 2026-09-01 (**⚠ R28 등재 — 전술 리롤 재설계, 문서만** — 칸별 리롤 → 전체 + 잠금(tactic_card_design §5-6 신설). 계약(INTERFACE §2-7 `toggleTacticLock` 신설·`rerollTactic` 시그니처·**세이브 v8→v9**·결과 코드 `allLocked`·**rng 순회 순서**)과 화면 규칙(SCREEN_DESIGN §13-2)을 먼저 적었고 **코드는 미착수**. ⚠ **비용 곡선이 기획 미정**이라 구현이 막혀 있다. 사용자 지시) · 2026-09-01 (**한손 개념 폐지 · 보조 슬롯 폐지 · 무기군 재편 — R27** — §1 현황 1행 · §3-1 행 신설 · §3-3 **R27 반영 완료** · 세이브 v8 · CSV 6종 · `game_logic` 3종 · `ui` 5종 · 단정 155 → **157** · 골든 재촬영. ⚠ 캘리브레이션 101 30% → 70%(무기 밑수 흡수분) · 102 는 0% 그대로. 화면(페이퍼돌 3행)은 `/ui` 로 넘김. 사용자 확정) · 2026-09-01 (**연구 탭 두 섹션 한 화면 + 연구 목업** — §3-1 행 신설 · §4 부채 신설(`mock.js:RESEARCH` — 기획이 이름·비용 곡선·해금 순서를 안 정해 CSV 로 안 갔다 · 확정 시 함수와 상수를 함께 삭제) · SCREEN_DESIGN §13 개정(뒤집는 문턱 300px 명시). 사용자 지시) · 2026-09-01 (**약어 AGI → DEX** — `hero_attribute.csv` 표시 컬럼 하나. 골든의 **CSV 지문 단정이 잡아** 재촬영했고, 다시 찍은 골든의 차이는 **`meta.csvHash` 두 줄뿐**(hero_attribute + 다른 세션의 combat_stat) — `runs` 40런·`parties`·`balance` 전부 불변이라 **약어가 로직을 안 건드린다는 증거**가 됐다. PASS 156/156 · §6 화면 개정(막대 정렬·등고·세로 압축)은 SCREEN_DESIGN. 사용자 지시) · 2026-09-01 (**「파견」 탭 → 「마을」** — 탭 id `dispatch` → `town` · 파견처 순서 변경. 탭 이름만 장소로 바뀌고 그 안의 「파견처」 어휘는 그대로. ⚠ 기획서(「거점」)↔화면(「마을」) 통일은 미결. 사용자 지시) · 2026-09-01 (**§8-1 수색 칸(화면만)** — §3-1 행 신설 · R9 의 「수색 미반영」을 「화면 자리만 잡힘」으로 갱신. 사용자 지시) · 2026-09-01 (**탭 10 → 7** — 스킬은 캐릭터 안의 창으로 · 거점·탐험·선술집은 파견 탭 한 곳으로. §3-1 행 교체 · SCREEN_DESIGN §1·§2(창 레이어)·§7·§8 개정. 사용자 지시) · 2026-08-31 (**탭 10** — 의뢰·거점·탐험이 탭이 됐다(속은 미착수 안내). SCREEN_DESIGN §1 화면 지도 개정 · §11 재편 · §3-1 행 신설) · 2026-08-31 (**관전 — 창 뱃지 줄 + 빈 스킬 칸** · §3-1 행 신설 · SCREEN_DESIGN §4-2 개정(옛 「빈 칸을 그리지 않는다」 폐기)) · 2026-08-31 (**`hero_hp_base` 230 → 100** — 사용자 지시로 돌린 손잡이 하나. §3-1 캘리브레이션 3차 기록 · ⚠ 목표 대역 미달(101 30% · 102 0%) · 골든 재촬영 · PASS 155/155 / **시작 화면 후보 초상 확대**(SCREEN_DESIGN §3)) · 2026-08-31 (**08-31 개정분 코드 반영 — R25 완료 · R24/R26 은 열리는 만큼만** — §1 현황에 강화 구현 + 나머지 둘이 왜 안 닫히는지 · §2 **M8 착수**(🔧) · §3-1 「강화」행 신설 · §3-3 **R25 반영 완료**(세이브 v7 · balance 6키 · 단정 13 · 캘리브레이션 불변) · **R24 문서·안내만** · **R26 잔재 정리만**(hero_attribute 지능 행 · 도움말 파견처 5) · 단정 142 → 155 · 골든 재촬영(입력 지문만 변동)) · 2026-08-31 (**의뢰·강화·파견처 개편 등재** — §3-2 「의뢰」행 추가 · §3-3 **R24**(의뢰 신설, 컨셉 락 개정) **R25**(강화 재정의) **R26**(연구소 삭제·채집 신설·연구 탭 분리) 신규 등재, 전부 미반영 · 화면 반영은 `/ui`) · 2026-08-31 (**골든 사각지대 보강** — §1 현황 1행 · §3-1 검증 행 단정 **142** · 골든 행에 입력 지문 · §3-1 캘리브레이션 주에 PASS 142(표 불변) · §4 **부채 #7-a 등재**(1차가 실측으로 못 잡던 6종 + 골든이 던지면 나머지 단정이 사라지던 것) · §5-A #4 갱신. 계약은 INTERFACE §5-5 · 못 잡는 것 목록은 src/dev/README.md) · 2026-08-31 (**M7 이관 · 밸런스 재보정 · 골든 스냅샷** — §1 현황 3행 · §2 M6·M7 🔧 · §3-1 골든 스냅샷 행 + 단정 137 · §3-1 캘리브레이션 재측정 주(`monster_atk_scale` 0.1→0.2 로 **목표 대역 충족**) · §4 부채 #5 축소(9→3) · **#7 해소**(rng 순서 그물) · **#16 판정 정정**(계승 장비 6종은 「미연결」이 아니라 **「연결 대상 아님」** — 테이블별 근거표 신설) · §5-A #4 ✅ · §5-B #2·#3·#5·#7 ✅ 과 `nm`·`eliteName` 이관 · 세이브 v5 오기를 v6 으로) · 2026-08-30 (**파티 전술 구현** — §3-3 R23 신설 · R14 반영 완료로 갱신 · §3-1 현황 1행 · §3-2 에서 전술카드 항목 삭제(기획이 닫혔다) · §4 부채 #26(리롤 재화가 골드) · §5-A 스냅샷 주의 · 세이브 v6 · 단정 135) · 2026-08-30 (**기획↔프로토타입 대조 반영** — 확정 기획과 어긋나 있던 다섯을 코드가 따라갔다: 귀환 룰(#14 해소 · `reason:'retreat'` · 캘리브레이션 재측정) · 선술집 명단 규칙(R9 절반 — 리롤 쿨다운 · 고용한 칸만 빔 · 세이브 v5) · 레벨 상한(R12 절반 — `hero_level_cap` 발행) · 관전이 실제 스킬·회복·창·재생을 그린다(#13·#22·#25 해소 · `skill` 이벤트에 `ready` 추가) · 실효 쿨 사본 제거(#3 해소). 부채 #20 해소 · #19 에 단정 그물. `balance.csv` 2키 발행(`hero_level_cap`·`active_slots`) · 리포트에 `roundsCleared` · 단정 127) · 2026-08-28 (**스킬 탭 3차** — 단계를 세로로(위가 먼저) · 「T1·T2·T3」를 화면·도움말에서 제거 · 칸 2/3 축소(판 폭 상한 + 칸 높이 고정)) · 2026-08-28 (**스킬 탭 마스터리 화면** — SCREEN_DESIGN §7 개정(가지×깊이 → 단계 쌓기) · 찍기/초기화 실동작 · 트리 목업 3종 삭제 · §3-1 행 추가) · 2026-08-28 (**부채 #24 수정** — `state.canLoad` 신설로 시작 화면의 세이브 거부 해소 · 회귀 단정 1) · 2026-08-28 (**마스터리 수치층 + 스킬 태그 반영** — §3-3 R15 절반 반영·R21 반영 완료 · §3-1 마스터리 행 · §3-2 T3·포인트 곡선을 미작성으로 등재 · §4 부채 #24(시작 화면이 이관 가능한 세이브를 거부) · #25(관전이 `regen` 을 모름) 등재 · 세이브 v4) · 2026-08-28 (§3-3 **R22 등재** — 스킬 3층 확정 · `skill.csv` 14행은 「직업 액티브」가 아니라 **전직 액티브의 프로토타입 임시분** · 이름은 전직인데 배정은 `owner_kind=job` — 갈래 테이블 미발행이라 R16과 한 묶음 · R16 「3 중 1택」으로 갱신) · 2026-08-28 (§3-3 **R21 등재** — 스킬 태그 13종 확정 · `skill.csv:tags` 컴럼 신설(코드 미독)) · 2026-08-28 (**CSV 형태 최적화** — 13종 재작성 · `chapter`·`codex_series` 신설 · `equipment_option_override` 폐기 · 부채 #12·#17·#18 해소 · #16·#19 비고 · R20 등재 · §5-B #1·#4 ✅ · 단정 104 · 캘리브레이션 재측정) · 2026-08-28 (**액티브 스킬 엔진 구현** — R19 반영 완료 · `skill.js` 신설 · `skill.csv` 14행 재작성 · 부채 #15 해소 · #3 절반 해소 · 신규 #21~#23 · 단정 92 · 스킬 도입 후 캘리브레이션 재측정(손잡이 미조정)) · 2026-08-27 (부채 #13 범위 갱신 — 관전 쿨 줄이 아이콘 + 바가 되면서 스킬 아이콘도 목업 목록에 포함) · 2026-08-27 (§3-3 **R15~R19 등재** — 액티브/패시브 2층 · 전직 3갈래(마법사 확정) · 원소 4종 성격 · 진형 폐기/타겟팅 · `skill.csv` 낡음) · 2026-08-27 (문서↔코드 대조 감사 — 부채 #14~#20 등재(귀환 룰 미구현 · `skill.csv` 미독 · 계승 장비 6종 미연결 · `spawn_grade` hp_mult 역행 · 보스 이중 elevation · `concurrent_expedition_parties` 죽은 키 · 빗나감 0건 미구분) · §3-2 3건 추가 · **고친 것**: 챕터1 챕터보스 몰록→사탄 · i18n 폐기 어휘 6건 · 문서 8곳 정정) · 2026-08-27 (부채 #13 등재 — 관전 스킬 쿨 게이지 목업) · 2026-08-27 (밸런스 전멸 해소 — `monster_hp_scale`·`monster_atk_scale`·`hero_hp_base` 캘리브레이션 조정, 101/102/103·104 목표 대역 충족) · 2026-08-26 (스킬 구조 개정 반영 — 「액티브 슬롯」 → 액티브 3 · 마스터리 2 + 전직 특화) · 2026-08-26 (R14 세트효과 폐기 → 전술카드 이관 등재 · §3-2 전술카드 추가) · 2026-08-26 (R1~R8·R10·R11 코드 반영 완료 — 세이브 v3 · 부채 #1 #8 해소 · #2 는 기획 공백으로 이관 · 캘리브레이션 전멸 기록) · 2026-08-26 (R12 레벨 상한·곡선 · R13 엔드게임·레이드·해금 등재) · 2026-08-26 (수치 대역 재설계 CSV 반영 완료 — R10·R11 추가, 코드 미반영 상태 명시) · 2026-08-26 (§3-3 기획 개정 미반영 R1~R9 등재) · 2026-08-26 (최초 작성 — 구현 현황을 game_logic/README 에서 이관, 부채 12건 등재)*
