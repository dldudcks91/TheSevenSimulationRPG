# SCREEN_DESIGN — 화면 설계

> **무엇을 보여주고 어떤 결정을 받는가.** DOM·CSS 가 아니다 — 그건 `src/ui/` 의 구현이고 Phase 2 에서 버려진다. 여기 적힌 것은 엔진이 바뀌어도 **그대로 들고 간다.**
> 화면을 바꾸려면 **이 문서를 먼저** 고친다 (TheSevenSimulation 관례 — 씬 변경은 문서가 코드보다 앞선다).
> 폭·배율·최소 크기 같은 구현 정책은 [src/ui/README.md](../../src/ui/README.md).

관련: [ARCHITECTURE.md](ARCHITECTURE.md) · [INTERFACE.md](INTERFACE.md)(호출하는 함수의 계약) · 컨셉 락 [CLAUDE.md](../../CLAUDE.md)

---

## 1. 화면 지도

```
시작 ──(새 게임 / 이어하기)──▶ 게임 ── 탭 7 ──┬ 원정   : 편성 ⇄ 관전 ⇄ 리포트          ← 접속 중의 본편 (컨셉 락)
                                            ├ 마을   : 파견처 목록 6 → 고른 것의 속 (§8)  ← 오프라인 활동 전부
                                            ├ 의뢰   : 보스/던전 지정 → 편성 → 관전   ☐ 미착수
                                            ├ 캐릭터 : 장비·능력치·가방 + 현재 스킬 → [스킬 트리 열기] 창 (§6·§7)
                                            ├ 연구   : 연구(☐ 미착수) + 파티전술 7칸 (§13)
                                            ├ 도감   : 몬스터 카드
                                            └ 도움말 : 규칙·안내 전문 (§12)      ← 인게임 패널에서 걷어낸 설명의 종착지
```

**[개정 2026-09-01 사용자 지시] 탭 10 → 7.** 탭이 **컨셉 락의 시간축과 그대로 맞물린다** — 실시간 전투는 원정·의뢰, 오프라인은 **마을** 하나, 아이템 정리는 캐릭터. 흩어져 있던 오프라인 활동 셋(거점 · 탐험 · 선술집)이 **마을** 탭 하나로 모이고, 스킬은 자기 탭을 잃고 캐릭터 안으로 들어간다.

**[개정 2026-09-01 사용자 지시] 탭 이름은 「파견」이 아니라 「마을」이다** — 처음 세울 때 붙인 「파견」(활동 이름 = 컨셉 락의 오프라인 칸)을 **「마을」(장소)로 바꾼다.**
- ⚠ **경고 — 시간축 대응이 한 겹 흐려진다.** 나머지 탭은 활동으로 불리는데(원정 · 의뢰 · 연구) 이 하나만 장소다. 08-26 에 「시설」 → 「파견처」로 어휘를 정리한 방향과도 반대쪽이다
- **그래도 맞는 이유** — 목록에 든 여섯 중 다섯(선술집 · 제련소 · 광산 · 채집 · 상단)이 **장소**고 활동은 탐험 하나다. 담는 그릇을 장소로 부르는 편이 속과 어긋나지 않는다. 기획서의 **「거점」**(GAME_DESIGN §7 · base_expedition §2)이 가리키던 바로 그 자리다
- **「파견」 어휘는 안 죽는다** — 탭 **안의** 칸은 여전히 **파견처**이고, 컨셉 락의 오프라인 활동 이름도 여전히 **파견**이다. 코드의 `dp.*` 키와 `.dp-*` 클래스가 이름을 그대로 두는 근거가 이것이다
- ⚠ **기획서 어휘 정리는 남았다** — 기획은 「거점」, 화면은 「마을」이다. 하나로 통일할지는 기획 결정 (`/game-design`)

| 옛 탭 | 새 자리 | 근거 |
|---|---|---|
| 스킬 | **캐릭터 탭 안** — 「기본 옵션」의 현재 스킬은 그대로 두고, 거기 있던 [스킬 트리 열기]가 탭 이동 대신 **창을 띄운다** (§7) | 스킬을 보는 이유는 언제나 "이 영웅"이다. 영웅을 고르는 자리(영웅 띠)가 캐릭터 탭에 이미 있는데 탭을 갈아타면 같은 띠를 두 번 그리게 된다 |
| 거점 | **마을 탭** = 파견처 목록 (선술집·제련소·광산·채집·상단) | 기획서의 「거점」이 가리키던 자리다. 탭 이름은 장소, 그 안의 칸은 파견처(활동) — 위 「탭 이름은 마을이다」 참조 |
| 탐험 | **파견처 목록의 5번째 칸** | 파견처와 **같은 맥락**이다 (2026-09-01 사용자 판단): 오프라인 · 영웅을 보낸다 · 중도 회수(base_expedition §3-2)가 똑같이 걸린다 |
| 선술집 | **파견처 목록의 첫 칸** (기본으로 열려 있다) | 파견처 5 중 하나라 제자리를 찾아간 것이다 |

- ⚠ **탐험은 화면에서만 나란히 선다 — 기획의 「파견처 아님」은 유지된다** (base_expedition §2-2). 가르는 것은 **인원**이다: 파견처 = 1인 배치 / 탐험 = 파티. 그래서 탐험 칸에는 **「파티」 표기**를 달아 인원 문법이 다름을 칸에서 읽히게 한다. 「열린 파견처 수 = 동시 파견 상한」(§2-1)을 셀 때 탐험은 **안 센다** — 화면 배치가 그 숫자를 흔들면 안 된다
- ⚠ **선술집을 접는 대가** — 선술집은 파견처이면서 동시에 **영웅 획득 창구**(명단 · 수색)라, 탭을 잃으면 고용까지 한 단계 깊어진다. 완화책은 **파견 탭을 열면 선술집 칸이 골라져 있는 것**이다 (§8). 옛 문장(「선술집이 탭으로 남는 이유 … 거점이 들어오면 다시 본다」)은 예고대로 이 개정에서 폐기됐다
- **파견 리포트도 탭이 아니다** — 원정의 리포트가 원정 탭의 한 상태인 것과 같다 (§11)

**[개정 2026-08-31] 전투 탭은 원정·의뢰 둘이 된다** — 지금 구현된 전투 탭은 원정 하나이고, 그 하나가 편성·관전·리포트 세 상태를 갖는다. 의뢰가 들어오면 **탭이 하나 늘고 관전 대상은 유저가 보고 있는 탭이 정한다**(CLAUDE.md 따름정리 2 개정 · §11). 옛 문장(「탭을 늘려 전투를 복제하지 않는다」)은 이 결정으로 폐기됐다 — 다만 **파티는 완전히 분리**되므로 같은 인원을 두 탭에서 굴리는 화면은 여전히 만들지 않는다.

---

## 2. 공통 셸

| 요소 | 내용 |
|---|---|
| 브랜드 | 상단 좌측 |
| 상단바 | 현재 위치(crumb) · 자원 3종(골드 · 가루 · 낙인) · **언어 토글**(ko/en) |
| 탭 내비 | 원정 · 마을 · 의뢰 · 캐릭터 · 연구 · 도감 · 도움말 (게임 화면에서만) — **탭 7** (§1) |
| 메인 | 탭 내용. **항상 전체 다시 그림** |
| 툴팁 레이어 | 아이템 비교 툴팁 (마우스 따라다님, 화면 밖 넘침 보정) |
| **창 레이어** | 탭 위에 겹쳐 뜨는 판 하나 [신설 2026-09-01]. 지금 쓰는 곳은 **스킬 트리**(§7) 뿐이다. 바깥을 누르거나 `Esc` 를 누르면 닫힌다 — **닫기가 세 갈래**(닫기 버튼 · 바깥 · `Esc`)인 이유는 창이 화면을 덮기 때문이다. 창을 두 장 겹치지 않는다.<br>**닫기 버튼 = 정사각 X · 머리 오른쪽** [2026-09-01 사용자 지시] — **창이 여럿이 돼도 모양도 자리도 같다.** 창마다 다르면 닫는 법을 창마다 다시 배워야 한다. 글자가 아니라 글리프라 ko/en 에서 폭이 안 흔들린다 — 이름(`ui.close`)은 `title` 이 든다(문구를 지운 게 아니라 옮겼다) |
| 플래시 | 한 번만 보이는 안내/오류 한 줄 (i18n 키 + 파라미터). 다음 렌더에 사라진다 |
| **도움말 탭** | 규칙·안내 문구는 전부 여기(§12). 인게임 패널에는 설명 문장을 두지 않는다 — 숫자·상태·오류만 (2026-08-26 사용자 지시) |

**다국어** — 화면의 모든 문자열은 `t(key)`(문구) 또는 `L({ko,en})`(데이터). 렌더러에 한국어 리터럴 없음.

---

## 3. 시작 화면

**목적** — 새 게임의 첫 결정(시작 파티 3명)과 이어하기.

| 보여준다 | 결정 | 규칙 |
|---|---|---|
| 세이브 요약 (저장 시각 · 영웅 수 · 클리어 수 · 골드) | **이어하기** | **열 수 없는 세이브**일 때만 이어하기 대신 사유를 보여준다. 이관 가능한 옛 버전(v2·v3)은 그냥 열린다 — 판정은 화면이 아니라 `state.canLoad` 가 한다 ([INTERFACE §2-7](INTERFACE.md)). v1 은 스키마 단절이라 계속 거부 |
| 후보 카드 3장 — 초상(§5 영웅 초상 규격) · 이름 · **등급 → 죄종**(이름 줄, 나란히) · 직업 · 역할/무기군 · 기본 능력치 7 막대 · 최대 HP · 합계 | **리롤** (무제한, 횟수 표시) | 같은 리롤 횟수 = 같은 3명 (고정 시드) |
| | **시작** | 세이브가 있으면 **두 번 눌러야** 덮어쓴다 — 되돌릴 수 없는 행동은 오클릭 한 번으로 일어나지 않는다 |

**초상 크기 [2026-08-31 사용자 지시]** — 후보 카드의 초상은 **카드에서 가장 큰 조각**이다. 시작 화면은 그림을 처음 보는 자리이고 고르는 근거의 절반이 인상이라, 이름·직업 줄에 딸린 아이콘 크기(52px)로는 얼굴이 안 읽힌다. 고정 px 이 아니라 `clamp()` 로 창 폭을 따라간다 — 폭 정책은 [src/ui/README.md](../../src/ui/README.md).
- **선술집 명단(§8)은 안 따라간다** — 같은 `.ng-card` 를 쓰지만 거기는 카드가 셋 나란히 서고 위에 로스터 띠가 얹혀 세로 예산이 다르다. `.tv-cands` 가 자기 크기를 계속 든다
- **세 장은 여전히 같은 무게다** — 하나만 키우면 그쪽이 정답처럼 읽힌다 (`.ng-row` 규칙)
- **[개정 2026-09-01 사용자 지시] 선술집 명단도 같은 카드다** — 「선술집은 세로 예산이 달라 안 따라간다」는 08-31 판단을 뒤집는다. 카드가 **한 벌**이면 시작 화면에서 익힌 읽는 법이 선술집에서 그대로 통하고, 초상이 작아 인상을 못 읽던 자리가 없어진다. 선술집 전용으로 남는 것은 **고용 버튼 하나**뿐이다 (§8-1)

**[개정 2026-09-01 사용자 지시] 카드에서 뺀 것 · 옮긴 것**

| | 어떻게 | 왜 |
|---|---|---|
| **시작 특성** | 카드에서 **뺀다** | ⚠ 08-26 부터 카드 항목이던 값이다. 값 자체는 살아 있고(`hero.trait` · 전투에 그대로 먹는다) **화면에서만 내린다** — 카드가 고르는 자리인데 특성 한 줄이 능력치 7 막대와 같은 무게로 서서 시선을 나눠 먹었다. ⚠ **지금은 어느 화면에도 안 나온다** — 다시 보이게 할 자리는 영웅 툴팁(§5)이 1순위다 |
| **죄종 칩** | 이름 줄로 올려 **등급 칩 오른쪽에** 붙인다 (옛 자리 = 그 아래 독립 줄) | 등급과 죄종은 **같은 층의 정체성**이다 — 둘 다 「이 영웅이 무엇인가」이고 능력치처럼 재는 값이 아니다. 줄을 따로 쓰면 죄종이 능력치 쪽 정보로 읽힌다 |

호출: `hero.rollStartParty` · `game.newGame` · `game.deserialize` · `game.closeRun`

---

## 4. 원정 탭

세그먼트 3 — **편성 / 관전 / 리포트.** 관전은 재생 중인 전투가 있을 때만, 리포트는 마지막 리포트가 있을 때만 활성.
**공통 영웅 띠(§5)는 편성의 편성 패널 안에만** — 관전·리포트에는 두지 않는다. 전투가 시작되면 전투 화면만 본다 (2026-08-27).

### 4-1. 편성

**어디를 갈지 먼저, 누구를 보낼지 나중** (2026-08-27) — 스테이지 목록이 이 화면의 본문이고, **지역을 누르면 그때 그 행 바로 아래로** 편성 패널이 펼쳐진다(2026-08-28 — 목록 위에 따로 뜨던 것을 접이식으로 바꿨다: 위에 뜨면 어느 지역의 편성인지 눈이 잇지 못한다). 왼쪽 굵은 선을 위 행과 같은 죄종 색으로 이어 두 칸을 한 덩어리로 묶는다. 영웅 띠는 그 패널 안에만 있다. 갈 곳이 안 정해진 편성은 결정이 아니라서, 띠를 상시로 띄우면 화면 첫 줄이 "지금 누를 것"이 아닌 것에 먹힌다.
**패널을 자동으로 열지 않는다** — 마지막 런으로 미리 골라 두는 안은 08-27 에 넣었다가 사용자 지시로 철회했다: 누르지 않았는데 떠 있으면 "고르면 뜬다"는 인과가 안 읽힌다.

| 영역 | 보여준다 | 결정 |
|---|---|---|
| 재접속 알림 (있을 때) | "꺼져 있던 사이 런이 마무리됨" + 스테이지 | 리포트 보기 / 닫기 |
| **편성 패널** (지역을 골랐을 때만 · 그 행 바로 아래) | **머리가 없다** (2026-08-28) — 무엇에 딸린 패널인지는 바로 위 행이 말한다 | **같은 행을 다시 누르면 접힌다** (닫기 버튼 폐지 — 여는 자리와 닫는 자리가 같다) |
| | 영웅 띠 (§5) — 로스터 전원을 한 줄에. **전투 파티는 카드 겉 테두리**로 보인다(2026-08-28). 위칸 글씨는 **대기 / 치료 중**만 — 「보낼 수 있는가」와 「골랐는가」는 다른 정보라 자리를 나눈다. 파티 첫 슬롯은 **리더** 표시 | 카드 클릭 = **파티에 넣고 뺀다**. 넣을 수 있는 건 **대기**뿐 — 치료 중은 거절(플래시)이고 커서로도 미리 막는다 |
| | 파티 없음 / 부상자 있음 경고 (띠 아래 · **줄은 항상 잡는다**) | |
| | 액션 — **같은 크기 버튼 둘** | **반복 원정**(ON/OFF) · **보내기**(고른 지역으로 출발) |
| 스테이지 목록 | 화면 전폭 — 해금된 챕터까지 전부, 잠긴 스테이지도 보인다 ("어디까지 가야 하는지가 보여야 한다"). 죄종 · Ch-스테이지 · 이름 · 클리어 표시 · dlvl · 예상 소요(분) · **스테이지 원소** · 배경 · 오른쪽에 「원정」 칩 | 행 클릭 = **이 지역으로 간다**(편성 패널이 열린다). 고른 행은 칩과 테두리로 표시가 남는다. 잠긴 행은 거절 → 플래시 |
| | 접이식 "구성 보기": 등장 몬스터 얼굴·이름 · 보스 · 정예 라운드 · 보스 라운드 | 펼치기는 선택과 별개다 |

패널이 없는 상태(아무 지역도 안 누른 상태)가 이 화면의 **기본**이다. 한 번 고른 선택은 탭에 머무는 동안 남는다 — 전투를 마치고 편성으로 돌아오면 방금 간 지역이 그대로 열려 있다.

**패널 크기는 상태에 흔들리지 않는다** (2026-08-28 사용자 지시) — 열려 있는 한 언제나 같은 크기다. 흔들던 것 셋을 구조에서 걷어냈다:
- 머리(제목 · 닫기) → **통째로 삭제** (2026-08-28). 스테이지 이름이 접히며 높이가 늘던 것도 같이 사라졌다
- 파티 없음 / 부상자 경고 → **줄은 항상 잡고** 글자만 있고 없다
- 반복 원정 → 별도 줄이 아니라 **액션 버튼**. 옛 줄은 「런의 스테이지일 때만」 붙어서 패널이 커졌다 작아졌다

**반복 원정은 출발 전에도 켤 수 있다** — `run.repeat` 은 「진행 중인 런」의 값이라 첫 출발 전에는 쓸 곳이 없다. 그래서 화면이 의사를 들고 있다가 출발할 때 런에 옮긴다. 이미 그 지역을 도는 런이 있으면 누르는 즉시 런에도 쓴다(관전의 자동 진행이 그 값을 읽는다).

**스테이지 원소** — 그 스테이지가 어느 저항을 요구하는지를 편성 **전에** 보여준다 (battle_design §9-8). 값은 `battle.stageElement(stage)` 가 정한다 — 화면이 몬스터 테이블을 훑지 않는다.
- **물리 스테이지도 그대로 찍는다** — `stageElement` 는 원소 몬스터가 없으면 `physical` 로 떨어지므로 Ch1 은 전부 "원소 물리 / Element Physical" 로 보인다. 숨기지 않는 쪽을 택했다: 칸이 사라지면 "아직 안 정해졌다"로 읽힌다
- **저항 미달 경고는 하지 않는다** — 문턱을 정하는 CSV 키가 없다. 경고를 넣으려면 기획이 먼저 문턱을 정해야 한다

호출: `game.toggleParty` · `game.stageUnlocked` · `battle.stagePool` · `battle.stageElement` · `game.resolveBattle`(출발) · `game.dismissNotice`
오류 → 플래시: 잠김(잠긴 행 클릭 · 출발) / 파티 없음 / 출발 불가(부상) / 파티 정원

### 4-2. 관전 (재생기)

**정산은 출발 순간 끝났다.** 이 화면은 결과를 시간에 맞춰 보여줄 뿐이다.

| 영역 | 내용 |
|---|---|
| 헤드 | 챕터 — 스테이지 이름 · 라운드 n / 총 · 라운드 종류 · 경과 시계 · **라운드 트랙**(1..9, 정예·보스 색) |
| 컨트롤 | 배속 ×1 / ×2 / ×4 · 일시정지 · **건너뛰기** |
| 아레나 | **적(위) / 파티(아래) 상하 대치** — 카드가 진영마다 한 줄로 나란히 (좌우 대치는 2026-08-27 사용자 지시로 철회) · 배경 = 스테이지 배경. **유닛 카드 = 이름 위칸 + 가로형 본문** (2026-08-27 개편 · 참고: Lootun 카드) — 위칸에 이름(적: 죄종 칩 · 정예 · 보스 태그), 본문은 왼쪽 초상(§5 얼굴 규격) · 오른쪽에 위에서부터 (정예 특성) / HP 바(**수치는 바 가운데**) / 행동 게이지(마지막 행동 이후 경과 ÷ 주기) / **스킬 쿨 칸**(가로 — 그 유닛이 실제로 든 액티브 수만큼). 카드는 **몬스터·영웅·보스가 전부 같은 고정 크기**(옛 폭의 약 2/3). XP 바는 두지 않는다 — XP 는 리포트가 정산한다. 세로 예산은 카드 두 줄 + 구분선 높이 — 아래에 가방이 오므로 줄였다 (2026-08-27). 피해/**빗나감**/치명/카드 팝업 (`dodge` 이벤트 = 적중 실패. 회피 스탯은 폐지됐다) |
| 우측 탭 | **로그** — 라운드 시작(편성) · **모든 타격**(공격자 → 대상 · 피해 · 쓴 스킬) · 빗나감 · 치명 · 반사 · 처치/전투불능 · 카드 · 종료. **누적 데미지** — 유닛별 가한 피해 합계: 파티 / 적 두 묶음 · 막대(묶음 안 최대 기준) · 묶음 안 점유율 · 스킬별 내역. 재생기가 이벤트의 `dmg` 를 더한 **표시값**이다(정산 아님). 두 탭은 재생 중 자유롭게 오간다 (2026-08-27) |
| 결과 띠 | 재생 끝 → 승/패 띠 → [리포트로]. **반복 ON + 승리면 잠깐 세고 다음 원정 자동** |
| 가방 | 아레나 아래에 §6 가방 패널 그대로(부위 필터 · 분해 모드 · 비교 툴팁 · 클릭 = 장착). 장착 대상 = 현재 선택 영웅(머리에 이름). **접속 중 = 원정 전투 + 아이템 정리**를 한 화면에 — 정산은 출발 순간 끝났으므로 여기서 정리해도 이 전투는 안 바뀐다 (2026-08-27) |

**유닛 카드 = 위칸 + 본문** (2026-08-27) — 위칸은 §5 영웅 띠와 **같은 배치**다: 왼쪽에 태그(적: 죄종 칩 · 정예 · 보스), **오른쪽에 이름**. 본문 오른쪽 열에는 수치 칸만 남는다.

**카드 크기는 몬스터·영웅·보스가 전부 같은 고정값** (2026-08-27 사용자 지시) — 진영마다 카드 수가 달라도, 정예 특성 줄이 붙어도, 보스여도 같은 박스다. 위아래 두 진영이 같은 격자로 읽히는 것이 카드가 자기 내용에 맞춰 커지는 것보다 낫다. 그래서 **보스의 초상 확대도 폐지**했다 — 보스 표시는 태그와 테두리가 든다. 넘치는 내용(긴 이름 · 정예 특성 3줄)은 잘리고 툴팁이 전문을 든다.
- 폭 고정은 [src/ui/README.md](../../src/ui/README.md) 화면 폭 정책(라벨 칸 고정 px 금지)의 **의도된 예외**다. 한 진영 최대 3장(`balance.csv:party_size_max` · `balance.csv:wave_monster_max`)이라 하한 폭 1280 의 아레나에 3장이 들어간다. 상한이 늘면 이 고정을 다시 본다
- **초상은 카드에서 가장 큰 칸**이고 본문 높이를 정한다 — 카드 높이가 초상 하나로 결정되므로 진영이 달라도 같은 높이가 된다

**스킬 쿨 = 가로 아이콘 칸** (2026-08-27 · 세로 3줄에서 개정). 슬롯 순 = 우선순위, **왼쪽부터** 1·2·3번. **[개정 2026-08-31 사용자 지시] 아레나도 빈 칸을 그린다** — 칸 수는 언제나 `[balance.csv:active_slots]` 이고, 액티브가 둘뿐인 영웅(전사 ③ 미정)은 **셋째 칸이 빈 채로 남는다.** 옛 결정(「빈 칸을 그리지 않는다 · 셋 중 하나가 비었다는 스킬 탭이 답한다」)은 폐기한다 — 칸이 사라지면 **카드마다 아이콘 줄의 길이가 달라져** 같은 격자로 읽히지 않고, 「이 영웅은 스킬이 둘」과 「셋째가 아직 안 정해졌다」가 화면에서 구분되지 않는다. 빈 칸은 §4-1 의 「값은 항상 찍는다」와 같은 이유로 자리를 지킨다. **이름도 % 도 찍지 않는다** — 이름과 표기 쿨은 툴팁에 남는다(값을 지운 게 아니라 옮겼다).
- **눕힌 이유** — 세로로 쌓으면 오른쪽 열의 가로 폭 중 아이콘이 한 뼘만 쓰고 세로만 먹는다. 아이콘이 안 읽히던 건 자리가 없어서가 아니라 배치가 세로였기 때문이다. 눕히면 같은 자리에서 아이콘이 커지고(13 → 32px) 세로 예산은 오히려 준다
- **남은 쿨** = 아이콘을 덮은 어두운 판이 위에서부터 걷힌다. 다 걷히면 준비 완료(테두리가 산다). 옛 바가 들던 "얼마나 남았나"를 아이콘 자신이 든다

**올려놓으면 뜬다** (2026-08-28) — 유닛 카드에 커서를 올리면 **영웅 기본 능력치**(§5 영웅 띠와 **같은 카드**), 스킬 칸에 올리면 **그 스킬**(이름 · 표기 쿨 · **실효 쿨** · 설명)이 뜬다. 몬스터 카드에는 영웅 카드가 없다.
- **실효 쿨** = `올림(표기 쿨 ÷ 행동 주기) × 행동 주기` (battle_design §6) — "표기 5초"만 봐서는 주기 2.4초인 영웅이 실제로 7.2초마다 쓴다는 걸 못 읽는다. 스킬 탭이 이미 쓰던 표기(`sk.base` · `sk.eff` · `sk.aligned`)를 그대로 쓴다
- 스킬 칸 툴팁은 카드 툴팁 **안에** 겹쳐 있다 — 칸에서 나오면 카드 툴팁으로 되돌아간다
- 툴팁 문구는 인게임 패널의 설명 문장 금지(§12)에 걸리지 않는다: 툴팁은 그 규칙이 허용하는 자리다

**켜져 있는 창은 카드 밖 아래 뱃지 줄이 든다** [신설 2026-08-31 사용자 지시 · 자리 개정 2026-08-31] — 유닛에 걸린 창(버프/디버프)을 **카드 밖, 카드 바로 아래 작은 칩**으로 하나씩 그린다. 칩은 그 창을 만든 스킬의 아이콘이고, 이름·남은 시간은 툴팁이 든다.

| 규칙 | 내용 |
|---|---|
| 색 | **이로운 창 = 초록 테두리 / 해로운 창 = 빨강 테두리.** 가르는 기준은 창의 값 부호(`buff` 이벤트의 `v`)다 — 음수면 해로운 창 |
| 자리 | **카드 밖 · 카드 바로 아래** [개정 2026-08-31 사용자 지시]. 옛 자리(카드 안의 마지막 줄)는 폐기한다 — 줄을 카드 안에 두면 그만큼 박스가 커져 위 「카드 크기는 전부 같은 고정값」의 **그 값이 달라진다.** 줄을 밖으로 내고 카드는 뱃지 줄이 생기기 전 크기로 되돌렸다. 창이 없어도 **줄은 자리를 지킨다** — 높이가 창 개수를 따라 흔들리면 카드가 위아래로 뛴다 |
| 대상 | 파티·적 **양쪽 다.** 적에게 걸린 창도 같은 규칙으로 그린다 |
| 넘침 | 칸 폭을 넘으면 잘린다 — 카드 폭이 고정이라 줄바꿈으로 키우지 않는다 |

- **테두리 발광(`.unit.buffed`)은 그대로 둔다** — 「무언가 걸려 있다」는 카드 전체가, 「무엇이 걸려 있나」는 뱃지가 답한다. 둘은 겹치는 정보가 아니다
- ⚠ **지금 도는 창 4종은 전부 이롭다**(`함성`·`수호의 방벽`·`도발`·`신속` — `skill.csv`). 빨간 테두리는 **해로운 창이 생기면** 그때 켜진다 — 상태이상 7종이 미구현이라 아직 출처가 없다 (DEV_PLAN §3-2)

**「방금 뭘 썼나」는 게이지가 아니라 팝업이 답한다** (2026-08-27) — 준비 상태는 **상태**라 게이지가 맞지만, 발동은 **사건**이라 게이지로는 안 보인다(조용히 리셋될 뿐). 그래서 발동하면 그 칸이 한 번 튀고 **스킬 이름이 카드 위로 떠오른다** — 피해 숫자와 같은 팝업 층이되 자리가 다르다(피해는 초상 위 · 스킬은 카드 가운데 위). 몬스터는 스킬이 없어 이 연출도 없다.

**스킬 칸은 실제 시전을 그린다** (2026-08-30 — 목업 폐기). 칸의 내용물(이름 · 표기 쿨 · 설명)은 `skill.csv` 에서 오고, 켜지고 꺼지는 것은 타임라인의 `skill` 이벤트다. 재생기는 **자기 규칙으로 쿨을 리셋하지 않는다** — 시전 사건이 오면 그 스킬 칸만 덮고, 남은 쿨은 `formula.effectiveCd` 가 아니라 시뮬이 쓴 실제 쿨(`cool_sec × 쿨감소`)로 걷힌다. 아이콘·설명도 `skill.csv`(`icon` · `desc_kr` · `desc_en`)에서 온다 — `mock.js` 의 표시 사전은 2026-09-01 에 사라졌다(데이터는 CSV).
- **회복 · 버프 · 재생도 그린다** — `heal` 은 초록 팝업과 HP 바 상승, `buff`/`buffEnd` 는 카드에 창 표식, `regen` 은 조용한 HP 회복이다. 이 넷을 무시하면 화면의 HP 가 시뮬과 어긋난다(재생기는 모르는 이벤트를 조용히 버린다 — [DEV_PLAN 부채 #6](DEV_PLAN.md))
- **몬스터는 액티브가 없다** — 쿨 칸도 시전 팝업도 없다 (`skill.csv` 는 영웅 전용)

**재렌더에도 재생은 이어진다** (2026-08-27) — 가방 클릭 · 언어 전환 · 세그먼트 이동이 화면을 통째로 다시 그려도, 렌더러가 재생 위치(t · 배속 · 일시정지 · 탭)를 받아 뒀다가 다음 mount 에 넘긴다. 재생기는 그 시각까지 이벤트를 팝업 없이 되감는다 — 로그 · 게이지 · 누적은 다시 쌓이므로 언어를 바꿔도 같은 자리에서 같은 내용이 나온다.

재생기가 아는 이벤트 12종(`round` `hit` `dodge` `reflect` `down` `card` `regen` `skill` `heal` `buff` `buffEnd` `end`)과 순서 보장은 [INTERFACE.md §2-6](INTERFACE.md#2-6-battlejs--헤드리스-전투). 언어를 바꿔도 같은 타임라인을 다시 재생한다.

### 4-3. 리포트

| 영역 | 보여준다 | 결정 |
|---|---|---|
| 판정 | **클리어 / 철수(전투불능 발생 · 시간 초과) / 패배(전멸)** · 스테이지 · 소요 · 사유 | |
| 획득 | 골드 · 가루 · XP(1인당) · 라운드 n / 9 · 전투불능 수 · 카드 수 · **빗나감** | |
| 레벨업 | 영웅별 from→to + 오른 능력치 | |
| 카드 | 몬스터별 장수, 이번 카드로 도감 레벨이 올랐으면 ▲ | |
| 부상 | 전투불능자 이름 · 남은 치료 시간 | |
| 액션 | | **다시 출발** / **편성으로** / 반복 토글 |
| 드롭 | 이름(희귀도 색) · 희귀도 · 부위 · ilvl · 비교 툴팁 · 가방 초과로 버린 수 | 행별 **분해** (아직 가방에 있을 때만) |
| 라운드 로그 | R1..9 · 종류 · 처치 목록 · 정예 죄종 | |

**철수는 실패가 아니다** — 전투불능자가 하나 나오면 파티가 그 자리에서 돌아온다(base_expedition_design §1-1 귀환 룰). 루팅은 전량 보존되므로 판정 색은 패배(전멸)와 갈라 놓는다 — 같은 색으로 칠하면 "졌다"로 읽혀 중도 귀환이 성장 사다리라는 사실이 안 보인다.

**빗나감** — 파티 기준 `{빗나간 수} / {직격 시도 수} ({비율}%)`. 적중은 레벨 차 하나로만 정해지므로(§9-4) 이 칸은 **"레벨이 모자란다"의 전용 신호**다 — 패배 원인이 화력인지 자격인지를 가른다.
- 아무것도 안 빗나가도 `0 / N (0%)` 로 **항상 숫자를 찍는다** — 칸이 비면 "안 재고 있다"로 읽힌다
- "없음"은 `strikes` 가 아예 없는 리포트에만 — v3 이전 세이브에서 이관된 옛 리포트뿐이다 ([INTERFACE §2-7](INTERFACE.md))

호출: `game.salvage` · `game.codexLevel` · `game.resolveBattle`(다시)

---

## 5. 공통 영웅 띠 (원정 편성 · 캐릭터 상단)

네 탭이 **같은 띠, 같은 자리, 같은 순서**로 로스터를 보여준다. **카드 = 초상이 전체를 채우는 정사각** (2026-08-27) — 글자는 초상 위에 얹는 **위칸** 하나뿐이다. 유니크는 표시 구분. 빈 칸은 `+` 로 `roster_cap` 까지.

| 영역 | 보여준다 | 규칙 |
|---|---|---|
| 초상 | 카드 전체(정사각). 아트 없으면 직업 글리프를 카드 절반 크기로 | 얼굴 그림은 아래 **영웅 초상 규격** 그대로 — 관전·후보 카드와 같은 그림, 크기만 다르다 |
| 위칸 | 왼쪽: **지금 하는 일**(치료 중(남은 시간) 빨강 / 대기 회색) · 오른쪽: 이름 | 초상 위에 반투명 띠로 얹는다. 안 들어가면 두 줄로 접힌다 — 고정 px 없음 |
| 테두리 | 상단 죄종 색 · **겉 테두리 = 전투 파티**(2026-08-28) · 안쪽 하이라이트 = 지금 보고 있는 영웅 · 부상 흐림 · 유니크 하단 색 | 두 표시는 뜻이 달라 겹쳐도 서로 안 먹는다 — 하나는 밖, 하나는 안 |
| 툴팁 | 이름 · 직업 · 레벨 · 죄종 · 등급 + **기본 능력치 7** (2026-08-28). 옛 브라우저 기본 툴팁(`title` 한 줄)을 카드로 바꿨다 — 값이 늘어 한 줄에 안 들어간다. 파생 전투치는 넣지 않는다(캐릭터 탭의 몫) | |

**전투 파티는 「하는 일」이 아니라 「고른 것」이다** (2026-08-28 사용자 지시) — 옛 화면은 카드를 누르면 위칸 글씨가 「대기」→「전투 파티」로 바뀌었다. 그러면 클릭이 고른 티가 안 나고, 그 한 줄이 「보낼 수 있는가」와 「골랐는가」 두 가지를 겸해 읽힌다. 파티는 카드 **겉 테두리**로 옮기고 위칸은 서 있는 상태(대기 / 치료 중)만 말한다. 파견이 들어오면 파견지가 그 자리에 앉는다.

**리더 = 파티에 제일 먼저 넣은 영웅** — `party` 배열이 넣은 순서 그대로이고 화면은 `party[0]` 에 리더 표시를 붙인다. 리더를 빼면 다음 사람이 리더가 되고, 다시 넣으면 맨 뒤로 간다. 로스터 순서로 다시 줄 세우지 않는다 (단정: `dev/test.js` 「toggleParty: 파티 순서 = 넣은 순서」).

이름과 지금 하는 일은 뺄 수 없다 — 얼굴이 겹칠 수 있고(그림 장수 < 로스터) 리포트·로그가 이름으로 말한다 / 이 띠가 접속·오프라인 상태를 한눈에 보는 자리다(아래). 다만 둘 다 주인공 자리에서 내려와 위칸의 라벨이다.
관전 유닛 카드(§4-2)는 **얼굴 그림만 같고 구성은 가로형**이다 — HP·쿨 3줄 같은 수치가 본체라 초상을 왼쪽에 두고 오른쪽에 세로로 쌓는다.
클릭 = 그 영웅 선택 (선술집에서는 캐릭터 탭으로 이동). **원정 편성만 예외 — 클릭이 파티 넣고 빼기다** (2026-08-27): 그 화면의 결정이 편성이고, 띠가 옛 파티·벤치 두 패널을 대신한다. 원정에서 띠가 서는 곳은 편성 패널 안뿐이다 (§4-1) — 스테이지를 골라야 열리고, 관전·리포트는 전투 화면만 본다.
파견이 생기면 "지금 하는 일"에 파견지가 들어온다 — 이 띠가 컨셉 락의 접속/오프라인 상태를 한눈에 보여주는 자리다.

**영웅 초상 규격 (2026-08-27) — 영웅의 생김새는 어디서나 같다.** 영웅 띠 · 후보 카드(§3 · §8) · 관전 유닛 카드(§4-2)가 **같은 네모 박스 + 같은 그림**을 쓴다. 원형은 몬스터 얼굴 전용이다. 죄종 색은 카드 상단 테두리가 들고 초상은 색을 갖지 않는다.

**축소 보간은 스타일이 정한다** [2026-09-01] — 초상은 어디서나 원본보다 **작게** 그려진다(가장 큰 후보 카드가 152px, 가장 작은 구성 칩이 26px). 그래서 「줄이는 방법」이 화질을 가른다:

| 스타일 | 보간 | 이유 |
|---|---|---|
| `pixel16` (도트그래픽) | **끈다** (`pixelated`) | 보간하면 도트가 뭉개진다 |
| `cartoon` (손그림) | **켠다** (브라우저 기본) | 끄면 픽셀을 건너뛰며 샘플링해 선이 끊기고 계단이 진다 — 26px 칸은 20픽셀 중 1픽셀만 남는다 |

갈래는 `<html data-face>` 를 CSS 가 읽어 **토큰 하나**(`--face-render`)로 편다 — 몬스터 원형 얼굴 · 관전 유닛 카드까지 **초상 전부**에 같이 걸린다(§4-2 · §9). 스타일을 늘릴 때 손댈 곳은 그 토큰 한 줄이다.

⚠ **배경 아트는 이 규칙 밖이다** — 거점(`body::before` · 선술집 패널) · 아레나 배경은 스타일과 무관하게 계승 픽셀아트라 언제나 `pixelated` 다.

**어느 그림인가는 영웅의 이름이 정한다** [2026-08-30] — `faces/<스타일>/hero_<n>.png` (`mock.js heroFace`).
- **uid 로 고르지 않는다** — 시작 화면·선술집의 **후보는 아직 uid 가 없다**(고용할 때 발급된다). uid 로 고르면 뽑을 때 본 얼굴과 고용한 뒤의 얼굴이 갈려 「어디서나 같다」가 첫 화면에서부터 깨진다. 이름은 굴리는 순간 정해져 끝까지 안 바뀐다
- **매 렌더 굴리지 않는다** — 다시 그릴 때마다 얼굴이 바뀌면 그건 초상이 아니다. 같은 이름 = 늘 같은 그림
- **그 스타일에 영웅 그림이 없으면 직업 글리프로 떨어진다** — 그림을 글리프 **위에** 덮고 `onerror` 로 img 만 빼는 방식이라, 그리는 중인 스타일로도 화면이 안 빈다 (몬스터 얼굴과 같은 규칙 · §10 `?face=`). 글리프 표는 한 곳(`mock.js classGlyph`)
- **장수가 로스터보다 적으면 얼굴이 겹친다** — 지금 그림은 `mock.js:HERO_FACE_MAX` 장이고 로스터는 `[balance.csv:roster_cap]` 명이라 겹치는 것이 정상이다. 줄이는 방법은 **그림을 늘리는 것 하나**(상수 하나를 올린다) — 겹침을 피하려고 「파티 안에서 중복 제거」를 하면 얼굴이 편성에 따라 바뀌어 「어디서나 같다」가 깨진다

---

## 6. 캐릭터 탭 — "아이템 정리"

세로 3단: ① 영웅 띠 ② **같은 폭 · 같은 높이의 4칸** — 장비 / 기본 옵션 / 세부 옵션 1 / 세부 옵션 2 (2026-08-27) ③ 가방(전폭). **장착·해제·분해가 실제로 일어나는 곳.**

| 패널 | 보여준다 | 결정 |
|---|---|---|
| 장비 | **페이퍼돌** — 신체 위치대로 착용 위치 8 (부위 7, 반지 ×2). 3열 × 3행: 투구·목걸이 / 무기·갑옷·장갑 / 반지·신발·반지 (2026-09-01 재배치 — 보조 슬롯 폐지로 4행 → 3행). 희귀도 색 테두리 · **잠기는 칸은 없다**(전 무기가 양손이라 배타가 사라졌다) · 착용 n / 8 | 착용 칸 클릭 = **해제** |
| | 접사 죄종 **태그** 집계 — 세트포인트 아님(폐기). 전술카드 조건이 이 숫자를 읽으므로 **카운터는 필수**다 (tactic_card_design.md §2-1) | |
| 기본 옵션 | 기본 능력치 7 막대 (범위 표시) — **일곱 줄의 막대가 같은 x 에서 시작한다** [2026-09-01 사용자 지시] · 그 아래 **현재 스킬** — 액티브 3 을 **정사각 카드**로(번호 · 이름 / 빈 슬롯 · **고유 스킬 칸은 번호 옆에 「고유」** [2026-09-01 — 1번 칸, 생성 시 굴린 것]) + 행동 주기 | **[스킬 트리 열기]** → 창이 뜬다 (§7) |
| 세부 옵션 1 · 2 | 전투 능력치 25 중 `impl=1` **21행**을 **두 칸에 나눠 스크롤 없이** — **카테고리 제목 없이** `[combat_stat.csv:sheet_order]` 순서. **머리 3줄 = 대표값**(물리 공격력 · 마법 공격력 · 최대 HP): 한 단계 키우고 그 아래 선 하나. 1 = 대표 3 + 공격 나머지 + 물리 방어 + 저항 4 + 최대 저항 증가(13행) · 2 = 피해 감소부터 끝까지(8행). 칸마다 값 있는 것 n / 그 칸의 행 수, 담당 기본 능력치 약어. 저항 4행은 **값 + 현재 상한** · 물리 방어 행은 **값 + 감쇠율 %** | |
| 가방 | **제목 줄이 없다** [개정 2026-09-01 사용자 지시] · 칸 `inventory_cap` 개 · 부위 필터 · **분해 모드 토글** · **강화 모드 토글** · 도구 줄 오른쪽 끝에 **칸 수**(관전에서는 장착 대상도) · 비교 툴팁(이 아이템 vs 교체될 착용품 — 반지는 빈 칸 우선) | 칸 클릭 = **장착** (분해 모드면 **분해** · 강화 모드면 **강화**) |

옛 "핵심 전투치 4"(공격력 · 방어 · 최대 HP · 행동 주기) 줄은 세부 옵션이 흡수했다 — 공격력·최대 HP·행동 주기는 같은 이름의 행이 있고, 감쇠율은 물리 방어 행에 병기한다. 공격 타입(원소)은 무기 툴팁이 든다.

**정렬은 `sheet_order` 하나가 정한다** [2026-09-01] — 카테고리 제목을 두지 않기로 한 이상 **순서가 유일한 구조 신호**다. 그런데 그 순서는 「카테고리 묶음 순 → 그 안은 CSV 행 순」이라는 **암묵값**이었다. 순서를 바꾸려면 CSV 행을 옮겨야 했고, 그러면 이 표가 행 순서에 결정론이 걸린 표들(`affix` · `item_base` · `hero_name` — [INTERFACE §5-2](INTERFACE.md))과 같은 모양이 되어 「이 표도 옮기면 게임이 달라지나」를 매번 되물어야 한다. 그래서 표시 순서를 **명시 컬럼**으로 꺼냈다.
- **머리 3줄은 대표값** — 물리 공격력 · 마법 공격력 · 최대 HP. 「얼마나 때리고 얼마나 버티나」가 시트 맨 위에 있어야 한다. 카테고리 순(공격 전부 → 방어)을 그대로 두면 최대 HP 가 여덟 번째로 밀린다. 제목을 새로 두지 않고 **글씨 한 단계 + 선 하나**로만 가른다 — 08-26 의 「카테고리 제목 삭제」를 뒤집지 않는다
- **물리·마법 공격력은 늘 나란히 있고, 둘 중 하나는 항상 꺼져 있다** — `computeCombat` 은 무기 종류에 따라 **하나만** 낸다(물리 무기를 들면 마법 공격력 행이 비고, 그 반대도 같다). 꺼진 줄을 지우지 않는 것이 결정이다: 회색으로 남은 그 자리가 「내 빌드가 어느 쪽인가」를 말한다
- **저항 4행은 안 가른다** — 값 뒤에 같은 상한이 붙는 한 묶음이라 칸을 넘기면 같은 상한이 두 번 나온다. 그 상한을 움직이는 `res_max_bonus` 도 같은 칸에 둔다
- **칸 경계는 「밑수」와 「부가 효과」** — 1 = 대표값 · 공격 · 물리 방어 · 저항(때리고 막는 밑수) / 2 = 피해 감소 · 반사 · 유지 · 템포 · 유틸. 13 / 8 이라 오른쪽 칸 아래가 비지만 **행 수를 맞추려고 저항 묶음이나 대표 3줄을 가르지 않는다** — 네 칸은 어차피 같은 높이고 가장 긴 칸이 그 높이를 정한다
- **`impl=0` 인 4행도 `sheet_order` 를 갖는다** — 타입·사이즈·상태이상 특효는 공격 끝(8~10), 상태이상 회복 속도는 행동 주기 뒤(22). 구현되는 날 어디에 앉을지가 이미 정해져 있어야 표가 두 번 흔들리지 않는다

**[신설 2026-09-01 사용자 지시] 세 가지 — 막대 정렬 · 칸 높이 · 세로 예산**

| | 규칙 | 왜 |
|---|---|---|
| **막대 시작점** | 기본 능력치 7 줄의 막대는 **같은 x 에서 시작한다.** 라벨 열 하나를 일곱 줄이 **공유**하고, 열 폭은 여전히 내용이 정한다(고정 px 없음 — ko/en 어느 쪽에서도 안 깨진다) | 줄마다 라벨 폭이 다르면(「힘」 vs 「매력」 / `Luck` vs `Intelligence`) 막대가 들쭉날쭉해 **길이를 눈으로 못 비교한다.** 막대는 비교하라고 있는 것이다 |
| **약어 자리** | 라벨 칸 안에서 **이름은 왼쪽 · 약어는 오른쪽 끝** — 약어가 막대 왼쪽 변에 **붙어 한 열로 선다** [2026-09-01 사용자 지시] | 약어를 이름 뒤에 그냥 붙이면 이름 길이를 따라 x 가 흔들리고 막대와의 사이가 들쭉날쭉해진다(영어의 `Luck` ↔ `Intelligence`). 약어는 **언어 중립**이라 한 열로 서면 ko/en 어느 쪽에서도 같은 자리에서 읽힌다 |
| **네 칸의 높이** | 장비 · 기본 옵션 · 세부 옵션 1 · 2 는 **위아래가 다 같다.** 세로로 쌓을 때 쓰는 패널 사이 여백이 격자 안으로 새면 **첫 칸만 높이 서므로** 격자에서는 그 여백을 끈다(간격은 `gap` 이 든다) | 08-27 「같은 폭 · 같은 높이의 4칸」이 실제로는 안 지켜지고 있었다 |
| **세로 예산** | 네 칸을 **눌러서** 아래 가방이 화면 밖으로 밀리지 않게 한다 — 패널 안쪽 여백 · 제목 아래 여백 · 시트 줄 간격을 줄이고 **가방의 제목 줄을 없앤다.** ⚠ 지운 것은 **글자뿐이고 수치는 도구 줄로 옮겼다** (§4-1 「값은 항상 찍는다」) | 1280×700 에서 가방이 통째로 접혀 있었다 |

- ⚠ **가방 전체가 한 화면에 들어오지는 않는다** — 칸 `[balance.csv:inventory_cap]` 개가 격자 네 줄이라, 위의 압축만으로는 도구 줄과 첫 줄까지가 한계다. 더 줄이려면 남은 자리는 **영웅 띠**(§5)뿐이고 그건 별도 결정이다

**감쇠율** — `formula.mitigation(방어값)` 하나로 계산한다. 인자는 방어값뿐 — **공격자 레벨이 곡선을 흔들지 않는다**(K 상수, battle_design §9-3). 그래서 이 %는 상대가 누구든 같은 값이고, 시트에 적어 둘 수 있다.

**저항 4행의 상한 표기** — `res_fire/cold/lightning/poison` 은 값 뒤에 `/ 상한 {cap}%` 를 붙인다. `cap = formula.resCap(res_max_bonus)` — 저항은 상한형이라(§9-5) 값만 보이면 "더 쌓으면 되는지"를 읽을 수 없다.
- 이 네 행은 **값이 0이어도 상한이 붙는다.** `computeCombat` 이 `res_*` 4개를 항상 내므로 "값 있는 것 n / 그 칸의 행 수" 집계에서도 **항상 채워진 행**이다 — 다른 행처럼 비는 일이 없다는 뜻이고, 의도된 예외다
- 최대 저항 증가(`res_max_bonus`)는 아직 드롭 접사 풀에 없어 상한은 사실상 `res_cap_base` 고정으로 보인다 ([INTERFACE §2-4](INTERFACE.md))

호출: `game.equip` · `game.unequip` · `game.salvage` · `game.equipTarget` · `game.heroCombat` · `formula.mitigation` · `formula.resCap` · `item.groupOf`
오류 → 플래시: `ch.err.<code>` (직업 전속 / 가방 초과 / 없음 — ~~양손~~ 은 2026-09-01 보조 슬롯 폐지로 사라졌다)

**툴팁 규격** — 이름(희귀도 색, **강화했으면 `+n` 을 이름 앞에**) · 희귀도 · 부위 · ilvl · (무기) 무기군 + 착용 직업 + 공격력 + 타입/원소 + 주기 · Implicit · 접사 목록 · 죄종 태그 · 힌트. 비교 시 "이 아이템 / 착용 중" 두 장.

**스킬은 이 탭이 품는다** [개정 2026-09-01 사용자 지시] — 스킬 탭은 없어졌다. 「기본 옵션」의 **현재 스킬**(액티브 3 카드)이 **평상시에 보이는 전부**이고, 더 깊이 볼 때만 [스킬 트리 열기]로 **창**을 띄운다 (§7).
- **탭이 아니라 창인 이유** — 스킬을 보는 이유는 언제나 「이 영웅」이다. 대상 영웅을 정하는 영웅 띠가 이 탭 위에 이미 있는데 탭을 갈아타면 **같은 띠를 두 번 그리게 된다.** 창은 탭을 떠나지 않으므로 띠도 대상도 그대로다
- **평상시 화면은 안 무거워진다** — 마스터리 판 셋은 캐릭터 탭의 세로 예산 밖에 있다가 필요할 때만 올라온다. §6 도 §7 도 각자 「한 화면에 전부」를 지킨다

**강화 [신설 2026-08-31 — R25]** — 강화 모드에서 칸을 누르면 골드를 내고 한 단계 오른다 (item_design §1).

- **툴팁이 다음 비용과 다음 옵션 계단을 함께 든다** — 「강화 +3 · 다음 364G · 6강에서 옵션 상승」. 얼마가 나가는지 모르고 누르는 것이 사고라는 규칙은 전술 리롤(§13)과 같다
- **공격력·방어력은 강화 배율이 먹은 값으로 찍는다** — 툴팁의 숫자가 캐릭터 시트의 숫자와 갈리면 안 된다 (`item.effective`, [INTERFACE §2-5](INTERFACE.md))
- **3·6·9강에서 오른 접사는 플래시로 알린다** — 어느 옵션이 올랐는지는 굴림이라, 말해 주지 않으면 목록을 눈으로 대조해야 한다
- 상한(`equip_upgrade_max`)에 닿은 아이템은 칸에 **`MAX`** 를 적고 누르면 `ch.err.maxUp`
- **가방·착용 어느 쪽이든 강화된다** — 다만 화면에서 누를 수 있는 자리는 지금 가방뿐이다(착용 칸 클릭 = 해제라 겸칠 수 없다)

---

## 7. 스킬 창 (캐릭터 탭에서 연다)

> **[개정 2026-09-01 사용자 지시] 탭이 아니라 창이다.** §6 「기본 옵션」의 [스킬 트리 열기]가 이 판을 띄운다. 대상 영웅은 **캐릭터 탭이 이미 고른 그 영웅**이라 창은 영웅 띠를 다시 그리지 않는다 — 탭이었을 때 필요했던 띠 한 벌이 통째로 없어진다.
> - **닫기는 세 갈래** — 닫기 버튼 · 판 바깥 클릭 · `Esc` (§2 창 레이어). 창은 화면을 덮으므로 나가는 길이 하나뿐이면 갇힌 것처럼 읽힌다
> - **창 안에서 랭크를 찍어도 창은 닫히지 않는다** — 찍기는 되돌릴 수 있고(초기화 무료·전액 환급) 연속으로 찍는 것이 정상 흐름이다. 화면은 통째로 다시 그려지지만 창은 열린 채 남는다
> - **아래 배치·규칙은 탭이던 시절 그대로다** — 판 셋이 나란히, 판 안에서 단계가 세로. 창으로 옮겨도 「한 화면에 전부」는 유지된다
> - **판 셋은 아래로 꽉 찬다** [2026-09-01 사용자 지시] — 아랫변이 왼쪽 칸(포인트 · 액티브)과 나란히 맞는다. 판마다 아랫변이 다른 데서 끝나면 「나란히」가 위쪽에서만 성립하고 창 아래가 뜯긴 것처럼 읽힌다.<br>**칸이 같이 부풀지는 않는다** — 칸 높이 고정(2026-08-28 사용자 지시)은 그대로고, 남는 자리는 판 안 아래쪽 여백이 된다. 기획이 아랫줄을 채우면 그 여백부터 찬다

**한 화면에 전부** (2026-08-28 사용자 지시) — 왼쪽 한 칸(포인트 · 액티브 3) + **판 셋이 나란히**. 판 안에서는 **단계가 위에서 아래로 쌓인다** (2026-08-28 사용자 지시): 마스터리는 가지가 갈리는 트리가 아니라 쌓는 구조다(skill_design §3-4). **줄 하나 = 한 단계**이고, 줄 안의 칸이 그 단계의 노드다. **윗줄이 먼저 열리고 아랫줄일수록 늦게 열린다.**

- **「T1 · T2 · T3」는 화면에 쓰지 않는다** [2026-08-28 사용자 지시] — 내부 어휘다. 도움말 탭도 마찬가지로 「맨 윗줄 / 그 아랫줄」로 부른다. 단계 번호를 지우면 그 번호가 들던 정보(해금 · 미작성)가 갈 곳을 잃으므로 **아래 두 줄이 그 자리를 대신한다**
- **잠금은 칸이 말한다** — 아직 못 여는 칸은 랭크 자리에 **필요 레벨**을 찍는다. 잠김을 숨기면 "없는 것"으로 읽힌다(§4-1)
- **미작성은 점선 프레임이 말한다** — 빈 칸에 「미작성」이라 적지 않는다. 설명 문구는 도움말 탭 전용이다(ui 원칙 4). **3줄 × 3칸 프레임은 비어 있어도 그린다** — 어디까지 갈 수 있는지가 보여야 한다(§4-1 스테이지 목록과 같은 이유)
- 칸은 **축 이름 · 현재 총합 · 랭크 n/최대** 셋을 찍는다. 랭크 0 이어도 `0 / 5` 와 `+0%` 를 찍는다 (§4-1 "값은 항상 찍는다"). 총합과 랭크는 **한 줄**에 둔다 — 칸을 낮게 유지하려는 것
- **축 이름과 단위는 `mastery_node.csv:stat` 에서 파생한다** — 접사 채널이면 접사 이름, 아니면 `combat_stat.csv`. 화면이 노드 이름 사전을 따로 갖지 않는다(가지면 CSV 와 갈린다)
- 칸 폭은 판 폭이 정한다(고정 px 없음). 긴 이름은 두 줄에서 잘리고 툴팁이 전문을 든다

| 패널 | 보여준다 | 결정 | 상태 |
|---|---|---|---|
| 포인트 | **남은 / 전체**(= 남은 + 찍은 합) · 초기화 | **초기화** | ✅ 실동작 — 죄종·직업이 **한 풀을 공유**한다 (skill_design §1-4) |
| 액티브 3 | 출처 라벨(**고유 / 무기군 / 전직**) · 행동 주기 · 칸별 표기 쿨 → **실효 쿨**(정렬 손실 %) — battle_design §6 "실효 쿨 표시 필수". 우선순위 변경(무료) | 우선순위 | 부분 — **「고유」 라벨만 실동작** [2026-09-01 — 1번 칸 = 생성 시 굴린 고유 스킬(hero_design §1 · skill_design §9-0), 번호 옆에 `sk.innate`]. 무기군·전직 라벨은 그 층이 없어 아직 없다 · 우선순위 변경 미구현 |
| 죄종 마스터리 | 영웅 죄종 색 · 윗줄(7죄종 공통 3) · 가운뎃줄(죄종 정체성) · 아랫줄(반응형 — 빈 줄) | **칸을 눌러 1랭크** | ✅ 실동작 |
| 직업 마스터리 | 같은 형태. 전사만 윗줄 3칸이고 나머지 4직업은 **첫 줄부터 빈 프레임** (기획 미작성 — skill_design §7) | **칸을 눌러 1랭크** | ✅ 실동작 |
| 전직 특화 | 전직이 준 액티브 3개 중 **찍은 하나**가 트리의 뿌리. 레벨 해금 · 잠김 표시 | 갈래 · 액티브 택1 | **미구현** (전직 층 자체가 없다) |
| 롤백 | 마스터리는 포인트 패널의 초기화 하나. 전직 특화는 미구현 | — | ✅ 마스터리만 |
| 무기군 스킬 비교 | 무기 교체 시 액티브가 바뀌므로 "옵션 vs 스킬" 비교가 화면에 있어야 결정이 성립한다 (skill_design §2-1) | — | **미구현** |

**규칙**

- **초기화는 한 번 클릭이다** — §3 "되돌릴 수 없는 행동은 두 번"의 대상이 아니다. 롤백이 **무료·수시**이고(skill_design §5) 포인트가 **전액** 돌아오므로 손실이 0 이다. 두 번 누르게 하면 기획이 준 자유를 화면이 깎는다. 찍은 것이 없으면 버튼을 끈다
- **못 찍는 이유를 플래시로 낸다** — `locked`(해금 레벨) · `maxRank` · `points`. 결과 코드는 `state.learnMastery` 가 낸다 ([INTERFACE §3](INTERFACE.md))
- 판정(해금·상한·포인트)은 **렌더러가 하지 않는다** — `state.masteryState` 하나가 칸마다 `unlocked` · `canLearn` 을 실어 온다

호출: `SYS.game.masteryState` · `SYS.game.learnMastery` · `SYS.game.resetMastery` · `SYS.game.heroCombat` (§6 과 같은 대상 영웅)

기획이 채워지면 그대로 늘어난다 — 죄종 가운뎃줄 빈 칸 5 · 아랫줄 전체 · 직업 4종. **화면은 CSV 행 수를 세지 않고 프레임만 고정**하므로 CSV 에 행이 늘면 칸이 찬다.
실효 쿨 공식은 현재 렌더러에 있다 — game_logic 으로 옮길 것 ([DEV_PLAN 부채 #3](DEV_PLAN.md#4-기술-부채--결함)).

---

## 8. 마을 탭 — 오프라인 활동 전부

> **[신설 2026-09-01 사용자 지시]** 옛 탭 셋(거점 · 탐험 · 선술집)이 여기 모였다. 근거와 대가는 §1.

**목록 → 고른 것의 속.** §4-1 원정 탭이 쓰는 문법과 **같다**(지역 목록을 누르면 그 아래에 편성 패널이 열린다). 새 문법을 만들지 않는다.

| 영역 | 보여준다 | 결정 |
|---|---|---|
| **파견처 목록** | 칸 6 — **선술집 · 제련소 · 광산 · 채집 · 탐험 · 상단** [순서 2026-09-01 사용자 지시]. 칸마다 이름 · **담당 능력치** · **인원**(1인 / **파티**) · 미착수면 배지 | **칸 클릭 = 그 파견처를 연다** |
| 열린 파견처의 속 | 선술집이면 아래 명단, 나머지 5는 미착수 안내 (§11) | 그 파견처의 결정 |

- **순서는 선술집 → 제련소 · 광산 · 채집 → 탐험 → 상단** [2026-09-01 사용자 지시] — 영웅을 얻는 자리가 맨 앞, 재료·제작 계열이 가운데, 교환(상단)이 끝이다
- **들어오면 선술집이 골라져 있다** — 목록만 있고 아무것도 안 열려 있으면 첫 화면이 비고, 지금 여기서 **실제로 할 수 있는 유일한 일**(고용)이 클릭 한 번 뒤로 숨는다. 선술집을 탭에서 내린 대가를 이 기본값이 갚는다 (§1)
- **미착수 칸도 그린다** — 어디까지 열리는지가 보여야 한다(§7 마스터리 빈 프레임 · §4-1 스테이지 목록과 같은 이유). 다만 **누르면 안내가 뜨므로** 「고장」으로 읽히지 않는다 — 누를 수 없는 칸을 그리지 않는다는 §8 옛 규칙(수색 칸)과 어긋나지 않는다
- **탐험 칸의 「파티」 표기가 문법 차이를 든다** — 나머지 5는 1인 배치다. 기획에서 탐험은 **파견처가 아니고**(base_expedition §2-2) 「열린 파견처 수 = 동시 파견 상한」에도 안 들어간다 — 화면이 나란히 놨다고 그 숫자가 6이 되지 않는다 (§1)
- **담당 능력치를 칸에 찍는다** — 배치 결정의 근거가 그 하나다(base_expedition §2-2). 미정인 칸(채집 · 탐험)은 **`—` 를 찍는다** — 빈 칸은 "안 재고 있다"로 읽힌다 (§4-1)
- **영웅 띠는 두지 않는다** [개정 2026-09-01 사용자 지시] — 로스터 전원(최대 `[balance.csv:roster_cap]`)이 목록 위에 한 줄로 서면 **정작 고를 것(파견처)이 화면 아래로 밀린다.** 마을 탭에서 영웅을 고르는 자리는 파견처를 연 **다음**이고(배치 화면 — 미착수), 띠는 그때 그 안에 든다. 지금 여기서 하는 유일한 결정(고용)은 띠를 안 쓴다

### 8-1. 선술집 — 명단 · 고용

| 영역 | 보여준다 | 결정 |
|---|---|---|
| 명단 | 후보 카드 `[balance.csv:tavern_candidates]`장 — **시작 화면(§3)과 완전히 같은 카드**다 [개정 2026-09-01 사용자 지시]: 초상 크기도 줄 구성도 같고, 더해지는 것은 고용 버튼뿐. **고용한 칸은 빈 칸으로 남는다** | **고용** (골드 · 로스터 정원 검사) |
| **수색 칸** | 명단 **맨 오른쪽**, 후보 카드와 **같은 크기**의 칸 하나 [신설 2026-09-01 사용자 지시]. 「미착수」 배지 · 확정 수치(인원 `[balance.csv:tavern_search_slots]` · 소요 `[balance.csv:tavern_search_hours]`) · 수색 버튼 | **수색** — 지금은 누르면 미착수 안내 |
| 도구 | 리롤 버튼 — 쿨다운이 끝났으면 **무료**, 남아 있으면 **골드 + 남은 시간** | **리롤** |

**명단은 저절로 갈리지 않는다** (base_expedition_design §2-4 확정 2026-08-26) — 자동 갱신이 없으므로 자리를 비운 사이 등장이 사라지지 않는다(방치형 계약). 화면이 그 계약을 읽히게 하는 방법은 둘이다.
- **빈 칸을 그대로 둔다** — 고용한 자리는 채워지지 않고 빈 칸으로 남는다. 명단이 통째로 갈리면 "고용이 곧 리롤"로 읽혀 쿨다운이 무의미해진다
- **버튼이 쿨다운을 든다** — 무료로 열려 있으면 비용을 적지 않고, 쿨다운 중이면 즉시 리롤 비용과 남은 시간을 함께 적는다. 남은 시간을 숨기면 "왜 골드를 내야 하는지"가 안 읽힌다

**[개정 2026-09-01 사용자 지시] 수색 칸을 그린다** — 옛 규칙(「칸을 미리 그리지 않는다 — 누를 수 없는 칸은 "고장"으로 읽힌다」)은 폐기한다. 명단 오른쪽에 후보 카드와 **같은 크기**의 칸이 서고, 그 안에 수색 버튼이 든다. `game_logic` 에 수색이 없으므로 **동작은 아직 없다** (DEV_PLAN §3-2).

| 규칙 | 내용 |
|---|---|
| 자리 | 명단의 **맨 오른쪽 · 셋째 칸.** `[balance.csv:tavern_candidates]`(2) + 수색 1 = **3칸**이 기획이 정한 구성이고(base_expedition §2-4) 격자는 이미 3열이었다 — 빈 채로 남아 있던 그 자리를 채우는 것이다 |
| 크기 | 후보 카드와 **같다.** 같은 행이라 높이는 카드가 정하고 칸이 따라 늘어난다 |
| 「고장」이 아닌 이유 | **「미착수」 배지**를 달고, 눌러도 오류가 아니라 **안내**가 뜬다(`todo.lead` 재사용 — 새 문구를 안 쓴다). 파견 목록의 미착수 칸(§8)과 **같은 문법**이라 「아직 안 열렸다」로 읽힌다 |
| 값은 찍는다 | 기획이 확정한 수치 둘(**인원** `[balance.csv:tavern_search_slots]` · **소요** `[balance.csv:tavern_search_hours]`)을 칸에 찍는다. 미착수라도 빈 칸으로 두면 「안 정해졌다」로 읽힌다 (§4-1) |

- **수색은 파견 슬롯을 먹지 않는다** (base_expedition §2-4) — 파견처 목록의 또 한 칸이 아니라 **선술집 안**에 있는 이유다
- 결과 칸(수색이 물어온 후보 1명)은 **구현될 때 이 자리를 그대로 쓴다** — 버튼이 카드로 바뀐다. 자리를 미리 잡아 두는 것이 이 개정의 반쯤이다

호출: `game.tavernState` · `game.hire` · `game.tavernReroll`. 명단은 시드+카운터라 저장하지 않아도 같은 사람들이 다시 나온다 — 저장하는 것은 **리롤 시각과 산 칸 번호**뿐이다.

---

## 9. 도감 탭 — 몬스터 카드

| 영역 | 보여준다 |
|---|---|
| 헤드 | 카드 드롭 확률 · 레벨별 필요 장수 |
| 챕터 세그먼트 | Ch1..7 이름 · 잠김이면 안내 · 챕터 죄종 |
| 스테이지 행 | 번호 · 이름 · **스테이지 보정**(계열 스탯 +%) · 완성 여부 · 잠김이면 `???` |
| 몬스터 카드 | **초상**(카드 높이를 정한다) · 이름 · 보스 표시 · 카드 수 · 레벨 핍 · 처치 수 · 다음 레벨 필요 장수 + 보정 % · 진행 막대 |

**초상이 카드의 주인이다** (2026-08-30 개정 — 사용자 지시). 도감은 **모으는 화면**이고 모으는 대상은 몬스터다. 종전 카드는 초상이 38px 칩이라 수치가 주인공이었고, 그 크기에서는 몬스터가 서로 구분되지 않았다 — 「무엇을 모으고 있나」가 안 읽히면 도감이 표가 된다.
- **크기는 초상이 정한다** — 카드 높이 = 초상 높이. 영웅 띠 · 관전 유닛 카드와 같은 규칙이다(§5 · §4-2 「초상은 카드에서 가장 큰 칸이고 본문 높이를 정한다」)
- **원형 마스크는 유지한다** — 몬스터는 원형, 영웅은 네모라는 구분이 화면 전체에 걸려 있다(편성 화면의 구성 칩도 원형이다). 초상이 커져도 그 구분은 안 바꾼다
- **여전히 한 챕터(4스테이지)가 한 화면**이다 — 세로 예산은 [src/ui/README.md](../../src/ui/README.md) 폭 정책의 700px. **이 제약이 초상 크기의 상한을 정한다**: 더 키우면 스테이지 4가 접힌다
  - ⚠ **영어는 하한 폭에서 ~30px 더 든다** — 챕터 세그먼트(Ch1~7)가 1280 에서 두 줄로 접히기 때문이다. 한국어는 700 에 맞고 영어는 730 이 필요하다. 초상을 더 줄여 영어까지 맞추는 것보다, 한 줄 스크롤을 받는 쪽을 골랐다 — 도감은 훑는 화면이지 결정을 받는 화면이 아니다
- **이름이 줄어들고 장수는 안 줄어든다** — 카드 수(`0장` / `0 cards`)는 줄바꿈 금지, 폭이 모자라면 **이름이 말줄임**된다. 영어에서 숫자가 두 줄로 접히면 그 카드만 높이가 달라져 줄이 어긋난다
- **안 켜진 레벨 핍도 보인다** — 「몇 번 더 올릴 수 있나」가 도감의 목표치라, 빈 칸이 안 보이면 목표가 안 보인다
- 아트가 없는 몬스터는 죄종 색 원판 + 이니셜 — 커진 자리에서도 같다 (`monster.csv:face`)

호출: `game.codexLevel` · `game.codexBonusAt` · `game.codexMaxLevel` · `game.stageUnlocked`. 결정 없음 — 조회 화면.

> ⚠ **스테이지 3 계열은 지금 죽은 축을 그린다.** 계열 스탯 줄이 "명중률 +x%", 완성 보상이 "회피율 +2%" 로 나오는데 **두 축은 08-26 에 폐지됐다** — 화면에는 보이지만 전투에 아무 영향이 없다(`computeCombat` 이 `acc_pct` 를 읽지 않는다).
> 문구를 임의로 바꾸지 않는 이유: 계열 재배정은 기획 결정이고, 정해지면 라벨과 효과가 함께 바뀐다 ([DEV_PLAN §3-2](DEV_PLAN.md) · [GAME_DESIGN §10](../game_design/GAME_DESIGN.md) "도감 스테이지 3 계열 스탯"). 스테이지 1·2·4 계열(공격력·최대 HP·피해)은 정상 동작한다.

---

## 10. URL 라우트 (개발용)

| 파라미터 | 동작 |
|---|---|
| `?screen=start` | 시작 화면 강제 |
| `?dev=newgame` | 현재 후보로 즉시 시작 |
| `?dev=battle` | 첫 스테이지 즉시 정산 → 리포트 |
| `?dev=play` | 첫 스테이지 관전 — `&bt=dmg` 면 우측 탭을 누적 데미지로 연다 |
| `?dev=offline` | 반복 켠 채 껐다 켠 상황 → 런 마무리 배너 |
| `?dev=form` | 첫 스테이지를 고른 상태 → **편성 패널이 열린 화면** (§4-1). 패널은 클릭으로만 열리므로 헤드리스가 닿을 길을 따로 냈다 |
| `?dev=tactics` | 전술 칸이 **전부 열린** 연구 탭 (§13). 칸은 합산 레벨로만 열려 클릭으로 만들 수 없다 — `?dev=form` 과 같은 이유 |
| `?dev=tree` | **스킬 창이 열린** 캐릭터 탭 (§7). 창은 버튼으로만 열리므로 헤드리스가 닿을 길을 따로 냈다 — `?dev=form` 과 같은 이유 [신설 2026-09-01] |
| `?tab=<탭>` | 탭 바로 열기 — `?dev` 뒤에 적용. 이름은 탭 7 뿐이다 [개정 2026-09-01]: `expedition` · **`town`** · `commission` · `character` · `research` · `codex` · `help`. 없어진 `skill` · `base` · `explore` · `tavern` 은 **안 먹는다**(무시하고 원정으로 연다) — 스킬은 창(§7), 나머지 셋은 파견 탭 안의 칸(§8)이라 탭 이름으로는 도달할 자리가 없다 |
| `?lang=ko\|en` | 언어 — localStorage 에 남는다 |
| `?face=<스타일>` | **몬스터 얼굴 아트 스타일** — `src/assets/art/faces/<스타일>/` 폴더 하나가 스타일 하나다. localStorage 에 남으므로 한 번 걸면 계속 그 스타일로 돈다. 목록은 `ui/mock.js:FACE_STYLES` · 없는 이름이면 무시하고 목록의 첫 항목. **고른 스타일은 `<html data-face>` 에 남는다** [2026-09-01] — 초상 축소 보간을 CSS 가 스타일별로 가르는 데 쓴다 (§5) |

---

## 11. 미착수 탭의 속 (탭 자리는 §1 에 있다)

**[개정 2026-09-01] 미착수는 탭 하나(의뢰) + 마을 탭 안의 칸 다섯이다** — 거점·탐험이 탭 자리를 잃고 마을 탭(§8)의 칸으로 들어갔다. 의뢰만 탭으로 남는다. 어느 쪽이든 **속이 비어 있어** 지금은 「미착수」 배지와 안내 한 덩어리만 든다. 아래 표는 그 속이 채워질 때 받을 결정이다. 기획: [base_expedition_design.md](../game_design/base_expedition_design.md).

> **미착수 탭은 §12 「설명 문구는 도움말 탭 전용」의 의도된 예외다** — 안내를 빼면 빈 화면만 남는다. 대신 **문구를 새로 쓰지 않고** 도움말이 쓰던 키를 그대로 부른다.

| 화면 | 기획 상태 | 화면이 받을 결정 (예상) |
|---|---|---|
| **마을 탭의 칸 5** (제련소 · 광산 · **채집** · 탐험 · 상단) | 골격 확정 | 넷은 **영웅 배치**(능력치 ↔ 파견처) · 제작 버튼 / 탐험은 파티 편성 → 출발 → 리포트 (원정 문법 재사용, 전투 없음) |
| 파견 리포트 | 미정 | 결과 수치 + 사건 텍스트. **중도 회수**(언제든 부르고 흐른 만큼 비례 지급 — base_expedition §3-2)가 붙는다 |
| **의뢰 탭** (전투 채널 둘째) | 골격만 — 쿨다운·드랍·해금 시점 미정 | 보스/던전 지정 → 파티 편성 → **원정과 동시에** 실시간. **관전 대상은 지금 보고 있는 탭**이 정하고, 원정 파티와 **인원이 겹칠 수 없다** (base_expedition §1-3) |

⚠ **의뢰는 화면만의 일이 아니다** — `battle.js` 가 전투 인스턴스를 하나만 가정하므로(`run` 이 state 에 하나뿐이다) 탭을 그리기 전에 계약이 먼저 바뀐다 ([DEV_PLAN §3-3 R24](DEV_PLAN.md)).

이 화면들이 들어오면 영웅 띠의 "지금 하는 일"과 `closeRun` 의 전제가 함께 바뀐다 ([INTERFACE §8 항목 7](INTERFACE.md#8-명문화한-암묵-계약)).

---

## 12. 도움말 탭

**목적** — 규칙·설계 근거·미구현 안내를 **한 곳에 모은다.** 인게임 패널은 숫자·상태·오류·버튼·툴팁만 남긴다 (2026-08-26 사용자 지시).

**한 페이지 스크롤.** 읽기 폭 상한만 잡고 장식은 없다. 결정 없음 — 조회 화면.

| 영역 | 내용 |
|---|---|
| 제목 | 도움말 |
| 점프 세그먼트 | 섹션 버튼 — 누르면 해당 `<h2>` 로 스크롤 |
| 섹션 (`<h2>`) | **탭 순서를 따른다** [개정 2026-09-01] — ① 원정 ② **마을** ③ 캐릭터 ④ 스킬 ⑤ 연구 ⑥ 도감 ⑦ 새 게임. **마을**은 옛 「거점」+「선술집」 두 섹션이 합쳐진 것이고, **스킬**은 탭을 잃고도 섹션으로 남는다 — 게임의 개념이지 탭 이름이 아니기 때문 |
| 소제목 (`<h3>`) | 패널 이름 그대로 + (있으면) 그 패널이 쓰던 부제 |

| 섹션 | `<h3>` 묶음 |
|---|---|
| 원정 | (머리글) 전투 파티는 한 팀 · 파티 · 벤치 · 원정 지역 · 반복 원정 · 전투 관전 · 리포트(부상 포함) |
| 캐릭터 | 장비 · 접사 죄종 · 기본 옵션 · 세부 옵션 · 아이템(가방) |
| 스킬 | (머리글) 트리 격자 규약 · 스킬 포인트 · 액티브 슬롯 · 죄종 트리 · 마스터리 · 전직 트리 |
| 마을 | 벤치(파견 대기) · 탐험 · 영입 후보(등급) · 후보 교체(리롤) · 유니크 영웅 |
| 도감 | 몬스터 도감 |
| 새 게임 | 첫 파티 |

**텍스트 출처 = 기존 i18n 키.** 도움말은 문구를 새로 쓰지 않는다 — 인게임에서 걷어낸 `*.note` / `*.sub` / `*.hint` 를 **같은 키 그대로** 렌더한다. 수치 파라미터(`{n}` `{cap}` `{e}` `{b}` `{pct}` 등)도 인게임과 같은 값(`D.balance.*` · `D.eliteRounds` · `D.bossRound` · `D.codexLevels`)을 넣는다. 죄종·직업이 들어가는 스킬 트리 제목은 **현재 선택된 영웅**의 값을 쓴다.

새 키는 넷뿐 — `nav.help` · `help.title` · `help.newgame`(섹션 제목용) · `help.exp.party` / `help.exp.bench`(인게임에서 숫자만 남기며 원문이 옮겨온 자리).

---

## 13. 연구 탭 — 연구 + 파티 전술

> **[개정 2026-08-31] 연구 탭은 「연구」 + 「파티전술」 두 섹션이다** (GAME_DESIGN §9 08-31). **연구**(스킬 노드·레시피 해금 · 채집 재료 소모 · **영웅 파견 없음**)는 비용 곡선·해금 순서가 미정이다.

**[개정 2026-09-01 사용자 지시] 두 섹션은 세그먼트가 아니라 한 화면에 세로로 선다** — 위가 연구, 아래가 파티전술.

| 왜 세그먼트가 아닌가 | |
|---|---|
| **자리가 이미 있다** | 파티전술은 칸이 **전부 열려도** 1280×700 에서 세로 362px 만 쓴다(4열 격자 2줄). 아래로 240px 이 비어 있어 연구가 그대로 들어간다. 세그먼트는 그 자리를 버리고 클릭을 하나 더 만든다 |
| **세그먼트는 배타적 상태를 가르는 장치다** | 이 화면 체계에서 세그먼트·목록은 **한 번에 하나만 성립할 때** 쓴다 — 원정의 편성/관전/리포트(§4), 마을의 파견처 6(§8). 연구와 파티전술은 배타가 아니라 **같이 서 있어도 모순이 없다** |
| **빈 세그먼트를 안 만든다** | 연구가 미착수라 세그먼트로 가르면 하나가 **빈 탭**이 된다. 원정의 비활성 세그먼트는 사유가 분명한데(재생 중인 전투가 없다) 이건 「아직 안 만들었다」뿐이라 **「고장」으로 읽힌다** (§8-1 수색 칸에서 피한 함정과 같다) |
| **연구의 크기가 아직 안 정해졌다** | 비용 곡선·해금 순서가 미정이다(GAME_DESIGN §10 「스킬의 자리」). 크기를 모르는 것에 맞춰 큰 구조를 먼저 정하면 두 번 바꾸게 된다 |

> **⚠ 뒤집는 문턱 — 연구 섹션이 세로 300px 을 넘으면 세그먼트 2로 전환한다.** 연구가 마스터리(§7) 같은 트리·격자로 확정되면 한 화면이 무너진다. 문턱을 여기 적어 두는 것은 **그때 같은 논쟁을 다시 하지 않기 위해서**다.

### 13-1. 연구 — ⚠ 목업

> **⚠ 화면에 보이는 연구 항목은 전부 목업이다** [2026-09-01 사용자 지시로 신설]. 기획이 이름도 비용 곡선도 해금 순서도 안 정했으므로(GAME_DESIGN §10) **자리와 읽는 법만 먼저 세운 것**이다. 데이터는 `ui/mock.js:RESEARCH` 에 있고 **CSV 로 가지 않았다** — 확정 전에 SSOT 를 만들면 그 CSV 가 기획을 앞질러 굳는다. 스킬 트리 목업(`SKILL_TREES`)이 같은 길을 갔다가 실동작이 오면서 삭제됐다. **부채로 등재한다** (DEV_PLAN §4).

| 영역 | 보여준다 | 결정 |
|---|---|---|
| 진척 | 완료 n / 전체 · **가진 재료**(⚠ 채집 재료는 이름도 자원 칸도 아직 없다 — 목업이 숫자를 지어낸다) | — |
| 칸 (완료) | 이름 · 해금한 것 한 줄 · **완료** 표시 | — |
| 칸 (열림) | 이름 · 해금할 것 · **비용**(재료 + 골드) | **연구** — 목업이라 누르면 미착수 안내 |
| 칸 (잠김) | 이름 · **선행 연구 이름** — 비용은 안 보인다 | — |

- **파티전술 칸(§13-2)과 같은 생김새를 쓴다** — 한 탭 안의 두 섹션이 서로 다른 카드 문법을 쓰면 같은 화면으로 안 읽힌다. 다른 것은 칸이 답하는 질문뿐이다(전술 = 「지금 켜졌나」 / 연구 = 「살 수 있나」)
- **비용은 잠긴 칸에 안 찍는다** — 「값은 항상 찍는다」(§4-1)의 예외다. 잠긴 연구의 비용은 **아직 굴려지지 않은 값**이라 찍으면 확정으로 읽힌다. 대신 **선행 조건**을 찍는다 — 그게 잠긴 칸이 답해야 할 질문이다
- **누르면 미착수 안내** — 문구는 새로 쓰지 않고 `todo.lead` 를 그대로 부른다 (§11 · §8-1 수색 버튼과 같은 처리)

### 13-2. 파티 전술

**목적** — 파티 단위 「조건 → 효과」를 고르는 자리. **카드를 줍는 화면이 아니다**: 칸은 합산 레벨이 열고, 칸의 내용은 재화로 간다 ([tactic_card_design.md §5](../game_design/tactic_card_design.md)).

**한 화면에 전부.** 세로 2단 — ① 진척 줄(합산 레벨 · 열린 칸 수) ② 칸 `[tactic_slot.csv]` 개의 격자. 영웅 띠는 **두지 않는다** — 전술은 영웅 하나가 아니라 편성 전체에 걸리므로 고를 영웅이 없다 (§5 의 띠가 붙는 탭 목록에 연구가 없는 이유).

| 영역 | 보여준다 | 결정 |
|---|---|---|
| 진척 | **합산 레벨**(로스터 전원의 레벨 합) · 열린 칸 n / 전체 · 다음 칸까지 남은 레벨 · **잠근 칸 n** ⚠[개정 2026-09-01] | — |
| **리롤 줄** ⚠[신설 2026-09-01] | 버튼 하나 — **전체 리롤 + 비용.** 비용은 잠근 칸 수가 정하고 **버튼에 적는다**. 굴릴 칸이 없으면(전부 잠금) 꺼진다 | **전체 리롤** (골드) |
| 칸 (열림) | 칸 번호 · **조건 문장 + 카운터 「지금 / 필요」** · **효과**(축 이름 + 값) · 켜짐/꺼짐 · **잠금 토글** ⚠[개정 2026-09-01 — 칸별 리롤 버튼을 대체한다] | **잠그기 / 풀기** (무료) |
| 칸 (잠김) | 칸 번호 · **필요 합산 레벨** — 내용은 안 보인다 | — |

**규칙**

- **잠긴 칸도 그린다** — 칸 수는 CSV 행 수이고 프레임은 항상 `[tactic_slot.csv]` 행만큼 그려진다. 어디까지 열리는지가 안 보이면 "없는 것"으로 읽힌다 (§4-1 · §7 과 같은 이유)
- **조건은 카운터를 달고 나온다** — 「분노 접사 3개 이상」 옆에 지금 값 `2 / 3`. 조건이 참인지 **화면에서 세어 볼 수 있어야** 하고, 안 보이면 함정이다 ([tactic_card_design §4](../game_design/tactic_card_design.md) 「화면 요구」). 카운터의 출처는 `game.tacticState` 하나다 — 렌더러가 파티를 세지 않는다
- **켜짐/꺼짐이 칸의 첫인상이다** — 꺼진 칸은 흐리게, 켜진 칸은 죄종 색이 아니라 **강조 테두리**로. 효과 수치는 켜짐과 무관하게 **항상 찍는다** (§4-1 "값은 항상 찍는다")
- **효과의 축 이름과 단위는 `tactic_option.csv:stat` 에서 파생한다** — 접사 채널이면 접사 이름, 아니면 `combat_stat.csv`. 마스터리 칸(§7)과 같은 규칙이고, 화면이 옵션 이름 사전을 따로 갖지 않는다
- **리롤은 한 번 클릭** — 되돌릴 수 없지만(§3) 잃는 것이 정해진 비용 하나이고 결과는 즉시 다시 굴릴 수 있다. 다만 **비용을 버튼에 적는다** — 얼마가 나가는지 모르고 누르는 것이 진짜 사고다
- **같은 옵션이 다시 나오지 않는다** — 잠긴 칸의 옵션 · 굴리기 직전에 들고 있던 옵션 · 이번에 뽑은 것은 후보에서 빠진다(`game.rerollTactic`). 돈을 내고 아무 일도 안 일어나는 경우를 구조로 막는다

**⚠ [개정 2026-09-01 사용자 지시 · 구현 전] 리롤은 칸별이 아니라 전체 + 잠금이다** (tactic_card_design §5-6 · INTERFACE §2-7). 화면이 받는 결정이 **둘로 갈린다**.

| | 어디서 | 값 |
|---|---|---|
| **잠그기** | 칸 안 — 옛 칸별 리롤 버튼 자리 | **무료.** 언제든 풀 수 있다 |
| **굴리기** | 격자 **위** 리롤 줄에 버튼 하나 | 잠근 칸 수가 정한다 |

- **비용은 리롤 버튼에만 적는다** — 칸에는 값을 안 적는다. 값이 칸마다 다르던 시절의 표기를 남기면 「이 칸을 굴리는 값」으로 읽힌다
- **잠금은 되돌릴 수 있으므로 한 번 클릭이다** (§3 의 「두 번 누르게」 대상이 아니다). 반대로 **리롤은 되돌릴 수 없지만** 잃는 것이 비용 하나라 역시 한 번 클릭이다 — 대신 값을 버튼에 적는다
- **잠근 칸은 첫인상이 달라야 한다** — 켜짐/꺼짐(조건)과 **다른 축**이다. 조건은 편성이 정하고 잠금은 플레이어가 정한다. 한 칸이 두 축을 동시에 들므로 표시가 겹치면 안 읽힌다
- **굴릴 칸이 없으면 버튼이 꺼진다** — 전부 잠갔거나 열린 칸이 없을 때(`canReroll`). 오류로 내지 않고 **누를 수 없게** 한다: 값을 치를 수 없는 버튼이 눌리는 것이 사고다
- 설명 문장은 두지 않는다 — 규칙은 도움말 탭(§12)

**전술이 실제로 전투에 걸린다** — 켜진 칸의 효과는 `game.heroCombat` 을 통해 **파티에 든 영웅의 전투 능력치**에 합류한다(접사·마스터리와 같은 채널). 그래서 캐릭터 탭 §6 의 세부 옵션 값이 편성에 따라 달라진다. **벤치 영웅에게는 안 붙는다** — 조건이 편성을 세는데 편성 밖 영웅이 결과를 받으면 인과가 깨진다.

호출: `game.tacticState` · `game.rerollTactic` · **`game.toggleTacticLock`** ⚠(신설 · 구현 전) — **연구 섹션은 아무것도 부르지 않는다**(목업이라 부를 계약이 없다 · §13-1)
오류 → 플래시: `rs.err.<code>` (`gold` 골드 부족 / `locked` 잠긴 칸)

---

*마지막 업데이트: 2026-09-01 (**§4-2 스킬 아이콘·설명의 출처 = `skill.csv`** — `ui/mock.js:SKILL_DISPLAY` 삭제. 화면 규칙은 안 바뀌고 출처만 옮겼다 · 로직 작업(`/client`)과 함께) · 2026-09-01 (**§6·§7 고유 스킬 칸 표시** — 액티브 1번 칸이 생성 시 굴린 고유 스킬이 됐으므로(skill_design §9-0 개정) 캐릭터 탭의 현재 스킬 카드와 스킬 창의 액티브 행에서 **번호 옆에 「고유」**(`sk.innate` · ko/en)를 단다. 무기군·전직 라벨은 그 층이 오면. 로직 작업(`/client`)과 함께 닫혔다) · 2026-09-01 (**⚠ §13-2 리롤 = 전체 + 잠금 — 화면 규칙만, 구현 전** — 칸별 리롤 버튼을 **잠금 토글**로 바꾸고 격자 위에 **전체 리롤 줄** 신설(비용은 버튼에만 적는다) · 잠금은 무료·되돌릴 수 있어 한 번 클릭 · **잠금과 켜짐/꺼짐은 다른 축**이라 표시가 겹치면 안 된다 · 굴릴 칸이 없으면 오류가 아니라 **버튼이 꺼진다**. 사용자 지시) · 2026-09-01 (**§6 페이퍼돌 = 3열 × 3행** — 보조 슬롯 폐지(GAME_DESIGN §9 · DEV_PLAN R27)로 착용 위치가 9 → 8 이 되어 4행이 3행으로 줄었다: 투구·목걸이 / 무기·갑옷·장갑 / 반지·신발·반지. **잠김 표시 삭제** — 전 무기가 양손이라 배타 자체가 없다(`.pd-cell.locked` · `pd.twoHand` 키 삭제). 오류 코드 목록에서 `twoHanded` 제거. ⚠ **문서가 코드보다 뒤에 왔다** — 보조 부위 삭제는 `equip_slot.csv` 를 지우는 순간 렌더가 깨지는 자리라 로직 작업(`/client`)에서 함께 닫혔고, 이 절은 그 결과를 받아 적은 것이다. 사용자 확정) · 2026-09-01 (**§13 연구 탭 = 한 화면 두 섹션** — 세그먼트 2 대신 세로 2단(위 연구 · 아래 파티전술)을 고른다: 파티전술이 전부 열려도 362px 만 써서 자리가 이미 있고 · 세그먼트는 배타적 상태를 가르는 장치인데 둘은 배타가 아니며 · 미착수인 연구가 **빈 세그먼트**가 되면 「고장」으로 읽힌다. ⚠ **뒤집는 문턱 명시**(연구가 세로 300px 초과 시 세그먼트 2로). **§13-1 연구 = 목업 신설**(데이터는 `mock.js:RESEARCH` · CSV 로 안 간다 · 부채 등재 · 누르면 미착수 안내). 사용자 지시) · 2026-09-01 (**§6 약어를 막대에 붙인다** — 라벨 칸 안에서 이름 왼쪽 · 약어 오른쪽 끝으로 밀어 약어가 막대 왼쪽 변에 한 열로 선다. 사용자 지시) · 2026-09-01 (**§6 막대 정렬 · 네 칸 등고 · 세로 압축** — 기본 능력치 막대가 같은 x 에서 시작한다(라벨 열을 일곱 줄이 공유) · 장비↔옵션 칸 높이를 맞춘다(세로 쌓기용 여백이 격자로 새던 것) · 네 칸을 눌러 아래 가방을 끌어올리고 **가방 제목 줄 삭제**(수치는 도구 줄로 이동 — 지운 건 글자뿐) · ⚠ 가방 전체가 한 화면에 들어오지는 않는다(남은 자리는 영웅 띠뿐). **약어 AGI → DEX** (`hero_attribute.csv:abbr` · 골든 재촬영). 사용자 지시) · 2026-09-01 (**「파견」 탭 → 「마을」 · 파견처 순서 변경** — 탭 이름을 활동(파견)에서 장소(마을)로 바꾼다(⚠ 시간축 대응이 한 겹 흐려지지만 목록 6 중 5가 장소라 속과 맞다 · 기획서의 「거점」이 가리키던 자리 · 「파견」 어휘는 탭 **안**의 칸 이름으로 살아 있다) · 목록 순서 **선술집 → 제련소 · 광산 · 채집 → 탐험 → 상단** · `?tab=town`. ⚠ 기획서(「거점」)↔화면(「마을」) 어휘 통일은 남았다. 사용자 지시) · 2026-09-01 (**§8-1 수색 칸 신설** — 명단 맨 오른쪽에 후보 카드와 같은 크기의 칸 + 수색 버튼. 옛 규칙 「칸을 미리 그리지 않는다」 폐기 — 「미착수」 배지 + 누르면 안내(기존 키 재사용)라 「고장」이 아니라 「아직」으로 읽힌다. 확정 수치 둘(인원·소요)을 칸에 찍는다. 구현되면 버튼이 결과 카드로 바뀐다. 사용자 지시) · 2026-09-01 (**§5 초상 축소 보간을 스타일이 정한다** — 초상 `<img>` 셋 전부에 `pixelated` 가 무조건 걸려 있어, 08-31 기본 스타일이 `cartoon`(512px 손그림)으로 바뀐 뒤 축소된 얼굴이 깨져 보였다(26px 칸 = 20픽셀 중 1픽셀만 남는다). `<html data-face>` + 토큰 `--face-render` 로 갈라 도트는 끄고 손그림은 켠다 · §10 `?face=` 행에 `data-face` 명시 · 배경 아트는 규칙 밖. 사용자 지시) · 2026-09-01 (**후보 카드 한 벌로 · 파견 탭에서 영웅 띠 삭제** — §3·§8-1 선술집 명단이 시작 화면과 **같은 카드**가 된다(08-31 「선술집은 안 따라간다」 뒤집기) · 카드에서 **시작 특성 삭제**(값은 살아 있고 화면에서만 내린다 — ⚠ 지금 어느 화면에도 안 나온다) · **죄종 칩을 등급 칩 오른쪽으로**(둘 다 같은 층의 정체성) · §8 파견 탭의 영웅 띠 삭제(로스터가 고를 것을 아래로 민다) · §5 제목에서 파견 삭제. 사용자 지시) · 2026-09-01 (**§6 세부 옵션 정렬 = `sheet_order` 명시 컬럼** — 시트의 행 순서를 `combat_stat.csv` 의 새 컬럼이 정한다(암묵값 「카테고리 묶음 순 → CSV 행 순」 폐기 · 화면 전용 사전 `ui/mock.js:COMBAT_CATS` 는 읽는 곳이 0이 되어 삭제) · **머리 3줄 = 대표값**(물리 공격력 · 마법 공격력 · 최대 HP — 글씨 한 단계 + 선 하나, 제목은 여전히 없다) · **칸 경계를 「불 저항 앞」에서 「피해 감소 앞」으로** (8 / 13 → 13 / 8 · 저항 4 와 최대 저항 증가가 한 칸에 모인다) · 행 수 표기를 25 → **impl=1 인 21** 로 정정. 사용자 지시) · 2026-09-01 (**창 닫기 = 정사각 X · 마스터리 판은 아래로 꽉 차게** — §2 닫기 버튼 규격을 **모든 창 공통**으로 못박고(모양·자리 동일 · 이름은 `title`), §7 판 셋의 아랫변을 왼쪽 칸에 맞춘다(칸 높이 고정은 유지 — 남는 자리는 판 안 여백). 사용자 지시) · 2026-09-01 (**탭 10 → 7 · 스킬은 창으로 · 오프라인은 파견 하나로** — §1 화면 지도 재작성(원정·파견·의뢰·캐릭터·연구·도감·도움말) · **스킬 탭 폐지** → §6 [스킬 트리 열기]가 §7 **창**을 띄운다(영웅 띠 한 벌이 사라진다) · **거점·탐험·선술집 → §8 파견 탭** 한 곳(목록 6 → 고른 것의 속 · 들어오면 선술집이 골라져 있다 · 탐험은 「파티」 표기로 문법 차이를 든다) · §2 **창 레이어** 신설 · §10 `?dev=tree` · `?tab=` 이름 7로 축소 · §11·§12 재편. **경고 2건은 §1 에 남겼다** — 선술집 접기의 대가 · 탐험은 화면에서만 파견처와 나란히 선다. 사용자 지시) · 2026-08-31 (**§4-2 창 뱃지 줄을 카드 밖으로 · 카드 크기 롤백** — 뱃지 줄을 카드 안 마지막 줄에 두니 그만큼 박스가 커져 「몬스터·영웅·보스가 전부 같은 고정 크기」의 그 값이 달라졌다. 줄을 카드 **밖 바로 아래**로 내고 카드는 뱃지 줄 이전 크기로 되돌린다. 사용자 지시) · 2026-08-31 (**§1 화면 지도 = 탭 10** — 의뢰 · 거점 · 탐험이 「미구현 목록」에서 **탭 자리**로 올라왔다(구현 7 · 미착수 3). 기획이 화면까지 확정한 셋이라 지도 밖에 두면 「아직 안 정해진 것」으로 읽힌다 · 파견처 5와 파견 리포트는 탭이 아니라는 근거 명시 · §11 을 「미착수 탭의 속」으로 재편(미착수 탭은 §12 설명 금지의 의도된 예외 — 문구는 도움말 키 재사용). 사용자 지시) · 2026-08-31 (**§4-2 관전 둘 — 창 뱃지 줄 신설 · 빈 스킬 칸 결정 뒤집기** — ①켜져 있는 창을 카드 아래 칩으로(이로운 창 초록 · 해로운 창 빨강 · 줄은 창이 없어도 자리를 지킨다 — 카드 크기 고정). ②「아레나는 빈 칸을 그리지 않는다」 폐기 → 칸 수는 언제나 `active_slots`, 액티브가 둘인 영웅은 셋째 칸이 빈 채로 남는다. 둘 다 사용자 지시) · 2026-08-31 (**§3 후보 카드 초상 확대** — 초상이 카드에서 가장 큰 조각이 된다(52px → `clamp(96px, 9.5vw, 152px)`). 시작 화면은 그림을 처음 보는 자리이고 고르는 근거의 절반이 인상이다. 선술집 명단(§8)은 세로 예산이 달라 안 따라간다. 사용자 지시) · 2026-08-31 (**기획 08-31 개정 반영** — §1 「전투 창구는 하나」 폐기(원정·의뢰 둘 · 관전은 보고 있는 탭이 정한다) · §6 가방에 **강화 모드 토글** + 강화 절 신설(툴팁이 다음 비용·다음 옵션 계단을 든다 · 3·6·9강 접사 상승은 플래시) · §11 **의뢰 탭** 행 신설 + 파견처 5 이름 갱신(연구소 삭제 → 채집) + 파견 리포트에 중도 회수 · §13 연구 탭 = 「연구」+「파티전술」 두 섹션임을 머리에 명시(연구는 미착수)) · 2026-08-30 (**§5 영웅 초상 연결** — `hero_<n>.png` 를 **이름**이 고른다(uid 는 후보에 없어서 못 쓴다) · 그림은 직업 글리프 위에 덮고 없으면 `onerror` 로 글리프가 드러난다 · 장수 < 로스터라 겹침은 정상 — 사용자 지시) · 2026-08-30 (**§13 연구 탭 신설** — 파티 전술 `[tactic_slot.csv]` 칸 · 합산 레벨 해금 · 골드 리롤 · 조건 카운터 · §1 탭 7 · §2 탭 내비 갱신 · §10 `?dev=tactics`) · 2026-08-30 (**얼굴 아트 스타일 전환** — §10 에 `?face=<스타일>` · `faces/<스타일>/` 폴더 하나가 스타일 하나 · 스타일에 없는 그림은 이니셜 폴백) · 2026-08-30 (**§9 도감 카드 — 초상이 카드의 주인** — 초상 38 → 72px(면적 3.6배) · 카드 높이를 초상이 정한다 · 핍과 「처치 · 다음」을 위아래로 분리 · 안 켜진 핍도 보이게 · 장수 줄바꿈 금지(영어) · 4스테이지 한 화면 유지(영어는 ~30px 초과) — 사용자 지시) · 2026-08-30 (**기획↔프로토타입 대조** — §4-2 관전이 실제 스킬을 그린다(목업 문단 폐기 · 회복·창·재생 추가 · 이벤트 12종) · §4-3 판정에 **철수(전투불능 발생)** 추가 · §8 선술집 재작성(리롤 쿨다운 · 고용한 칸은 빈 칸 · 수색 칸은 아직 없다)) · 2026-08-28 (**§7 3차 — 단계를 세로로 · 티어 어휘 제거 · 칸 축소**: 열 → **줄**(위가 먼저 열린다) · 「T1·T2·T3」를 화면·도움말에서 전부 뺐다(내부 어휘) · 잠금은 칸의 필요 레벨이 · 미작성은 점선 프레임이 대신 말한다 · 칸 높이 60→40px·값과 랭크를 한 줄로) · 2026-08-28 (**§7 스킬 탭 개정 — 마스터리 실동작**: 가지×깊이 → 단계 · 칸을 눌러 1랭크 · 포인트 패널에 초기화(한 번 클릭 — 무료·전액 환급이라 손실 0) · 축 이름은 `stat` 에서 파생 · 판정은 `masteryState` 가 실어 온다) · 2026-08-28 (**시작 화면 — 이어하기 판정 정정**: 「버전이 다르면」 → 「열 수 없으면」. 이관 가능한 v2·v3 는 그냥 열린다 · 판정은 state.canLoad) · 2026-08-28 (§4-1 · §5 — 편성 패널 머리·닫기 폐지(같은 행 재클릭으로 접는다) · **전투 파티 표시를 위칸 글씨에서 카드 겉 테두리로** · 위칸은 대기/치료 중만 · 파티에 넣을 수 있는 건 대기뿐(커서로도 막는다) · 리더 = 제일 먼저 넣은 영웅(단정 추가) — 사용자 지시) · 2026-08-28 (§4-1 편성 패널 — 목록 위 → **고른 행 바로 아래 접이식** · 패널 크기 고정(머리의 스테이지 이름 삭제 · 경고 줄 상시 확보 · 반복 원정을 액션 버튼으로) — 사용자 지시) · 2026-08-28 (§7 스킬 탭 배치 — 트리 판 셋을 **나란히**, 판 안에서 가지를 **세로로**. 스크롤 없이 한 화면 · 구조(3탭→2탭)는 미변경 — 사용자 지시) · 2026-08-28 (§5 · §4-2 툴팁 — 영웅 위 = **기본 능력치 7**(띠·관전 카드가 같은 카드) · 스킬 칸 위 = 이름·표기/실효 쿨·설명 · 툴팁 기계장치를 `ui/tip.js` 한 곳으로) · 2026-08-27 (§4-2 스킬 쿨 개정 — 세로 3줄 → **가로 3칸 아이콘**(13 → 32px) · 남은 쿨은 덮개가 걷히는 것으로 · **발동 연출 신설**(칸이 튀고 스킬 이름이 떠오른다 — 「방금 뭘 썼나」는 사건이라 게이지가 못 답한다) — 사용자 지시) · 2026-08-27 (§4-1 자동 선택 철회 — 편성 패널은 **지역을 눌렀을 때만** 뜬다 · 행 라벨 「고르기/고름」 → 「원정」 · 반복 토글은 런의 스테이지와 같을 때만 · §4-2 유닛 카드 **크기 고정**(몬스터=영웅=보스) · 초상 확대 · 이름을 위칸 오른쪽으로 · `?dev=form` 신설 — 사용자 지시) · 2026-08-27 (§4-1 편성 재편 — 영웅 띠를 상시 노출에서 내리고 **스테이지를 고르면 열리는 편성 패널** 안으로 · 출발 버튼이 스테이지 행에서 패널로 이동 · 들어오면 마지막 런 스테이지가 골라져 있다 · §4-2 유닛 카드 이름 위칸 + 폭 2/3 + 스킬 쿨 줄 아이콘·바(이름·% 삭제, 툴팁으로) — 사용자 지시) · 2026-08-27 (§4-3 부상 행 정정 — 설명 문장은 08-26 규칙대로 도움말 탭으로 이관됐다, 인게임 패널엔 이름·시간만) · 2026-08-27 (§4-1 편성 개편 — 파티·벤치 두 패널을 영웅 띠가 대신(클릭=파티 토글·리더 표시) · 지역 전폭 · 띠는 편성에만, 관전·리포트에는 없음) · 2026-08-27 (§4-2 좌우 대치 철회 → 상하 대치 복귀 — 사용자 지시) · 2026-08-27 (§4-2 관전 — 세로 예산 축소 · 아레나 아래 가방 · 로그에 모든 타격 · 로그/누적 데미지 탭 · 재렌더에도 재생 이어짐) · 2026-08-27 (§4-2 관전 유닛 카드 가로형 — 좌우 대치 · 스킬 쿨 3줄(목업) · XP 없음) · 2026-08-27 (§6 캐릭터 탭 4칸 등폭·등고 — 세부 옵션 2분할 · 현재 스킬을 기본 옵션 아래 정사각 카드로 · 페이퍼돌 재배치 · 핵심 전투치 4 줄을 세부 옵션으로 흡수) · 2026-08-27 (§4-2 관전 유닛 카드를 §5 와 같은 형태로 · HP 수치 바 가운데 · 영웅 띠 카드 축소) · 2026-08-27 (§5 영웅 띠 카드 개편 — 초상이 카드 전체 · 위칸에 상태 태그 + 이름 · 영웅 초상 규격 원형 → 네모 박스, 관전 유닛 카드·후보 카드와 통일 · 직업 글리프 출처 단일화) · 2026-08-26 (§12 도움말 탭 신설 — 인게임 패널의 설명 문장 전부 이관 · §2 「접이식 설명」 행 폐기 · §1 탭 6) · 2026-08-26 (관전 공격 모션 삭제 · 캐릭터 탭 패널명 기본 옵션/세부 옵션 · 세부 옵션의 카테고리 제목 삭제) · 2026-08-26 (§7 스킬 탭 목표 상태 재작성 — 액티브 3(출처 라벨) · 마스터리 2 + 전직 특화 · 롤백 · 무기군 스킬 비교. **현재 화면은 옛 구조라 미반영 표시**) · 2026-08-26 (「시설」 어휘 폐기 → 「파견처」 · 건설·업그레이드 없음 — 가짓수 = 챕터 해금 / 세기 = 배치된 영웅) · 2026-08-26 (§9 개정 반영 — 스테이지 원소 표기 · 리포트 빗나감 칸 · 저항 행의 현재 상한 · 도감 스테이지 3 계열의 죽은 축 명시) · 2026-08-26 (최초 작성 — src/ui/app.js · battle.js 에서 역추출)*
