# ADR-0015 — 고유 스킬 칸

| | |
|---|---|
| **상태** | 확정 · 2026-09-03 (신설) |
| **화면** | [SCREEN_DESIGN §3](../SCREEN_DESIGN.md) |
| **정한 사람** | 사용자 지시 |
| **대체** | — |
| **대체됨** | — |

## 결정

고유 스킬 칸  — 이름 블록을 초상 윗변에 맞춰 올리고(옛 규칙은 세로 가운데), 그렇게 생긴 오른쪽 열 아래 자리에 네모 칸 하나 + 스킬 이름을 놓는다.

## 근거 · 맥락

> 아래는 [SCREEN_DESIGN.md](../SCREEN_DESIGN.md) §3 본문에서 **그대로 옮긴 것**이다 (2026-09-09 이관). **쓰인 날의 문장이라 고치지 않는다** — 뒤집히면 새 ADR 이 이것을 대체한다.

**고유 스킬 칸 [신설 2026-09-03 사용자 지시]** — 이름 블록을 초상 **윗변에 맞춰 올리고**(옛 규칙은 세로 가운데), 그렇게 생긴 오른쪽 열 아래 자리에 **네모 칸 하나 + 스킬 이름**을 놓는다.

- **왜 고유 하나인가** — 액티브 3칸 중 **고용 시점에 정해져 있는 것은 고유뿐**이다 [정정 2026-09-03]. 무기군 칸은 **무기를 아직 안 줬으니 비어 있고**(시작 무기는 `newGame` 이 준다) 전직 칸은 **레벨 30 해금**이라 후보에게는 없다. 그래서 카드가 고유만 싣는 것은 생략이 아니라 사실이다. 「레어의 고유 스킬 = 리롤 목표」([hero_design.md §1](../../game_design/hero_design.md))를 화면이 그대로 가리키는 자리다 — 리롤 버튼이 무엇을 다시 굴리는지가 카드에서 읽힌다
- **왜 이름 바로 아래인가** — 고유는 능력치처럼 **재는 값이 아니라 「이 영웅이 무엇인가」**다. 등급·죄종 칩과 같은 층이라 능력치 막대 위, 정체성 줄 아래에 선다
- **칸에 「고유」를 적는다** — 3칸 중 하나만 보여주므로 왜 하나뿐인지가 칸 안에서 답해져야 한다. 문구는 캐릭터 탭(§6)이 쓰던 `sk.innate` 를 **그대로 재사용**한다
- **툴팁은 관전·캐릭터 탭과 같은 카드**(`tip.js:skillTipCard` — 이름 · 쿨 · 설명). 카드 본문에는 아이콘과 이름만 두고 나머지는 툴팁이 든다 (§12 「설명 문구는 도움말 전용」의 결을 지킨다). ⚠ ~~실효 쿨 줄은 접힌다~~ — **접을 것이 없어졌다** [2026-09-08 2차]: 초는 어디서나 표기 쿨이라 후보 카드도 산 영웅과 **같은 수**를 찍는다 (§4-2)
- **칸은 나중에 들어올 스킬 이미지의 자리다** [2026-09-03 사용자 지시] — 지금 든 이모지는 임시이고 칸 크기(44px)는 **그림이 들어와도 읽히는 크기**로 잡았다. 그림이 오면 칸을 통째로 채운다(`contain`)
- **역할 줄은 언제나 두 줄 자리를 잡는다** [2026-09-03] — 한국어는 한 줄, 영어는 직업에 따라 두 줄이라(「Ranged Physical · Bow / Crossbow」) **접히는 카드만** 키가 커져 세 장이 어긋났다. 자리를 늘 비워 두면 카드 높이가 언어·직업과 무관해지고, 그래야 칸을 키울 수 있다
- **오른쪽 열은 층 셋으로 읽힌다 — 정체성(칩 · 이름) / 직업 / 역할·무기군** [신설 2026-09-08 사용자 지시] — 칩 줄과 이름 사이를 **살짝**, 직업 줄과 역할 줄 사이를 **한 줄** 띄운다. 다섯 줄이 같은 간격으로 붙어 있으면 「누구인가」와 「무엇을 하는가」가 한 덩어리로 읽힌다. 값은 `style.css` 가 든다(`.ng-name` · `.ng-role`)
- **등급 칩과 죄종 칩은 같은 규격이다** [2026-09-03 사용자 지적] — 크기 · 줄높이 · 여백까지. 이름 줄에 나란히 서는 **같은 층의 정체성**인데 등급만 작으면 덜 중요한 것으로 읽힌다. 크기 계단의 SSOT 는 [src/ui/README.md](../../../src/ui/README.md) 「글씨 크기」
- **~~이름 줄은 한 줄 고정 — 긴 이름이 말줄임되고 칩은 밀리지 않는다~~** [2026-09-07] → **개정 2026-09-08 [사용자 지시] — 칩 줄과 이름 줄을 나눈다.** 영어에서 이름이 잘렸다: 3열 카드의 좁은 폭에 이름 + 등급 칩 + 죄종 칩(`Gluttony` 처럼 긴 말)이 한 줄에 들어가면 **줄어들 수 있는 것이 이름뿐**이라 말줄임이 상시로 걸렸다. **칩 둘이 윗줄, 이름이 아랫줄**이 되어 이름이 제 줄을 통째로 쓴다 — §5 관전 카드가 09-07 에 간 길(**신원 윗줄 · 이름 아랫줄**)과 같은 문법이고, 칩 둘이 나란히 서는 것도 그대로다. 아래는 폐기된 옛 규칙의 근거다 — 이름만 `…` 으로 줄고 등급·죄종 칩은 항상 이름 오른쪽 같은 줄에 남는다. `.ng-name` 에 `flex-wrap` 이 켜져 있어 긴 이름에서 칩이 **아랫줄로 밀리던** 것을 걷었다 — 플렉스는 wrap 이 켜지면 항목을 줄이기 전에 줄바꿈을 먼저 택해서, 준비돼 있던 말줄임이 발동하지 못했다. 시작 후보·선술집 명단(§8-1) 카드가 같은 컴포넌트라 함께 고쳐진다
- **칸은 초상 아랫변에 붙는다** — 이름은 윗변, 스킬 칸은 아랫변이다. 글 흐름에 그냥 두면 **역할 줄이 두 줄인 카드에서만** 칸이 내려앉아(영어 「Bow / Crossbow」) 세 장의 스킬 칸이 서로 다른 높이에 선다 — 「세 장은 같은 무게」가 깨진다
- **선술집 명단(§8-1)도 같이 바뀐다** — 09-01 「카드는 한 벌」 결정의 결과다. 고용도 고르는 자리라 같은 근거가 그대로 선다

---
*이 파일은 고치지 않는다. 「대체됨」 칸 한 줄만 예외다 — 규칙은 [README.md](README.md).*
