# TheSevenSimulationRPG — 게임 디자인

> 7대 죄악 테마의 **Lootun형 방치 파밍 RPG**. 확정 사항만 기재 — 수치는 전부 CSV(SSOT).

---

## 1. 게임 정의

> **"디아블로식 아이템 게임을, 경영 시뮬레이션처럼 운영한다"**

> **좋은 영웅과 좋은 아이템을 얻고, 적절한 파티 운영으로 챕터를 진행한다.** [2026-08-26 개정 — "장비가 주인공(A안)" 폐기]

- 수집 대상은 둘 — **영웅과 아이템**, 대등하다. 행위는 **파티 운영**(편성·배분·배치). 목표는 **챕터 진행**
- 방치형이므로 **성장할 때마다 기능이 하나씩 열린다** — 무엇을 어떤 순서로 열지는 §10
- 재료는 **다양한 경로**로 얻고, 그 재료로 **다방면의 아이템 획득·업그레이드**를 한다
- 파티를 편성해 자동전투 원정을 보내고, 루팅 리포트를 확인하고, 빌드(스킬/장비)를 재구성한다. 그래픽 최소화 — 텍스트/UI 중심. 조작이 아니라 **빌드·편성·배분 의사결정**이 본체
- 08-20 A안의 수치 가드는 유지한다 — 장비 0 이면 능력치도 0(battle_design.md §8) · **레벨 = 진입 자격 / 장비 = 세기**(§9-4)
- **게임에 끝은 없다** [2026-08-26] — 챕터 7 뒤 **난이도 티어**를 올려 다시 돌고(티어 ↑ = 더 나은 드롭), 그 뒤 **무한모드**. **특정 챕터를 깰 때마다 해금**이 열린다. 레이드는 5인. 티어와 무한모드의 관계·무한에서의 레벨 축은 §10

- **장르**: 방치형 파밍 RPG + 로스터 운영
- **참고작**: Lootun(게임 형태·거점) · Diablo 2(아이템 철학·스킬트리) · Melvor Idle(방치 숙련)
- **계보**: TheSevenRPG(아이템·몬스터·스토리 계승) + TheSevenSimulation(영웅 프레임 계승)

| 층 | 문법 | 비중 |
|---|---|---|
| 아이템 · 전투 · 빌드 · 파밍 | 완전 RPG (디아블로 언어) | **본편** |
| 거점 · 파견 | 시뮬레이션 감각 | 지류 (상한 있음) |

- 비전투 활동은 콘텐츠가 아니라 **파이프** — 파밍 루프에 재료를 공급한다
- 유저가 "파견이 본편"이라고 느끼면 잘못된 신호
- 사기·관계·폭주 미탑재 — 방치형 계약("자리 비워도 안전") 준수

### 세부 문서

| 문서 | 내용 |
|---|---|
| [hero_design.md](hero_design.md) | 영웅 2층 구조 · 능력치 두 층 · 직업 7종 |
| [skill_design.md](skill_design.md) | 스킬 트리 3탭 · 액티브 슬롯 3 |
| [battle_design.md](battle_design.md) | 행동 주기 · 발동 규칙 · 전투 능력치 24종 · **피해 계산 §9** |
| [item_design.md](item_design.md) | 슬롯 8부위 · 희귀도 · 접사 · 세트효과(보류) |
| [monster_design.md](monster_design.md) | 타입 3 · 베이스 16 · 정예 840 변형 · 스테이지 원소 |
| [story_chapter_design.md](story_chapter_design.md) | 바알 서사 · 7챕터 · 상태이상 7종 |
| [base_expedition_design.md](base_expedition_design.md) | 원정 · 거점 = 파견지 · 방치형 계약 |
| sin_mapping.md (예정) | 죄종 통일 매핑 — 첫 SSOT |

## 2. 타겟 유저 니즈 (설계 검증 기준)

모든 시스템 결정은 이 5개로 검증한다.

1. **드롭의 유의미함** — 접사 풀 희석 금지
2. **숫자로 읽히는 최적화** — 변경 효과가 수치로 보일 것
3. **병렬 가동** — 모든 영웅이 항상 뭔가를 하고 있을 것
4. **긴 체이스** — 유니크 수집·빌드 완성이라는 장기 목표 (세트 완성은 세트효과 보류 중 — item_design.md §4)
5. **순환 구조** — 비전투도 결국 전투 파밍을 가속할 것

## 3. 코어 루프 — 접속 층과 방치 층

| | **접속 중** | **자리 비움** |
|---|---|---|
| 인원 | 파티 `[balance.csv:party_size_max]`인 | 나머지 (로스터 `[balance.csv:roster_cap]` − 파티) |
| 활동 | **원정 = 전투** (관전) | **파견** (거점 시설) |
| 산출 | **장비 드롭** | 재료 · 재화 · 해금 |

> **보고 있을 때는 드롭이 떨어지고, 안 볼 때는 재료가 쌓인다.**

- 원정은 유저가 보는 상황에서 시작한다. **[2026-08-25 확정]** 반복 원정을 켜 두면 **게임이 켜져 있는 동안은 자리를 비워도 계속 진행** · **게임이 꺼지면 진행 중이던 전투(해당 몬스터)까지만 정산하고 런을 마무리** — 오프라인에 다음 라운드·다음 런은 돌지 않는다. 리포트는 다음 접속 때
- **[2026-08-26 개정] "장비 드롭은 원정 독점"(08-24) 삭제 — 크래프트로 아이템을 제작할 수 있다.** 파견 산출(재료)은 접속해서 거점(제련소)에서 써야 장비가 되므로 파견은 여전히 **본편의 연료**다 (니즈 5). 원정 없이 제작이 성립하는지의 가드는 미정(§10)

```
접속 → 원정 → 장비 드롭 ─┐
                          ├→ 거점: 분해 → 재료 → 제작·강화(낙인) → 더 깊은 원정
방치 → 파견 → 재료·재화 ─┘
```

## 4. 영웅 — 요약

상세: [hero_design.md](hero_design.md)

- **2층 구조**: 유니크 영웅 15명(수작업·고유 패시브 1·로스터 1명 제한) / 레어 영웅 무한 생성(전부 굴림·죄종×직업 35칸 커버리지 전담). 아이템 희귀도 계단의 영웅판
- **영웅 획득 창구 = 선술집 하나** [2026-08-26 확정] — 명단(리롤 쿨다운)과 수색(대기 영웅 1명 파견) 둘 다 선술집 안. **수색은 파견이 아니다.** 유니크는 어느 쪽에서도 **지정 불가 · 확률만** (base_expedition_design.md §2-4) · **해고 규칙 도입**(내용 미정 — §10)
- **직업 7종**: 본편 5(전사/기사/마법사/궁수/사제) + 확장 2(암살자/네크로맨서)
- **능력치 두 층**: 기본 능력치 7종(영웅 고유, 장비 불변) / 전투 능력치 24종(장비·스킬 파생). 게이트 없음
- **스킬**: 독립 트리 3탭(죄종/마스터리/전직) + 액티브 슬롯 3 — **출처 고정**(죄종 / 전직 / 무기군). 무기군 스킬은 무기군에 붙고 개체에 붙지 않는다

## 5. 아이템 — 요약

상세: [item_design.md](item_design.md). TheSevenRPG 코어 통째 계승 + 변경점만 문서화.

- 슬롯 **9개 / 부위 8종**(반지 ×2) · 희귀도 4단계(매직→레어→크래프트→유니크) · 죄종 접사 매트릭스 · 세트효과 **보류**(item_design.md §4) · 무기군 Implicit · 낙인 크래프팅
- 변경: 코스트 폐지 · 유틸 접사 축 신설 · 쿨감 옵션 신설 · 기본 능력치 접사 폐지

## 6. 죄종 — 하나의 축, 네 개의 일

7대 죄악이 게임 전 층을 관통하는 단일 축. 축은 하나, 일은 넷.

| 층 | 죄종의 역할 |
|---|---|
| 스킬 | 탭1 죄종 트리 (고유 메커니즘) |
| 아이템 | 접사 카테고리 · 낙인 · 지역 드롭 편향 (세트 — 보류) |
| 영웅 | 정체성 + 장비 궁합 (메인 죄종 +1 은 세트효과와 함께 보류) |
| 파견 | 죄종별 비전투 적성 |

> 통일 매핑 테이블 `sin_mapping.md` 가 첫 SSOT 과제 — 두 원작의 매핑이 서로 다르다.

## 7. 거점 · 파견 — 요약

상세: [base_expedition_design.md](base_expedition_design.md)

- **시설이 곧 파견지** — 시설은 플레이어가 굴리고, 배치된 영웅이 효율을 결정한다. 매력=선술집·상단(교역 — 매력만 둘) / 지능=연구소 / 운=제련소(공방 없음) / 힘=광산(⚠)
- **탐험** — 파티 단위 "제2의 원정". 시설이 아니라 원정 문법을 비전투에서 반복. 민첩·건강·통솔의 파견 역할 후보
- 상점 없음 · 합성은 스킬 카드 개념(후속) — 시설 아님
- **방치형 계약 (불변)**: ①자리 비워도 로스터 파괴 없음 ②오프라인 진행 지원 — 파견·탐험·치료 타이머(**원정은 오프라인에 진행하지 않는다**) ③모든 사건은 리포트로 — 실시간 개입 요구 금지

## 8. 기술

상세: [docs/client/](../client/) — [ARCHITECTURE.md](../client/ARCHITECTURE.md)(구조) · [INTERFACE.md](../client/INTERFACE.md)(이식 계약) · [DEV_PLAN.md](../client/DEV_PLAN.md)(계획·현황) · [SCREEN_DESIGN.md](../client/SCREEN_DESIGN.md)(화면). 규칙: [CLAUDE.md](../../CLAUDE.md)

| | Phase 1 (현재) | Phase 2 (스팀 출시) |
|---|---|---|
| 방식 | **무빌드 웹** — ES Modules + 순수 DOM/CSS | Godot 또는 Unity (미확정) |
| 이식 대상 | — | `game_logic/` + CSV 만. UI는 재작성 |

- 서버 없음(싱글), 데이터 CSV(SSOT — 엔진 이식 시에도 무변환), 세이브 LocalStorage
- 전투는 **결정론 시뮬레이션** (시드 주입) — 관전과 오프라인 계산의 결과 일치가 이식 검증 수단

## 9. 결정 로그

| 날짜 | 결정 |
|---|---|
| 08-20 | A안 — 장비가 주인공, 영웅은 빌드 슬롯. 사기/폭주 미탑재 |
| 08-21 | 층 배분 — RPG 본편 · 시뮬 지류. 로스터 소수 정예 |
| 08-21 | 계승 정책 — 아이템·몬스터·챕터/스토리 = TheSevenRPG 통째 계승. 1차 포크 25종 (읽기 전용) |
| 08-21 | 코스트 폐지 · 유틸 접사 도입 · 시뮬형 행동 보정은 접사 풀 배제 |
| 08-21 | 스킬 3탭(죄종/마스터리/전직) · 포인트 공유 배분 자유 · 전직 레벨 해금 |
| 08-21 | 직업 7종 — 본편 5 + 확장 2. 사제 서약 · 직업↔죄종 1:1 금지 |
| 08-21 | 영웅 2층 — 유니크 15 + 레어 무한. 고유 패시브는 유니크 전용 1개 |
| 08-21 | 전투 발동 규칙 — 슬롯 3, 한 차례 하나, 마나 없음, 쿨 실시간 |
| 08-21 | 원정 = 스테이지 런 · 파티 3 / 로스터 7 / 동시 원정 1 |
| 08-21 | 스테이지 = 9라운드 (정예 3/6 · 보스 9) · 중도 귀환 루팅 전량 보존 |
| 08-21 | 부상/치료 모델 — 귀환 무료 회복, 전투불능만 치료 타이머. 피로도 없음 |
| 08-21 | 세트 3/6/9 (8부위 기준) · 양손 무기 2포인트 · 메인 죄종 +1 |
| 08-21 | 엔드게임 = 신규 컨테이너 없이 챕터 재순환 |
| 08-21 | Phase 1 무빌드 웹 → Phase 2 엔진 이식 |
| 08-22 | 몬스터 데이터 재작성 — 구조 결함 5종 수정, 신규 SSOT 4테이블 (monster_design.md §0) |
| 08-22 | 도감 = 몬스터 처치 수 모델. 아이템 도감 폐기 |
| 08-22 | **능력치 두 층** — 기본 7 / 전투 27 분리. 장비의 기본 능력치 기여 0 (불변식) |
| 08-23 | **게이트 전면 폐지** — 능력치는 착용·습득을 막지 않는다. 착용 제약은 요구 레벨만 |
| 08-23 | **전투 계수 확정** — 힘=물공 / 민첩=공속·캐스팅 / 지능=마공 / 건강=타격회복·상태이상저항 / 감각=명중·회피 / 통솔=파티 스킬 / 매력=없음. 최대 HP 시작값 전 영웅 공통(`hero_hp_base`) |
| 08-23 | **거점 개정** — "영웅 판정 없음" 폐지. 시설 = 파견지, 배치 영웅이 효율 결정. 능력치 ↔ 시설 1:1 (매력=선술집 / 지능=연구소 / 감각=제련소 확정) |
| 08-24 | **idle 계약** — 원정은 관전으로 시작, 로그아웃해도 완주. 장비 드롭은 원정 독점, 파견은 재료·재화. 파견 영웅도 같은 8부위 로테이션. 부상 영웅 파견 불가 |
| 08-24 | **유니크 영웅 획득 = 선술집** — 주기 갱신 명단에 희귀 등장, 매력 배치가 확률을 민다 |
| 08-25 | 기획서 전면 정리 — 미확정 항목 삭제, 확정만 유지. 남은 과제는 §10 |
| 08-25 | **무기군 9종 × 직업 전속 배정** — 전사(도끼·둔기·창) / 기사(한손검·양손검) / 마법사·사제(스태프·완드 공유) / 궁수(활·석궁). 무기군 Implicit은 진형·타겟팅 확정까지 **보류** |
| 08-25 | **장비 슬롯 9개 — 반지 ×2 / 목걸이 ×1** (부위 종류는 8 유지). 죄종 상한 9→**10** → 각성(9) 후 **자유 슬롯 1** 발생, 유니크와 각성이 양립 |
| 08-25 | **비전투 활동 확정** — 시설 5: 영입·연구·제련 + **교역·채광**. **탐험 = 파티 단위 "제2의 원정"**(시설 아님). 합성 = 스킬 카드 합성(별개 개념, 후속). 상점 없음(매력의 "상점 가격" 역할 삭제) |
| 08-25 | **매력 = 선술집 + 상단** — 매력이 영입(등장 품질)과 교역(환율) 둘 다 민다. 능력치↔시설 1:1의 유일한 예외. "통솔=선술집" 안은 철회. **공방 없음** — 제작 계열은 제련소. 민첩·건강·통솔 파견 역할 미정(후보: 탐험 판정) |
| 08-25 | **죄종 세트효과 보류** — "접사의 죄종 = 자동 세트포인트 → 3/6/9 세트효과" 구조를 통째로 보류. 안 A(슬롯 8·상한 9·유니크 vs 각성)와 안 B(슬롯 9·상한 10·자유 슬롯 1) 모두 item_design.md §4에 보존. 세트에 걸려 있던 "세트 3 = 상태이상 부여"·죄종 트리 세트 시너지 노드·메인 죄종 +1·오만/폭식 특례도 같이 보류 |
| 08-25 | **오프라인 원정 규칙** — 반복 원정은 게임이 켜져 있는 동안만 계속 돈다(자리 비움 무관). 게임이 꺼지면 진행 중이던 전투까지 정산하고 런 마무리. "로그아웃해도 완주"(08-24) 폐기. 오프라인 산출은 파견·치료 타이머뿐 |
| 08-25 | **전투 능력치 27→24** — 삭제 7: 상태이상 적중 · 상태이상 저항 · 회복량(→ 마법 공격력 비례) · 파티 보정(→ 스킬 효과) · 스킬 레벨 · 파견 시간 단축 · 마법 방어. 추가 4: **원소 저항 전기·불·냉기·독**(마법 방어 대체). 계수 붙는 스탯 9→6 — 건강은 타격 회복만, 통솔은 전투 계수 없음. 공격의 원소 배정은 미정 |
| 08-25 | **액티브 슬롯 출처 고정** — 죄종 / 전직 / **무기군**. 무기군이 스킬 종류를 정하고 세기는 정하지 않는다(개체 귀속 금지 · 세기 = 배분 + 전투 능력치). 순서(우선순위)는 자유. "슬롯3 = 스킬 카드" 안은 불채택 |
| 08-25 | **도감 = 몬스터 카드 모델** — 처치 수 문턱(08-22) 대체. 카드 드롭(장비 파이프라인과 별개, `codex_card_drop_pct`) → 레벨별 필요 카드 수 `codex_level.csv`(점증 곡선) · 누적 카운트(소모 없음) · 처치 수는 기록만. 등급별 차등은 후속 (monster_design.md §8) |
| 08-25 | **피격 경직 폐지 + 건강 재배정** — 맞아도 행동 주기가 밀리지 않는다. 행동을 막는 규칙은 상태이상으로 일원화. `fhr` 은 "타격 회복 속도" → **"상태이상 회복 속도"**(건강 계수)로 의미 재배정 — 상태이상은 확률로 막지 않고 회복 속도로 상대한다. 계승 둔기=경직 Implicit 무효 (stat_id `fhr` 은 계승 접사 매핑 유지를 위해 보존) |
| 08-26 | **피해 계산 뼈대 확정** — `[1] 기본 공격력 = 능력치계수 × 무기공격력 × (1+Σ상시%)` → `[2] 타격 = 기본공격력 × 스킬배율 × 치명배수 × (1+Σ조건부%)` → `[3] 최종 = 타격 × (1−감소) × Π(1−피해감소)`. 물리·마법 공통이고 다른 것은 감소 단계뿐. **괄호 안은 덧셈, 괄호끼리만 곱셈** — 같은 총량이면 상시/조건부를 나눠 가진 쪽이 세다(장비 갈아끼우기의 동력). 조건부 축을 셋 이상으로 늘리지 않는다 (battle_design.md §9) |
| 08-26 | **무기 = 밑수** — 08-26 오전의 "무기 공격력 = 항 하나"를 **뒤집었다.** `+피해 %`는 무기 밑수만 곱하고 다른 슬롯의 고정 공격력은 이 괄호에 더하지 않는다(D2 계보). 슬롯 역할이 분화된다 — 무기=밑수 / 나머지 8슬롯=배율·조건부·방어·유틸. **고정 데미지는 물리·마법과 나란한 별개 채널**로 이 라인 밖 (정의는 후속) |
| 08-26 | **편차는 타격 항이 아니라 무기 개체 굴림** — 드롭 시 무기군 편차 폭으로 한 번 굴려 개체에 박는다. 타격당 편차는 타격 횟수가 늘면 평균으로 사라져 밸런스 축이 못 되지만, 개체에 박으면 영구히 남고 "잘 뜬 것을 찾는" 파밍 성격이 된다. 방어구 고유값도 같은 이유로 굴린다 |
| 08-26 | **방어 감쇠 = 롤(LoL) 방식 · `def_curve_k` 는 상수** — 같은 날 "K = def_curve_k × 공격자 레벨"을 폐기. `감쇠율 = 방어/(방어+K)`, K의 뜻은 "감쇠 50%가 되는 방어값". **실효 체력이 방어에 선형**이라 방어 1점의 가치가 항상 같고, 면역·무의미·상한 규칙이 전부 없다. 대신 **방어는 성장 축이 아니라 비율 축** — 성장은 HP·공격력이 담당하고 방어값은 게임 내내 한 대역에 머문다 (수치 대역 재설계가 숙제) |
| 08-26 | **피해 감소는 원천별 곱** — 덧셈이 아니다. 0에 수렴할 뿐 닿지 않아 상한 규칙이 필요 없고, 접사 하나의 실효 체력 기여가 항상 일정하다. 덧셈+상한은 상한 직전에 가치가 폭증했다 벽에서 죽는 최악의 곡선. 방어 곡선과 곱해지므로 한쪽 몰빵이 손해 |
| 08-26 | **명중·회피 폐지 → 레벨 차가 적중을 정한다** — 같은 날 "명중 = 대결 한 번"을 폐기. `적중률 = f(공격자 레벨 − 방어자 레벨)`, 오버레벨은 100% 상한 · 하한 존재 · **영웅 개별 레벨**(파티 평균 아님) · 몬스터 레벨 = 스테이지 `dlvl`. 이유: 회피는 피해 감소와 기대값이 같고 분산만 더해 자동전투에서 순수 부채이며, 운에 의한 전멸이 방치형 계약을 깬다. 얻는 것 — 분산이 통제 가능해지고(적정 레벨에서 분산 0), **성장 축마다 전용 신호**가 생긴다(빗나감↑ = 레벨 부족 / 빗나감 없이 패배 = 장비 부족). **레벨 = 진입 자격 / 장비 = 세기** |
| 08-26 | **원소 저항 = 상한형 %** (D2 계보) — 물리 방어와 규칙이 다르다. 축이 하나(방어)면 무한 곡선, 넷(저항)이면 상한형이 맞다. 저항은 직접 %·합연산, 기본 상한(`res_cap_base`)은 **최대 저항 증가**로만 뚫고 절대 상한(`res_cap_absolute`)이 별도. **관통 축 없음** — 저항 깎기는 그냥 +/− 개념. **하한 없음(음수 = 피해 증폭)**. 상한이 "이 챕터 저항 맞추기"라는 달성 가능한 체크포인트를 만든다 |
| 08-26 | **몬스터는 영웅과 같은 전투 능력치 체계를 쓴다** — 몬스터 방어 200과 영웅 방어 200이 같은 감쇠를 만든다. 계산이 한 곳(이식 대조도 같은 함수), 도감이 공략 정보가 되고, 정예 특성이 영웅 접사 어휘를 쓴다. 단 **기본 능력치 7종은 영웅 전용** — 몬스터는 전투 능력치를 직접 들고 대부분의 축은 값이 0인 부분집합 (battle_design.md §8-1) |
| 08-26 | **감각 → 운 · 비전투 전담** — 명중·회피 폐지로 전투 역할이 빈 자리를 재정의. 운은 **드랍률·골드 획득 계수**만 담당하고 전투 계산 밖. 파견은 제련소 유지(제작·강화 **성공률**이라 근거가 오히려 강해짐). 전투/드롭 양쪽에 걸면 무조건 우월 능력치가 되어 편성 결정이 사라지므로 전투 계수는 주지 않는다. **전투 계수 6종 → 4종**(힘·민첩·지능·건강) · 기본 능력치의 역할은 전투 계수 / 파견 판정 / **드롭 보정** 셋 |
| 08-26 | **수치 대역 재설계 (실행)** — `def_curve_k` 상수화의 후속. ① **성장 SSOT 키 `power_growth_per_level` 신설** — 무기 공격력·영웅 HP·공격/HP flat 접사가 이 기하 곡선을 타고, 방어·저항은 안 탄다(비율 축), % 접사는 ilvl 무관(곡선 밖). 값은 추정이 아니라 28스테이지 라운드 시간에서 **역산**(battle_design.md §9-0) ② **몬스터 방어·저항을 규칙 생성으로 재작성** — 감쇠를 정하는 것은 챕터가 아니라 **role**. 저항은 `resist` 단일 컬럼 → **원소별 4컬럼 직접 %**, 자기 원소에 강하고 대립 원소에 약하다(monster_design.md §7-1) ③ 저항은 등급 **배율** 대신 `spawn_grade.res_add` %p 가산 ④ 보스 HP 배율 하향. **검증: 28스테이지 x 3라운드 = 84칸 중 82칸이 목표 시간의 0.5~2.0배 안** (재설계 전 42/84) |
| 08-26 | **원소 배정 확정 — 원소는 스테이지가 정한다** — `attack_type` = `physical`/`fire`/`cold`/`lightning`/`poison` (`magic` 폐지). **스테이지 1개 = 원소 1종**(보스 포함)이라 저항 장비를 스테이지 단위로 갈아 끼우는 결정이 성립. 영웅은 마법 **무기 개체**가 원소를 들고 스킬 태그가 우선. **원소 ≠ 죄종**(4 ≠ 7) — 두 축을 잇지 않는다. 몬스터 저항은 `resist` 단일 컬럼 = 4원소 공통(`magic_defense` 개명) |
| 08-26 | **직격 / 비직격 격리** — 반사·도트·사망 폭발은 명중·편차·스킬 배율·치명·감쇠를 받지 않고 **아무것도 유발하지 않는다**(흡혈·반사·타격 발동 효과). 반격은 직격이되 반격을 유발하지 않는다. 두 참고작이 독립적으로 같은 장치를 갖는다 — 나태=반격 / 폭식=흡수 폭발을 지금 막는다 (battle_design.md §9-6) |
| 08-26 | **게임 정의 개정 — "장비가 주인공(A안)" 폐기.** 목표 = 좋은 영웅·좋은 아이템을 얻고 파티 운영으로 챕터 진행. **영웅·아이템은 대등한 수집 대상**, 행위는 파티 운영. 방치형이라 성장마다 기능이 하나씩 열린다 · 재료는 다양한 경로 · 아이템은 다방면 획득·업그레이드. A안의 수치 가드(장비 0 → 능력치 0 · 레벨=자격/장비=세기)는 유지 (§1) |
| 08-26 | **"장비 드롭은 원정 독점"(08-24) 삭제** — 크래프트로 아이템 제작 가능. 파견 재료 → 제련소 → 장비. 원정 없이 제작이 성립하는지의 가드는 미정 (§10) |
| 08-26 | **영웅 획득 = 선술집 + 수색** — 수색은 파견의 한 종류(상세 미정). **해고 규칙 도입**(내용 미정). 선술집/수색 분담·유니크 출처 재배정은 §10 |
| 08-26 | **엔드게임 = 티어 재순환 + 무한모드 — 게임에 끝이 없다.** 챕터 7 뒤 난이도 티어를 올려 챕터를 다시 돈다(D2 방식) · **티어 ↑ = 더 나은 아이템 드롭** · 그 뒤 **무한모드**. **특정 챕터를 깰 때마다 해금** — 시스템 해금의 게이트는 챕터 클리어. 08-21 "신규 컨테이너 없이 재순환"은 레이드·무한모드로 개정. 연맹(멀티)은 서버 없음과 충돌해 제외 |
| 08-26 | **레이드 = 5인** — 로스터 7 중 5 (파견에 2 남는다). 해금 시점 미정 |
| 08-26 | **레벨 상한 99 · 전직 30 · 50 이후 레벨업은 느리게** — 50 이후는 시간 게이트이며 **파밍의 부산물로만** 오른다(레벨 전용 콘텐츠 없음). 레벨이 주는 것은 진입 자격 · 스킬 포인트 · 능력치 자동 성장뿐(오버레벨 이득 없음 — §9-4). 99 = 빌드 완성 = 긴 체이스(니즈 4). **곡선의 숫자는 다른 수치(소요 시간·티어·드롭률) 확정 뒤 캘리브레이션** — 지금 정하지 않는다 |
| 08-26 | **선술집 확정 — 명단 + 수색, 둘 다 선술집 안.** 같은 날의 "수색 = 파견의 한 종류"를 정정한다 — **수색은 파견 슬롯을 먹지 않는다.** ①**명단 자동 갱신 폐지 → 리롤 쿨다운**(플레이어가 누른 뒤 `[balance.csv:tavern_refresh_hours]` · 오프라인에도 진행): 시계가 명단을 덮어쓰면 자리를 비운 사이 등장이 사라져 접속 압박이 생기고 방치형 계약과 충돌한다. 쿨다운은 행동에 걸리므로 명단이 저절로 사라지지 않는다 → 찜·무료 갱신권 같은 보상 장치가 불필요해진다. 고용한 칸은 빈 채로 남는다(고용이 무료 리롤 우회로가 되지 않게). ②**수색** = 대기 영웅 1명 · 동시 `[balance.csv:tavern_search_slots]` · 실패 없음 · 결과는 명단 밖 별도 칸에 수령까지 보존. 판정은 **매력**(품질·유니크 확률)과 **수색 숙련**(속도)뿐 — 장비·스킬 기여는 **보류**. ③**유니크는 어느 경로에서도 지정 불가 · 확률만** (08-24 "선술집 희귀 등장" 유지). 스테이지 조건 지정은 `target` 훅만 (base_expedition_design.md §2-4) |

## 10. 미확정 과제

확정되지 않은 것의 유일한 목록. 확정되면 §9 로 옮기고 여기서 지운다.

| 과제 | 상태 |
|---|---|
| **죄종 통일 매핑** (sin_mapping.md) | 두 원작의 매핑이 다르다 — 첫 SSOT 과제 |
| 진형 · 타겟팅 | 미작성. 무기군 Implicit 은 이것이 확정될 때까지 보류 |
| 신규 무기군 베이스 데이터 | 무기군 구조는 `weapon_group.csv` 발행(08-26). 베이스 · Implicit 수치는 미작성 |
| 탐험 상세 | 인원 · 산출 · 판정 (base_expedition_design.md §3-1) |
| 광산 / 힘 | 채광이 들어간다는 것만 확정. 힘 배정 · 시설명 · 산출은 제안 |
| 민첩 · 건강 · 통솔의 파견 역할 | 후보: 탐험 판정 |
| 스킬 카드 정의 · 합성 | skill_design.md §5 후속 |
| 기사 주력 축 | 건강 ⚠ |
| 동시 원정 1 의 영구 여부 | CLAUDE.md 컨셉 락 따름정리 2 의 강도를 정한다 |
| **파견 종류 어휘** — 탐험 · 채집 · 여행 | 여러 파견을 기획 중(08-26). 각각의 정체와 구분을 한 표로 정리. **수색은 여기서 빠진다** — 선술집 하위 기능으로 확정(08-26) |
| **무한모드의 형태** | 티어가 끝없이 오르는 것이 곧 무한인가(시스템 하나) / 유한 티어 뒤 별도 무한 던전인가(시스템 둘). 단순화 원칙은 전자 |
| **무한모드의 레벨 축** | 몬스터 레벨이 무한히 오르면 "레벨 = 자격"이 깨진다. 후보: 몬스터 레벨 고정 · HP·공격력만 스케일(§9 개정이 이미 성장 = HP·공격력, 방어 = 비율 축이라 맞물린다) |
| 유니크의 자리 | 유니크(유한·수작업)는 챕터·티어에, 무한모드는 굴림(레어·크래프트)만 — 후보 |
| 레이드 해금 시점 | 5인 확정. 어느 챕터/티어에서 열리나 |
| **XP 출처** | 레벨 XP 는 전투에서만 오나, 파견도 주나. 로스터 7 · 개별 레벨 · 50 이후 느림과 맞물려 **신규 영웅 캐치업**(티어 3 에서 발견한 레벨 1 유니크)을 정한다. 후보: 전투만 / 파견도 XP / 등장 레벨 진행 연동 |
| **데모 범위** | 챕터 7 까지 갈지, 데모에 어디까지 낼지 미정. 기준: 컨셉 락 한 바퀴(루프 + 해금 1회 + 껐다 켜기 1회)가 들어가는 최소 조각. 몬스터·스테이지 데이터는 7챕터 분량이 이미 있어 챕터 수가 아니라 시스템 범위의 문제 |
| **스킬의 자리 (큰 틀)** | 영웅·아이템 대등 수집 구조에서 스킬은 무엇인가 — 후보: 영웅을 빌드로 바꾸는 층, 티어마다 다시 짜는 리빌드 축 |
| 성장곡선 숫자 | 50 무릎 이후 곡선(구간식) · 몬스터 XP · 레벨당 시간 — **전부 다른 수치 확정 뒤** 캘리브레이션 |
| **크래프트 가드** | 원정 독점 삭제(08-26) 후 "원정 없이 제작이 성립하는가". 후보: 레시피 = 원정 재료(가루·카드) + 파견 재료 둘 다 요구 |
| **수색 조건 지정** | 지금은 무지정 전용(`target` 훅만 있다). 후보 — 완료한 스테이지를 골라 그 구역을 수색 · 유니크의 챕터는 지역 죄종 편향에서 규칙으로 도출하고 정확한 스테이지는 단서로 좁힌다(숨김 테이블은 쓰지 않는다). 유니크 15명·스테이지 데이터가 선행 (base_expedition_design.md §2-4) |
| **수색 판정에 장비·스킬이 기여하는가** | 보류. `attr_equip_bonus = 0` 불변식(hero_design.md §4-2)과의 관계를 함께 정한다 — 장비가 품질을 직접 밀면 "매력 높은 영웅 → 선술집" 배치 결정이 우회로로 무너진다 |
| **해고 규칙** | 반환물 유무 (아이템 분해의 영웅판 — 고용→해고 루프로 재료를 짜는 구멍 주의) |
| **기능 해금 사다리** | 시스템 해금의 게이트 = **챕터 클리어(08-26 확정)**. 남은 결정: 무엇을 여는가(종류 — 새 활동 / 기존 것의 새 층 / 용량 / 편의 / 새 무대) · 첫 시설 시점(그 전엔 오프라인 산출 0 — 데모 범위와 함께) · 반복 원정 잠금 여부 · 연구소 = 편의 해금의 SSOT 여부 |
| **몬스터 HP 곡선 정리** | 방어·저항은 규칙 생성으로 정리했으나 `hp`·`attack` 은 08-22 수작업 값. 기준 곡선 대비 0.57~1.55배로 흔들린다 (9개 스테이지가 ±38% 초과) |
| 보스 행의 HP 이중 elevation | 보스가 같은 스테이지 일반몹 평균의 1.70배(최대 3.07배) HP 를 들고 `spawn_grade.hp_mult` 를 또 받는다 — monster_design.md §0 결함 2 와 같은 문제 |
| **궁수의 주력 축** | 감각→운으로 `archer.keyAttr` 이 전투 계수를 잃었다. 기사의 `vit` 도 함께 볼 항목 |
| **능력치 계수의 형태** | 주력 축 분포가 상한에 짓눌려 있어 어떤 계수를 써도 개체차가 안 생긴다. `hero_attr_max` 로 분포를 펼지가 선행 결정 (hero_design.md §4-1-1) |
| 적중률 곡선의 모양 | 레벨 차 몇에서 몇 % 인지 (battle_design.md §9-4) |
| 고정 데미지 채널 | 물리·마법과 나란한 별개 채널 — 정의 미작성 |
| 사이즈 특효 축의 존치 | 종족은 스테이지 단위로 예측되지만 사이즈는 대상마다 갈려 인과를 읽기 어렵다. 퇴역 검토 |
| 매혹(charm) 재정의 | 계승 정의가 "명중률 대폭 감소"인데 명중 축이 사라졌다 |
| 통솔의 자리 | 전투·파견 둘 다 공백 (hero_design.md §4-1) |

---

*마지막 업데이트: 2026-08-26 (엔드게임 = 티어 재순환 + 무한모드 · 레이드 5인 · 레벨 99/전직 30/50 무릎 · §10 8건 추가) · 2026-08-26 (선술집 확정 — 명단 리롤 쿨다운 · 수색은 선술집 하위 기능(파견 정정) · 유니크는 확률만 · §10 「선술집 vs 수색」 해소) · 2026-08-26 (게임 정의 개정 — "장비가 주인공" 폐기 · 원정 독점 삭제 · 영웅 획득 = 선술집+수색 · §10 6건 추가) · 2026-08-26 (피해 계산 뼈대 전면 개정 — 무기=밑수 · 상시/조건부 두 괄호 · 방어 곡선 상수 K(롤) · 피해감소 원천별 곱 · 명중/회피 폐지→레벨 차 적중 · 원소 저항 상한형 · 몬스터=영웅 공통 체계 · 감각→운)*
