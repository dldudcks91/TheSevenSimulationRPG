# 아이템 설계

> 상위: [GAME_DESIGN.md](GAME_DESIGN.md) §5
> 데이터: `src/data/inherited/` (읽기 전용) + `src/data/` 신규 테이블

---

## 1. 구조

### 슬롯 — 부위 8종 · 착용 위치 9개 [확정 2026-08-25]

주무기 / 보조무기 / 투구 / 갑옷 / 장갑 / 신발 / 목걸이 / **반지 ×2**

- **드롭은 부위 단위** — 반지는 착용 **위치**가 둘일 뿐 부위는 하나다. 착용 위치 9 는 세트포인트 상한 논쟁(§4-2)에서 나왔지만 **그와 독립으로 확정**이고 세트 폐기에 딸려 가지 않는다
- **양손 무기 착용 시 보조 슬롯 잠김** — 무기군 선택이 부위 하나를 거는 트레이드오프
- 베이스 스탯 고정, **굴림이 세기를 정한다** — "레벨업이 아니라 굴림으로 강해진다". ilvl 이 미는 것은 **성장 축**(무기 밑수 · 공격력/HP flat 접사)이고 **% 옵션은 ilvl 과 무관하다** — 분류는 §2-1 [정정 2026-08-27]
- 무기군 Implicit: 수치가 아니라 전투 규칙을 바꾼다 (한손검=치명, 양손검=방무, 도끼=고데미지, 창=선제타). **둔기=경직은 무효** — 피격 경직 폐지 (battle_design.md §2, 08-25)

### 가방 — 보관 상한 [코드 반영분 문서화 2026-08-27]

가방 상한 `[balance.csv:inventory_cap]`. **이미 구현돼 돌아가는데 기획서에 없던 규칙이라 여기 적는다.**

| 상황 | 처리 |
|---|---|
| 장착·해제로 가방이 넘칠 때 | **거절** — 결과 코드 `bagFull` → 플래시 |
| 원정 정산에서 가방이 차 있을 때 | 그 드롭은 **버려진다** — 리포트에 버린 개수를 찍는다 |

- **가방이 원정 사이의 「정리」를 강제하는 장치다** — 컨셉 락의 "접속 중 = 원정 + 아이템 정리"가 여기서 성립한다. 상한이 없으면 분해·장착이 미룰 수 있는 일이 되고, 정리가 결정이 아니게 된다
- ⚠ 상한값은 제안값이고 **가방 확장 수단(재화 소모 등)은 미정** — §5-3

### 희귀도 4단계 = 통제 가능성의 계단

```
매직(완전 RNG) → 레어(옵션 수↑) → 크래프트(옵션을 고정해서 제작) → 유니크(고정 효과)
```

- 일반(Normal) 등급 없음 — 필드 드롭 전부가 유의미
- 통제권이 올라갈수록 비용이 올라가는 일관 곡선
- **크래프트는 드롭 등급이 아니라 제작의 결과다** [확정 2026-08-26] — 필드 굴림은 매직·레어까지고, 크래프트는 제련소에서 **특정 옵션을 고정해서** 만든다. 그중 죄종을 지정하는 수단이 낙인. 재료는 §5
- ⚠ **"옵션이 아니라 스킬을 고정한다"는 안은 미확정** — §3 의 "무기 개체는 스킬 전용 파워 축을 갖지 않는다(예외 후보는 유니크 무기뿐)"와 정면으로 부딪힌다. 열려면 그 가드레일부터 다시 연다

### 접사

**죄종 정체성 — 옵션 구상의 참고 축 [메모 2026-08-31]**

⚠ **절대 규칙이 아니다** — 옵션·마스터리를 구상할 때 참고하는 방향성일 뿐, 모든 칸이 이 정체성을 문자 그대로 따라야 하는 건 아니다.

| 죄종 | 정체성(참고) |
|---|---|
| 분노 | 오직 공격 |
| 나태 | 다양한 버프 |
| 색욕 | 흡수 |
| 시기 | 디버프 |
| 오만 | 강력하고 고귀한 |
| 폭식 | 성장 및 누적 |
| 탐욕 | 돈 관련 |

**죄종 × 부위 매트릭스 — 죄종 칸 [개정 2026-08-31 — 위 정체성 참고해 재구성, 부분 진행 중]**

계승 `equipment_prefix.csv`(읽기 전용)를 위 정체성 참고해 다시 짠 골격. 값은 CSV, 여기는 어떤 옵션이 어느 죄종·부위에 붙는지만. **(신규)** 표시는 계승 데이터에 없던 옵션.

| 부위 | 분노 | 나태 | 색욕 | 시기 | 오만 | 폭식 | 탐욕 |
|---|---|---|---|---|---|---|---|
| 무기 | 치명타 확률 | 공격속도 증가 | 흡혈 | 방어 무시 | 레벨당 공격력 | 공격력 % 추가증가 | 매직아이템 획득확률 증가 |
| 갑옷 | 공격력 증가%(신규) | 회복 속도(`fhr`) | 피해 흡수(신규) | 상태이상 확률(신규) | 레벨당 방어력 | 방어력 | 매직아이템 획득확률 증가 |
| 투구 | 치명타 피해(신규) | 체력 재생 | 흡혈(신규) | 대상 방어력 감소(신규) | 전체 스킬 레벨 | 체력 증가 | 매직아이템 획득확률 증가 |
| 장갑 | 분쇄 타격 | 쿨타임 감소(신규) | 흡혈(신규) | 상태이상 확률 | ⚠전체 능력치(§2 잠정 교체 대상) | 공격 속도(⚠ 성장·누적 정체성과 이질적) | 매직아이템 획득확률 증가 |
| 신발 | 공격 속도(신규) | 공격 속도(신규) | 피해 흡수(신규) | 상태이상 확률(신규) | ⚠전체 능력치(§2 잠정 교체 대상) | 체력 증가(신규) | 매직아이템 획득확률 증가 |

- **탐욕 칸 전체를 매직아이템 획득확률 증가로 통일** — 골드 획득·아이템 드랍 확률 증가는 죄종 전용에서 빠지고 **통합옵션으로 이동**(아래). 탐욕이 위 드롭 파이프라인 절의 "매직찬스가 희귀도의 유일한 플레이어 통제 축" 서술의 실제 담당 죄종이 된다는 뜻 — 다른 6죄종 장비 빌드는 매직찬스 접근권이 없다는 함의는 아직 미확인 (GAME_DESIGN.md §10 "매직찬스가 별개 스탯인가")
- 색욕·시기(신발)·갑옷(분노·색욕·시기)·투구(분노·색욕·시기) 다수가 **신규 stat_id 발행 대상**(피해 흡수 · 대상 방어력 감소 · 상태이상 확률 등)이다 — 발행 전이라 이름만
- **보조·목걸이·반지 3부위는 이 매트릭스 자체에 칸이 없다** — 계승 표가 5부위뿐이라 신규 3부위는 죄종 칸도 통합옵션 칸도 아직 없다 (드롭 경로 공백은 위 파이프라인 절 참고)
- **`affix.csv`(2026-08-31 신규 발행)와의 관계는 미정** — `affix.csv` 는 죄종 칸이 없는 별도 20행 풀이라, 이 매트릭스를 흡수해서 죄종 컬럼을 추가할지 별개로 둘지 확정 전
- **마스터리(skill_design.md §3-1~§3-3)는 아직 이 정체성으로 안 맞췄다** — 이미 확정된 T2/T3 중 결이 다른 죄종이 있다(예: 오만 T2 는 피해감소·최대저항증가 등 "불가침"에 가깝고 "강력하고 고귀한"과는 다른 결). 손댈지는 별도 판단 (GAME_DESIGN.md §10 "매직찬스가 별개 스탯인가" 교차 참조)

**통합옵션 — 죄종 무관, 부위 공통 [확정 2026-08-31]**

| 부위 | 통합옵션 |
|---|---|
| 무기 | 최소/최대 피해 · vs Normal/Demon/Undead 추가 피해 · 골드 획득 증가 · 아이템 드랍 확률 증가 |
| 갑옷·투구·장갑·신발 (공통 템플릿) | 방어력 · 체력 · 회피율(⚠ §2 확정 폐기 스탯 잔존) · vs Normal/Demon/Undead 피해 감소 · 골드 획득 증가 · 아이템 드랍 확률 증가 |

- **대형/중형/소형(사이즈) 추가 피해·피해 감소 — 삭제** [2026-08-31] — 사이즈는 대상마다 갈려 관전 중 인과를 읽기 어렵고(통제성 우선 위반), 접사 풀 희석(검증 기준 1)에도 걸린다. GAME_DESIGN.md §10 "사이즈 특효 축의 존치"가 "퇴역 검토"였던 걸 확정 처리 — vs 타입(Normal/Demon/Undead)은 몬스터 카드·도감에 이름표가 노출돼 상대적으로 더 읽히는 축이라 남긴다
- 계승 `equipment_common_option.csv` 에서 **명중률·힘·민첩·코스트 감소**를 뺐다 — 전부 §2 확정 폐기 항목(명중/회피 폐지 · 기본 능력치 접사 폐지 · 코스트 폐지)과 겹친다
- 골드 획득·아이템 드랍 확률 증가는 원래도 이 통합옵션 목록에 있었다(계승 데이터가 죄종 칸과 중복 보유하고 있었다) — 탐욕 칸 교체로 그 중복이 풀렸다

**고정 옵션 — 부위면 무조건 붙는 것 [신설 2026-08-31]**

죄종 칸·통합옵션과 달리 **굴림 대상이 아니라 그 부위 종류면 항상 붙는다.**

| 부위 | 고정 옵션 |
|---|---|
| 무기 | 공격력 +% |
| 갑옷·투구·장갑·신발 | 방어력 +% |

- **무기 쪽은 이미 있던 구조를 그대로 승격한 것** — "무기=밑수, `+피해 %`는 무기 밑수만 곱한다"(08-26 확정, battle_design.md §9-1)의 `+피해 %`를 굴림이 아니라 무조건 붙는 것으로 바꾼 것뿐이다. 방어구 4종도 같은 모양(밑수 + 무조건 곱하는 %)으로 맞췄다
- **밑수(개체 굴림)와는 다른 층이다** — 무기 공격력·방어구 고유값(§2 개체 굴림, 위 드롭 파이프라인 6단계)이 이미 존재하는 베이스이고, 이 고정 옵션은 그 위에 얹는 무조건 배율 한 겹이다
- 이중 스택 걱정 없음 — 피해 계산 뼈대가 "괄호 안은 덧셈"(08-26 확정, battle_design.md §9)이라 죄종 칸·통합옵션에서 같은 축이 또 나와도 곱이 아니라 더해질 뿐이다

- **지역(챕터) 죄종 드롭 편향 — 폐기** [2026-08-27] 접사의 죄종은 **드롭 확률에 관여하지 않는다.** 전 죄종 균등이다. 몬스터에 죄종 편향을 주는 안은 후속으로 열어 둔다
  - 대가 — 타겟 파밍 축이 하나 사라진다. 남은 축은 **부위**(몬스터가 정한다) 하나뿐이고, 그것이 당분간 스테이지 선택의 유일한 근거다. 전술카드가 죄종 접사를 셀 때 통제 경로가 낙인 하나로 줄어드는 문제는 §4-4 와 한 묶음
- 한 장비에 같은 죄종 중복 금지 — **장비당 같은 죄종 최대 1개**. 세트 폐기로 "포인트"라는 통화는 소멸했고(§4-1), 중복 금지만 접사 규칙으로 존치한다
- **접사는 기본 능력치를 주지 않는다** — 장비가 주는 것은 전투 능력치(`combat_stat.csv`)뿐 (§2)

### 세트포인트 — **폐기** [2026-08-26]

"장비에 붙은 죄종 접사가 자동으로 세트포인트가 되어 세트효과를 낸다"는 구조를 **닫는다**. 08-25 보류를 해제하지 않고 폐기한 것이다 — 자동 발현은 장비 한 칸을 바꿀 때마다 9슬롯 전체를 재계산하게 만들어 **복잡도가 디폴트**가 된다. 접사의 죄종이 하는 일은 **접사 카테고리 · 낙인 지정** 둘이다 (지역 편향은 08-27 폐기).

**그 자리는 전술카드가 가져간다** — 조건 → 효과 카드 한 장이 접사 죄종을 **센다**. 복잡도가 플레이어의 선택(카드를 꽂는다)이 되고, 안 꽂으면 장비 선택은 순수 스탯 비교로 남는다. 설계 자산은 §4에 보존 — 이관이지 내용물 폐기가 아니다. → [tactic_card_design.md](tactic_card_design.md)

### 낙인(stigma)

챕터 보스 드롭 재료. 크래프팅 시 접사의 죄종을 플레이어가 지정 — "통제권에 대한 프리미엄".

- **소모처는 둘** [2026-08-26] — 크래프트(죄종 지정) · **전술카드**. 공급은 챕터 보스 하나뿐이라 **공급량은 아이템 쪽에서 잠그고 전술카드는 그 예산을 나눠 쓴다** (§5-1)

### 드롭 파이프라인 [확정 2026-08-27]

**처치 1회당 드롭은 최대 1개다.** 굴림 순서는 이렇다 — rng 소비 순서는 이식 계약이므로 바꾸려면 [INTERFACE.md §5-2](../client/INTERFACE.md) 를 먼저 고친다.

| # | 단계 | 무엇이 정하나 |
|---|---|---|
| 1 | **드롭 여부** | 몬스터 등급 + 아이템 드랍률. **레벨은 보지 않는다** |
| 2 | **부위** | 몬스터별 확률 |
| 3 | **ilvl** | 스테이지 레벨(dlvl) + 몬스터 등급 |
| 4 | **희귀도** | 몬스터 등급 + 매직찬스. **ilvl 은 보지 않는다** |
| 5 | **접사** | 개수는 희귀도가, 종류와 값은 굴림이 |
| 6 | **개체 굴림** | 무기 공격력 · 방어구 고유값 (§2) · (마법 무기) 원소 |

- **레벨과 희귀도를 서로에게서 뗀 것이 이 파이프라인의 핵심이다.** 레벨은 **대역**만 정하고(3), 희귀도는 **몬스터와 플레이어**가 정한다(4). 한쪽이 둘 다 밀면 저레벨 스테이지가 두 겹으로 죽고, 무엇을 만져야 하는지도 안 보인다
  - 그래서 **매직찬스가 희귀도의 유일한 플레이어 통제 축**이 된다. 유틸 접사를 신설한 이유("전투력 vs 파밍 효율" 스왑 결정, §2)가 여기서 실제로 성립한다
  - 저레벨 스테이지를 매직찬스로 착취하는 경로는 열리지 않는다 — ilvl 이 낮으면 희귀도가 높아도 **점수가 낮다**(§6)
- **처치당 최대 1개.** 등급 차등을 드롭롤 **횟수**(`spawn_grade.csv:drop_roll`)로 주지 않는다 — 상한을 1로 두고 차등을 **확률**로 옮긴다. "한 마리 = 최대 하나"라야 리포트에서 인과가 1:1로 읽힌다 (철학 2 — 통제성)
- **카테고리 롤은 넣지 않는다.** 골드·장비·카드·재료를 한 표에서 굴리는 안도 있으나, 카드가 별개 판정(monster_design.md §8) · 골드가 처치마다 무조건 · 가루가 정예/보스 고정이라 **굴릴 대상이 장비 하나뿐**이다. 선택지가 하나인 롤은 단계만 늘린다. 몬스터 재료가 확정되면(§5-3) 그때 1단계와 합쳐 `{안 나옴 / 장비 / 재료}` **한 번의 롤**로 만든다
- 유니크·크래프트는 이 경로에 없다 — 유니크는 챕터 보스 전용 추가 판정, 크래프트는 제작의 결과(§1)
- **[개정 2026-08-31] 강화 = 베이스 능력치 성장 + 옵션 값 상승** — 무기 공격력·방어구 방어력 같은 **베이스 능력치**(§2 개체 굴림)를 **+9강**까지 올린다. **3강마다**(3/6/9강) 보유 옵션(접사) 중 랜덤 1개의 **값만** 오른다 — **재굴림 없음**(어떤 옵션이 뽑힐지는 랜덤, 옵션 종류·존재는 절대 안 바뀐다. 파밍한 옵션 조합을 강화가 뒤엎지 않는다). 골드 소모(골드의 최종 소모처). 옛 "강화 = 접사 증폭"을 대체. 제작·강화가 먹는 자원 전체는 **§5**
- **의뢰(전투)도 이 파이프라인을 그대로 탄다** — 원정과 같은 급의 전투이므로 드롭 경로를 공유한다 (base_expedition_design.md §1-3). 재사용 세부는 미정

**⚠ 파이프라인에 남은 미확정**

| 항목 | 상태 |
|---|---|
| **2단계의 "몬스터별"이 어느 단위인가** | `monster.csv` 의 `monster_base`(종족) 와 `role`(전투 역할) 두 후보. `role` 은 방어 관련 수치와 대략 상관돼 있어(검증된 산출 규칙은 아님 — monster_design.md §7-1, 08-31 삭제) 겸직시키면 "탱커라 단단하다"와 "탱커라 갑옷을 준다"가 섞일 소지가 있다 |
| **신규 3부위의 드롭 경로** | `monster_drop_equipment.csv` 는 5부위뿐이라 보조·목걸이·반지는 **경로 자체가 없다** (inherited_data_gaps.md G3-b). 새 테이블이 필요하다 |
| **매직찬스가 별개 스탯인가** | 드랍률(1단계)과 매직찬스(4단계)를 나누면 유틸 접사가 3종이 된다. §2 는 유틸 축을 "드랍률/골드" **둘**로 적어 뒀고, D2 는 MF 하나가 개수와 등급을 함께 민다. 접사 풀 희석(검증 기준 1)에 직접 걸린다 |
| **부위 편향이 스테이지마다 실제로 달라야 한다** | 몬스터 구성이 스테이지마다 비슷하면 편향이 평준화되고, 스테이지 선택이 "갈 수 있는 가장 높은 곳"으로 자동 수렴한다. 몬스터 배치를 짤 때의 제약 |
| **`drop_roll` 컬럼의 처리** | 최대 1개 확정으로 `spawn_grade.csv:drop_roll` 이 할 일을 잃는다. 확률로 옮기든 폐기하든 정리가 필요하다 |

## 2. 변경점

| 항목 | 이전 안 | 현행 | 이유 |
|---|---|---|---|
| **코스트(장비 용량)** | 수평 제약 | **폐지** | 유니크 희소성은 드롭률이 대체. ("접사 없음 → 세트포인트 0" 트레이드오프는 세트포인트 폐기로 함께 소멸 — §4) |
| **착용 제약** | 요구 레벨 + 코스트 | **요구 레벨만** | 요구 능력치 게이트도 폐지 — 무기군 선택은 직업이 담당 (hero_design.md §4) |
| **기본 능력치 접사** | `str`/`dex` 공통옵션 + `all_stats` | **전면 폐지** | 장비가 능력치를 주면 개체차·파견 배치 결정이 붕괴 (hero_design.md §4-2) |
| **유틸리티 접사** | 탐욕 접사만 | **유틸 축 확장** — 드랍률/골드 (파견 시간 단축은 08-25 삭제) | "전투력 vs 파밍 효율" 스왑 결정 생성 (Lootun·D2 MF 계보) |
| **무기군** | 5종 (전부 물리 근접) | **지팡이·활 추가**(본편) + 단검(확장) | 직업 7종 체계. 지팡이 = 마법 공격 체계의 그릇 |
| **마법 무기의 원소** | 없음 | **개체가 원소 하나를 든다** — 드롭 시 굴림 (불·냉기·전기·독) | 무기군은 스킬 종류를, 개체는 **상대할 저항**을 정한다. 세기가 아니라 대상 선택이라 "무기군 스킬은 개체에 붙지 않는다"(skill_design.md §3)와 충돌하지 않는다 (battle_design.md §9-5) |
| **원소 저항 접사** | `magic_resist` 1종 | **전 원소 공통 + 원소별 4종**, 단위는 **직접 %**(상한형) | 마법 방어 폐지(08-25)의 후속. 스테이지가 원소를 갖게 되면서(monster_design.md §2) 저항을 갈아 끼우는 결정이 생긴다 |
| **최대 저항 증가** | 없음 | **신규 축** — 저항 기본 상한을 뚫는 유일한 수단 | 유니크·크래프트·낙인의 자리. 상한 근처에서 실효 체력 기여가 커 무게가 충분하다 (battle_design.md §9-5) |
| **무기의 지위** | 밑수 | **밑수 유지** — `+피해 %`는 무기 밑수만 곱한다 (08-26 개정) | 슬롯 역할 분화: 무기=밑수 / 나머지 8슬롯=배율·조건부·방어·유틸. 다른 슬롯의 고정 공격력은 이 괄호에 더하지 않는다 (battle_design.md §9-1) |
| **무기 개체 편차** | `min_damage`~`max_damage` | **드롭 시 한 번 굴려 개체에 박는다** (무기군 편차 폭) | 타격당 편차는 타격 수가 늘면 평균으로 사라져 축이 못 되지만, 개체에 박으면 영구히 남아 "잘 뜬 것을 찾는" 파밍 성격이 된다. 방어구 고유값도 같은 이유로 굴린다 |
| **조건부 공격 접사** | `vs_*_dmg` (무기 전용) | **무기 슬롯 전속 유지** | 별도 괄호로 곱해지므로 슬롯 9개에 쌓이면 폭발한다. 무기 1슬롯이면 합이 구조적으로 상한을 갖는다 (battle_design.md §9-2) |
| **명중·회피 접사** | `accuracy` / `evasion` | **전면 폐지** | 적중은 레벨 차만으로 정해진다. 회피는 피해 감소와 기대값이 같고 분산만 더해 자동전투에서 부채다 (battle_design.md §9-4) |
| **쿨타임 감소** | 없음 | **신규 옵션 축** | 스킬 쿨과 행동 주기의 정렬 최적화 손잡이 (battle_design.md §6) |
| **접사 풀 순도** | — | 시뮬형 행동 보정(채집 효율 등)은 접사 풀에 **넣지 않음** | 접사 풀 희석 방지 (니즈 1) |

> **피해 감소 접사의 값은 낮게 유지한다** — 원천별 곱연산이라 실효 체력이 접사 개수에 **지수**로 자란다(방어는 선형). 값이 크면 방어 축을 몇 개만에 추월한다. 방어 ↔ 피해 감소의 균형 손잡이는 이 교환비 하나뿐이다 (battle_design.md §9-3)

### 2-1. 접사의 ilvl 스케일링 [개정 2026-08-26 — 수치 대역 재설계]

접사가 ilvl 로 어떻게 커지는지는 **접사마다 다르다.** battle_design.md §9-0 의 세 분류를 그대로 따른다.

| 분류 | 스케일링 | 해당 접사 |
|---|---|---|
| **성장 축** | 굴림 × `[balance.csv:power_growth_per_level]` ^ (ilvl − 1) | 공격력 flat · HP flat |
| **비율 축** | 굴림 + ilvl × 완만한 계수 | 물리 방어 flat |
| **곡선 밖** | **ilvl 과 무관** — 굴림 범위 그대로 | 모든 % 접사 · 치명 확률/피해 · 공속 · 흡혈 · 방어 무시 · 저항 · 유틸 |

- **% 접사에서 ilvl 가산(`perIlvl`)을 전부 제거했다** — 밑수가 이미 기하로 자라는데 % 까지 ilvl 로 커지면 곱셈층이 두 번 자라 후반이 폭주한다. % 는 "이 챕터 안에서 얼마나 더 센가"만 표현한다
- **저항 접사는 곡선 밖 + 직접 %** — `res_all` 과 원소별 4종. 상한이 목표라 값이 자랄 이유가 없다 (battle_design.md §9-5)
- **무기 공격력·방어구 고유값은 접사가 아니라 개체 굴림** — 드롭 시 편차 폭으로 한 번 굴려 개체에 박힌다 (§2 표)

### 데이터 조치 — `equipment_option_override.csv`

`src/data/inherited/` 는 읽기 전용이므로 오버라이드 테이블로 덮는다.

- `exclude` — 기본 능력치 접사(`str`/`dex`) · 코스트 감소 접사 · **명중/회피 접사(`accuracy`/`evasion`)** [08-26 추가]
- `replace_provisional` — 오만 `all_stats` → 잠정값 (장갑=치명타 피해, 신발=**상태이상 회복 속도** `fhr` — 상태이상 저항이 08-25 삭제되어 08-26 치환). sin_mapping.md 에서 정식 교체

## 3. 스킬/영웅 시스템과의 접점

> **치명 배수의 출처가 아이템 쪽으로 넘어왔다** [2026-08-27] — 치명 배수는 피해 공식 [2] 에서 **곱의 항**이라 하나만 풀려도 전체가 튄다. 그래서 **죄종 마스터리에서 뺐다**([skill_design.md](skill_design.md) §3). 남는 출처 후보는 **유니크**와 **낙인 크래프트**(통제권 프리미엄 — §3-1)뿐이고, 레어 접사로 열면 흔해진다. 아껴 두면 치명 배수가 **파밍의 정점 스탯**이 된다 — D2 가 Deadly Strike 의 배수를 고정하고 확률만 쌓게 한 것과 같은 처리. 확정은 아직 없다 ([GAME_DESIGN.md](GAME_DESIGN.md) §10)

- **죄종 마스터리**에 죄종 접사 시너지 노드 — 스킬과 아이템이 같은 죄종 축에서 만난다. 세트포인트 기반 노드는 세트효과 폐기와 함께 소멸했고, 스킬이 **전술카드 조건을 미는** 방향으로 재정의된다 (tactic_card_design.md §3)
- **직업 마스터리**의 무기 숙련 ↔ 무기군 Implicit 연동 (무기 숙련은 직업 마스터리 귀속으로 확정 — 08-31, skill_design.md §3-4)
- **기본 공격은 무기군이 결정** — 무기군 Implicit이 전투에 직접 닿는 지점
- **무기군 = 액티브 3 중 한 칸** — 착용 무기군이 스킬 종류를 준다 (skill_design.md §2). 「슬롯」 개념은 08-26 폐기. 무기 **개체**는 기본 공격에 기여하는 접사만큼만 스킬에 기여하고, 스킬 전용 파워 축을 갖지 않는다. 예외 후보는 유니크 무기뿐이며 "유니크 ≤ 잘 굴린 레어" 가드레일이 먼저다
- 장비가 기여하는 것은 **전투 능력치**뿐. 방향은 한쪽: 기본 능력치 → (계수) → 전투 능력치 (battle_design.md §8)
- 쿨감 옵션은 폭주하지 않는다 — 순차 발동이 구조적 천장이라 자연 체감

## 4. 이관 — 죄종 세트효과 → 전술카드 (설계 자산 보존)

**[2026-08-26 이관]** 08-25 에 보류했던 죄종 세트효과를 **전술카드로 옮긴다**. 폐기한 것은 **자동 발현 구조**(접사 = 세트포인트)이고, 아래 설계 자산은 카드 인스턴스의 재료로 남는다. 결정의 근거·원칙은 [tactic_card_design.md](tactic_card_design.md), 여기는 **아이템 쪽에서 무엇이 어떻게 됐는지**만.

### 4-1. 공통 골격 — 무엇이 살고 무엇이 죽나

| 규칙 | 상태 |
|---|---|
| 브레이크포인트 **3 / 6 / 9** | **산다** — 카드 조건의 계단 재료. 단 이진 문턱은 "9개에서 멈춘" 구간이 길어 계단형으로 (tactic_card_design.md §3) |
| 죄종별 성격 (폭식 = 역세트 · 오만 = 올 오어 낫씽) | **산다** — 개별 카드의 성격으로. 카드는 7×3 매트릭스를 채울 의무가 없어 오만의 3포인트 공백 문제가 소멸 |
| **영웅 메인 죄종 = +1포인트** | **죽는다 → 조건으로** 부활. 포인트 우회 없이 "메인 죄종이 분노인 영웅이 분노 접사 N개를 들면" |
| 접사 1개 = 1포인트 · 한 장비에 같은 죄종 중복 금지 | 중복 금지는 **접사 규칙으로 존치**(§1). "포인트"라는 통화는 소멸 — 카드는 접사 개수를 직접 센다 |
| **양손 무기의 죄종 접사 = 2포인트** | **죽는다** — 보조 슬롯 잠금 보상은 무기군 3축(피해 편차 · 행동 주기 · 한손/양손)이 담당 |
| 값은 CSV 무수정 — 신규 세트 테이블로 오버라이드 | **신규 카드 테이블**로 대체. `equipment_set_bonus.csv` 는 안 건드린다 (CLAUDE.md 규칙 3) |
| 낙인(§1)으로 죄종을 지정하는 크래프트 = 완성 경로 | **산다** — 카드 조건을 채우는 통제 경로가 그대로 낙인이다 |

### 4-2. 소멸한 논쟁 — 안 A / 안 B (기록)

세트포인트 상한 논쟁(안 A: 슬롯 8·상한 9 / 안 B: 슬롯 9·상한 10)은 **포인트가 장비 슬롯 수에 묶여 있어서** 생긴 문제였다. 카드 조건은 접사를 직접 세므로 슬롯 수와 독립이고, 선택 자체가 사라진다.

- 안 B 의 열린 질문("각성 빌드가 최고 유니크 1 + 죄종 8 로 정답 수렴하지 않는가")도 함께 소멸 — 카드 여러 장이 서로 다른 조건으로 경쟁하면 단일 정답이 안 선다
- **다만 잃은 것 하나** — 안 A 의 **"유니크 확실성 vs 각성 폭발력" 양자택일**은 세트가 슬롯을 먹어서 성립했던 좋은 긴장이었다. 카드는 슬롯을 안 먹으므로 **약해진다**(접사 칸 압력은 남으므로 소멸은 아니다). 대체 손잡이는 카드 슬롯 수 — tactic_card_design.md §3-1

### 4-3. 세트에 걸려 있던 다른 규칙 — 이관 결과

| 규칙 | 원래 위치 | 이관 후 |
|---|---|---|
| 세트 3포인트 = 상태이상 부여 (영웅→몬스터 경로) | story_chapter_design.md §3 | **카드 효과로** — 영웅 측 부여 경로가 여기서 살아난다 (효과 정의는 미확정) |
| 죄종 트리의 세트 시너지 노드 ("세트포인트당 …") | skill_design.md §2 | **소멸** — 스킬이 카드 조건을 미는 방향으로 재정의 |
| 메인 죄종 +1 (영웅-아이템 궁합) | hero_design.md §3 · GAME_DESIGN.md §6 | **카드 조건으로** (§4-1) |
| 코스트 폐지 근거의 "접사 없음 → 세트포인트 0" | §2 | 근거만 제거 — 코스트 폐지 자체는 유지 |

### 4-4. 아이템 쪽에 남는 숙제

- **죄종 접사를 읽는 카드는 초반에 확보 가능해야 한다** — 세트는 장비 교체만으로 자동으로 켜졌지만, 카드는 없으면 접사 죄종이 다시 카테고리 라벨로만 남는다. 해금 시점이 늦으면 게임의 3/4 동안 죄종 접사가 죽어 있다
- **접사 죄종 카운터를 화면이 세줘야 한다** — 안 보이면 조건이 함정이 된다 (SCREEN_DESIGN.md §6 캐릭터 탭 · 편성 화면 양쪽)
- **유니크 vs 레어 긴장** — 세트효과 보류로 드롭률 하나뿐이던 상태가 이관으로 해소되지 않았다. 카드가 접사 카운트를 조건으로 쓰면 접사 0 인 유니크가 다시 불리해질 수 있어, 카드 조건 언어를 정할 때 함께 본다 (skill_design.md §3 의 유니크 무기 예외 가드레일과 한 묶음)
- **"세트를 맞췄다"의 절대 기준이 없다** [2026-08-31] — 브레이크포인트 3/6/9(§4-3)는 카드 조건이 참조할 수 있는 재료일 뿐, 실제 발동 문턱은 **카드마다 제각각**이고 그 카드가 없으면 문턱 자체가 없다. 08-31 "파티 전술은 이 게임의 중심 시스템"(GAME_DESIGN.md §9) 확정으로 카드 슬롯 경쟁이 커질수록, 죄종 접사를 몰아 낀 아이템 투자가 슬롯 부족만으로 통째로 죽을 위험도 커진다 — 통제성 계약(CLAUDE.md 철학 2)과 부딪힌다. 후보 — ①카드 확보 시점을 보장(위 항목과 한 묶음) ②"N개 이상이면 세트로 인정"이라는 판정 기준을 카드와 분리해 먼저 고정 ③지금 구조(카드 없으면 죽는다)를 "복잡도는 옵트인" 원칙의 의도된 결과로 받아들이고 카드 다양성·획득 경로만 넓힌다. **미확정**

## 5. 자원 — 아이템 층이 먹는 것 [2026-08-26]

> 아이템이 무엇으로 만들어지고 무엇으로 강해지는가. 공급처의 상세는 [base_expedition_design.md](base_expedition_design.md), 여기는 **아이템 쪽 소모처** 기준으로 적는다.

### 5-1. 확정된 자원

| 자원 | 층 | 출처 | 아이템 쪽 소모처 |
|---|---|---|---|
| **골드** | 증폭 | 원정 정산 | **강화** — 베이스 능력치 성장 + 3강마다 옵션 값 상승 (골드의 최종 소모처, §1 개정 2026-08-31) |
| **광석** | 틀·양 | 파견(광산) | **크래프트 제작**의 바탕 |
| **낙인** | 지정 | 챕터 보스 | **크래프트**(죄종 지정) · **전술카드** |
| **몬스터 카드** | 기록 | 원정(처치) | 없음 — 도감 전용이고 소모하지 않는다 (monster_design.md §8) |

- **광석이 파견에서만 온다는 것이 컨셉 락과 정렬한다** — 꺼 두면 재료가 쌓이고, 켜서 제련소에서 써야 장비가 된다. 크래프트 처리량을 쥐는 것은 광산에 **배치된 영웅**이다 — 파견처에 레벨이 없으므로 세기를 미는 축은 배치 하나다 (base_expedition_design.md §2-1)
- **낙인 공급량은 아이템 쪽에서 잠근다** — 챕터 보스 = 유한 공급인데 소모처가 둘이다. 전술카드는 이 예산을 **나눠 쓰는 쪽**이고 자기 공급원을 따로 갖지 않는다 (§1 낙인)

### 5-2. 규칙 — 소모처 없는 자원은 만들지 않는다

새 컨텐츠가 생기면 **기존 자원으로 표현되는지 먼저 검증**하고, 안 될 때만 자원을 판다. 자원을 정의하는 것은 공급이 아니라 **소모처**다 (CLAUDE.md 철학 3 — 단순화가 정답).

> 반례가 이미 하나 있다 — **가루**는 이 규칙 없이 생겨서, 정예·보스 처치와 분해 두 경로로 쌓이는데 **소모처가 0**이고 기획서에 정의도 없다 (`balance.csv` 의 `dust_*` · `salvage_dust_*` 전부 ⚠제안값). §5-3 에서 함께 푼다

### 5-3. 미확정

| 항목 | 상태 |
|---|---|
| **몬스터별 재료** | 만들지 여부부터 미정. 단위 후보는 **몬스터 베이스**(`monster.csv` 의 이름 단위는 재료 창고에서 니즈 1 이 재발한다 — 베이스 단위는 챕터를 넘어 재등장하므로 초반 재료가 안 죽는다). 채택하면 **몬스터 카드와 통합할지**가 딸려 온다 — monster_design.md §8 이 "카드의 소모처는 후속"으로 훅을 남겨 뒀다 |
| **가루의 정체** | 몬스터 재료 결정에 물려 있다. 채택하면 가루는 **분해 전용**으로 축소되고, 안 하면 가루가 처치 재료 자리를 계속 든다. 어느 쪽이든 「아이템 정리」(분해 UI)와 함께 확정한다 |
| **크래프트 가드** — 원정 없이 제작이 성립하는가 | **미해결.** 지금 크래프트 입력은 광석(파견) + 낙인(보스)뿐이라 **원정 없이 성립해 버린다.** 몬스터 재료를 채택하면 그것이 원정 쪽 입력이 되어 별도 규칙 없이 구조에서 풀린다 (GAME_DESIGN.md §10). ⚠ 채집(2026-08-31 신설)의 재료는 **연구가 소모**하고 크래프트에는 안 들어가므로 이 가드를 악화시키지 않는다 |
| **에디셔널 옵션** — 새 옵션 칸 추가 | 강화(§1 개정 2026-08-31, 베이스 능력치 성장 축)와는 다른 후반 콘텐츠 축. 구체 설계 미정 |
| **지정재가 두 일을 겸한다** | 낙인이 죄종 지정과 옵션 고정을 함께 맡으면 "통제권이 오를수록 비용이 오른다"(§1)는 곡선이 한 칸에서 접힌다. 지정재를 둘로 나눌지는 전술카드 쪽과 함께 본다 |

## 6. 아이템 가치 — 지표 둘 [확정 2026-08-27]

> 방치형이라 아이템이 대량으로 들어온다. **무엇을 버리고 무엇을 낄지**를 유저가 매번 툴팁으로 비교할 수는 없다. 그 판단의 근거를 화면에 올린다.

### 6-1. 지표는 둘이고, 합치지 않는다

| 지표 | 답하는 것 |
|---|---|
| **ilvl** | 어느 **대역**인가 — 앞으로도 쓸 물건인가, 강화·크래프트의 바탕감인가 (미래 가치) |
| **점수** | 이 개체가 얼마나 **잘 떴나** (현재 가치) |

합치면 "ilvl 낮은데 완벽하게 뜬 것"과 "ilvl 높은데 쓰레기"가 같은 값이 된다. 그 둘은 플레이어에게 전혀 다른 물건이다 — 전자는 지금 쓸 것, 후자는 보관 후보다.

### 6-2. 점수 = 옵션별 구간 점수의 합

옵션마다 **구간 폭**이 있고, 굴림값이 그 폭의 몇 번째 칸에 앉았는지가 그 옵션의 점수다. 전부 더한 것이 아이템 점수다.

- **구간 폭은 옵션마다 따로 정한다.** 값의 단위가 접사마다 다르다 — 공격력 flat 과 치명 확률 % 를 같은 자로 잴 수 없으므로 전역 폭 하나로는 성립하지 않는다
- **그 폭이 곧 그 옵션의 가치 계수다.** 좁게 잡으면 그 옵션이 점수를 많이 먹고, 넓게 잡으면 적게 먹는다. 기획 의도를 넣는 자리가 여기 하나로 모인다
- **점수는 절대값이다 — 정규화하지 않는다.** 값이 ilvl 로 커지면 점수도 따라 커진다. 정규화하면 저레벨 완벽굴림이 영원히 만점이라 필터에 안 걸리고, 진행해도 낡지 않는다
- **접사가 아닌 값도 점수 대상이다.** 무기 공격력과 방어구 고유값은 접사가 아니라 개체 굴림이지만(§2), 무기에서는 이것이 **밑수**라 접사 하나보다 무게가 크다 (battle_design.md §9-1)
- **상한이 같이 나온다** — `희귀도의 최대 옵션 수 × 접사별 최대 점수`. 「점수 / 최대」 두 칸이 "더 잘 뜰 여지가 남았나"를 답한다

> **접사별 「가치 계수」 테이블 안은 폐기한다** [2026-08-27]. 계수가 효과의 *세기*를 표현하려면 곱셈층·상한·조합을 전부 반영해야 하는데 고정 계수로는 원리적으로 불가능하다 — 치명 확률이 0이면 치명 피해의 가치도 0이고, 저항은 상한을 넘으면 0이 된다. 구간 점수는 **세기가 아니라 굴림의 운**을 재므로 그 문제에 걸리지 않고, growth/band/flat 3분류(§2-1)를 몰라도 된다.

### 6-3. 표기 자리를 가른다

같은 아이템이라도 **묻는 질문이 다른 자리**에는 다른 것을 보여준다.

| 자리 | 질문 | 보여주는 것 |
|---|---|---|
| 가방 · 드롭 목록 · 자동 분해 설정 | **버릴까** | **점수** (+ 최대 대비) |
| 페이퍼돌 · 비교 툴팁 | **바꿀까** | **전투 수치 델타** — 점수 아님 |

- **착용 자리에 점수를 두지 않는다.** 게임 정의가 "빌드·편성·배분 의사결정이 본체"인데 숫자 하나가 "이게 더 좋음"을 대신 답해 버리면 남는 건 클릭이다. 착용은 전투 능력치를 두 번 계산한 **차이**로 답한다 (검증 기준 2 — "숫자로 읽히는 최적화")
- **획득 자리는 반대다.** 거기서 유저가 원하는 건 결정이 아니라 **결정하지 않을 자유**다 — 대량 유입을 일일이 볼 이유가 없다
- 화면 반영은 아직 없다 — 구현 시 [SCREEN_DESIGN.md](../client/SCREEN_DESIGN.md) 를 먼저 고친다

### 6-4. 미확정

| 항목 | 상태 |
|---|---|
| **구간 폭의 기준 ilvl** | 폭을 어느 ilvl 기준으로 적을지. ilvl 1 기준은 숫자가 잘아 표가 안 읽힌다 |
| **접사별 구간 폭 값** | 접사 풀이 확정돼야 채운다. **선행 조건** — 접사 정의가 CSV 가 아니라 `ui/mock.js` 에 있고 [INTERFACE.md §7](../client/INTERFACE.md) 이 이를 **이식 차단 항목**으로 등재해 뒀다. 폭 컬럼을 붙이려면 CSV 로 내리는 것이 먼저다 |
| **자동 분해가 무엇을 보호하나** | 점수에서 빠지는 축(저항·유틸·죄종 태그·조건부)은 문맥 의존이라 점수가 낮아도 버리면 안 될 수 있다. 전부 보호하면 필터가 아무것도 못 거른다 |
| **접사 stat 가중치** | 옵션 간 가치를 **등장 확률**로도 표현할지. 균등이면 좋은 옵션이 흔해져 점수의 변별력이 떨어지고, 가중치를 두면 희소성이 생긴다. `equipment_prefix.csv` 에 `weight` 컬럼이 있으나 코드는 읽지 않는다 |
| **자동 분해 자체** | 「저품질 자동 분해」는 base_expedition_design.md §5 에 이름만 있다. 선을 무엇으로 긋는지(ilvl 하한 · 점수 하한 · 둘 다)는 미작성 |


---

*마지막 업데이트: 2026-08-31 (**낡은 참조 정정** — §1 파이프라인 미확정 표의 "role 은 이미 방어 감쇠를 정하고 있어"가 monster_design.md §7-1 삭제로 낡아 "검증된 규칙은 아님"으로 완화) · 2026-08-31 (**§4-4 「세트 완성 기준 부재」 등재** — 죄종 접사를 몰아 낀 투자가 카드 슬롯 하나에 전적으로 의존해 발동 문턱 자체가 없다는 문제 · 08-31 "카드=중심 시스템" 확정으로 위험 확대 · 후보 3안, 미확정) · 2026-08-31 (**사이즈 특효(대형/중형/소형) 삭제** — 통합옵션에서 vs 대형/중형/소형 추가 피해·피해 감소를 뺀다. 인과 가독성(통제성 우선)·접사 풀 희석 둘 다 걸림 — GAME_DESIGN.md §10 "사이즈 특효 축의 존치" 퇴역 검토를 확정 처리(§9 이관). vs 타입(Normal/Demon/Undead)은 유지) · 2026-08-31 (**죄종 정체성 축 도입 + 매트릭스 재구성 + 고정 옵션 신설** — 죄종 정체성 7종(분노=오직 공격 · 나태=다양한 버프 · 색욕=흡수 · 시기=디버프 · 오만=강력하고 고귀 · 폭식=성장 및 누적 · 탐욕=돈 관련)을 **참고용 — 절대 규칙 아님**으로 명시 · 이 축을 참고해 §1 죄종×부위 매트릭스 재구성(다수 칸에 신규 stat_id 필요) · **고정 옵션** 신설(무기=공격력 +% · 갑옷/투구/장갑/신발=방어력 +%, 기존 밑수·개체굴림과 별개 층 — 08-26 "무기=밑수" 구조를 방어구까지 확장) · 방어구 죄종 칸 부위 표기를 "방어구"→**"갑옷"**으로 정정(`equip_slot.csv` 실제 슬롯명과 일치) · 마스터리와의 정합은 미착수로 등재) · 2026-08-31 (**죄종×부위 접사 매트릭스 골격 정리** — §1 접사에 계승 `equipment_prefix.csv`/`equipment_common_option.csv` 를 정리한 죄종 칸 표 + 통합옵션 표 신설. 탐욕 칸 전체를 **매직아이템 획득확률 증가**로 통일(골드·아이템 드랍 확률 증가는 통합옵션으로 이동) · 통합옵션에서 명중률·힘·민첩·코스트 감소 제외. 명중·회피·전체 능력치가 남은 죽은 칸과 `affix.csv` 와의 관계는 다음 정리로 넘김) · 2026-08-31 (**강화 재정의** — "강화 = 접사 증폭"을 대체해 **베이스 능력치 성장(+9강) + 3강마다 옵션 값 상승(재굴림 없음)**으로 개정 · §5-1 골드 소모처 갱신 · §5-3 「에디셔널 옵션」·「크래프트 가드에 채집 영향 없음」 등재 · 의뢰(전투)도 드롭 파이프라인을 공유한다고 명시) · 2026-08-30 (**원작 계보 서술 전면 삭제** — 두 전작의 인용과 「계승」 어휘를 걷어내고 본작의 규칙을 직접 서술한다. 실재하는 CSV 경로는 유지) · 2026-08-27 (§3 머리에 **치명 배수의 출처** 인계 — 죄종 마스터리에서 빠져 유니크·낙인이 후보가 됐다) · 2026-08-27 (**드롭 파이프라인 확정** — 처치당 최대 1개 · 레벨(대역) ↔ 희귀도(몬스터+매직찬스) 분리 · 카테고리 롤 보류 · 지역 죄종 편향 폐기 / **§6 아이템 가치 신설** — 지표 둘(ilvl·점수) · 옵션별 구간 점수 합산 · 구간 폭 = 가치 계수 · 절대값(정규화 안 함) · 표기 자리 분리(획득=점수 / 착용=델타) · 가치 계수 테이블 안 폐기) · 2026-08-27 (문서↔코드 대조 정정 — §1 "ilvl 이 %옵션 롤 범위를 정한다"가 §2-1 과 정반대라 삭제 · **가방 상한** 신설(구현돼 있는데 문서에 없던 규칙)) · 2026-08-26 (스킬 구조 개정 반영 — 탭1/탭2 → 죄종·직업 마스터리 · 「액티브 슬롯」 → 액티브 3) · 2026-08-26 (자원 층 §5 신설 — 골드·광석·낙인·카드 확정 · "소모처 없는 자원은 만들지 않는다" · 몬스터 재료/가루/크래프트 가드 미확정 / 착용 위치 9 확정 표기 · 크래프트 = 제작 결과 · 낙인 소모처 둘) · 2026-08-26 (죄종 세트효과 → 전술카드 이관 · 세트포인트 구조 폐기 — §4 재작성) · 2026-08-26 (접사 ilvl 스케일링 3분류 §2-1 — % 접사의 perIlvl 전면 제거 / 피해 공식 개정 반영 — 무기=밑수 · 무기 개체 편차 · 조건부 접사 무기 전속 · 저항 직접 %/최대 저항 증가 · 명중/회피 접사 폐지) · 2026-08-26 (오만 신발 잠정 접사 상태이상 저항 → 회복 속도 `fhr`) · 2026-08-25 (무기군 = 액티브 슬롯 한 칸 · 유틸 축 2종 · 둔기 경직 Implicit 무효)*
