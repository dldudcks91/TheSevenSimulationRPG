# cartoon — 다크 SD 카툰 흉상 (2026-08-30, 미리보기)

**2026-08-31 부터 기본 스타일이다** (`FACE_STYLES` 의 첫 항목 = 기본값 — `ui/mock.js`). `?face=pixel16` 로 옛 스타일로 돌아갈 수 있고, 그 선택은 localStorage 에 남는다.

원본은 [`../source/`](../source/) 의 스타일 앵커다. 여기 있는 것은 그중 골라서
`monster/<idx>.png` · `hero/<직업id>_<k>.png` 규칙으로 이름만 바꾼 **사본**이다 — 그림 자체는 source 가 SSOT.

## ⚠ 몬스터 배정은 아직 절반이 임시다

**챕터 1 의 `face = 1` 열한 자리가 전부 제 전용 그림을 받았다** [2026-09-09] — 마지막까지 남아 있던
**인간 셋(1201~1203)이 인간 시트로 교체**되면서 임시 배정이 사라졌다. **사탄(1900)이 09-14 에 열두 번째로 들어왔다** — 남은 것은 `face = 0` 인 다섯 자리(아래)뿐이다. **레비아탄(2900)이 같은 날 챕터 2 의 첫 초상으로 들어왔다** — 챕터 2 의 나머지 16종은 아직 `face = 0` 이다.
⚠ **1202·1203 은 2026-09-06 에 개명됐다** — ~~인간 창병~~ → **인간 궁수** · ~~트롤 돌격병~~ → **인간 기사**(`monster_base` 도 Troll → Human).
아래 표에서 ✕ 인 줄은 "화면을 채워서 스타일을 보기 위한" 배정이지 최종 아트가 아니다.

| idx | 몬스터 | 쓴 그림 | 맞나 |
|---|---|---|---|
| 1101 | 고블린 척후병 | `goblin_cloak_longear` | ◎ **제 그림** [09-06 교체] 갑옷 없는 망토 = 척후 |
| 1102 | 고블린 전사 | `goblin_helm` | ◎ **제 그림** [09-06 교체] 투구·견갑 = 「정규 병사」 |
| 1103 | 오크 전사 | `orc_tusks_strap` | ◎ **제 그림** [09-06] 굵은 엄니 · 어깨가 프레임을 채운다(`size_type = 2` 대형) |
| 1150 | **아바돈** (스테이지 보스) | `abaddon_plain` | ◎ **제 그림** [09-06] 용암 균열 · `size_type = 3` 최대. ⚠ 가시 왕관 없는 타일이라 보스 격은 약하다 |
| 1201 | 인간 보병 | `wraith_topknot_soldier` | ◎ **제 그림** [09-10 교체] 상투 머리 · 이빨까지 드러난 해골 얼굴 · 판금 어깨 |
| 1202 | 인간 궁수 [09-06 개명] | `wraith_masked_archer` | ◎ **제 그림** [09-10 교체] 짧게 친 머리 · 진홍 천으로 입을 가림 · 화살통 |
| 1203 | 인간 기사 [09-06 개명] | `wraith_bald_captain` | ◎ **제 그림** [09-10 교체] 민머리 · 검은 수염 · 이마 흉터 · 큰 고리 걸쇠로 여민 진홍 망토 |
| 1301 | 스켈레톤 전사 | `skeleton_nasal_helm` | ◎ **제 그림** [09-09 교체] 코가리개 철투구 + 뺨가리개 |
| 1302 | 스켈레톤 궁수 | `skeleton_bare_quiver` | ◎ **제 그림** [09-09 교체] 맨 두개골 + 화살 3발 화살통 |
| 1303 | 스켈레톤 기사 | `skeleton_greathelm_plume` | ◎ **제 그림** [09-09 교체] 바이저 great helm + 주황 깃 |
| 1350 | 둘라한 (스테이지 보스) | `skeleton_mage` | ○ 후드 = 머리 없는 인상. ⚠ **혼자 1차 해골 시트에 남았다** — 09-09 에 셋이 2차 시트로 갈려 톤이 갈린다 |
| 1900 | **사탄** (챕터 보스) | `satan_horns_chain` | ◎ **제 그림** [09-14] 붉은 살결 · 굽은 검은 숫양 뿔 · 목에 건 쇠사슬 · `size_type = 3`. 얼굴 정렬(§3-0) k 1.20 — 여섯 장보다 한 단계 크다 |
| 2900 | **레비아탄** (챕터 2 챕터 보스) | `leviathan_skull_fangs` | ◎ **제 그림** [09-14] 청회 비늘 · 뼈색 두개골 얼굴 · 붉은 눈 · 송곳니 · `size_type = 3`. **챕터 2 첫 초상** · 얼굴 정렬 k 1.05 — 머리 크기를 사탄에 맞췄다 |

`monster.csv:face` 가 1 인 몬스터는 이 표의 **13종**이다 — 챕터 1 의 12종(전 슬롯이 채워져 있다 — 09-06 에 아바돈, 09-14 에 사탄이 늘었다) + 챕터 2 의 레비아탄(09-14).
⚠ **챕터 1 은 17종이다.** 나머지 다섯(`1250` 레기온 · `1401~1402` 임프 둘 · `1403` 청동 수호상 · `1450` 몰록)은 `face = 0` 이라 화면에서 폴백으로 빠진다 — 아트가 들어오면 그림을 넣고 그 값을 1 로 올린다.
남은 발주 대상은 **`face = 0` 인 다섯**뿐이다 — `1250` 레기온(스테이지 2 보스 · Human) · `1401` 화염 광신도 · `1402` 임프 제사장(Imp) · `1403` 청동 수호상(Gargoyle) · `1450` 몰록(스테이지 4 보스 · Gargoyle — 붉은 황소 뿔 데몬). 스테이지 4 수하는 **몰록 교단**이다 — 광신도 · 제사장 · 신전 문지기.

⚠ **스테이지가 색으로 갈린다** [09-09 확인] — 스1 악마는 올리브·회록(luma 70~91 · 채도 7~15) · 스3 언데드는 뼈 황갈(luma 127 · 채도 24) ·
**스2 인간은 잿빛 해골 얼굴 + 진홍 천**이다 — 배경 `background_stage_102`(불타는 야영지)의 유일한 채도가 진홍 깃발인 것과 같은 자리고, **스3 해골(뼈 황갈)로 넘어가는 다리**이기도 하다 [09-10 망령 교체].

⚠ **`monster/1103`(오크)은 배경이 일부 불투명하다** — 알파 0 비율 34% · 왼쪽 아래 모서리 알파 255. 이번 인간 셋(55%)과 나란히 두면 칸에 회색 상자가 보인다. 별건이고 미수정.

## hero/*.png 는 **세이브에 박힌 `face` 가** 고른다 [2026-09-06 저장형 전환 · 2026-09-07 직업 분류]

`mock.js:heroFace` 는 영웅이 태어날 때 **제 직업 풀에서** 굴려 세이브(v13)에 저장한 `face` id 로
`hero/<직업id>_<k>.png` 를 조립한다 — 장수는 `mock.js:HERO_FACES` 가 직업별로 든다.
옛 판(이름 해시)에서 따라오던 둘 — **얼굴 쏠림**(hero_1·3·7 이 어떤 이름으로도 안 나왔다)과
**장수를 바꾸면 전원 재배정** — 은 09-06 저장형 전환으로 사라졌고, **직업 무관**이라는 마지막 결함이 09-07 에 닫혔다.
⚠ 이제 `HERO_FACES[<직업>]` 은 **줄일 때만** 영향이 있다(범위 밖 저장값은 그 직업 풀 안에서 접어서 쓴다).
늘리는 쪽은 기존 영웅에 무해하다.

**⚠ 영웅 초상은 14장 · 5직업 풀(`HERO_FACES = {warrior:3, knight:5, mage:1, archer:1, priest:4}`)이다**
[2026-09-07 저녁~밤 — **사제 3종 + 마법사 1종 추가**: `hero_priest_2` 올리브 후드 + 룬 문신 노인(시트2 · 2번) ·
`hero_priest_3` 흰 두건 + 나무 십자가(시트3 · 1번) · `hero_priest_4` 역병 의사(시트4 · 2번) ·
`hero_mage_1` 후드 + 안대 + 수정구(마법사 시트 · 3번 — **마지막 직업 공백이 닫혔다**).
전부 늘리는 방향이라 기존 영웅 얼굴은 안 바뀐다.]
이전 상태 ↓ [2026-09-07 — **직업 분류 리네임**. 파일명이 `hero_<번호>.png` → `hero_<직업id>_<k>.png` 로 바뀌었고
(매핑표는 아래), 세이브의 `face` 도 정수 → `'<직업id>_<k>'` **문자열 id**(v13 — 전 영웅 전면 재굴림).
그림은 한 장도 안 바뀌었다 · 이름과 배정 규칙만 바뀐 것이다. **마법사는 0장** = `face: null` = 화면 빈 칸. 사용자 지시]
이전 상태 ↓ [2026-09-07 — **사제 추가**(`hero_10` = `../source/priest_cross_buzz` · 반삭 + 검은 수단 + 금 십자가 — 사제 시트 4변주 중 **2번 타일**, 처음 지시는 1번이었다가 변경). 세트 첫 사제 얼굴. 저장형 전환 뒤의 **늘리는 방향**이라 기존 영웅 얼굴은 안 바뀐다.] 이전 상태 ↓ [2026-09-06 밤 — **`hero_10..11` 을 지우고 11 → 9 로 되돌렸다**(사용자 지시). 「레인저는 `hero_8` 하나만」이 지시였는데 궁수 시트의 갈색·청회 후드 2타일이 더 들어가 있었다. **게임 사본만 지웠고 원본 타일은 `../source/archer_hood_brown · archer_hood_slate` 로 남는다** — 되돌리려면 다시 깔면 된다. ⚠ 줄이는 방향이라 세이브에 `face = 10·11` 이 박힌 영웅은 `heroFace` 가 접어서 1·2 를 준다. `hero_8` 의 여백 재정렬(어깨폭 100% → 74%)은 살려 뒀다.] 이전 상태 ↓ [2026-09-06 밤 — 궁수 후드 2종 추가(`hero_10..11`). `hero_8` 과 **같은 시트**(`source_sheet_archer.png`)의 2·3번 타일이다. 저장형 전환 뒤라 **9 → 11 은 기존 영웅에 무해하다**(새로 태어나는 영웅의 굴림 범위만 넓어진다). `hero_8` 도 같은 절차로 다시 깔아 **어깨폭 100% → 74%** 로 잡았다(그림은 그대로 · 여백만 — 앵커 71~75% 대). ⚠ 「`hero_8` 밝기 32 로 세트 최저」 우려는 이 둘로 완화된다 — 갈색 63 · 청회 58. ⚠ **카키(4번 타일)는 버렸다** — ✦ 워터마크가 배경이 아니라 **인물 위**(후드 안쪽 검정 + 카키 테두리를 가로지름)에 얹혀 있어 복원이 안 된다. 밝기 94 로 세트에서 바바리안(97)에 가장 가까운 장이라 아쉬운 손실이고, **재발주하면 그 색을 1~3번 타일에 두는 편이 낫다**.] 이전 상태 ↓ [2026-09-06 밤 — **외치는 기사 추가**(`hero_9` = `../source/knight_shout_coif` · 사슬 코이프 + 붉은 서코트). 8 → 9 라 **또 전원 재배정**. ⚠ 세트에서 유일하게 입을 벌린 표정이라 반복 노출에서 피로할 수 있다 — 같은 시트 2번이 덜 격한 대안.] [2026-09-06 밤 — **인간 궁수 추가**(`hero_8` = `../source/archer_hood_black` · 후드 + 화살통). 장수가 7 → 8 이라 **기존 영웅 전원의 얼굴이 재배정됐다**. ⚠ 밝기 중앙 32 로 세트 최저 — 칸 배경(18)과 14단계 차이라 작은 칸에서 뭉갤 수 있다(대안: 시트의 2·3번 타일). ⚠ 직업 대응은 아니다 — 이름 해시라 궁수 얼굴이 전사에게 갈 수 있다.] [2026-09-06 — 기사 2장을 **3판**으로 또 갈아끼웠다(인물은 그대로 · 프레임이 더 물러나 두상이 커졌다). 장수가 안 변해 **얼굴 배정은 그대로다**. 2판 파일은 원본·사본 모두 삭제.] [2026-09-05 낮 — Gem 기사 2종 추가: `hero_6` = `../source/knight_gold_bald`(민머리 · 금테 견갑 · 붉은 서코트) · `hero_7` = `../source/knight_sun_greybeard`(백발백염 · 태양 문장). 2×2 시트(`../source/source_sheet_knight_gem.png`)에서 **위 두 타일만** 땄다 — 아래 두 장은 사용자 지시로 안 썼다. ⚠ `HERO_FACE_MAX` 가 5 → 7 이라 **기존 영웅 전원의 얼굴이 재배정된다**]. 이전 상태 ↓ [2026-09-04 저녁 — 무안면 로마군 3종 추가: `hero_3..5` = `../source/roman_mane_red / roman_mane_black / roman_band_red` 키잉 512². 2×2 시트 발주 1차 산출물(`../source/source_sheet_roman.png` · 우하단 타일은 ✦ 워터마크로 버림)]. 이전 상태 ↓ [2026-09-04 사용자 지시 · 같은 날 세 번째 개편] — `hero_1` = 검투사(`../source/gladiator_helm.png` 512 축소 · **누끼 보류라 배경 불투명**), `hero_2` = 신판 바바리안(`../source/barbarian.png` 초록 키잉 → 투명 512²). 구판 바바리안 변주 3장은 스타일 불합격(밝기 중앙 116 · 밝은 영역 3배 — 글레디에이터 기준 이탈)으로 **원본·사본 모두 삭제**했다(커밋된 적 없어 git 에도 없다). 신판(맨몸 문신판 · 같은 날 재교체)은 밝기 중앙 97 · 밝은 영역 41% — 문신이 어둡지만 맨살 면적이 넓어 수치는 되레 기준(81 · 13%)에서 멀다. 화면 판단으로 채택.

스타일·비율 확인용 잠정 세트지만, **직업 대응이 생겼다** [2026-09-07 사용자 지시] — 영웅은 제 직업 풀에서만 얼굴을 굴리므로 궁수 얼굴이 전사에게 가지 않는다. **본편 5직업이 전부 최소 1장을 갖췄다** [09-07 밤 — 마법사 1장으로 마지막 공백이 닫혔다]. 0장은 확장 직업(암살자·네크로맨서)뿐 — 풀이 0장이면 얼굴이 `null` 이고 그 칸은 빈 칸으로 남는다(자리표시를 안 깐다 — SCREEN_DESIGN §5).
⚠ **이미 `null` 로 굴려진 영웅은 그대로 빈 칸이다** — 얼굴은 생성 시 1회·불변이라, 마법사 그림이 생기기 **전에** 태어났거나 v13 이관을 거친 마법사는 소급을 못 받는다. 소급하려면 「`face = null` 만 재굴림」하는 이관이 따로 필요하다(미결).

### 영웅 파일 매핑 (구 번호 → 새 이름)

| 구 파일 | 새 파일 | 그림 | 원본 타일 |
|---|---|---|---|
| `hero_1` | `hero/warrior_1` | 검투사 (⚠ 누끼 보류 — 배경 불투명) | `../source/gladiator_helm` |
| `hero_2` | `hero/warrior_2` | 바바리안 (맨몸 문신) | `../source/barbarian` |
| `hero_7` | `hero/warrior_3` | 백발백염 기사 · 태양 문장 | `../source/knight_sun_greybeard` |
| `hero_3` | `hero/knight_1` | 무안면 로마군 (붉은 볏) | `../source/roman_mane_red` |
| `hero_4` | `hero/knight_2` | 무안면 로마군 (검은 볏) | `../source/roman_mane_black` |
| `hero_5` | `hero/knight_3` | 무안면 로마군 (붉은 띠) | `../source/roman_band_red` |
| `hero_6` | `hero/knight_4` | 민머리 · 금테 견갑 · 붉은 서코트 | `../source/knight_gold_bald` |
| `hero_9` | `hero/knight_5` | 외치는 기사 · 사슬 코이프 | `../source/knight_shout_coif` |
| `hero_8` | `hero/archer_1` | 후드 궁수 · 화살통 | `../source/archer_hood_black` |
| `hero_10` | `hero/priest_1` | 반삭 · 검은 수단 · 금 십자가 | `../source/priest_cross_buzz` |
| — (신규) | `hero/priest_2` | 올리브 후드 · 룬 문신 노인 | `../source/priest_hood_runes` |
| — (신규) | `hero/priest_3` | 흰 두건(윔플) · 처진 눈 · 나무 십자가 | `../source/priest_wimple_cross` |
| — (신규) | `hero/priest_4` | 역병 의사 — 검은 챙모자 · 흰 부리 가면 | `../source/priest_plague_hat` |
| — (신규) | `hero/mage_1` | 후드 · 안대 · 수정구 | `../source/mage_orb_eyepatch` |

⚠ **분류는 그림의 인상으로 정한 것이다** (사용자 확정) — 백발백염(구 7)은 기사 시트에서 왔지만 전사로, 민머리(구 6)·외치는 기사(구 9)는 기사로 갔다. 위 이력 문단의 번호는 전부 **구 파일명**이라 이 표로 읽는다.
새 파일 열은 2026-09-14 폴더 분리 뒤의 이름이다 — 09-07 ~ 09-14 사이에는 `hero_<직업id>_<k>.png` 였다(이력 문단의 `hero_priest_2` 같은 이름).

키잉 절차(초록 배경 전용 · 기사·해골 시트의 flood fill 과 다르다): `g − max(r,b)` 40~120 을 알파 경사로, 남는 픽셀은 `g ≤ max(r,b)` 로 눌러 번짐 제거(despill), premultiply 상태로 LANCZOS 축소 후 되돌린다 — 인물이 전 층 저채도라 오검출이 구조적으로 없다.

## 규격

- 512×512 RGBA · 투명 배경 · 장당 150~360KB (몬스터 13 + 영웅 14 = 27장) — ⚠ `hero/warrior_1`(검투사)만 배경 불투명(누끼 보류)
- 용량은 최적화 전이다. 플랫 카툰이라 256색 양자화가 잘 먹을 것으로 보이나 **화질 확인이 먼저**라 원본 그대로 뒀다
  (참고: `pixel16` 은 8장 합계 250KB)
- ⚠ **「화면에서 깨져 보인다」를 해상도 문제로 읽지 말 것** (2026-09-01) — 원인은 `image-rendering: pixelated` 가 이 스타일에도 걸려 있던 것이었다. 512는 가장 큰 칸(152px @ DPR2 = 304px)을 덮는 정상 해상도다. 줄이는 것은 **용량**을 위한 선택이지 화질을 위한 선택이 아니다 (`../../README.md` 규격)

---
*마지막 업데이트: 2026-09-14*
