# cartoon — 다크 SD 카툰 흉상 (2026-08-30, 미리보기)

**2026-08-31 부터 기본 스타일이다** (`FACE_STYLES` 의 첫 항목 = 기본값 — `ui/mock.js`). `?face=pixel16` 로 옛 스타일로 돌아갈 수 있고, 그 선택은 localStorage 에 남는다.

원본은 [`../example/`](../example/) 의 스타일 앵커다. 여기 있는 것은 그중 골라서
`monster_<idx>.png` · `hero_<n>.png` 규칙으로 이름만 바꾼 **사본**이다 — 그림 자체는 example 이 SSOT.

## ⚠ 몬스터 배정은 임시다

새 스타일로 그려진 것은 **해골 9종 + 인간 기사 5종**뿐이라, 고블린·오크·트롤에 맞는 그림이 없다.
아래 표는 "화면을 채워서 스타일을 보기 위한" 배정이지 최종 아트가 아니다.

| idx | 몬스터 | 쓴 그림 | 맞나 |
|---|---|---|---|
| 1101 | 고블린 척후병 | `skeleton_rogue` | ✕ 임시 (후드·단검 = 척후 느낌만) |
| 1102 | 고블린 전사 | `skeleton_plain` | ✕ 임시 |
| 1103 | 오크 전사 | `skeleton_pirate` | ✕ 임시 |
| 1201 | 인간 보병 | `knight_1_young_blond` | ◎ 인간 · 검 |
| 1202 | 인간 창병 | `knight_2_veteran_helm` | ○ 인간 · 투구 (창이 아니라 검) |
| 1203 | 트롤 돌격병 | `knight_4_old_greybeard` | ✕ 임시 |
| 1301 | 스켈레톤 전사 | `skeleton_soldier` | ◎ 정확 |
| 1302 | 스켈레톤 궁수 | `skeleton_archer` | ◎ 정확 |
| 1303 | 스켈레톤 기사 | `skeleton_king` | ○ 격 높은 해골 |
| 1350 | 둘라한 (스테이지 보스) | `skeleton_mage` | ○ 후드 = 머리 없는 인상 |

`monster.csv:face` 가 1 인 몬스터는 챕터 1의 이 10종뿐이라 **전 슬롯이 채워져 있다.**

## hero_*.png 는 이름이 고른다 — ⚠ 지금은 한 장뿐이라 해시가 죽어 있다

`mock.js:heroFace` 가 **영웅 이름의 안정 해시**로 `hero_1..HERO_FACE_MAX.png` 중 하나를 준다 — uid 가 아니라 이름인
이유는 시작 화면·선술집의 후보가 아직 uid 가 없어서다(뽑을 때 본 얼굴과 고용 뒤 얼굴이 갈리면 안 된다).
직업과는 무관하다.

**⚠ 영웅 초상은 5장 · `HERO_FACE_MAX = 5` 다** [2026-09-04 저녁 — 무안면 로마군 3종 추가: `hero_3..5` = `../example/roman_mane_red / roman_mane_black / roman_band_red` 키잉 512². 2×2 시트 발주 1차 산출물(`../example/source_sheet_roman.png` · 우하단 타일은 ✦ 워터마크로 버림)]. 이전 상태 ↓ [2026-09-04 사용자 지시 · 같은 날 세 번째 개편] — `hero_1` = 검투사(`../example/gladiator_helm.png` 512 축소 · **누끼 보류라 배경 불투명**), `hero_2` = 신판 바바리안(`../example/barbarian.png` 초록 키잉 → 투명 512²). 구판 바바리안 변주 3장은 스타일 불합격(밝기 중앙 116 · 밝은 영역 3배 — 글레디에이터 기준 이탈)으로 **원본·사본 모두 삭제**했다(커밋된 적 없어 git 에도 없다). 신판(맨몸 문신판 · 같은 날 재교체)은 밝기 중앙 97 · 밝은 영역 41% — 문신이 어둡지만 맨살 면적이 넓어 수치는 되레 기준(81 · 13%)에서 멀다. 화면 판단으로 채택.

스타일·비율 확인용 잠정 세트고, 직업 대응은 여전히 없다 — 마법사·궁수도 이 두 얼굴을 쓴다.

키잉 절차(초록 배경 전용 · 기사·해골 시트의 flood fill 과 다르다): `g − max(r,b)` 40~120 을 알파 경사로, 남는 픽셀은 `g ≤ max(r,b)` 로 눌러 번짐 제거(despill), premultiply 상태로 LANCZOS 축소 후 되돌린다 — 인물이 전 층 저채도라 오검출이 구조적으로 없다.

## 규격

- 512×512 RGBA · 투명 배경 · 장당 150~360KB (몬스터 10 + 영웅 2 = 12장) — ⚠ `hero_1`(검투사)만 배경 불투명(누끼 보류)
- 용량은 최적화 전이다. 플랫 카툰이라 256색 양자화가 잘 먹을 것으로 보이나 **화질 확인이 먼저**라 원본 그대로 뒀다
  (참고: `pixel16` 은 8장 합계 250KB)
- ⚠ **「화면에서 깨져 보인다」를 해상도 문제로 읽지 말 것** (2026-09-01) — 원인은 `image-rendering: pixelated` 가 이 스타일에도 걸려 있던 것이었다. 512는 가장 큰 칸(152px @ DPR2 = 304px)을 덮는 정상 해상도다. 줄이는 것은 **용량**을 위한 선택이지 화질을 위한 선택이 아니다 (`../../README.md` 규격)

---
*마지막 업데이트: 2026-09-04 (**무안면 로마군 3종 추가 — `HERO_FACE_MAX` 2 → 5** · 시트 `source_sheet_roman.png` 3타일 키잉 · 사용자 지시) · 2026-09-04 (**영웅 = 검투사 + 신판 바바리안 · `HERO_FACE_MAX` 3 → 2** — 구판 바바리안 3장 스타일 불합격 삭제(원본·사본 모두 · 미커밋), `hero_1` 검투사(불투명) · `hero_2` 신판 바바리안(키잉 투명). 사용자 지시) · 2026-09-04 (**영웅 초상 = Gem 바바리안 3장 · `HERO_FACE_MAX` 1 → 3** — 초록 배경 키잉 → 투명 512² · 원본은 `../example/barbarian_1..3.png` · 검투사 교체. 사용자 지시) · 2026-09-04 (**영웅 초상 14장 → 1장** — `hero_1..14.png` 를 전부 지우고 검투사(`source_sheet_gradiator.png` 오른쪽 위 타일 · 누끼 없음)를 `hero_1.png` 으로. `HERO_FACE_MAX` 14 → 1 이라 **이름 해시가 죽고 전 영웅이 같은 얼굴**이다. 사용자 지시) · 2026-09-03 (**영웅 초상 9장 추가 — `hero_6..14.png`** · `example_hero.png` 에서 잘랐다. ⚠ `HERO_FACE_MAX` 는 5 그대로라 **아직 화면에 안 나온다** — 올리면 해시 나머지가 달라져 기존 영웅 얼굴이 전부 재배정된다. 사용자 지시) · 2026-09-01 (규격에 ⚠ 한 줄 — 축소 시 깨짐의 원인은 해상도가 아니라 보간 설정이었다) · 2026-08-31 (「hero_*.png 는 아직 안 보인다」 정정 — 08-30 커밋으로 `heroFace` 가 이름 해시로 이미 고르고 있었다) · 최초 작성: 2026-08-30*
