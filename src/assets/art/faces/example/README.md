# 스타일 앵커 (faces/example/)

몬스터·영웅 초상 아트의 **스타일 기준 이미지**. 게임이 로드하지 않는다 — 생성 시 레퍼런스로 첨부하는 용도.
게임에서 보려면 이름을 바꿔 넣은 [`../cartoon/`](../cartoon/) 을 `?face=cartoon` 으로 켠다.

`_source_sheet_*.png` · `source_sheet_*.png` 는 Gemini 가 한 번에 뽑은 원본 시트(해골은 2752×1536, 검투사·로마군·기사·고블린은 2048×2048 — 전부 불투명).
나머지는 거기서 잘라 **배경을 알파로 되돌리고 512×512 정사각으로 맞춘** 것이다 —
⚠ `gladiator_helm.png` 만 예외다(누끼 없음 · 1013×1013 원본 해상도 · 아래 참조).

## 이 세트가 정의하는 스타일

- 클린 카툰 흉상 · SD 비율(두상이 전체의 약 절반) · 굵고 균일한 어두운 외곽선
- 플랫 베이스 + 부드러운 그림자 1단 + 둥근 면의 작은 광택 하이라이트
- **거의 정면**, 아주 살짝 틀어짐
- **머리 + 어깨 + 가슴 일부 + 한 손** — 무기가 프레임에 들어와 직업을 읽게 한다
- 배경 투명 · 저채도 팔레트

## 몬스터 — 해골 9종 (`_source_sheet_skeleton.png`)

| 파일 | 내용 | 규격 |
|---|---|---|
| `skeleton_plain.png` | 장비 없는 순수 두상 | ◎ **두상 비율·팔레트의 기준** |
| `skeleton_soldier.png` | 투구 + 한손검 | ◎ **장비 있는 표준 앵커** |
| `skeleton_archer.png` | 화살통 + 활 | ◎ `monster.csv:1302` 스켈레톤 궁수 |
| `skeleton_mage.png` | 후드 + 지팡이 | ◎ |
| `skeleton_rogue.png` | 후드 + 단검 2 | ◎ |
| `skeleton_king.png` | 왕관 | ◎ 보스급 표현 참고 |
| `skeleton_pirate.png` | 삼각모 · 안대 · 붉은 두건 | ○ 세트에서 유일하게 채도 있는 색 — 강조색 분량 참고 |
| `skeleton_bard.png` | 바이올린 | ○ 톤 이탈 (본작 서사와 무관) |
| `skeleton_chef.png` | 요리사 모자 + 국자 | ✕ 톤 이탈 — 앵커로 쓰지 않는다 |

원본 시트에는 사제(전신)가 한 장 더 있었으나 **혼자 전신이라 크기·눈높이가 안 맞아 삭제**했다.

## 몬스터 — Gem 고블린 3종 (`source_sheet_goblin.png`) [2026-09-05]

2×2 시트(2048² · 검정 격자선 `x/y = 1014~1033` · 초록 배경). **우하단 타일은 ✦ 워터마크가 있어 버렸다**(상투 + 금목걸이).
`monster.csv` 의 고블린 둘(1101 척후병 · 1102 전사)에 **처음으로 제 그림이 붙었다** — 그전까지는 해골을 돌려 쓴 임시 배정이었다.

| 파일 | 내용 | `cartoon/` 사본 |
|---|---|---|
| `goblin_hood.png` | 겨자색 후드 · 그늘진 얼굴 | `monster_1101.png` (고블린 척후병) |
| `goblin_kerchief.png` | 맨머리 · 붉은 목수건 · 큰 귀 | `monster_1102.png` (고블린 전사) |
| `goblin_headband.png` | 붉은 머리띠 · 뒤로 늘어뜨린 끈 | — 예비 (배정 없음) |

⚠ **오크(1103)에는 안 붙였다.** `monster.csv:1103` 은 `monster_base = Orc · size_type = 2`(대형)이라
고블린 흉상을 넣으면 종족도 덩치도 어긋난다. 세 번째 고블린을 예비로 남긴 이유이기도 하다 —
1101~1103 이 같은 스테이지라 비슷한 고블린 셋이 나란히 서면 관전에서 구분이 죽는다.

**원형 마스크 확인** — 도감 `.face` 가 원형이라 귀 끝이 잘린다. 실측으로 원 밖으로 나가는 그림의 양은
`1101` 4.6% · `1102` 5.2% 로, 이미 쓰던 해골(`1301` 4.7% · `1302` 6.0%)과 같은 수준이라 그대로 뒀다.

## 영웅 — Gem 기사 2종 (`source_sheet_knight_gem.png`) [2026-09-05 · **2판**]

2×2 시트(2048² · 검정 격자선 `x/y = 1016~1031` · 초록 배경) 중 **위 두 타일만 채택**(사용자 지시).
아래 두 장(성직 칼라 + 십자가 · 붉은 스카프 노병)은 안 땄다 — 우하단 타일엔 ✦ 워터마크도 있다(시트당 1개 · 우하단 고정).

| 파일 | 내용 | 밝기 중앙 · 밝은 영역 | `cartoon/` 사본 |
|---|---|---|---|
| `knight_gold_bald.png` | 민머리 중년 · 금테 견갑 · 붉은 서코트 | 72 · 4% (검투사 앵커 81 · 13%) | `hero_6.png` |
| `knight_sun_greybeard.png` | 백발백염 노년 · 사슬 목가리개 · 금빛 태양 문장 | 96 · 8% | `hero_7.png` |

**1판(같은 날 낮)은 같은 넷을 더 당겨 그려 흉상이 프레임 좌우로 잘려 나갔다** — 2판은 같은 인물을 물려
어깨까지 프레임 안에 들어오고, 갑옷의 흠집·핏자국이 늘었다. 1판 시트와 타일 2장은 사용자 지시로 **삭제**했다
(커밋된 적이 없어 git 에도 없다). 2판의 밝기가 1판보다 낮은 것(81 → 72)은 갑옷 오염이 늘어서다.

로마군·바바리안과 같은 **초록 키잉**으로 512² 투명 PNG 를 만들었다(절차는 [`../cartoon/README.md`](../cartoon/README.md)).
경사 구간(`g − max(r,b)` 40~120)에 걸린 픽셀이 타일당 **0.3%** 뿐이라 판정이 사실상 이진이었다 — 견갑이 뚫리지 않았다.

### 옛 인간 기사 5종은 남아 있지 않다

`_source_sheet_knight.png` 와 `knight_1_young_blond` 등 5장은 **2026-09-04 에 삭제됐다**(아래 「검투사」 절 참조).
지금의 `knight_*.png` 는 위의 Gem 기사 2종이고, 옛 파일과 이름 규칙만 겹칠 뿐 다른 그림이다.

## 영웅 — 무안면 로마군 3종 (`roman_*.png` · `source_sheet_roman.png`) [2026-09-04 저녁]

2×2 시트 발주 1차 산출물(2048² · 검정 격자선 `x/y=1013~1034` · 초록 배경). **우하단 타일은 ✦ 워터마크가 있어 버렸다** — 시트 발주의 설계대로다(워터마크는 시트당 1개, 우하단 고정).
넷 다 무안면(투구 개구부 = 순검정 void) · 갈기 목~어깨 도달 · 빨강은 타일당 한 곳(갈기 또는 망토).

| 파일 | 내용 | `cartoon/` 사본 |
|---|---|---|
| `roman_mane_red.png` | 빨강 갈기형 + 검은 망토 | `hero_3.png` |
| `roman_mane_black.png` | 검정 갈기형 + 빨간 망토 | `hero_4.png` |
| `roman_band_red.png` | 빨강 띠형 + 검은 망토 | `hero_5.png` |

## 영웅 — Gem 바바리안 (`barbarian.png`) [2026-09-04 · 같은 날 3판]

Gem 재발주 산출물(2048² · 불투명 **초록 `#00FF00` 배경**). 초록 배경은 「transparent」 지시가 두 번 안 먹은 뒤의
우회다: 인물이 전 층 채도 ≤ 0.25 라 `g − max(r,b)` 판정 하나로 기계 키잉이 된다(절차는
[`../cartoon/README.md`](../cartoon/README.md)). 게임용 사본은 `cartoon/hero_2.png`(키잉 투명 512²).

구판 변주 3장(`barbarian_1..3.png`)은 **스타일 불합격으로 삭제**(미커밋 — 복구 불가): 밝기 중앙 116 · 밝은 영역
35~38% 로 글레디에이터 기준(81 · 13%)을 크게 벗어났다. 신판(맨몸 문신판)은 97 · 41% — 맨살 면적이 넓어 수치상은 밝지만 화면 판단으로 채택. 발주 방법도 바뀌었다:
**장문 규칙서 대신 매 채팅에 `gladiator_helm.png` 첨부 + 「match the attached portrait's exact style」 단문**
(Gem Knowledge 의 이미지는 생성기에 전달되지 않는 것으로 판명 — 첨부만 믿는다).

## 영웅 — 검투사 1종 (`source_sheet_gradiator.png`) [2026-09-04 · 교체됨]

~~영웅 초상은 이 한 장이 전부다~~ → **같은 날 Gem 바바리안 3종으로 교체됐다(위 절).** 이 파일은 스타일 앵커로 남는다.
옛 14종(기사 5종 `knight_*` 사본 + 다인종 흉상 9종 `hero_*`)은 2026-09-04 사용자 지시로 `cartoon/` 과 여기서 **모두 삭제**했다 — 다인종 흉상의 원본 시트 `example_hero.png` 도 함께 지웠다(git 이력에는 남아 있다).

| 파일 | 내용 | `cartoon/` 사본 |
|---|---|---|
| `gladiator_helm.png` | 닫힌 코린트식 투구 · 상처 난 판금 · 맨 어깨 + 가죽 벨트 | `hero_1.png` |

원본 시트는 **2048×2048 · 2×2 · 검정 격자선**(`x = 1016~1031` / `y = 1016~1031`, 폭 16px)이고
쓴 것은 **오른쪽 위 타일** — `(1035, 0) ~ (2048, 1013)` 로 잘랐다(격자 쪽만 3px 안쪽으로 깎아 정사각 유지).
나머지 3종(머리띠 맨얼굴 · 독수리 투구 · 볏 투구)은 아직 안 땄다.

### ⚠ 이 시트는 누끼를 안 했다 — 배경이 외곽선보다 밝다

`gladiator_helm.png` 도 `cartoon/hero_1.png` 도 **배경이 불투명**이라 초상 칸에 네모 박스로 보인다(사용자 지시로 보류).

이 시트는 앞의 둘과 조건이 반대다: 배경이 회색이 아니라 **어두운 텍스처(밝기 17~30)** 이고,
그게 **외곽선(밝기 8~11)보다 밝다.** 기사·해골 시트의 「중성 회색 판정(채널 편차 ≤ 12 · 밝기 18~150)」을
그대로 걸면 **외곽선이 배경보다 어두워서 판정 밖으로 빠지고** 씨앗이 그림 안으로 샌다.
누끼를 하려면 판정식을 새로 잡아야 한다 — 아래 「누끼 작업 메모」의 절차는 이 시트에 그대로 안 통한다.

## 쓰는 법

1. 새 초상을 뽑을 때 `skeleton_plain` + `skeleton_soldier` 두 장만 첨부하고
   `match this exact style, outline weight, proportions, shading, and framing` 을 덧붙인다
   — `chef` · `bard` 를 붙이면 생성기가 그쪽 소품을 끌고 온다
2. 인간을 뽑을 때는 `knight_gold_bald` 를 함께 붙인다 (갑옷 인물이면 `gladiator_helm` 도 같이 — 닫힌 투구의 눈높이 기준이다)
3. 시트로 여러 장을 뽑으면 **같은 시트 안에서 눈높이·머리 크기가 흔들리는지** 먼저 본다
4. 한 명만 튀면 그 한 명만 다시 뽑는다

## 누끼 작업 메모

원본이 투명 PNG 가 아니라 UI 스크린샷이라 배경이 픽셀로 구워져 있었다.
**중성 회색 판정(채널 편차 ≤ 12 · 밝기 18~150) + 테두리에서 flood fill** 로 배경만 지운다.

⚠ **색 판정만으로는 갑옷이 뚫린다.** 기사 시트의 배경 회색(밝기 65)과 판금 갑옷의 중간톤이
사실상 같은 값이라, 외곽선에 틈이 있으면 flood fill 이 갑옷 안으로 새어 들어간다
(1차 시도에서 노년 기사의 견갑이 통째로 파였다). 그래서 **flood 전에 그림 쪽(barrier)을 7px 팽창시켜
외곽선 틈을 막고, flood 후 같은 크기로 배경을 되돌린다.** 이 순서를 지켜야 갑옷이 남는다.

이 스타일의 굵고 어두운 외곽선(약 `#0B060C`)이 자동 누끼를 가능하게 한다 — 픽셀아트나 회화체면 같은 방법이 안 통한다.

---
*마지막 업데이트: 2026-09-05 저녁 (**기사 시트 2판으로 교체 + Gem 고블린 3종 신설** — 기사는 같은 넷을 **덜 당겨 그린 2판**(격자 1016~1031)으로 갈고 1판 시트·타일 2장은 삭제(미커밋 · 복구 불가), 위 두 타일이 다시 `hero_6..7`. 고블린은 `source_sheet_goblin.png`(격자 1014~1033 · 우하단 ✦ 워터마크 제외)에서 3장을 따 `monster_1101`(후드=척후병) · `monster_1102`(목수건=전사) — **고블린이 처음으로 제 그림을 받았다**. 셋째(머리띠)는 예비 · 오크(1103)는 대형이라 안 붙였다. 사용자 지시) · 2026-09-05 (**Gem 기사 2종 추가** — `source_sheet_knight_gem.png`(2048² · 2×2 · 검정 격자선 1014~1033) 의 **위 두 타일만** 잘라 초록 키잉 → `knight_gold_bald.png` · `knight_sun_greybeard.png` 512² 투명, `cartoon/hero_6..7.png` 사본. 아래 두 장은 사용자 지시로 안 썼다. 삭제된 옛 기사 5종 절을 교체하고 「쓰는 법」의 `knight_1` 참조를 정정. 사용자 지시) · 2026-09-04 (**영웅 초상을 검투사 1종으로 교체** — `source_sheet_gradiator.png`(2048² · 2×2 · 검정 격자선) 오른쪽 위 타일을 `gladiator_helm.png` 로 자르고 `cartoon/hero_1.png` 사본(512²). 옛 `hero_*.png` 9장 + 원본 시트 `example_hero.png` + `cartoon/hero_1..14.png` 는 **전부 삭제**, `mock.js:HERO_FACE_MAX` 14 → 1. ⚠ 누끼는 안 했다 — 이 시트는 배경이 외곽선보다 밝아 기존 판정식이 안 통한다. 사용자 지시) · 2026-09-03 (**영웅 흉상 9종 추가** — `example_hero.png`(3×3 · 흰 격자선)을 잘라 `hero_<설명>.png` 9장 + `cartoon/hero_6..14.png` 사본. 누끼가 기사 시트보다 한 단계 어렵다: 로닌의 기모노가 배경과 **같은 회색**이라 씨앗을 아래 테두리에 놓으면 옷이 파인다 — 위 + 좌우 상단만 씨앗으로 쓴다. 사용자 지시) · 2026-08-30 (인간 기사 5종 추가 · 512 정사각 통일 · 외곽선 틈 봉합 절차) · 2026-08-30 (최초 작성)*
