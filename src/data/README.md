# src/data — CSV (SSOT)

**수치는 전부 여기.** 코드 하드코딩 금지, 기획서엔 절대 수치 금지 — 기획서는 키 참조(`[balance.csv:key]`) · 체감 범위 · 공식 변수명 · 테이블 링크만.

- Godot / Unity 둘 다 그대로 읽는다 — **엔진별 포맷 변환 금지**
- **읽히지 않는 SSOT 는 두지 않는다** — 여기 있는 CSV 는 전부 `ui/data.js:FILES` 가 fetch 한다. 어긋나면 `dev/test.html` 의 `csv: 로더가 읽는 목록 = src/data/*.csv 전부` 단정이 빨간불

## 공통 규약 (2026-08-28 확정)

| 규약 | 내용 |
|---|---|
| **첫 컬럼 = id** | `key` · `stat_id` · `monster_idx` · `stage_id` · `chapter_id` · `grade` … |
| **표시 문자열은 `_kr`/`_en` 쌍** | `monster_name_kr`/`monster_name_en` · `stage_name_kr`/`stage_name_en` · `name_kr`/`name_en` · `grade_kr`/`grade_en` |
| **이름은 영어가 원본 — 한글은 직역** [2026-09-09 사용자 확정] | **아이템·스킬 이름**은 `_en` 을 먼저 짓고 `_kr` 은 **의역하지 않는다**. `Morning Star` → **모닝스타**(~~별철퇴~~) · `Long Bow` → **롱 보우**(~~장궁~~). 두 언어가 같은 물건을 가리킨다는 것이 눈으로 대조돼야 하고, 참고작 어휘와의 계보도 끊기지 않는다. ⚠ 적용 범위는 **아이템·스킬 이름**이다 — UI 문구(`i18n.js:STRINGS`)·몬스터·스테이지·영웅 이름은 이 확정에 없다. ~~⚠ 기존 데이터가 규칙을 어긴다~~ → **재작업 완료 2026-09-09** (DEV_PLAN R64) — `item_base.csv` **22행** · `skill.csv` **23행**을 음차로 갈았다(~~날개 투구~~ → **윙드 헬름** · ~~지각균열~~ → **어스스플릿**). **직역 = 음차**다 [사용자 확정 2026-09-09] — 위 예시 둘이 그 판례다 |
| **자유 서술은 `description_kr` 하나** | 파일당 **마지막 컬럼**. `note_kr`·`reason_kr` 같은 별칭을 쓰지 않는다 |
| **불리언은 `0`/`1`** | `face` · `bg` · `impl` · `knob`. `TRUE`/`FALSE` 문자열을 쓰지 않는다 |
| **없음은 `-`** | `attr` · `combat_stat` · `dispatch` · `element` · `effect_stat` … 파서는 빈 셀을 빈 문자열로 주므로 명시한다 |
| **리스트는 `\|`** | `classes` (`mage\|priest`) · `combat_stat` (`item_find\|gold_find`) · `dispatch` (`tavern\|trade`) · `tags` (`boost\|sacrifice`) |
| **파생 가능한 것은 컬럼으로 두지 않는다** | `skill.csv` 의 `광역`·`단일`·`다단히트` 는 `target`·`hits` 에서 나오므로 `tags` 에 적으면 **로드가 실패한다**. `mastery_node.csv` 에 노드 이름 컬럼이 없는 것도 같은 이유(`stat` 이 답이다) |
| **다른 CSV 의 키를 가리킬 땐 `_key` 접미사** | `mastery_node.csv` 의 `value_key`·`max_rank_key`·`unlock_key` → `balance.csv` 의 `key`. 수치를 두 곳에 적지 않기 위한 규약 |
| **퍼센트 · 배율은 비율로 저장한다** [2026-09-17 사용자 지시 · R111] | 5% = `0.05` · 배율 310% = `3.1` · 저항 25% = `0.25`. **이름의 `_pct` 는 그대로**(값의 눈금만 비율이다 — 설명문의 「%」도 뜻은 같다). 코드는 `/100` 을 하지 않고 **화면이 찍을 때만** 100 을 곱한다(`ui/mock.js:pctNum`). 장비 옵션의 퍼센트 값은 **1% 단위**(소수 둘째 자리 · `formula.pctOption`) — 오만 `dmg_per_level_pct` 한 행만 0.1% 단위다. **대상이 아닌 것** — 상대 가중치(`rarity_w_*` · `make_rarity_w_*` · `tactic_grade_weight_*` · `hero_tier.csv:weight` — 분모를 매번 센다) · 곱셈 배수(`monster_atk_scale` · `power_growth_per_level` · `def_mult` · `hp_mult` 등 — 원래 비율이다) · 고정값(`hp_flat` · `def_flat` · `hp_regen` · 방어 · 회복량) · 초 · 개수 · 레벨 · 골드. 옛 눈금이 한 칸이라도 남으면 `dev/test.html` 의 `csv: 퍼센트는 비율 눈금이다` 단정이 빨간불 |
| **확정 여부는 컬럼으로** | `balance.csv` 의 `status`(`fixed`/`proposed`). 설명문에 `⚠제안값` 을 적지 않는다 — 기계가 못 읽는다 |
| **표 전체가 제안이면 행마다 적지 않는다** | `skill.csv`(skill_design §12-8 — **값 전부 미발행**이라 배율·타수·쿨·지속이 전부 ⚠임시) · `codex_level.csv`(monster_design §8) · `spawn_grade.csv` · `round_budget.csv`. 확정되면 그때 `status` 컬럼을 준다 |

### 파서 계약 (`game_logic/csv.js`)

- 첫 줄 = 헤더 · **한 행 = 한 줄**(셀 안 줄바꿈 없음 — 줄바꿈이 필요한 글은 `\n` 두 글자). **쉼표가 든 셀은 큰따옴표로 감싼다** · 셀 안의 큰따옴표는 `""` · 따옴표로 **시작하는** 셀만 감싼 셀로 읽는다 [개정 2026-09-15 — ~~셀에 쉼표·따옴표를 쓰지 않는다~~ · 스토리 글(`stage.csv:story_kr/_en`)이 쉼표를 쓴다 · INTERFACE §2-2]
- 숫자로 읽히는 셀은 **숫자**로 변환된다. `0`/`1` 불리언은 `=== 1` 로 비교한다
- 빈 셀은 빈 문자열이다 — `=== 0` 이 아니라 `undefined`/`''` 를 다뤄야 한다

## 파일 (44종 — 로더가 전부 읽는다)

| 파일 | 행 | 내용 |
|---|---|---|
| `balance.csv` | 156 | `key,value,status,knob,description_kr`. `knob=1` **89** (2026-09-15 재집계) = [`src/dev/README.md`](../dev/README.md) 의 밸런스 손잡이 표. ⚠ **이 두 수는 잘 낡는다** — 키가 늘고 주는 것이 일상이라, 세야 할 일이 있으면 파일을 세는 쪽이 빠르다(2026-09-09 재집계: 옛 「109행 · 손잡이 48」은 오래 뒤처져 있었다) |
| `chapter.csv` | 7 | 챕터 id · 죄종 · 이름 `_kr`/`_en`. 몬스터 id 앞자리 = 챕터 (1101 → 1) |
| `stage.csv` | 35 | 스테이지 — 이름 `_kr`/`_en` · `bg`(계승 배경 자산 유무) · 타입 / dlvl / 보스 / 보스 등급 · **`round_set`**(`stage_round.csv` 의 세트 — 챕터의 마지막 스테이지는 `boss_only` · 2026-09-11) · **`story_kr`/`story_en`**(출정 창 「이야기」 칸의 입장 텍스트 · 2026-09-14 — 원고 각 장 「스테이지별 입장 텍스트」를 **스테이지 번호로** 옮겼다: 원고 「보스 스테이지」 글 = 스테이지 4 · 스테이지 5 는 원고 대면 절에서 뽑은 초안 · `_en` 은 직역. **전 챕터를 원고의 스테이지보스 이야기(과거 · 정체 · 바알의 내면)를 녹여 다시 썼다** — 2026-09-15 사용자 지시 · 문체는 1장이 기준 · 이름은 CSV 를 따르고 원고와 갈린 보스는 몬스터 설명에 맞춰 지었다(ADR-0117). **줄바꿈은 셀 안의 `\n` 두 글자**(`data.js:stageStory` 가 바꾼다). **이름은 자리표시자** — `{m:<monster_idx>}` · `{leader}`(파티 리더) · 조사 쌍 `{…|이/가}`(`data.js:fillStory` · 2026-09-15 · 없는 몬스터 번호는 `test.html` 단정이 잡는다). `game_logic` 은 안 읽는다) |
| `stage_round.csv` | 10 | 스테이지 내부 라운드 구조 — **`(round_set, round_num)` 복합키**. `standard` 9라운드(정예 3·6 · 보스 9) · `boss_only` 1라운드(챕터보스 단독). **라운드 수 = 세트의 행 수**다 — ~~`balance.csv:rounds_per_stage`~~ 는 2026-09-11 퇴역 |
| `round_budget.csv` | 4 | 라운드 타입별 편성 상한 + 목표 전투시간 |
| `spawn_grade.csv` | 4 | 등급 — 세기 칸은 **`hp_mult` 하나**(~~atk/def/res_add~~ 2026-09-11 퇴역 — 장비가 낸다) · 장비 `gear_ilvl_add`·`gear_rare_bonus_pct` · 스킬 칸 `skill_slots`(1/2/3/3) · 보상 exp/gold/**`drop_chance_mult`**. 스킬 칸 밖의 축은 등급순 단조 증가 |
| `monster.csv` | 119 | 몬스터 — 이름 `_kr`/`_en` · `face` · **`face_elite`**(정예로 나왔을 때 쓸 전용 초상이 있나 — 1 이면 `<idx>_elite.png` · 2026-09-17) · 분류 · **영웅과 같은 모양**(직업 `cls` · 기본 능력치 7 · `innate_skill` · `weapon_group` · `wear_slots` — 2026-09-11 · **값은 ⚠임시**) · 몸값 `defense` · 저항 4원소 직접 비율(0.25 = 25%) · 처치 XP 계수 `exp_coef`(몬스터별 조정 칸 — XP 는 몬스터 레벨이 정한다 · 2026-09-14 R85) · **이야기 `story_kr`/`story_en`**(도감 툴팁 — 보스는 길게 · 일반은 한두 문장 · 한글이 원본 · 비면 안 뜬다 · 2026-09-21 monster_design §8). ~~`hp`·`attack`·`action_period` 소재값~~ · ~~`exp_reward` 소재값~~ 은 삭제 — 장비·레벨이 낸다 |
| `weapon_group.csv` | 10 | 무기군(본편 8 + 확장 2) — 직업 전속(5직업 × 2) · **전부 양손**(2026-09-01 한손 개념 폐지로 `hands` 컬럼 삭제) · **`damage_kind`**(physical/magic) · 행동 주기 · 변동 폭(`variance_pct` — 비율) · **`release`**(main/expansion) |
| `armor_group.csv` | 10 | **방어구 갈래** — 키는 `(slot, group_id)` 쌍이다(`leather` 가 투구 · 장갑 · 신발에 겹친다) [신설 2026-09-16 · R108 갑옷군 3 · **2026-09-18 `slot` 칸 + 투구 3 · 장갑 2 · 신발 2** · item_design §1]. `classes` = 숙련(마스터리)이 붙는 직업(**하드 게이트가 아니다** · 갑옷만 · 나머지는 `-`) · `def_mult` 방어 계수(그 부위 배수 위에 곱한다 — 갑옷 1.6 / 1.0 / 0.5 · 투구 1 / 0.4 / 0.2 · 장갑 · 신발 1 / 0.333) · `aspd_pct` 공속(중갑 −0.1 · 경갑 +0.05) · `cdr_pct` 쿨감(로브 0.15) — 비율 · `release`. 공속 · 쿨감은 **낀 방어구마다** 제 갈래 값을 더한다(지금 값이 있는 것은 갑옷군뿐 · 장갑 갈래 고정값은 사용자 보류) |
| `skill.csv` | 38 | **몬스터 전용 1**(`owner_kind=monster` · `owner_id=-` · `innate_pool=0` — 영웅 풀 · 무기 스킬 · 도감에 안 든다 · 「고블린 소환」 · 2026-09-18 skill_design §12-9) + **직업 스킬 풀 5직업**(전사 7 · 기사 8 · 궁수 6 · 마법사 8 · 사제 8 — `owner_kind=job` · `owner_id` = 직업 id). **1스킬 = 1직업**이고 직업 사이에 안 겹친다 [사용자 확정 2026-09-08 · skill_design §12]. ~~무기군 전용 10행~~ 은 **무기군 고정 폐기**로 삭제됐다(2026-09-09) — 무기는 스킬의 **그릇**이라 제 행을 갖지 않고, 드롭 때 그 무기군의 **직업 풀**에서 하나를 굴려 `item.skill` 에 담는다. **기획 37 전부 발행**(2026-09-09 · DEV_PLAN R61 — ~~37 중 22만~~) · 다섯은 근사다(오오라 자동 켜기 · 「라운드 종료까지」 · 「양 옆의 아군」 · 독화살 · 적 데미지 감소 — skill_design §12-8). — **`owner_kind,owner_id`** 가 출처(`job`/`advance`/`unique` — `weapon_group` 은 2026-09-09 어휘에서 삭제) · 종류·타겟·타수·배율·감쇠·쿨·지속·원소·버프 스탯·발동 조건·`status`(코드 미독)·**`tags`(영문 id · `\|` 구분 최대 2 — `skill.js` 가 검증하고 전투는 안 읽는다. skill_design §11)**·우선순위 · **`icon`·`desc_kr`·`desc_en`**(플레이어 표시) · **`innate_pool`**(0/1 — 고유 풀 소속. 지금은 전 행이 1) · **`note`**(설계 노트 — 플레이어에게 안 보인다) · **`proc_chance_pct`·`proc_mult_pct`**(확률로 터지는 추가 피해 — 확률 · 배수 둘 다 비율 · 기본 0 · `decay_pct` 바로 뒤 컬럼 · 확률 > 0 이면 `attack` 이고 배수 ≥ 1 · 확률 0 이면 배수 0 · 2026-09-10 R72) · **`scale1~3_field`·`_attr`·`_coef`**(스케일링 슬롯 셋 — `field` ∈ `mult_pct`·`hits`·`effect_value`·`duration_sec`·`decay_pct`·`proc_chance_pct`·`proc_mult_pct` · `attr` = `hero_attribute.csv` id · `coef` = 능력치 1당 더해지는 값(≥ 0) · 빈 슬롯은 `- · - · 0` · 한 행에 같은 field 두 번 금지. **계산은 `skill.js:scaleDef` 한 곳** — `mult_pct` 는 배율에 안 더하고 결과에 더한다 · 슬롯이 민 감쇠의 상한은 `[balance.csv:skill_decay_cap_pct]` — skill_design §13-1 · 2026-09-10 R72) | **액티브 3칸은 출처가 정한다**(고유 · 무기 · 전직 — skill_design §2). **두 출처(고유 · 무기)가 직업 풀에서 하나씩** 가져간다 — 고유는 **영웅 직업**의 풀에서 생성 시, 무기는 **그 무기군이 지정한 직업**의 풀에서 드롭 시 굴린다(§12-1 규칙 2 · 3 — 2026-09-10). ⚠ **행 순서가 결정론에 걸린다**(INTERFACE §5-2).
| `skill_tag.csv` | 14 | **스킬 태그 어휘** — `tag_id` · 대분류 4(`damage`/`buff`/`debuff`/`other`) · `derived`(0/1 — 파생 3종 `aoe`/`single`/`multihit`) · 이름 ko/en. `skill.js` 가 어휘로 · `tactic_option.csv` 의 `skill_tag` 인자 · 화면 이름표 (skill_design §11 · 2026-09-01 — 종전 코드 배열) |
| `hero_attribute.csv` | 7 | 기본 능력치 7종 — 전투 계수(`combat_stat`) + 담당 파견처(`dispatch` — `mine`/`gather`/`log`/`forge`/`tavern\|trade`/`-` · ~~`lab`~~ 은 2026-08-31 연구소 삭제로 사라졌다). 장비로 불변(`balance:attr_equip_bonus = 0`) |
| `combat_stat.csv` | 24 | 전투 능력치 — 장비·스킬·**마스터리** 파생. **`impl`** = `computeCombat` 이 실제로 내는가(1 = 22종 · 0 = 2종 — 08-28 `hp_regen`·`cooldown_reduction` 구현으로 둘이 넘어왔다 · 2026-09-17 `vs_size_damage` 삭제 R112 · 같은 날 `fhr`(타격 회복 — 출처 미정이라 값은 0) R110). 캐릭터 시트는 `impl=1` 만 그린다. **`sheet_order`** = 그 시트의 **행 순서**(1~24 · 빠짐·중복 없음 — `impl=0` 행도 갖는다). **행 순서가 아니라 이 컬럼이 화면을 정한다** — 표시 순서를 바꾸려고 CSV 행을 옮기지 않는다(행 순서에 결정론이 걸린 표들과 헷갈리지 않게 · [SCREEN_DESIGN §6](../../docs/client/SCREEN_DESIGN.md)) |
| `mastery_node.csv` | 38 | **마스터리 노드** (skill_design §3 확정 08-28 · 직업 09-07) — 죄종 T1 공통 3(`owner_id=*`) + 죄종 T2 16 + 직업 T1 15 + 직업 T2 4. `tree_kind`(sin/class) · `tier` · `stat`(접사 채널 또는 `combat_stat` id) · **값은 `value_key`·`max_rank_key`·`unlock_key` 로 `balance.csv` 를 가리킨다**(수치를 여기 적지 않는다). 표시 이름은 `stat` 에서 파생하므로 컬럼이 없다. T3(반응형)는 값 미정이라 행이 없다 |
| `commission_kind.csv` | 2 | **의뢰의 유형** (GAME_DESIGN §9 확정 **09-07 — 목표형**) — **처치**(전투가 저절로 센다) · **수집**(드롭·파견·탐험 산출이 채운다). `rides_on` = 무엇이 목표를 채우는가(`battle`\|`yield`). ⚠ **09-08 전면 교체** — ~~4종(사냥·파견·약탈·보호)~~ 과 ~~`form`(세는 형/가는 형)~~ · ~~`channel`(실시간/오프라인)~~ 은 **「전장을 여는 의뢰」를 전제한 09-01 판**이라 09-07 개정으로 축째 소멸했다(약탈·보호는 탐험으로 이관). **이름과 「어떻게 도는가」 줄이 `_kr`/`_en` 쌍으로 여기 있다** — 화면은 i18n 이 아니라 이 표를 `L()` 로 푼다 (SCREEN_DESIGN §14 · DEV_PLAN R45) |
| `commission.csv` | 5 | **의뢰 게시판의 행** — ⚠ **표 전체가 임시 자리채움이다**(`mine_node.csv` 와 같은 처지). 목표·보상·명성이 기획 미정이라(GAME_DESIGN §10 「의뢰 세부」) 값이 전부 지어낸 것이고, **굴림이 생기면 이 표가 템플릿 표가 된다.** **칸 수 = 행 수** — 화면이 개수를 안 박는다(`tactic_slot` 과 같은 문법) |
| `tactic_slot.csv` | 7 | **파티 전술의 칸** (tactic_card_design §5 확정 08-30) — `unlock_total_level`(로스터 **합산 레벨** 문턱 · 오름차순) · `reroll_cost_gold`. **칸 수 = 행 수** — 코드가 칸을 세지 않는다 |
| `tactic_option.csv` | 66 | **파티 전술의 옵션** — 「조건 → 효과」 1행 × 등급 3단. ⚠ **행 전체가 임시 자리채움이다** — 나중에 더 창의적인 전술로 교체한다 (tactic_card_design §5-7). **`(option_id, grade)` 복합키** — `option_id` 가 가족(조건·스탯 고정) · `grade`(`common`/`magic`/`rare`) 가 `value` 만 가른다. **중복 방지·풀 크기는 가족 수(22)로 센다.** `cond_kind`(어휘 **6종**은 `tactic.js` — `two_hand` 은 2026-09-01, `party_size`·`damage_kind` 는 2026-09-02 폐지) + `cond_arg`(죄종 / 스킬 태그) + `cond_n` · `stat`(접사 채널 또는 `combat_stat` id) + `value`. 표시 이름은 `stat` 에서 파생하므로 컬럼이 없다(`mastery_node.csv` 와 같은 이유). **가족 수 > `tactic_slot.csv` 행 수**여야 리롤이 성립한다. (`tactic.js` 반영 완료 2026-09-02 — 로드 시 **3등급 완비 · 가족 안 조건·stat 동일 · 값 오름차순**을 검증하므로 이 표를 교체할 때 등급을 빠뜨리면 게임이 안 뜬다) |
| `codex_level.csv` | 4 | 도감 레벨별 **누적 처치 문턱**(**`kills_total`** — 2026-09-21 카드 → 처치 수) + 레벨별 보정 `bonus_pct`(비율 · 레벨마다 더한다) |
| `codex_series.csv` | 4 | 스테이지 번호 → 도감 계열 스탯 (1 공격 / 2 체력 / 3 **명중 — 폐지돼 갈 곳 없음** / 4 피해) · **5(챕터보스 단독)는 행이 없다** — 배정 미정(GAME_DESIGN §10 · 2026-09-11) |
| `class.csv` | 7 | **직업** (hero_design §2) — 본편 5 + 확장 2. `key_attr` = 이 직업을 미는 기본 능력치(생성 굴림이 이 축을 최고치로 민다) · `release`(main/expansion — `weapon_group.csv` 와 같은 어휘. `stage` 는 스테이지를 뜻해 안 쓴다). 무기군은 여기 없다 — 직업 전속 배정은 `weapon_group.csv:classes` 가 SSOT. **행 순서 = 표시 순서** |
| `equip_slot.csv` | 8 | **장비 부위 7 + 착용 위치 8을 한 표로** (item_design §1 · 반지 ×2 · 2026-09-01 보조 삭제). `slot_order` = 착용 위치 순서(세이브 `equipped` 의 키) · `part_order` = 부위 순서(`ring2` 는 `ring1` 과 같은 부위라 `-`). 드롭·접사·필터는 **부위**, 페이퍼돌·`equipped` 는 **위치**. ⚠ 부위 순서가 드롭 굴림의 결정론에 걸린다 |
| `item_base.csv` | 42 | **아이템 베이스 이름** — 갑옷 10 · 투구 10(시작 1 + 갈래 3 × 티어 3) · 장갑 7 · 신발 7(시작 1 + 갈래 2 × 티어 3 · 2026-09-18) + 목걸이 · 반지 각 4. **무기는 없다**(무기의 베이스는 무기군 자체 = `weapon_group.csv`). 이름 조립은 `game_logic/naming.js`. ⚠ 부위별 행 순서가 결정론에 걸린다 · 수치 근거 없는 임시 이름 풀이다 |
| `sin_word.csv` | 28 | **아이템 이름의 죄종 단어** (item_design §1 「이름」 확정 2026-09-19) — 죄종 7 × 단 4. `word_id,sin,tier,tier_min_ilvl,name_kr,name_en,description_kr`. `name_en` = **형용사**(Raging) · `name_kr` = **명사**(격노 — 조립이 「의」를 붙인다) · 첫 단은 원래 죄종 이름(`ui/mock.js:SINS` 의 `adj` · `ko` 와 같다). 로더는 죄종마다 `tier` 로 정렬해 읽고 **죄종마다 단이 1 부터 같은 수만큼 있지 않으면 멈춘다** — 행 순서는 계약이 아니지만 **단의 순서는 계약이다**(드롭이 단 번호를 굴린다 · INTERFACE §5-2). ⚠ `tier_min_ilvl` 은 **아직 안 읽는다** — 목표는 레벨 구간이고 밸런싱 전까지는 넷 중 균등이다 |
| `weapon_sin_option.csv` | 12 | **무기 죄종 칸 후보** (item_design §1 「무기 옵션」 · 2026-09-11 R78) — `sin` · `applies_to`(`all` / 무기군 `damage_kind` / 직업 id — 시기 칸이 물리 · 마법사 · 사제로 갈린다) · `stat` · `scale`(`flat` = 1% 단위 / **`fine`** = 0.1% 단위 — 둘 다 비율) · `min` · `max`. 한 죄종 · 한 무기군에 행이 여럿이면 그중 하나를 굴린다(탐욕 셋 · 시기-사제 둘). 로드 검증은 `item.js`. ⚠ 수치는 전부 제안 · 행 순서가 결정론에 걸린다 |
| `weapon_common_option.csv` | 13 | **무기 통합옵션 후보** (같은 절) — `family`(종류 — **종류를 먼저 뽑고 그 안에서 변형**) · `stat` · `applies_to` · `scale` · `min` · `max`. 개수는 `[balance.csv:weapon_common_opt_normal]` · `_magic` · `_rare`. ⚠ 보류 셋(최소/최대 피해 · 원소 피해 추가 · 타격 시 발동)은 행이 없다 · 수치는 전부 제안 · 행 순서가 결정론에 걸린다 |
| `armor_sin_option.csv` | 30 | **방어구 죄종 칸 후보** (item_design §1 「갑옷 옵션」 · 「투구 옵션」 · 신발 행 · 2026-09-18) — `slot`(armor · helmet · boots — **장갑 행은 없다**: 장갑은 `weapon_sin_option.csv` 를 그대로 읽는다 ⚠임시) · `sin` · `stat` · `scale` · `min` · `max` · `per_ilvl`(`band` 행만 — **0 이면 아이템 레벨과 무관한 정수**: 레벨당 방어력 · 레벨당 체력). 한 부위 · 한 죄종에 행이 여럿이면 그중 하나를 굴린다(탐욕 셋 · 투구 시기 원소 넷). `counter_chance` 의 `max` 는 1 미만(로드 검증). ⚠ 수치는 전부 ⚠제안 · 행 순서가 결정론에 걸린다 |
| `armor_common_option.csv` | 36 | **방어구 공통옵션 후보** (같은 절) — `slot` · `group`(`all` 또는 그 부위의 갈래 id — **투구는 갈래별 풀** · 갈래가 없는 시작 칸은 그 부위의 모든 행) · `family`(종류 — **종류를 먼저 뽑고 그 안에서 변형**) · `stat` · `scale` · `min` · `max` · `per_ilvl`. 개수는 `[balance.csv:armor_common_opt_normal]` · `_magic` · `_rare`. ⚠ **장갑 행은 옛 `affix.csv` 장갑 풀을 옮긴 임시**(기획 미정) · 신발 빙결 · 중독 시간 감소는 효과가 없는 채 들어 있다(상태이상 기계 대기) · ⚠ 수치는 ⚠제안 · 행 순서가 결정론에 걸린다 |
| `affix.csv` | 13 | **목걸이 · 반지의** [2026-09-11 R78 — 무기는 위 두 표 · **2026-09-18 방어구 네 부위도 아래 두 표**로 옮겨 방어구 전용 다섯 행(`def_flat` · `aspd_pct` · `def_ignore` · `reflect_damage` · `damage_reduction`)이 빠졌다] **접사 정의** — 첫 컬럼 `stat`(= `combat_stat.csv` 의 축, **그 자체가 id** — 별도 `affix_id` 를 두지 않는다: 파생 컬럼 금지) + 굴림 범위 + **`scale` 3분류**(item_design §2-1): `growth` 굴림 × 성장 곡선(데미지·HP flat) / `band` 굴림 + ilvl × `per_ilvl`(물리 방어 — **`per_ilvl` 은 이 행들만**) / `flat` 굴림 그대로 ilvl 무관(% · 저항 · 유틸 전부 — **비율**, 1% 단위). `slots` = 붙을 수 있는 부위(`\|` 리스트). **여기 있는 축은 전부 전투에 실제로 걸린다** — 안 걸리는 접사는 넣지 않는다(거짓 선택지 금지). ⚠ 수치는 전부 프로토타입 임시값 · 행 순서가 결정론에 걸린다 |
| `hero_name.csv` | 24 | **레어 영웅 이름 풀** — 무한 생성이라 이름도 뽑는다 (hero_design §1). 유니크 15명은 고정 명단이라 여기 없다. ⚠ 행 순서가 결정론에 걸린다 |
| `hero_trait.csv` | 12 | **시작 특성 풀** — 영웅 1명당 1개, 반고정 생성의 세 번째 축(이름 + 메인 죄종 + 시작 특성). ⚠ **효과 미작성** — 지금은 이름표만 굴린다 (hero_design §3). 행 순서가 결정론에 걸린다 |
| `hero_tier.csv` | 4 | **영웅 등급 — 일반 / 매직 / 레어 / 유니크** (hero_design §1 확정 2026-09-07 · 신설 09-08 · 일반 2026-09-14). 등급이 정하는 것은 **능력치 총합 대역(`attr_total_min/max`)과 분포 모양(`shape`)** 둘뿐이고 상한은 `[balance.csv:hero_attr_max]` 하나로 전 영웅 공통이다 — ~~등급은 출발선이지 천장이 아니다~~ → 기본 능력치가 레벨업으로 안 올라 **등급 차이는 끝까지 간다**(2026-09-14). `weight` = 생성기의 등장 비중(**0 = 안 뽑는다** — 유니크는 수작업). 이름·색도 이 표가 든다(~~`ui/mock.js:HERO_TIER`~~ 대체 — 대역과 표기가 갈리면 SSOT 가 둘이 된다). ⚠ **대역 · weight 는 사용자 지시값이고 shape 는 모양 지시(일반 · 레어 = 정규 / 매직 = 균등)를 옮긴 값** — 대역은 **겹쳐도 된다**(2026-09-14 · ~~겹치지 않는 두 구간~~) · 직접 굴려 보며 맞춘다 |
| `hero_unique_candidates.csv` | 40 | **유니크 영웅 후보 풀** (hero_design §1 — 2026-09-10 논의 자료). `id,class_id,status,name_kr,name_en,origin_kr,look_kr,look_hex,battle_kr,description_kr`. `status`(confirmed/proposed) — confirmed 7행은 `ui/mock.js:HERO_FACE_NAMES` 에 이미 이름이 박힌 초상(막시무스·바바리안·검황·오디세우스·한니발 + **포세이돈**·**노스트라다무스** — 2026-09-16 `MAGE-P3`·`MAGE-P5` 후보가 `mage_2`·`mage_3` 초상으로 채택돼 id 도 초상 id 로 갈렸다), proposed 33행은 아직 채택 안 한 후보다. `look_kr`/`look_hex`는 `.claude/skills/art-prompt` 발주 프롬프트 골격(소재 원형 · 실루엣 · 강조색)을 따른 발주 초안이다. ⚠ **`game_logic` 이 아직 안 읽는다** — 유니크 영웅 개체 테이블 자체가 없어(§1 "그 개체 테이블은 아직 없다") 로더·골든에만 걸려 있다. **죄종·시작특성 컬럼이 없다** — 실제 유니크 SSOT 가 되려면 그 축들이 더해져야 한다 |
| `search_story.csv` | 28 | **수색 진행 문구** (base_expedition_design §2-4 확정 2026-09-09 · ADR-0062). `story_id,phase,phase_order,sin,text_kr,text_en`. 나가 있는 동안 **막마다 한 줄**이 열리고 문구는 여기서 굴린다 — 후보는 **공통(`sin = -`) + 보낸 영웅의 죄종**이다. ⚠ **막의 어휘도 순서도 이 표가 든다** — `phase` 가 막 이름이고 `phase_order`(1부터 연속)가 순서라 **막을 늘리는 일이 행 추가뿐**이다(코드에 막 상수가 없다). 검증은 로드 시(`state.js`) — 막마다 공통 행이 최소 하나 있어야 한다(없으면 그 죄종에서 후보가 빈다). ⚠ **행 순서가 결정론 계약**이다 (INTERFACE §5-2) |
| `search_meeting.csv` | 14 | **수색 만남** (base_expedition_design §2-4 확정 2026-09-09 · ADR-0068). `meeting_id,sin,rumor_kr,rumor_en,prompt_kr,prompt_en`. `sin` = 만난 사람의 죄종이고 **소문이 이걸 보내기 전에 그대로 알려준다**(숨기지 않는 것이 이 기능의 전제다). 어느 만남이 뜨는지는 수색 **회차 번호**가 정한다 — 보낸 영웅과 무관해야 미리 알려줄 수 있다. ⚠ **행 순서가 결정론 계약** (INTERFACE §5-2) |
| `search_answer.csv` | 42 | **만남의 답** (같은 확정). `answer_id,meeting_id,need_sin,hit_sin,answer_kr,answer_en`. **컬럼 둘이 서로 다른 질문에 답한다** — `need_sin` = **누가 갔나**(그 죄종을 보냈을 때만 이 답이 **보인다** · `-` 는 언제나) · `hit_sin` = **누굴 만났나**(만난 죄종과 같으면 이 답이 **먹힌다**). 그래서 할인이 두 층이다(`[balance.csv:tavern_search_meet_hit_pct]` · `[balance.csv:tavern_search_meet_key_pct]`). **답 수 = 행 수** — 2지선다도 3지선다도 된다. 검증은 로드 시(`state.js`) — 만남마다 **공통(`-`) 답이 최소 하나** 있어야 한다 |
| `mine_node.csv` | 7 | **채광 갱도 — 단계 1~7** (⚠ **값은 구조 검증용 임시값이다** · **이름은 2026-09-10 로 챕터에 맞춰 확정 규칙을 얻었다** — 단계 n = 챕터 n 의 지역 · 1~5 실재 · 6~7 신화. ⚠ 그때 옛 값의 오류 둘을 고쳤다 — `ore_t3` **강철** → **사금**(합금이라 캘 수 없었고 갱도 이름 「모래금」과 어긋났다 · 화폐 골드와 겹치지 않게 「금」이 아니다) · `ore_t5` **흑철** → **수은**). `mine_id` = id · `tier` · `unlock_chapter` · 이름 `_kr`/`_en` · `ore_id`/`ore_name_kr`/`ore_name_en` · **`yield_per_hour`**(단계가 오를수록 **줄어든다** — 하위 단계가 「양」을 맡아 상위로 수렴하지 않는다). 값을 행에 직접 두는 것은 `tactic_option.csv` 와 같은 이유(전역 손잡이가 아니다). **죄종 컬럼을 두지 않는다** — 죄종을 지정하는 재료는 없다(2026-09-15 크래프트 재정의로 낙인의 죄종 지정도 사라졌다 · item_design §4-4). **`game_logic` 이 읽는다**(2026-09-15 · R96) — 제작 레벨대의 광석 id 를 여기서 가져간다(`state.js:makeBands` — tier = 챕터). 산출 정산(파견)은 아직 없다 |
| `gather_node.csv` | 7 | **채집장 — 단계 1~7** (⚠ **표 전체가 구조 검증용 임시값이다**). `mine_node.csv` 와 **같은 모양**이고 산출물 컬럼만 갈린다 — `gather_id` · `tier` · `unlock_chapter` · 이름 `_kr`/`_en` · `herb_id`/`herb_name_kr`/`herb_name_en` · `yield_per_hour`(상위 단계일수록 **줄어든다**). **담당 능력치는 지능 · 산출 계열은 약초 · 소모처는 연구**로 기획 확정이다(GAME_DESIGN §9 09-10) — 확정이 아닌 것은 **값**(이름 · 수량)과 **여는 챕터**뿐이다. ⚠ **`game_logic` 이 아직 안 읽는다** — 배치·산출 정산이 없어 로더·화면·골든에만 걸려 있다 |
| `log_node.csv` | 7 | **벌목지 — 단계 1~7** (⚠ **표 전체가 구조 검증용 임시값이다**). 위와 같은 모양 · 컬럼은 `log_id` · `timber_id`/`timber_name_kr`/`timber_name_en`. **담당 능력치는 건강 · 산출 계열은 목재**로 기획 확정이고 **소모처는 제작**(2026-09-15 — 레시피가 광석과 **함께** 먹는 보완재라 통화 비중복이 산다 · item_design §5-1 · §7-1). **`game_logic` 이 읽는다**(R96 — `state.js:makeBands`) · 산출 정산(파견)은 아직 없다 |
| `make_recipe.csv` | 7 | **제작 레시피 — 부위마다 필요량** (item_design §7-1 확정 2026-09-15 · R96 · ⚠ **값은 구조 검증용 임시값**이고 부위 차등이 없다). `part`(= `equip_slot.csv:part`) · `ore_units` · `timber_units` · `dust_units`. **어느 단계의 광석 · 목재인지는 표가 아니라 레벨대가 정한다**(레벨대 n = 챕터 n = `mine_node` · `log_node` 의 tier n). **셋 다 1 이상이어야 한다** — 광석 · 목재는 보완재 · 가루는 원정 쪽 입력이라 로드 시 `state.js` 가 검증하고 어기면 던진다 |
| `potion.csv` | 5 | **물약 단계** (battle_design §7-1 · item_design §7-4 확정 2026-09-15 · R103 · ⚠ **값은 사용자 지정 임시값**). `potion_id` · `kind`(지금은 `heal` 하나 — 모르는 종류는 로드에서 멈춘다) · `tier`(종류 안에서 1 부터 연속) · 이름 `_kr`/`_en`(**영어가 원본 · Diablo 2 이름 · 한글은 음차**) · `heal`(정해진 회복량 — 단계마다 커진다) · `craft_gold`(제작 골드 — 지금은 재료 없이 골드만) · `craftable`(0/1 — ⚠ **지금 제작이 열린 단계** · 해금 조건은 기획 미정이라 임시 칸) · `start_owned`(**시작 개수** — 새 게임이 갖고 시작하는 개수 · 0 = 안 가짐 · 2026-09-21 R124 — 전엔 0/1 플래그. 그 물약은 **편성 1 의 칸**에도 한 칸씩 든다). 칸 수 · 마시는 HP 비율 · 쿨은 `balance.csv:potion_*` 가 든다. 검증은 로드 시 `state.js` |

**⚠ 배열 순서가 곧 계약인 표들** — `equip_slot`(부위) · `item_base`(부위별) · `affix` · `hero_name` · `hero_trait` · **`skill`**(고유 스킬 풀 — 09-01) · **`hero_tier`**(등급 굴림 — 09-08) · **`weapon_sin_option` · `weapon_common_option`**(무기 옵션 — 09-11) · **`armor_sin_option` · `armor_common_option`**(방어구 옵션 — 09-18) 은 코드가 `rng` 로 **인덱스를 굴린다**. 행을 재정렬하면 같은 시드가 다른 결과를 낸다 ([INTERFACE §5-2](../../docs/client/INTERFACE.md)). 행 추가는 **끝에** 한다.

**아직 CSV 가 아닌 것** — 죄종(`SINS`) · 정예 특성(`SIN_TRAITS`/`COMMON_TRAITS`) 셋은 `ui/mock.js` 에 남아 있다. 전부 죄종 매핑 미확정([GAME_DESIGN §10](../../docs/game_design/GAME_DESIGN.md))에 걸려 있다.

**삭제 — `equipment_option_override.csv` (2026-08-28)**: 34행짜리 계승 옵션 패치 기록으로, 코드가 읽은 적이 없다. 표는 [inherited_data_gaps.md 부록 A](../../docs/reference/inherited_data_gaps.md) 로 옮겼다.

## `inherited/` — TheSevenRPG 포크 25종 · **읽기 전용**

스키마 무변환, 재동기화 가능. `src/assets/art/backgrounds/source/pixel/` 에 남은 **계승 원본 셋**(101 · 102 · `town`) 도 같은 규칙 — 규격·재동기화는 그쪽 README. `src/assets/art/` 의 나머지(`backgrounds/` 설치본 · `faces/` · `icons/`)는 신규 아트가 직접 들어가는 활성 폴더라 이 규칙에서 제외 (assets/art/README.md).

계승분을 바꿔야 하면 **수정하지 말고** 이 폴더(`src/data/`)에 신규 테이블을 만들어 **대체**하고, 무엇이 무엇을 대체했는지 문서에 남긴다.

- 대체 이력: [monster_design.md §7](../../docs/game_design/monster_design.md)
- 계승 데이터의 빈 구멍: [inherited_data_gaps.md](../../docs/reference/inherited_data_gaps.md)

---
*마지막 업데이트: 2026-09-21*
