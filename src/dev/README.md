# src/dev — 검증

브라우저가 유일한 JS 런타임이다(빌드 없음, node 없음). 단정과 캘리브레이션은 전부 `test.html` 에서 돈다.

## 검증 방법

1. `start.bat` (로컬 http 서버 — ES Modules 는 `file://` 에서 CORS 로 막힌다)
2. `http://localhost:8777/dev/test.html` — game_logic 단정 + 밸런스 캘리브레이션 표
   - 실패 사유는 `fail()` 로 **던진다**. 문자열 반환은 통과로 집계되므로 쓰지 않는다

## 개발용 URL (`src/index.html`)

흐름을 헤드리스 스크린샷으로 태울 때 쓴다.

| URL | 상태 |
|---|---|
| `?dev=newgame` | 새 게임 시작 화면 |
| `?dev=battle` | 즉시 정산 → 리포트 |
| `?dev=play` | 관전 — `&bt=dmg` 면 우측 탭이 누적 데미지 |
| `?dev=offline` | 반복 켠 채 껐다 켠 상황 — 런 마무리 배너 |
| `?dev=form` | 편성 패널이 열린 화면 (지역을 고른 상태) |
| `?screen=start` | 시작 화면 |
| `?tab=character` | 캐릭터 탭 |
| `?face=<스타일>` | 몬스터 얼굴 아트 스타일 교체 — `src/assets/art/faces/<스타일>/`. 목록은 `ui/mock.js:FACE_STYLES` · localStorage 에 남는다 |

헤드리스 (Edge) — 스크린샷:
```
msedge --headless=new --screenshot=<out.png> --virtual-time-budget=8000 <URL>
```

헤드리스 — **DOM 을 읽어 단정 결과·표를 그대로 가져오기.** 반드시 PowerShell 도구로 돌린다(git-bash 에서는 `msedge` 가 즉시 반환하고 출력이 0바이트다):
```powershell
$edge="C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"; $prof="$env:TEMP\edgeprof"; if (Test-Path $prof) { Remove-Item -Recurse -Force $prof }
Start-Process $edge -ArgumentList @('--headless=new','--disable-gpu','--no-first-run',"--user-data-dir=$prof",'--disk-cache-size=1','--virtual-time-budget=20000','--dump-dom','http://127.0.0.1:8777/dev/test.html') -RedirectStandardOutput "$env:TEMP\dom.html" -Wait -NoNewWindow
$h = Get-Content "$env:TEMP\dom.html" -Raw -Encoding UTF8; if ($h -match '<title>(.*?)</title>') { $Matches[1] }    # → "PASS n/n"
[regex]::Matches($h, '<li class="fail">(.*?)</li>') | % { $_.Groups[1].Value -replace '<[^>]+>',' ' }                 # 실패 목록
```
같은 방법으로 `index.html?dev=battle&lang=ko` 를 덤프해 `class="report-head"` 가 있는지 보면 렌더 예외까지 잡힌다(예외가 나면 비어 있다).

> **덤프가 0바이트면 페이지가 아니라 단정이 멈춘 것이다** (2026-08-30) — `--dump-dom` 은 가상 시간이 다 흘러야 찍는데, 단정 안에서 무한 루프가 돌면 그 시간이 오지 않아 **아무것도 안 나온다**(오류 메시지도 없다). 같은 URL 을 `--screenshot` 으로 찍으면 그때까지 그려진 화면이 남으므로 **어느 단정에서 멈췄는지가 보인다**. `index.html` 덤프는 되는데 `test.html` 만 0바이트면 이 경우를 먼저 의심한다.

## 밸런스 손잡이 (`src/data/balance.csv`)

> **`balance.csv:knob = 1` 이 손잡이의 SSOT 다** (44키, 2026-08-30 재집계). 손잡이인지 아닌지를 사람 기억이 아니라 컬럼이 들고 있어야 캘리브레이션이 재현된다.
> 아래 표는 그중 **전투 대역을 직접 미는 15키**를 이름으로 적은 것이고, 나머지 **29키는 마스터리 랭크당 값**(`mastery_*`)이다 — 08-28 에 손잡이로 들어왔다. 랭크가 0 이면 기여가 0 이라 지금 캘리브레이션 표는 안 움직이지만, 찍은 파티를 재려면 그것도 손잡이다.

| 키 | 역할 |
|---|---|
| `power_growth_per_level` | **성장 SSOT** — 레벨/ilvl 1당 곱. 무기 공격력 · 영웅 최대 HP · growth 접사가 전부 이 하나를 탄다. 건드리면 전 구간이 함께 움직인다 |
| `def_curve_k` | 감쇠 곡선 `D/(D+K)` 의 **상수**. 뜻은 "감쇠가 정확히 50% 가 되는 방어값" — 공격자 레벨과 무관하다 |
| `hit_base_pct` · `hit_per_level_deficit_pct` · `hit_min_pct` | 적중률 = `clamp(base − 부족레벨 × per_level, min, base)`. 언더레벨 게이트의 세기 — 세 값이 "몇 레벨 모자라면 몇 % 맞는가"를 정한다 |
| `res_cap_base` · `res_cap_absolute` | 원소 저항 상한(직접 %) · 최대 저항 증가로도 못 넘는 절대 상한 |
| `monster_hp_scale` | 라운드 소요의 주 손잡이 (`round_budget.time_target_sec` 대조) |
| `monster_atk_scale` | **생존의 주 손잡이** — `hero_hp_base` 와 짝이다. 9라운드 내내 회복 수단이 없으므로 이 값이 승률을 거의 혼자 정한다 |
| `monster_def_scale` | 몬스터 물리 방어 배율 (저항은 직접 %라 배율을 받지 않는다) |
| `weapon_atk_base` · `hero_hp_base` | 무기 공격력 밑수 · 레벨 1 최대 HP |
| `xp_rate` · `gold_rate` | 보상 배율 |

- 값을 바꾸면 **test.html 표를 다시 찍는다**
- 헤드리스로 표를 찍을 땐 **브라우저 캐시를 끈다** — 프로필 폴더를 지우고 `--disk-cache-size=1`. 안 그러면 CSV 가 캐시돼 손잡이를 돌려도 같은 표가 나온다

### 마지막 측정 (2026-08-30 — **귀환 룰 반영 후 재측정 · 손잡이 미조정** · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

> **움직인 것은 104 하나뿐이다** (100% → 75%). 귀환 룰(base_expedition_design §1-1 — 전투불능자 하나 = 파티 귀환)이 들어가면서, 쓰러진 채로 계속 싸워 이기던 런이 철수로 끝난다.
> 101~103 은 **전투불능이 아예 안 나서** 걸릴 자리가 없다 — 표가 안 바뀐 것이 정상이고, 바뀌었다면 그게 회귀였다. 104 의 `전투불능 0.25` 는 직전 측정과 **같은 값**이다: 전에도 같은 빈도로 쓰러졌고 그 뒤에도 싸웠을 뿐이다. 이 25%p 는 밸런스가 나빠진 게 아니라 **없던 안전장치가 생긴 값**이다.
> 골드 7525 → 5785 도 같은 이유다 — 철수한 5 런이 챕터보스 골드를 못 받는다.

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 164 s | 0.00 | 1292 | 3.0 | 1.9 |
| 102 | 20 / 20 | 9.0 | 289 s | 0.00 | 1593 | 3.1 | 1.9 |
| 103 | 20 / 20 | 9.0 | 270 s | 0.00 | 1935 | 3.1 | 1.9 |
| 104 | **15 / 20** | 8.8 | 223 s | 0.25 | 5785 | 2.5 | 1.9 |

시간 초과 0 · 단정 **PASS 126 / 126**. 목표 대역(102 30~70% · 103/104 는 지는 게 정상)은 여전히 위로 벗어나 있다 — 손잡이 재보정은 남은 별도 결정이다.

---

### 직전 측정 (2026-08-28 — **마스터리·태그 반영 후 재측정 · 손잡이 미조정** · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

> **표가 직전 측정과 완전히 동일하다.** 마스터리·HP 재생·쿨타임 감소를 넣었지만 **랭크가 0 이면 기여가 0** 이고, 캘리브레이션은 레벨 1 시작 파티라 포인트를 쓴 적이 없다. 재생·쿨감소는 접사 풀에도 없어 출처가 마스터리뿐이다 — 그래서 **밸런스 대역이 안 흔들린 것이 정상**이고, 흔들렸다면 그게 회귀였다.


| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 164 s | 0.00 | 1292 | 3.0 | 1.9 |
| 102 | 20 / 20 | 9.0 | 289 s | 0.00 | 1593 | 3.1 | 1.9 |
| 103 | 20 / 20 | 9.0 | 270 s | 0.00 | 1935 | 3.1 | 1.9 |
| 104 | 20 / 20 | 9.0 | 229 s | 0.25 | 7525 | 2.8 | 1.9 |

시간 초과 0. **손잡이는 하나도 돌리지 않았다** — 표를 움직인 것은 데이터 결함 수정 셋이다:

| 무엇 | 어디 | 표에 미친 영향 |
|---|---|---|
| 보스 raw 정규화 (부채 #18) | `monster.csv` 28개 보스 행의 `hp`·`attack` → 그 스테이지 일반몹 3종 평균 (예: 사탄 hp 183→60 · atk 22→15) | **104 가 15% → 100%** · 소요 단축. 등급 elevation 은 이제 `spawn_grade` 배율 하나뿐이다 |
| 정예 `hp_mult` 3.0 → 2.0 (부채 #17) | `spawn_grade.csv` | 정예 라운드(3·6) 소요 단축 — 101 198s → 164s |
| 드롭 = 처치당 최대 1개 (item_design §1) | `battle.js onKill` · `spawn_grade.drop_chance_mult` | 드롭 수가 등급별 굴림 N회에서 **1회 × 확률 배율**로 — 평균 3.4 → 3.0 대역 |

**104 골드 7525 는 이제 챕터보스를 실제로 잡기 때문이다** (`exp_reward × exp_mult × gold_mult × gold_rate` — 사탄 116 × 20 × 10 × 0.3 = 6960 이 한 방에 들어온다).

⚠ **네 스테이지가 전부 100% 다 — 목표 대역(102 는 30~70% · 103/104 는 지는 게 정상)을 위로 벗어난다.**
재보정은 손잡이 하나로 될 일이 아니라 **스킬 예산(`skill_dps_budget_pct`) · 몬스터 축 · 귀환 룰(DEV_PLAN 부채 #14)** 을 함께 보는 별도 결정이다. 이번 작업의 범위는 "표를 다시 찍는 것"까지다.

<details><summary>이전 측정 (2026-08-28 — 스킬 도입 직후 · CSV 형태 최적화 전)</summary>

#### (2026-08-28 — **스킬 도입 후 · 손잡이 미조정** · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 198 s | 0.00 | 1295 | 2.8 | 1.6 |
| 102 | 20 / 20 | 9.0 | 314 s | 0.00 | 1589 | 3.5 | 1.9 |
| 103 | 20 / 20 | 9.0 | 301 s | 0.05 | 1922 | 3.4 | 1.9 |
| 104 | 3 / 20 | 8.0 | 310 s | 2.70 | 1627 | 1.8 | 2.0 |

시간 초과 0. **손잡이는 하나도 돌리지 않았다 — 스킬 도입에 따른 변화만 찍은 표다.**
직전 측정(08-27, 스킬 없음)은 101 20/20 · 102 10/20 · 103 1/20 · 104 0/20 이었다. 직업 기본 액티브가 전부 켜지면서 화력이 급증해 **102·103 이 목표 대역(102 는 30~70%)을 위로 벗어났다** — skill_design §9-0 이 예고한 ⚠ 그대로다. 재보정은 스킬 예산(`skill_dps_budget_pct`)과 몬스터 축을 함께 보는 **별도 결정**이라 이번에는 손대지 않았다.

<details><summary>이전 측정 (2026-08-27 — 전멸 캘리브레이션 손잡이 조정 · 스킬 없음)</summary>

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 |
|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 283 s | 0.00 | 1298 |
| 102 | 10 / 20 | 8.3 | 408 s | 1.55 | 984 |
| 103 | 1 / 20 | 7.7 | 378 s | 2.85 | 550 |
| 104 | 0 / 20 | 5.8 | 234 s | 3.00 | 431 |

**손잡이 3개를 조정했다** — `monster_hp_scale` 2.4→1.0 (킬 속도가 라운드 목표 20s 대비 한참 느려 라운드가 71s 안팎으로 늘어져 있었다), `monster_atk_scale` 0.25→0.1 (그 이하로 내리면 개별 타격이 `dmg_min`=1 바닥에 깔려 정예·보스의 공격력 배율이 무의미해진다), `hero_hp_base` 100→230 (몬스터 공격력을 낮춘 뒤에도 9라운드 무회복 구조에서 누적 피해를 버틸 체력 총량이 부족했다 — 101/102 목표 승률로 역산).
`weapon_atk_base`·`power_growth_per_level`은 28스테이지 전체의 역산값이라 건드리지 않았다 — 이 손잡이는 전투 시작 시점의 생존 축(HP 대 몬스터 공격력·HP)에 한정된다.
</details>
</details>

---
*마지막 업데이트: 2026-08-28 (**마스터리·스킬 태그 단정 13 추가** — mastery_node 무결성 2 · 마스터리 계산 4 · 포인트/거절/화면상태 3 · 세이브 v4 이관 1 · 태그 3 · 캘리브레이션 재측정(직전과 동일)) · 2026-08-28 (CSV 형태 최적화 — CSV 무결성 단정 12 추가(104 단정) · 손잡이 표 = `balance.csv:knob=1` · 캘리브레이션 재측정(보스 raw 정규화·정예 hp_mult·드롭 1회)) · 2026-08-28 (스킬 단정 15 추가 — 92 단정 · 캘리브레이션 재측정(스킬 도입 후 · 손잡이 미조정)) · 2026-08-28 (단정 추가 — 파티 순서 = 넣은 순서 · party[0] 이 리더) · 2026-08-27 (개발용 URL `?dev=form` 추가 — 편성 패널) · 2026-08-27 (전멸 캘리브레이션 해소 — monster_hp_scale·monster_atk_scale·hero_hp_base 조정) · 2026-08-26 (손잡이 표 개정 — 성장 SSOT · K 상수 · 적중 3키 · 헤드리스 DOM 덤프 절차 · 캘리브레이션 재측정) · 2026-08-26 (CLAUDE.md 에서 분리)*
