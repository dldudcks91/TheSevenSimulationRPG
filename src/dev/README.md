# src/dev — 검증

브라우저가 유일한 JS 런타임이다(빌드 없음, node 없음). 단정과 캘리브레이션은 전부 `test.html` 에서 돈다.

## 검증 방법

1. `start.bat` (로컬 http 서버 — ES Modules 는 `file://` 에서 CORS 로 막힌다)
2. `http://localhost:8777/dev/test.html` — game_logic 단정 + 밸런스 캘리브레이션 표
   - 실패 사유는 `fail()` 로 **던진다**. 문자열 반환은 통과로 집계되므로 쓰지 않는다

## 골든 시드 스냅샷 (`golden.js` · `golden.json`)

**Phase 2 엔진 이식 검증의 실제 도구다** — 같은 시드로 같은 지문이 나오면 이식이 성공한 것이다.
시드 1~10 × 스테이지 101~104 = **40 런**의 요약 지문 + **입력 지문**(CSV 해시 · `balance` 전 키 · 시작 파티)을
`golden.json` 에 박아 두고 `test.html` 이 매번 대조한다. 계약(무엇을 고정하는가·필드의 뜻)은 [INTERFACE §5-5](../../docs/client/INTERFACE.md).

- 타임라인 전체가 아니라 **요약**을 고정한다 — 해시는 "달라졌다"만 알려 주고 **어디가** 깨졌는지는 못 읽는다. 지금은 `[seed 2 stage 101] events: 1168 → 836` 처럼 런·필드 단위로 찍힌다
- 드롭은 **접사 stat·값·순서까지** 적는다. `rollAffixes` 가 풀에서 뽑는 순서 변화는 여기서만 잡힌다

### 단정 4개 — **입력 → 출력 순서로 읽는다**

| # | 단정 | 무엇을 잡나 |
|---|---|---|
| ① | `meta.csvHash` — CSV 22종 **원문** 해시 | 어느 **파일**이 달라졌는지. "`monster.csv` 가 달라졌다"가 즉시 나온다. 코드가 안 읽는 컬럼(설명문)까지 걸린다 |
| ② | `meta.balance` — `balance.csv` **전 106키** (2026-09-01 `two_hand_atk_mult` 폐기로 107 → 106) | `키: 옛값 → 새값` 을 최대 8개. 손잡이 5키는 **통과 메시지의 문구일 뿐**이다 |
| ③ | `meta.parties` — 시드 10개의 시작 파티 | 영웅 생성 굴림(이름·죄종·직업·특성·능력치·**히든 상한**)과 시작 무기. `hero_name.csv`·`hero_trait.csv` 행 순서는 여기서만 잡힌다 |
| ④ | `runs` — 40런 지문 | 전투 결과 + `grew`(정산 후 성장) · `kills` · `casts` · `elites` · `tactics` · `drops` |

> **순서가 규칙이다.** ①②가 빨간불이면 ④의 불일치는 원인이 아니라 **증상**이다 — 입력이 달라졌는데 출력만 보고 회귀를 찾으면 시간을 버린다. 그래서 화면에도 이 순서로 찍는다.
> ①②③은 **전투를 한 번도 안 돌린다.** ④가 던져도(어느 런이 예외를 내도) 위 셋의 결과와 **나머지 단정 전부**가 화면에 남는다 — 지문 생성이 최상위에서 돌던 판(08-31 최초)은 DOM 이 1.4KB 로 죽어 140 단정 결과가 통째로 사라졌고, README 아래의 「0바이트 = 무한 루프」 증상과 구분도 안 됐다.

### 다시 찍는 법

**손잡이·공식·CSV 를 의도적으로 바꿨을 때만** 다시 찍는다. 그게 아닌데 깨졌으면 그건 회귀다 — 지문을 갈아 끼워 덮지 말 것.

> ⚠ **정규식이 매치에 실패하면 예외가 아니라 빈 문자열을 준다** — 가드 없이 쓰면 `golden.json` 이 **빈 파일로 덮어써진다.** 아래 절차의 `throw` 세 줄과 백업 한 줄이 그것을 막는다. `?golden=write` 없이 덤프하면 `<pre id="golden">` 자체가 없으므로 이 함정을 반드시 밟는다.

1. 덤프 — **URL 에 `?golden=write` 가 들어가야 한다.** 프로필 폴더는 매번 새로 (CSV 캐시 방지) · `--headless=new` (구 모드는 0바이트) · **PowerShell 도구로** (git-bash 에서는 즉시 반환한다)
```powershell
$root="C:\...\TheSevenSimulationRPG"; $json="$root\src\dev\golden.json"; $dom="$env:TEMP\golden_dom.html"
$edge="C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"; $prof="$env:TEMP\edgeprof_g"; if (Test-Path $prof) { Remove-Item -Recurse -Force $prof }
Start-Process $edge -ArgumentList @('--headless=new','--disable-gpu','--no-first-run',"--user-data-dir=$prof",'--disk-cache-size=1','--virtual-time-budget=60000','--dump-dom','http://localhost:8777/dev/test.html?golden=write') -RedirectStandardOutput $dom -Wait -NoNewWindow
```
2. 추출 — **가드 먼저.** `<pre id="golden">` 은 지문 생성이 던지면 비어 있다(빈 지문을 저장하면 안 된다)
```powershell
$h = Get-Content $dom -Raw -Encoding UTF8
if ($null -eq $h -or $h.Length -lt 1000) { throw '덤프가 비었다 — 단정이 멈췄는지 확인 (아래 0바이트 주석)' }
$m = [regex]::Match($h, '(?s)<pre id="golden">(.*?)</pre>')
if (-not $m.Success) { throw '<pre id="golden"> 이 없다 — URL 에 ?golden=write 를 붙였는지 확인' }
$body = $m.Groups[1].Value -replace '&lt;','<' -replace '&gt;','>' -replace '&amp;','&'
if ($body.Trim().Length -lt 1000) { throw '지문이 비었다 — 골든 생성이 던졌다. test.html 의 골든 단정 메시지를 먼저 읽어라' }
```
3. **백업 후** 저장. **BOM 없이 UTF-8** — PowerShell 의 `Out-File -Encoding utf8` 은 BOM 을 붙이므로 쓰지 않는다
```powershell
if (Test-Path $json) { Copy-Item $json ($json + '.bak') -Force }
[System.IO.File]::WriteAllText($json, $body, (New-Object System.Text.UTF8Encoding($false)))
$b = [System.IO.File]::ReadAllBytes($json); "$($b.Length) bytes · first3 = $($b[0..2] -join ' ')"   # 239 187 191 이면 BOM 오염
```
4. `test.html` 을 다시 돌려 PASS 를 확인하고, **일부러 깨뜨려 본다.** 통과만 하고 아무것도 안 잡는 단정은 없느니만 못하다. CSV 임시 수정은 **Edit 도구로**(PowerShell 리다이렉트는 BOM 을 붙인다) 하고 **반드시 되돌린다**:

| 바꿀 것 | 잡아야 하는 단정 | 나와야 하는 말 |
|---|---|---|
| `balance.csv:attr_growth_chance_pct` 35→0 | ④ `grew` | `grew: L12/A224/M9 → L12/A210/M9` (①② 도 함께 켜진다) |
| `hero_name.csv` 행 역순 | ③ `meta.parties` | `[seed 1] 영웅 0: …\|Delphine\|… → …\|Ursula\|…` · **④는 안 켜진다**(이름은 전투에 안 들어간다 — 그래서 ③이 필요하다) |
| `balance.csv:def_curve_k` 100→150 | ② + ④ | `def_curve_k: 100 → 150` · `40/40 런 불일치` |
| `monster.csv` 설명문 한 글자 | ① `meta.csvHash` | `monster: 59940b40 → 25b11e2d` · **①만 켜진다**(게임이 안 바뀌는 변경이라 다른 그물은 원리상 못 잡는다) |

5. INTERFACE §5-5 · DEV_PLAN §3-1 을 같이 고친다

### 그래도 못 잡는 것 (2026-08-31 — 정직하게)

지문이 통과한다고 `game_logic` 전체가 맞다는 뜻이 **아니다.** 40런이 지나가지 않는 코드 경로는 아무것도 보장하지 않는다:

- **선술집** — `rollCandidates` 0회 · `tavernReroll` 0회. 명단 굴림은 시작 파티와 같은 함수지만 **시드 스트림(`^ 0x5A17`)이 다르고** `hired` 규칙은 전혀 안 밟힌다
- **전술 리롤** — `rerollTactic` 0회. `tactics` 필드는 **첫 배정**(`initialAssign`)만 본다. `TC.pick` 의 「든 것 제외」 규칙은 미검증
- **`chapter_boss`** — 표본 1 (스테이지 104 뿐). `boss_guaranteed_drop` · `dust_boss` 의 보스 분기는 한 종류만 지난다
- **`inventory_cap` 넘침** — 0회. 런당 드롭이 3 안팎이라 `discarded` 경로가 안 열린다
- **마스터리 랭크 > 0** — 0회. 시작 파티는 포인트를 쓴 적이 없어 `masteryBonus` 는 항상 빈 값이다 (`grew` 는 포인트 **합**만 본다)
- **세이브 왕복 · 이관** — 골든은 `serialize`/`deserialize` 를 안 지난다 (그쪽은 별도 단정이 본다)
- **레벨 상한 근처** — 40런의 최고 레벨이 4 대역이라 `hero_level_cap` 분기는 CSV 를 바꿔야만 밟힌다
- **csvHash 는 이식 계약이 아니다** — 개발 중 "CSV 가 바뀌었나"에만 답한다. 개행은 `\n` 으로 정규화하고 BOM 을 떼고 세므로(파서가 둘 다 무시하니까) **줄바꿈 차이로는 안 켜진다**

## 개발용 URL (`src/index.html`)

흐름을 헤드리스 스크린샷으로 태울 때 쓴다.

| URL | 상태 |
|---|---|
| `?dev=newgame` | 새 게임 시작 화면 |
| `?dev=battle` | 즉시 정산 → 리포트 |
| `?dev=play` | 관전 — `&bt=dmg` 면 우측 탭이 누적 데미지 |
| `?dev=offline` | 반복 켠 채 껐다 켠 상황 — 런 마무리 배너 |
| `?dev=form` | 편성 패널이 열린 화면 (지역을 고른 상태) |
| `?dev=tactics` | 연구 탭 — 전술 칸이 **전부 열린** 상태 (칸은 합산 레벨로만 열려 클릭으로 못 만든다) |
| `?screen=start` | 시작 화면 |
| `?tab=character` | 캐릭터 탭 |
| `?face=<스타일>` | 몬스터 얼굴 아트 스타일 교체 — `src/assets/art/faces/<스타일>/`. 목록은 `ui/mock.js:FACE_STYLES` · localStorage 에 남는다 |

헤드리스 (Edge) — 스크린샷:
```
msedge --headless=new --screenshot=<out.png> --virtual-time-budget=8000 <URL>
```

헤드리스 — **DOM 을 읽어 단정 결과·표를 그대로 가져오기.** 반드시 PowerShell 도구로 돌린다(git-bash 에서는 `msedge` 가 즉시 반환하고 출력이 0바이트다):
```powershell
$edge="C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"; $prof="$env:TEMP\edgeprof"; if (Test-Path $prof) { Remove-Item -Recurse -Force $prof }
Start-Process $edge -ArgumentList @('--headless=new','--disable-gpu','--no-first-run',"--user-data-dir=$prof",'--disk-cache-size=1','--virtual-time-budget=20000','--dump-dom','http://127.0.0.1:8777/dev/test.html') -RedirectStandardOutput "$env:TEMP\dom.html" -Wait -NoNewWindow
$h = Get-Content "$env:TEMP\dom.html" -Raw -Encoding UTF8; if ($h -match '<title>(.*?)</title>') { $Matches[1] }    # → "PASS n/n"
[regex]::Matches($h, '<li class="fail">(.*?)</li>') | % { $_.Groups[1].Value -replace '<[^>]+>',' ' }                 # 실패 목록
```
같은 방법으로 `index.html?dev=battle&lang=ko` 를 덤프해 `class="report-head"` 가 있는지 보면 렌더 예외까지 잡힌다(예외가 나면 비어 있다).

> **덤프가 0바이트면 페이지가 아니라 단정이 멈춘 것이다** (2026-08-30) — `--dump-dom` 은 가상 시간이 다 흘러야 찍는데, 단정 안에서 무한 루프가 돌면 그 시간이 오지 않아 **아무것도 안 나온다**(오류 메시지도 없다). 같은 URL 을 `--screenshot` 으로 찍으면 그때까지 그려진 화면이 남으므로 **어느 단정에서 멈췄는지가 보인다**. `index.html` 덤프는 되는데 `test.html` 만 0바이트면 이 경우를 먼저 의심한다.

## 밸런스 손잡이 (`src/data/balance.csv`)

> **`balance.csv:knob = 1` 이 손잡이의 SSOT 다** (48키 — 2026-09-01 에 폐기한 `two_hand_atk_mult` 는 `knob=0` 이었으므로 손잡이 수는 안 변했다). 손잡이인지 아닌지를 사람 기억이 아니라 컬럼이 들고 있어야 캘리브레이션이 재현된다.
> 아래 표는 그중 **전투 대역을 직접 미는 15키**를 이름으로 적은 것이고, 나머지는 **마스터리 랭크당 값 29**(`mastery_*`, 08-28)와 **강화 4**(`equip_upgrade_*`, 08-31)다 — 08-28 에 손잡이로 들어왔다. 랭크가 0 이면 기여가 0 이라 지금 캘리브레이션 표는 안 움직이지만, 찍은 파티를 재려면 그것도 손잡이다.

| 키 | 역할 |
|---|---|
| `power_growth_per_level` | **성장 SSOT** — 레벨/ilvl 1당 곱. 무기 공격력 · 영웅 최대 HP · growth 접사가 전부 이 하나를 탄다. 건드리면 전 구간이 함께 움직인다 |
| `def_curve_k` | 감쇠 곡선 `D/(D+K)` 의 **상수**. 뜻은 "감쇠가 정확히 50% 가 되는 방어값" — 공격자 레벨과 무관하다 |
| `hit_base_pct` · `hit_per_level_deficit_pct` · `hit_min_pct` | 적중률 = `clamp(base − 부족레벨 × per_level, min, base)`. 언더레벨 게이트의 세기 — 세 값이 "몇 레벨 모자라면 몇 % 맞는가"를 정한다 |
| `res_cap_base` · `res_cap_absolute` | 원소 저항 상한(직접 %) · 최대 저항 증가로도 못 넘는 절대 상한 |
| `monster_hp_scale` | 라운드 소요의 주 손잡이 (`round_budget.time_target_sec` 대조) |
| `monster_atk_scale` | **생존의 주 손잡이** — `hero_hp_base` 와 짝이다. 9라운드 내내 회복 수단이 없으므로 이 값이 승률을 거의 혼자 정한다 |
| `monster_def_scale` | 몬스터 물리 방어 배율 (저항은 직접 %라 배율을 받지 않는다) |
| `weapon_atk_base` · `hero_hp_base` | 무기 공격력 밑수 · 레벨 1 최대 HP |
| `xp_rate` · `gold_rate` | 보상 배율 |

- 값을 바꾸면 **test.html 표를 다시 찍는다**
- 헤드리스로 표를 찍을 땐 **브라우저 캐시를 끈다** — 프로필 폴더를 지우고 `--disk-cache-size=1`. 안 그러면 CSV 가 캐시돼 손잡이를 돌려도 같은 표가 나온다

### 마지막 측정 (2026-09-01 — **스킬 구조 재편(R31)** · 손잡이 미조정 · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 액티브 = 고유 1 + 직업 채움)

> **손잡이는 안 돌렸다 — 행동 변경 둘이 표를 움직였다.** ①발동 동률이 CSV `priority` 가 아니라 **칸 순서**로 갈려 1번 칸(고유)이 첫 차례에 확정으로 나간다 ②함성(`atk_pct` 창)이 **회복 밑수까지** 올린다(battle_design §9-2). 둘 다 위쪽 스테이지를 조금 올리는 방향이다.
> 구조 리팩터(등록표 · 런타임 분리 · `makeUnit`)는 **결과 불변**이었다 — 골든 40런의 타임라인 해시(`tl` · 이번에 지문에 추가)가 비트 단위로 같았다. 표가 안 움직인 것이 정상이고, 움직였다면 그게 회귀였다.

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | **10 / 20** | 7.8 | 132 s | 0.50 | 779 | 2.5 | 1.4 |
| 102 | **3 / 20** | 5.7 | 145 s | 0.85 | 455 | 1.5 | 1.4 |
| 103 | **2 / 20** | 3.9 | 106 s | 0.90 | 350 | 0.8 | 0.6 |
| 104 | 0 / 20 | 2.3 | 50 s | 1.05 | 135 | 0.8 | 0.2 |

시간 초과 0 · 단정 **PASS 175 / 175**(160 → 175 · 런타임 단위 11 · 회귀 1 · CSV/태그 4) · 골든 재촬영 2회 — 기준선(`tl` 추가 + `partyMembers` 버그 수정 · 실제로 움직인 런은 seed 10 넷) · B(CSV 3종 해시 `skill`·`balance`·**신규 `skill_tag`** + 40런). `meta.parties` 는 두 번 다 불변.
⚠ **목표 대역은 여전히 아래로 벗어난다** — 101 50%(≥70%) · 102 15%(30~70%). 재보정 선택지 셋은 그대로이고 이번에도 손대지 않았다 — 구조 변경과 손잡이 효과가 섞이면 어느 쪽이 표를 움직였는지 못 읽는다.

---

### 직전 측정 (2026-09-01 — **레어 고유 스킬 프로토타입 배정(R30)** · 손잡이 미조정 · 시드 20 · Lv1 시작 파티 · 방어구 없음 · **액티브 = 고유 1(생성 시 굴림) + 직업 채움**)

> **손잡이는 하나도 안 돌렸다 — 배정 규칙이 표를 움직였다.** 영웅이 생성 시 `skill.csv` 전 행에서 고유 스킬 1개를 굴려 1번 칸에 싣고(skill_design §9-0 개정), 나머지 칸을 직업 행이 `priority` 순으로 채운다.
> 그래서 ⓐ 자기 직업 행을 뽑은 전사는 중복 제거로 액티브가 **2칸**이 되고(전사 ③ 미정), ⓑ 남의 직업 행을 뽑은 영웅은 `priority` 3 의 단쿨기(아이스 블라스트 · 질주 · 연사 · 심판)가 상한 `active_slots` 에서 **잘린다.**
> 101 이 내려간 것은 ⓐ·ⓑ 로 파티 화력이 얇아진 쪽, 102 가 0 → 2 로 올라온 것은 시드 몇 개가 강한 고유(장쿨 강타 · 파티 배리어)를 받은 쪽 — **분산이 커졌다.** 같은 시드라도 시작 파티가 달라졌으므로(rng 소비 1회 증가) 직전 표와 시드 단위 비교는 되지 않는다.

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | **10 / 20** | 7.7 | 128 s | 0.50 | 779 | 2.3 | 1.5 |
| 102 | **2 / 20** | 5.5 | 146 s | 0.90 | 389 | 2.0 | 1.4 |
| 103 | 0 / 20 | 3.4 | 91 s | 1.00 | 201 | 0.9 | 0.8 |
| 104 | 0 / 20 | 2.0 | 46 s | 1.00 | 112 | 0.3 | 0.6 |

시간 초과 0 · 단정 **PASS 160 / 160**(157 → 160 · 고유 스킬 3) · 골든 재촬영(`balance` 해시(설명 셀 1줄) · `meta.parties` 30명 전원 · 40런 전부 — rng 순서와 액티브 구성을 의도적으로 바꿨으므로 회귀가 아니다. `meta.balance` 106키는 값 불변).
⚠ **목표 대역은 여전히 아래로 벗어난다** — 101 은 70% → **50%** 로 하한(≥70%) 밑으로 다시 내려갔고, 102 는 0% → **10%** 로 30~70% 에 못 미친다.
직전 측정에서 열어 둔 선택지 셋(①`hero_hp_base` 를 230 으로 ②`monster_atk_scale` 을 0.1 대역으로 ③둘 사이)은 **그대로 남아 있다.** 이번에도 손대지 않았다 — 배정 규칙의 효과와 손잡이의 효과가 섞이면 어느 쪽이 표를 움직였는지 못 읽는다.
⚠ 고유 풀이 전직 임시분이라 **물리 직업이 회복(치유의 빛)을 뽑으면 회복량 0** 인 채 차례를 먹는다(DEV_PLAN 부채 #28) — 표에 그 영웅이 섞여 있을 수 있다.

---

### 이전 측정 (2026-09-01 — **한손 개념 폐지 · `weapon_atk_base` 2.3 → 3.34** · 손잡이를 목표로 돌린 것이 아니다 · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

> 돌린 값은 하나지만 **밸런스를 맞추려고 돌린 게 아니다** — `two_hand_atk_mult`(1.45)가 전 무기 양손화로 전역 상수가 되면서 `weapon_atk_base` 에 흡수된 것이고,
> 곱해지는 총량은 옛 양손 무기와 같다. 움직인 것은 **옛 한손 무기(둔기·완드·한손검)를 든 영웅**이고, 시작 파티에 그런 영웅이 섞여 있어 승률이 올랐다.

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | **14 / 20** | 8.3 | 122 s | 0.30 | 972 | 2.3 | 1.3 |
| 102 | **0 / 20** | 5.8 | 136 s | 1.00 | 273 | 1.8 | 1.1 |
| 103 | 0 / 20 | 4.2 | 101 s | 1.00 | 233 | 0.8 | 0.9 |
| 104 | 0 / 20 | 2.0 | 47 s | 1.00 | 127 | 0.3 | 0.6 |

시간 초과 0 · 단정 **PASS 157 / 157** · 골든 재촬영(입력 지문 6종 + 40런 전부 — CSV 를 의도적으로 바꿨으므로 회귀가 아니다).
⚠ **목표 대역은 여전히 아래로 벗어난다** — 101 은 30% → **70%** 로 올라와 하한(≥70%)에 닿았지만, 102 는 30~70% 여야 하는데 **0% 그대로**다.
직전 측정에서 열어 둔 선택지 셋(①`hero_hp_base` 를 230 으로 ②`monster_atk_scale` 을 0.1 대역으로 ③둘 사이)은 **그대로 남아 있다.**
이번에도 손대지 않았다 — 무기 재편의 효과와 손잡이 조정의 효과가 섞이면 어느 쪽이 표를 움직였는지 못 읽는다.

---

### 이전 측정 (2026-08-31 3차 — **`hero_hp_base` 230 → 100** · 사용자 지시 · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

> **돌린 손잡이는 하나다** — `hero_hp_base` 230 → 100. **목표 대역을 아래로 벗어난다.**
> 230 은 2026-08-27 캘리브레이션이 101·102 목표 승률에서 **역산한 값**이었고, 그때 `monster_atk_scale` 은 0.1 이었다.
> 08-31 에 그 손잡이가 **0.2 로 올라간 뒤**라 생존 축이 두 번 깎인 상태다 — HP 43% × 몬스터 공격력 2배.
> 회복 수단이 9라운드 내내 없으므로(§ 캘리브레이션 조건) 이 둘은 사실상 같은 축이고, 되돌리려면 `monster_atk_scale` 을 함께 봐야 한다.

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | **6 / 20** | 7.6 | 133 s | 0.70 | 590 | 2.4 | 1.1 |
| 102 | **0 / 20** | 4.8 | 132 s | 1.00 | 230 | 1.3 | 1.1 |
| 103 | 0 / 20 | 3.0 | 86 s | 1.00 | 157 | 0.7 | 0.5 |
| 104 | 0 / 20 | 1.6 | 43 s | 1.00 | 93 | 0.2 | 0.2 |

시간 초과 0 · 단정 **PASS 155 / 155** · 골든 재촬영(40런 전부 갱신 — 손잡이를 의도적으로 돌렸으므로 회귀가 아니다).
⚠ **목표 대역 미충족** — 101 은 ≥70% 여야 하는데 30% 이고, 102 는 30~70% 여야 하는데 0% 다. 첫 스테이지부터 못 넘는다.
되돌리는 선택지는 셋: ①`hero_hp_base` 를 230 으로 ②`monster_atk_scale` 을 0.2 → 0.1 대역으로 ③둘 사이 값. **어느 쪽도 이번 작업 범위 밖이라 손대지 않았다.**

---

### 이전 측정 (2026-08-31 — **손잡이 재보정: `monster_atk_scale` 0.1 → 0.2** · `hero_hp_base` 230 · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

> **돌린 손잡이는 하나다.** 승률 100% 의 원인은 킬 속도가 아니라 **파티가 안 죽는 것**이었다(101~103 의 전투불능이 0.00) — 죽지 않는 것을 고치는 손잡이는 몬스터 공격력이다. `monster_hp_scale` 을 올리면 승률과 함께 라운드 시간이 같이 늘어 `round_budget.time_target_sec` 을 깨뜨린다.
> 탐색: 0.15(102 가 아직 높다) → **0.2(채택)** → 0.25(102 20%) → 0.3(102 0%) — 0.25 부터는 대역 아래로 지나친다.
> `hero_hp_base`·`skill_dps_budget_pct` 는 건드리지 않았다 — 같은 축(생존)을 두 손잡이로 동시에 돌리면 어느 쪽이 효과를 냈는지 못 읽고, 스킬 예산은 `skill.csv` 14행 전체의 역산 밑값이다.

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 166 s | 0.00 | 1296 | 2.8 | 1.9 |
| 102 | **11 / 20** | 8.5 | 243 s | 0.45 | 1043 | 2.5 | 1.4 |
| 103 | 1 / 20 | 7.3 | 211 s | 0.95 | 516 | 1.9 | 1.6 |
| 104 | 0 / 20 | 4.8 | 124 s | 1.00 | 351 | 1.0 | 1.1 |

시간 초과 0 · 단정 **PASS 155 / 155** (08-31 골든 보강으로 140 → 142 · 같은 날 **강화(R25) 구현으로 142 → 155** — 두 번 다 **표는 한 칸도 안 움직였다.** 강화는 플레이어가 골드를 내고 누르는 행동이라 캘리브레이션(레벨 1 시작 파티·강화 0회)이 밟지 않는다. 움직였다면 그게 회귀였다 — 실제로 골든 재촬영에서 `runs` 40런과 `meta.parties` 가 **전부 그대로**인 것으로 확인했다).
**목표 대역을 충족한다** — 101 ≥ 70%(100%) · 102 30~70%(55%) · 103·104 는 102 보다 낮다.
**이 표가 골든 시드 스냅샷(`golden.json`)이 찍힌 지반이다** — 손잡이를 다시 돌리면 스냅샷도 다시 찍는다.

---

### 이전 측정 (2026-08-30 — **귀환 룰 반영 후 재측정 · 손잡이 미조정** · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

> **움직인 것은 104 하나뿐이다** (100% → 75%). 귀환 룰(base_expedition_design §1-1 — 전투불능자 하나 = 파티 귀환)이 들어가면서, 쓰러진 채로 계속 싸워 이기던 런이 철수로 끝난다.
> 101~103 은 **전투불능이 아예 안 나서** 걸릴 자리가 없다 — 표가 안 바뀐 것이 정상이고, 바뀌었다면 그게 회귀였다. 104 의 `전투불능 0.25` 는 직전 측정과 **같은 값**이다: 전에도 같은 빈도로 쓰러졌고 그 뒤에도 싸웠을 뿐이다. 이 25%p 는 밸런스가 나빠진 게 아니라 **없던 안전장치가 생긴 값**이다.
> 골드 7525 → 5785 도 같은 이유다 — 철수한 5 런이 챕터보스 골드를 못 받는다.

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 164 s | 0.00 | 1292 | 3.0 | 1.9 |
| 102 | 20 / 20 | 9.0 | 289 s | 0.00 | 1593 | 3.1 | 1.9 |
| 103 | 20 / 20 | 9.0 | 270 s | 0.00 | 1935 | 3.1 | 1.9 |
| 104 | **15 / 20** | 8.8 | 223 s | 0.25 | 5785 | 2.5 | 1.9 |

시간 초과 0 · 단정 **PASS 126 / 126**. 목표 대역(102 30~70% · 103/104 는 지는 게 정상)은 여전히 위로 벗어나 있다 — 손잡이 재보정은 남은 별도 결정이다.

---

### 이전 측정 (2026-08-28 — **마스터리·태그 반영 후 재측정 · 손잡이 미조정** · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

> **표가 직전 측정과 완전히 동일하다.** 마스터리·HP 재생·쿨타임 감소를 넣었지만 **랭크가 0 이면 기여가 0** 이고, 캘리브레이션은 레벨 1 시작 파티라 포인트를 쓴 적이 없다. 재생·쿨감소는 접사 풀에도 없어 출처가 마스터리뿐이다 — 그래서 **밸런스 대역이 안 흔들린 것이 정상**이고, 흔들렸다면 그게 회귀였다.


| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 164 s | 0.00 | 1292 | 3.0 | 1.9 |
| 102 | 20 / 20 | 9.0 | 289 s | 0.00 | 1593 | 3.1 | 1.9 |
| 103 | 20 / 20 | 9.0 | 270 s | 0.00 | 1935 | 3.1 | 1.9 |
| 104 | 20 / 20 | 9.0 | 229 s | 0.25 | 7525 | 2.8 | 1.9 |

시간 초과 0. **손잡이는 하나도 돌리지 않았다** — 표를 움직인 것은 데이터 결함 수정 셋이다:

| 무엇 | 어디 | 표에 미친 영향 |
|---|---|---|
| 보스 raw 정규화 (부채 #18) | `monster.csv` 28개 보스 행의 `hp`·`attack` → 그 스테이지 일반몹 3종 평균 (예: 사탄 hp 183→60 · atk 22→15) | **104 가 15% → 100%** · 소요 단축. 등급 elevation 은 이제 `spawn_grade` 배율 하나뿐이다 |
| 정예 `hp_mult` 3.0 → 2.0 (부채 #17) | `spawn_grade.csv` | 정예 라운드(3·6) 소요 단축 — 101 198s → 164s |
| 드롭 = 처치당 최대 1개 (item_design §1) | `battle.js onKill` · `spawn_grade.drop_chance_mult` | 드롭 수가 등급별 굴림 N회에서 **1회 × 확률 배율**로 — 평균 3.4 → 3.0 대역 |

**104 골드 7525 는 이제 챕터보스를 실제로 잡기 때문이다** (`exp_reward × exp_mult × gold_mult × gold_rate` — 사탄 116 × 20 × 10 × 0.3 = 6960 이 한 방에 들어온다).

⚠ **네 스테이지가 전부 100% 다 — 목표 대역(102 는 30~70% · 103/104 는 지는 게 정상)을 위로 벗어난다.**
재보정은 손잡이 하나로 될 일이 아니라 **스킬 예산(`skill_dps_budget_pct`) · 몬스터 축 · 귀환 룰(DEV_PLAN 부채 #14)** 을 함께 보는 별도 결정이다. 이번 작업의 범위는 "표를 다시 찍는 것"까지다.

<details><summary>이전 측정 (2026-08-28 — 스킬 도입 직후 · CSV 형태 최적화 전)</summary>

#### (2026-08-28 — **스킬 도입 후 · 손잡이 미조정** · 시드 20 · Lv1 시작 파티 · 방어구 없음 · 전직 액티브 전부 켬)

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 | 드롭 | 카드 |
|---|---|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 198 s | 0.00 | 1295 | 2.8 | 1.6 |
| 102 | 20 / 20 | 9.0 | 314 s | 0.00 | 1589 | 3.5 | 1.9 |
| 103 | 20 / 20 | 9.0 | 301 s | 0.05 | 1922 | 3.4 | 1.9 |
| 104 | 3 / 20 | 8.0 | 310 s | 2.70 | 1627 | 1.8 | 2.0 |

시간 초과 0. **손잡이는 하나도 돌리지 않았다 — 스킬 도입에 따른 변화만 찍은 표다.**
직전 측정(08-27, 스킬 없음)은 101 20/20 · 102 10/20 · 103 1/20 · 104 0/20 이었다. 직업 기본 액티브가 전부 켜지면서 화력이 급증해 **102·103 이 목표 대역(102 는 30~70%)을 위로 벗어났다** — skill_design §9-0 이 예고한 ⚠ 그대로다. 재보정은 스킬 예산(`skill_dps_budget_pct`)과 몬스터 축을 함께 보는 **별도 결정**이라 이번에는 손대지 않았다.

<details><summary>이전 측정 (2026-08-27 — 전멸 캘리브레이션 손잡이 조정 · 스킬 없음)</summary>

| 스테이지 | 승률 | 라운드 | 소요 | 전투불능 | 골드 |
|---|---|---|---|---|---|
| 101 | 20 / 20 | 9.0 | 283 s | 0.00 | 1298 |
| 102 | 10 / 20 | 8.3 | 408 s | 1.55 | 984 |
| 103 | 1 / 20 | 7.7 | 378 s | 2.85 | 550 |
| 104 | 0 / 20 | 5.8 | 234 s | 3.00 | 431 |

**손잡이 3개를 조정했다** — `monster_hp_scale` 2.4→1.0 (킬 속도가 라운드 목표 20s 대비 한참 느려 라운드가 71s 안팎으로 늘어져 있었다), `monster_atk_scale` 0.25→0.1 (그 이하로 내리면 개별 타격이 `dmg_min`=1 바닥에 깔려 정예·보스의 공격력 배율이 무의미해진다), `hero_hp_base` 100→230 (몬스터 공격력을 낮춘 뒤에도 9라운드 무회복 구조에서 누적 피해를 버틸 체력 총량이 부족했다 — 101/102 목표 승률로 역산).
`weapon_atk_base`·`power_growth_per_level`은 28스테이지 전체의 역산값이라 건드리지 않았다 — 이 손잡이는 전투 시작 시점의 생존 축(HP 대 몬스터 공격력·HP)에 한정된다.
</details>
</details>

---
*마지막 업데이트: 2026-09-01 (**스킬 구조 재편(R31)** — 「마지막 측정」 갱신(102 10% → 15% · 103 0% → 10% · 손잡이 무변동 — 동률 = 칸 순서 · 회복 밑수 버프) · 골든 런 지문에 **`tl`**(타임라인 해시 — 결과 불변 리팩터의 잠금장치 · INTERFACE §5-5) · 단정 160 → **175** · CSV 23종) · 2026-09-01 (**레어 고유 스킬 프로토타입 배정(R30)** — 「마지막 측정」 갱신(101 70% → 50% · 102 0% → 10% · 손잡이 무변동 — 배정 규칙이 움직였다) · 단정 157 → **160** · 골든 재촬영(30명 · 40런). 캘리브레이션 조건 문구를 「전직 액티브 전부 켬」에서 「고유 1 + 직업 채움」으로. 목표 대역 재보정은 이번에도 손대지 않았다) · 2026-09-01 (**한손 개념 폐지 · 무기군 재편** — 「마지막 측정」 갱신(101 30% → 70% · 102 0% 그대로). 돌린 값은 `weapon_atk_base` 2.3 → 3.34 **하나**인데 이건 목표를 맞추려 돌린 게 아니라 폐기한 `two_hand_atk_mult` 1.45 를 흡수한 것이다 — 곱해지는 총량이 달라진 건 **옛 한손 무기를 든 영웅뿐**. 단정 155 → **157** · 골든 재촬영(CSV 6종 + 40런). 목표 대역 재보정은 이번에도 손대지 않았다) · 2026-08-31 (**`hero_hp_base` 230 → 100** 측정 기록 — 사용자 지시로 돌린 손잡이 하나. 101 100%→30% · 102 55%→0% 로 **목표 대역을 아래로 벗어난다**(230 은 `monster_atk_scale` 0.1 시절의 역산값인데 그 손잡이가 0.2 로 올라간 뒤다). 골든 재촬영 · PASS 155/155) · 2026-08-31 (**강화 구현 반영** — 단정 142 → 155(강화 13 + v6→v7 이관 1, 세이브 단정 갱신) · `balance.csv` 101 → 107키(손잡이 44 → 48) · **골든 재촬영**(입력 지문만 바뀌고 `runs` 40런·`meta.parties` 는 그대로 — 강화가 캘리브레이션을 안 밟는다는 증거)) · 2026-08-31 (**골든 사각지대 메우기** — 지문 생성을 `check()` 안으로(던져도 나머지가 산다) · 런 지문에 `grew`·`kills`·`casts`·`elites` 추가 · `tactics` 를 **열린 칸 전체**로 · `meta.parties`(시작 파티 10 — 영웅 생성 + 시작 무기) · `meta.balance` **전 키** + `meta.csvHash` **22파일**(손잡이 5키만 보던 판은 밖의 키를 통과시켜 오진을 만들었다) · 요약(`n/40 런 불일치`)을 맨 앞에 · 재촬영 절차의 **빈 파일 덮어쓰기 함정** 수정(가드 3 + 백업) · 못 잡는 것 목록 · 단정 140 → **PASS 142/142** · 캘리브레이션 불변) · 2026-08-31 (**골든 시드 스냅샷** — `golden.js`·`golden.json` 신설 · 다시 찍는 법(`?golden=write` · BOM 없이 저장 · 일부러 깨뜨려 보기) · 단정 2 추가로 **PASS 137/137** · 캘리브레이션 재측정(`monster_atk_scale` 0.1→0.2 로 목표 대역 충족)) · 2026-08-28 (**마스터리·스킬 태그 단정 13 추가** — mastery_node 무결성 2 · 마스터리 계산 4 · 포인트/거절/화면상태 3 · 세이브 v4 이관 1 · 태그 3 · 캘리브레이션 재측정(직전과 동일)) · 2026-08-28 (CSV 형태 최적화 — CSV 무결성 단정 12 추가(104 단정) · 손잡이 표 = `balance.csv:knob=1` · 캘리브레이션 재측정(보스 raw 정규화·정예 hp_mult·드롭 1회)) · 2026-08-28 (스킬 단정 15 추가 — 92 단정 · 캘리브레이션 재측정(스킬 도입 후 · 손잡이 미조정)) · 2026-08-28 (단정 추가 — 파티 순서 = 넣은 순서 · party[0] 이 리더) · 2026-08-27 (개발용 URL `?dev=form` 추가 — 편성 패널) · 2026-08-27 (전멸 캘리브레이션 해소 — monster_hp_scale·monster_atk_scale·hero_hp_base 조정) · 2026-08-26 (손잡이 표 개정 — 성장 SSOT · K 상수 · 적중 3키 · 헤드리스 DOM 덤프 절차 · 캘리브레이션 재측정) · 2026-08-26 (CLAUDE.md 에서 분리)*
