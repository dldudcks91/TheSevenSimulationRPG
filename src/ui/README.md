# src/ui — DOM 렌더러

**Phase 2(엔진 이식)에서 버려질 레이어.** 계산·난수 없음 — 상태(G)를 읽고 `game_logic` 시스템(SYS)을 부르고 `save()` 한다.

## 파일별 역할

| 파일 | 역할 |
|---|---|
| `app.js` | 화면 렌더링. 계산·난수 없음 |
| `battle.js` | 전투 관전 = `game_logic/battle.js` 타임라인 **재생기** (계산하지 않는다) |
| `data.js` | CSV fetch → `game_logic` 시스템 조립(주입). 수치 미러 없음. `D.csvText` 에 **원문**도 남긴다 — 읽는 곳은 `dev/golden.js` 의 CSV 해시 하나뿐이고 게임 로직은 안 본다 (2026-08-31) |
| `storage.js` | localStorage 어댑터 — 저장소를 만지는 **유일한** 파일 |
| `tip.js` | 툴팁 — **기계장치 한 곳**(표시 · 따라다니기 · 넘침 보정 · 겹친 툴팁 복귀). 두 렌더러가 함께 쓰는 **영웅 카드 · 스킬 카드**도 여기. 스킬 카드는 **문장을 조립한다**(2026-09-08 · SCREEN_DESIGN §4-2) — 틀은 `kind`×`target`, 파생값(실효 쿨·피해)과 **식 재료**는 `game_logic/skill.js:previewOf`(`.parts`)가 낸다. **Alt 계산식 상태도 여기 든다** — 누르는 동안만 켜지고 떠 있는 **스킬 카드만** 같은 인자로 다시 그린다. 숫자 자리는 기본 / Alt(값 + 괄호 식) / 값 없음(식만) 셋이고, 스킬 창 줄 문장(`skillLineHtml`)은 Alt 를 안 본다 (2026-09-10 · SCREEN_DESIGN §2 「스킬 설명창 규격」 · ADR-0089). 아이템 비교 카드는 `app.js` (app 헬퍼를 많이 탄다) |
| `i18n.js` | 한/영 사전 `STRINGS` + 언어 상태 |
| `mock.js` | 화면 전용 사전 — 죄종·희귀도 색 · 접사 **표기**(`AFFIX_LABELS` — ⚠ 19개 중 12개가 `combat_stat.csv` 와 이름·단위 중복이다. 08-31 에 `reflect_damage` 가 갈려 한 탭에서 다르게 찍히는 사고가 났다) · 페이퍼돌 배치(`PAPERDOLL`) · 마스터리 판 프레임(`MASTERY_GRID` — 티어 3 × 노드 3) · 자산 경로(`faceDir()`·`BG_DIR`·`stageBg`·`slotArt` — 미장착 칸의 부위 실루엣 · `itemArt` — 아이템 그림, ⚠ 방어구는 임시로 부위당 한 장) · **얼굴 아트 스타일 전환**(`FACE_STYLES`·`setFaceStyle`·`applyDocumentFace` — 폴더 하나 = 스타일 하나. 고른 스타일은 `<html data-face>` 로도 나간다 — CSS 가 **초상 축소 보간**을 가르는 신호다, 아래 「초상 보간」) · 액티브 표시 사전(`SKILL_DISPLAY` — 아이콘·설명만. 이름·쿨은 `skill.csv`). **직업·슬롯·아이템 베이스·접사 정의·영웅 이름/특성 풀은 2026-08-31 로 CSV 로 나갔다**(`class`·`equip_slot`·`item_base`·`affix`·`hero_name`·`hero_trait` → `data.js` 의 `D.classes`·`D.slots`/`D.equipSlots`·`D.itemBases`·`D.affixDefs`·`D.heroNamePool`·`D.heroTraitPool`). 이름 ko/en·얼굴 유무·기본 능력치 7·전투 능력치 25·도감 보정은 08-28 로 나갔다 — 헬퍼는 `data.js` (`monsterName`·`monsterFace`·`monsterSin`·`stageName`·`stageBgOf`·`chapterOf`·`eliteName`). **남은 게임 데이터는 죄종(`SINS`)·정예 특성(`SIN_TRAITS`·`COMMON_TRAITS`) 셋뿐** — 죄종 매핑 확정을 기다린다 |
| `style.css` | 스타일 |

- **공통 영웅 띠** (`app.js heroStrip`) — 캐릭터 탭 상단(스킬은 탭을 잃고 **창**이 됐고, 창은 띠를 다시 그리지 않는다 — SCREEN_DESIGN §7), 원정은 **출정 창 안 하나**(`departBody` — 스테이지를 골라야 열린다, `{flat:true}` 로 패널 껍데기를 벗는다 · 탭 최상단에 두던 판은 2026-09-10 에 걷었다 · ADR-0085). 창 안의 것만 `{deployed:true}` 라 파티 카드가 흐리다 — 흐림의 뜻이 「바로 아래 진형 보드에 내려가 있다」이기 때문이다(ADR-0058). 둘 다 같은 띠: 초상(`.hero-face` — 관전 유닛 카드와 같은 네모 박스 · 그림은 `mock.js heroFace` 가 **이름**으로 고르고 없으면 밑에 깔린 직업 글리프 `mock.js classGlyph` 가 드러난다, [SCREEN_DESIGN §5](../../docs/client/SCREEN_DESIGN.md))이 카드 전체, 그 위 위칸(`.hs-band`)에 지금 하는 일(출정 아웃 / 전투 파티 / 대기) + 이름. 직업·레벨·죄종·등급은 툴팁

## 다국어 (한/영) — 필수

화면에 나가는 모든 문자열은 한 곳에 ko/en 이 **나란히** 있어야 한다.

| 종류 | 위치 | 사용 |
|---|---|---|
| UI 문구 | `i18n.js` 의 `STRINGS` — 키 하나에 `{ko, en}` | `t('key')` |
| 데이터 문자열 | CSV 의 `_kr`/`_en` 컬럼 쌍 → `{ko, en}` (로더·헬퍼가 조립) · 아직 CSV 로 안 간 것은 `mock.js` | `L(value)` |

- **렌더러(`app.js` / `battle.js` / `tip.js`)에 한국어 리터럴 금지** — 주석 제외. 이게 누락 검증 기준이다
- 아이템·스킬 이름은 **영어가 원본**이고 한글은 **직역**이다 — 의역 금지 (`Morning Star` → 모닝스타, ~~별철퇴~~) [2026-09-09 사용자 확정] — `_en` 을 먼저 짓고 `_kr` 이 그것을 따라간다. UI 문구(`STRINGS`)는 이 규칙 밖이다 — 문장이라 직역이 성립하지 않는다. 규약 본문은 [src/data/README.md 공통 규약](../data/README.md)
- 이름 조립 규칙(어순·조사)은 렌더러에 두지 않는다 — **`game_logic/naming.js`** 가 든다 (`composeName` · `eliteName`, 2026-08-31 이관). `data.js:eliteName(sin, monsterId)` 은 id → 이름 조회만 하고 조립은 넘긴다
- **설명 문구는 도움말 탭 전용** — 인게임 패널에 안내 문장 금지 (2026-08-26 사용자 지시). 패널에는 숫자·상태·오류·버튼·툴팁만 남기고, 규칙 문구는 `app.js` 의 `helpSections()` 에 **기존 키를 재사용해** 넣는다 ([SCREEN_DESIGN §12](../../docs/client/SCREEN_DESIGN.md))

## 글씨 크기 — 토큰만 쓴다 [2026-09-03]

- 계단은 **`--fs-2xs` 9 · `--fs-xs` 12 · `--fs-sm` 13 · `--fs-md` 15 · `--fs-lg` 18 · `--fs-xl` 24** 여섯 단이다. 새 리터럴을 쓰지 않는다 — 같은 역할인데 크기가 갈리는 것이 이 화면에서 실제로 났다(등급 칩 9px ↔ 죄종 칩 12px)
- **`--fs-2xs` 는 칸 안 초소형 라벨 전용** — 능력치 약어 배지 · 마스터리 칸의 랭크/값 · 페이퍼돌 라벨 · 라운드 트랙 숫자 · 관전 특성 줄. 9px 와 10px 리터럴이 열 군데 흩어져 있던 것을 한 단으로 모았다. ⚠ **창 뱃지(`.buff-chip`)는 2026-09-03 에 빠졌다** — 칩이 그림을 들면서 16 → 24px 로 커졌고 글씨는 그림이 없을 때의 폴백이라 `--fs-xs` 로 올렸다
- **같은 줄에 서는 칩은 같은 규격이다** — 크기 · 줄높이 · 좌우 여백까지. 등급 칩과 죄종 칩은 둘 다 「이 영웅이 무엇인가」라 한쪽만 작으면 덜 중요한 것으로 읽힌다
- **예외는 아이콘 크기뿐이다** — `.cd-slot` 19 · `.inv-icon` 20 · `.pd-icon` 22 · `.unit .sprite` 38 은 **담는 칸 크기**(32 · 44 · 60 · 76px)가 정하는 값이라 한 단으로 못 모은다. 글자가 아니라 그림 자리다
- **스킬 아이콘은 2026-09-03 부터 진짜 그림이다** — 이모지가 들던 칸 다섯(`.cd-slot .cd-g` 32 · `.buff-chip` 16 · `.act-slot .ico` 28 · `.sk-card .ico` 40 · `.ng-skill .ico` 44 · `.tip-sk-ico` 22)에 `<img>` 가 들어간다. 크기를 정하는 것은 **칸**이고 그림은 `object-fit: contain` 으로 맞춰진다 — 그래서 위의 글씨 크기 계단과 무관하다. 경로 조립은 `mock.js:skillIcon` 한 곳 ([SCREEN_DESIGN §2](../../docs/client/SCREEN_DESIGN.md) · 자산은 [assets/art/README.md](../assets/art/README.md))
- **미장착 착용 칸도 그림이다** [2026-09-03] — 페이퍼돌의 빈 칸(`.pd-cell.art .pd-art img`)이 부위 실루엣을 옅게(`opacity`) 깐다. 축은 **부위**(`equip_slot.csv:part`)라 반지 두 칸이 같은 그림을 들고, 조립은 `mock.js:slotArt` 한 곳이다. 그림이 없는 부위(투구·목걸이)는 옛 이모지로 남는다 — **해시 폴백을 안 쓴다**(부위는 틀린 그림이 곧 틀린 정보다). ⚠ **CSS `background: url()` 이 아니라 `<img>`** 다: 상대 경로를 커스텀 프로퍼티에 담으면 `style.css` 기준(`/ui/assets/…`)으로 풀려 404 가 난다 ([SCREEN_DESIGN §6](../../docs/client/SCREEN_DESIGN.md))
- **아이템도 그림이다** [임시 2026-09-03] — 이모지가 들던 칸 넷(`.pd-icon`(착용) 34 · `.inv-icon` 34 · `.fg-row .fg-ic` 22 · `.fg-head .fg-ic` 30)이 `<img>` 를 받는다. ⚠ 이 칸들은 **크기를 새로 줘야 했다**: 이모지는 글씨라 `font-size` 가 칸을 정했지만 img 는 부모가 정한다. 조립은 `mock.js:itemArt` 한 곳이고, **무기는 제 그림 · 방어구는 임시로 부위당 한 장**(개체가 `base_id` 를 안 들고 다닌다) · 투구·목걸이는 이모지로 떨어진다 ([SCREEN_DESIGN §2](../../docs/client/SCREEN_DESIGN.md))

## 화면 크기 정책

- **화면은 1600×800 한 장이다** [2026-09-10 · [ADR-0087](../../docs/client/adr/0087-화면은-1600x800-한-장이다.md)] — 크기의 SSOT 는 `style.css` 의 `#stage` 하나이고, `app.js:fitStage` 가 창에 맞춰 **통째로** 줄이고 늘린다. 배율 `s = min(창 폭 / 1600, 창 높이 / 800)` — **상한 · 하한 없음** · 한 장은 창 가운데 · 남는 가장자리는 `body::before/::after` 배경이 채운다
  - **창 크기를 읽는 CSS 를 쓰지 않는다** — `vw` · `vh` · 미디어 쿼리는 한 장이 아니라 실제 창을 봐서, 창마다 한 장 안의 크기가 흔들린다. 그 자리는 1600×800 에서의 값으로 고정돼 있다
  - **마우스 좌표는 `tip.js:stagePoint` 를 거친다** — 툴팁 · 진형 드래그 고스트. `transform` 이라 고정 위치 자손(툴팁 · 창 레이어 · 고스트)의 기준이 한 장이다
  - **검증은 1600×800(배율 1)** 으로 찍는다 — 배율 확인은 1280×700 · 1920×1080 한 번씩
- **메인은 한 번 더 줄인다** [2026-09-10 · [ADR-0090](../../docs/client/adr/0090-메인은-한-번-더-줄인다-줄어든-폭은-탭-내비가-갖는다.md)] — `#app:not(.pregame) > .main { zoom: .9 }` · 탭 내비 **330px**(`#app` 첫 열). 메인 칸 1270×752 안에서 내용이 **1411×836** 으로 레이아웃된다 — 설계 폭은 줄이기 전(1412)과 같고 세로만 84px 늘었다. **아래 탭 줄들의 실측 px 는 이 설계 크기다**(화면에는 90%)
  - **안 줄이는 것** — 브랜드 · 상단바 · 내비 · `#tooltip` · `#modal`(한 장 직속) · 내비가 없는 화면(`#app.pregame` — 시작 · 프롤로그). 재생기 안의 로그 · 누적 창은 메인 안이라 같이 준다
  - **`transform` 이 아니라 `zoom`** — 칸 폭 안에서 레이아웃이 설계 크기로 돈다(`transform` 은 레이아웃에 안 걸려 감싸는 칸이 따로 든다). 메인 안에는 마우스 좌표를 쓰는 코드가 없다 — 툴팁은 한 장 직속 · 진형 드래그는 출정 창
  - **배율은 세로가 정한다** — 메인 칸 752 ÷ 가장 긴 탭의 설계 세로. **여유 2px**(캐릭터 탭 834 / 836) — 세 단이 더 자라면 다시 스크롤한다. 내비 폭은 따로 고르는 값이고 설계 폭을 1412 에 맞춘 것이 330 이다
  - **글자는 한 장 배율에 곱해진다** — `--fs-2xs` 9px 가 1600×800 에서 8.1px · 1366×768 에서 약 6.5px
- 영어는 같은 내용이 1.3~1.5배 길다 — 라벨이 들어가는 칸은 **고정 px 금지**(`minmax()` / `clamp()` 로 최소만 보장한다)
- **배율이 한 장을 줄이므로 문제는 폭이 아니라 글자 크기다** — 대략값: 1920×1080 배율 100% → 118% · 배율 125% → 91% · 1366×768 → 80%(탭 · 주소창 · 작업표시줄이 세로 약 130px 를 먹어 노트북에서는 세로가 배율을 정한다 · ADR-0087)
- ⚠ **열린 것 둘** — **1280 에 맞춰 줄여 둔 조정이 남아 있다**(선술집 카드 · 관전 헤드 등 — 1600 에서 안 깨지므로 화면별로 푼다) · **최소 배율이 없다**(작은 노트북에서 글자가 작아진다 — 필요하면 그때 정한다)
- **출정 창** [ADR-0084 · [ADR-0088](../../docs/client/adr/0088-출정-창은-2x2-다.md)]: **2×2**(위 줄 캐릭터 선택 ↔ 적 구성 · 아래 줄 진형 → 출정 방식) · 폭 `width: min(1520px, 100%)` — 내용 폭에 맡기면 **경고 줄의 글자 하나가 칸 폭을 정해** 창이 상태마다 넓어졌다 좁아졌다 한다(로그 창 `.bw-box` 가 같은 이유로 고정이다). 열은 `minmax(0, 1fr) auto` — 오른쪽 = 적 구성 내용 폭 · 왼쪽 = 남는 폭. 판 여백 `22/24/26` · 칸 사이 여백 26 · 줄 사이 28 + 28 · 칸 이름 아래 12
  - **`--fm-slot` 118 고정** — 적 구성 칸(4개)과 진형 축(3칸)이 같이 읽는다
  - **띠 카드는 공통 규격 그대로다** (`.hs-card` 상한 120) — 띠는 캐릭터 탭과 **같은 카드**여야 같은 부품으로 읽힌다(SCREEN_DESIGN §5)
  - ⚠ **선택자가 `.modal-box.depart-box` 다** — `.depart-box` 한 단이면 뒤에 선언된 창 공통 `.modal-box`(12/14/14)와 명시도가 같아 판 여백이 덮인다
  - 실측 1600×800 — 창 **1520×548**(ko · en · Ch7-4 · 파티 없음 전부 같다) · 열 950 / 520 · 띠 7장과 적 4칸이 각각 한 줄
- **리포트 상세 — 높이를 내용이 안 정한다** [2026-09-10 · [ADR-0086](../../docs/client/adr/0086-리포트-상세는-내용에-흔들리지-않는다-영웅의-일은-영웅-줄이-든다.md)]: 장비 격자는 칸 한 변 `--rep-cell` **44px** 한 줄 높이(넘치면 칸 안 스크롤) · 기여 표는 `party_size_max` 줄 · 획득 줄은 `minmax(max-content, n fr)` 몫 `.8 / 1 / .8 / .9 / 1.4 / 1.3`(하한이 내용 폭이라 몫을 넘는 값은 잘리지 않고 칸을 민다) · 런 목록은 `contain: size` 라 격자 행 높이에 안 들어간다. 실측 1600×800 — 상세 **663px** · `.main` 스크롤 없음(1런 · 3런 · ko/en)
- **도감 스킬 세그먼트 — 조밀 격자** [2026-09-10 · [ADR-0075](../../docs/client/adr/0075-스킬-세그먼트는-조밀-격자다-직업-다섯이-한-화면에-선다.md)]: `.ix-body.dense` 가 타일 패딩 6 → 3 · 묶음 패딩 `7/10/9` → `3/8/4` · 묶음 사이 10 → 5px 로 조이고, 칸 폭은 `minmax(112px, 1fr)` — 가장 큰 묶음이 한 줄에 서는 값이고, 접히는 순간 한 장을 넘는다.
  - **그림 칸 `--ix-art` 48px 고정** — 실측 1600×800 — 다섯 묶음이 묶음마다 한 줄로 한 장 안에 선다(패널 아래 여유 26px · ko/en 같다)
  - 캐릭터 · 아이템 세그먼트는 92px 고정 그대로 — 그쪽은 조일 이유가 없다
- **관전 아레나** [2026-09-03 사용자 지시]: 넓게 배치는 **판 전폭 · 남는 세로**(최소 350 · 비를 안 지킨다) [2026-09-11 · [ADR-0095](../../docs/client/adr/0095-넓게-보기의-아레나는-판-전폭이다-가방은-새-게임-줄에-붙는다.md)] — 관전 화면(`.bt-page` = 전투 판 + 가방)이 메인 세로를 채우고 아레나가 헤드 · 가방을 뺀 나머지를 먹는다(세로 flex). 숫자로 박지 않아 헤드 · 가방이 자라도 다시 잴 필요가 없다. 실측 1600×800 **1337×502**(설계 크기) · `.main` 스크롤 없음(ko/en). 나눔 배치는 **왼쪽 아레나 + 오른쪽 로그 열** [2026-09-11 · [ADR-0093](../../docs/client/adr/0093-나눔-보기는-큰-아레나와-로그-열이다-넓게-보기는-창으로-되돌린다.md)] — 열 `minmax(300px, 25%)` · 단 사이 14 · 아레나는 남는 폭에서 비 2.33 · 카드 · 진형 간격은 넓게와 같다. 실측 아레나 **989×424** · 열 **334×424** · 스크롤 없음 · 가방 아래 약 70px 빔
  - **관전의 가방 — 「새 게임」 줄에 붙고 칸이 한 단 크다** [2026-09-11 · ADR-0095]: 넓게 배치면 가방 아랫변 = 탭 내비 「새 게임」 아랫변(한 장 **788**) — 내비 아래 여백 12 는 배율을 안 타므로 **이 화면만** 메인 아래 여백이 `12px / .9`(`.main:has(> .bt-page)`). 칸 `minmax(51px, 1fr)` = 70칸이 3줄에 드는 가장 큰 폭(판 안 1337 · 간격 4 → **24열 · 한 변 52**) · 그림 **40px**. 실측 가방 **1371×219**. 캐릭터 탭 가방은 42 · 34 · 29열 그대로(`.bt-bag` 밖)
- **선술집 명단** [2026-09-08 사용자 지시]: 후보 카드가 시작 화면과 **같은 규격**이다(SCREEN_DESIGN §8-1). 실측 1600×800 — 시작 화면 카드 **344px** · 초상 **136px** / 선술집 카드 **437px** · 초상 **152px**(`clamp(96px, 44cqw, 152px)` 의 상한). **의뢰 줄까지 스크롤 없이 선다**(ko/en 같다 · 메인 배율) — 초상을 크게 읽는 규격 그대로 한 장에 든다
  - 카드 폭이 다른 것은 **그릇**이다(시작은 `.ng-wrap { max-width: 1060px }` · 선술집은 `.panel` 전폭). 초상은 **카드 안쪽 폭** 기준(`44cqw` · [ADR-0076](../../docs/client/adr/0076-후보-카드의-초상은-창이-아니라-카드를-기준으로-자란다.md))이라 창이 아니라 카드를 따른다
- **탐험 지도** [2026-09-04 사용자 지시]: `.ex-map` 은 메인 칸 **전폭**에 자산 화면비(2752×1211 · 2.27:1)를 그대로 지킨다. 실측 1600×800 — 지도 **1371×603**(설계 크기) · 아래 미착수 안내까지 **스크롤 없이 선다**(메인 배율). ⚠ 다음 지도도 **가로로 긴 비**여야 이 자리가 더 늘지 않는다 ([SCREEN_DESIGN §8-4](../../docs/client/SCREEN_DESIGN.md))
- **초상 보간** [2026-09-01] — 초상은 어디서나 원본보다 작게 그려진다(구성 칩 26px ~ 시작 후보 카드 152px). **줄이는 방법은 스타일이 정한다**: 토큰 `--face-render` 가 `:root` 에서 `auto`(손그림 — 기본값이자 안전한 쪽), `:root[data-face="pixel16"]` 에서 `pixelated`(도트). 초상 `<img>` 셋(`.face` · `.hero-face` · `.sprite.has-face`)이 그 토큰만 읽으므로 **스타일을 늘리면 손댈 곳은 토큰 한 줄**이다 ([SCREEN_DESIGN §5](../../docs/client/SCREEN_DESIGN.md))
  - ⚠ 배경 아트(`body::before` · `.arena.has-bg` · `.panel.town-bg` · `.zone.has-bg`)의 `pixelated` 는 **이 토큰과 무관**하다 — 그쪽은 스타일과 상관없이 늘 픽셀아트다. 걷어내지 말 것. **⚠ 예외 하나 — `.ex-map`(탐험 지도, 2026-09-04)**: 같은 `backgrounds/` 폴더에 있어도 계승이 아니라 **신규 카툰 아트**라 `pixelated` 를 안 건다(걸면 축소할 때 선이 끊긴다 — 손그림 초상과 같은 이유)
  - **예외 1건 — 관전 유닛 카드**(폭 고정은 카드를 감싼 칸 `.unit-slot` 이 든다 — 카드 밑에 창 뱃지 줄이 붙기 때문, 2026-08-27 사용자 지시): 몬스터·영웅·보스가 전부 같은 고정 크기다. 한 진영 최대 3장(`balance.csv:party_size_max` · `wave_monster_max`)이라 하한 폭에서 접히지 않는다. 긴 이름은 위칸에서 말줄임. **폭 200 → 226 · 초상 76 → 101** [2026-09-03] — 위칸을 초상 **위로** 올려 초상이 카드 높이를 통째로 쓴다(§5 영웅 띠와 같은 문법). 정사각이라 세로를 키우면 가로도 커지므로 오른쪽 수치 열 폭(104)을 지킬 만큼만 칸을 넓혔다. **카드 높이는 불변** ([SCREEN_DESIGN §4-2](../../docs/client/SCREEN_DESIGN.md))

---
*마지막 업데이트: 2026-09-11 (**관전 — 넓게 배치의 아레나가 판 전폭 · 남는 세로 · 가방이 「새 게임」 줄에 붙고 칸 52 · 그림 40** [사용자 지시 · ADR-0095] — 「관전 아레나」 줄을 고치고 관전 가방 줄 하나를 더했다. 실측 아레나 1337×502 · 가방 1371×219 · 가방 아랫변 788 = 「새 게임」 아랫변 · 캐릭터 탭 가방 그대로) · 2026-09-11 (**관전 아레나 — 넓게는 1184 가운데로 되돌리고 나눔이 왼쪽 아레나 + 로그 열** [사용자 지시 · ADR-0093 · ADR-0092 철회] — 나눔 989×424 · 열 334×424 · 출정 창 줄의 `.bw-box` 참조 복원) · 2026-09-11 (**관전 아레나 — 넓게 배치가 왼쪽 아레나 + 오른쪽 로그 열** [사용자 지시 · ADR-0092] — 989×424 · 열 334×424 · 로그 창(`.bw-box`)이 없어져 출정 창 줄의 그 참조를 걷었다) · 2026-09-11 (**관전 아레나 — 폭 1184 고정 · 판 가운데** [사용자 지시 · ADR-0091] — 넓게 배치가 비 2.33 : 1 그대로 한 장 세로에 들어간다(1184×508 · 스크롤 없음)) · 2026-09-10 (**메인은 한 번 더 줄인다 · 탭 내비 330** [사용자 지시 · ADR-0090] — 크기 정책에 줄 하나(`zoom: .9` · 설계 크기 1411×836 · 안 줄이는 것 · 배율을 정하는 식 · 글자 곱) · 선술집 · 탐험 · 관전 실측을 새 배율로) · 2026-09-10 (**`tip.js` 역할 — Alt 계산식 상태** [사용자 지시 · ADR-0089] — 파일별 역할 표의 `tip.js` 행에 한 문장. `window` 의 `keydown`/`keyup`(Alt 만 기본 동작을 막는다) · `blur` 로 상태를 들고 떠 있는 스킬 카드만 다시 그린다. 크기 정책 · 다국어 표 무변동) · 2026-09-10 (**「화면 폭 정책」 → 「화면 크기 정책」 — 화면은 1600×800 한 장** [사용자 지시 · ADR-0087 · ADR-0088] — 상한 1600 / 하한 1280 · 세로 예산 700 을 걷고 **한 장 + 배율**로 다시 썼다. 창 크기를 읽던 공식(`--fm-slot` · `--ix-art` · 스킬 창 폭 등)은 1600×800 에서의 값으로 고정 · 항목마다 1600×800 실측(출정 창 1520×548 · 리포트 상세 663 · 선술집 65 · 탐험 56px 스크롤 …) · 출정 창 2×2 · 판 여백이 `.modal-box` 에 덮여 안 먹던 것을 선택자 한 단(`.modal-box.depart-box`)으로 풀었다) · 2026-09-10 (**리포트 상세 높이 고정** [사용자 지시 · ADR-0086] — 「화면 폭 정책」에 리포트 줄 하나. 장비 격자 한 줄 44px · 기여 표 정원 줄 · 획득 줄 몫 · 목록 `contain: size` · 실측 662px) · 2026-09-10 (**도감 스킬 세그먼트 조밀 격자** [사용자 지시 · ADR-0075] — 「화면 폭 정책」에 값 두 줄. `.ix-body.dense` 하나로 여백을 조이고 칸 `minmax(112px,1fr)` · 그림 칸은 **창 높이를 따라 32 → 92px** 로 자란다(`--ix-art`), 스킬 타일의 파일명은 확장자를 뗀다(`artTile(…, stem)`). 캐릭터 · 아이템 세그먼트 무변동 · i18n 키 0개 · 단정 무변동) · 2026-09-09 (**이름은 영어가 원본 — 한글은 직역** [사용자 확정] — 다국어 절에 불릿 하나. 아이템·스킬 이름 한정이고 `STRINGS` 는 밖이다. 본문은 data/README 공통 규약) · 2026-09-08 (**세로 예산 예외 한 줄 — 선술집 명단** [사용자 지시] — 후보 카드가 시작 화면과 같은 크기가 되어 1280×700 에서 의뢰 줄이 접힌다. 관전 아레나 · 탐험 지도에 이은 세 번째 예외 항목 (SCREEN_DESIGN §8-1)) · 2026-09-04 (**탐험 지도 교체 — `.ex-map` 화면비 `2752/1536` → `2752/1211`** — 자산이 재작성되며 아래 워터마크 띠가 잘려 2.27:1 이 됐다. 가로로 길어진 덕에 같은 날 등재한 **세로 예산 예외가 해소**됐다(지도 ≈480px + 미착수 안내가 하한 1280×700 에 함께 선다) — 「화면 폭 정책」의 그 줄을 **해소 기록으로 바꿨다**. ⚠ 다음 지도도 가로로 긴 비여야 유지된다. CSS 한 줄 + 주석뿐 · JS 무변경. 사용자 지시) · 2026-09-04 (**탐험 탭 지도 — `.ex-map` 신설** — `mock.js:exploreMap`/`EXPLORE_MAP_CHAPTERS`(자산 재고 · 지금 챕터 1 하나 · `HERO_FACE_MAX` 와 같은 문법)를 `renderExplore` 가 읽어 `.ex-map` div 에 인라인 `background-image` 로 꽂는다(`.arena.has-bg` · `.zone.has-bg` 와 같은 문법). **호출하는 `SYS` 없음 · 새 i18n 키 0개 · 클릭 핸들러 없음.** ⚠ 정책 둘이 붙었다 — **세로 예산 예외**(전폭 + 원본 화면비라 미착수 안내가 접히는 자리로 내려간다 — 위 「화면 폭 정책」) · **`pixelated` 예외**(신규 카툰 아트라 보간을 켠다). 자산은 `src/assets/art/backgrounds/explore_chapter_1.webp`. 사용자 지시 · SCREEN_DESIGN §8-4) · 2026-09-04 (**전투 헤드 2줄 → 1줄 — `.bh-meta` 삭제 · `.battle-ctrl` 이 `.bh-top` 안으로** — 라운드 수치·경과 시계를 들던 `.bh-meta`(마크업 · CSS · `paintRound` 의 `.b-round`/`.b-kind` 갱신 · 틱 루프의 `.b-clock` 갱신 · i18n `bt.round`)를 전부 걷고, `.battle-ctrl` 을 `.bh-top` 의 마지막 칸(`margin-left:auto` · `flex-wrap: nowrap`)으로 옮겼다. `.battle-head` 는 세로 flex 지만 이제 자식이 하나다(`gap` 삭제). **한 줄에 맞추려고 넷을 좁혔다** — `bt.speed` ko `{n}배속` → `×{n}` · `bt.skip` `건너뛰고 리포트만/Skip to Report` → `건너뛰기/Skip` · `.round-track` 칸 18 → 16px(gap 3 → 2) · `.battle-ctrl .btn.sm` 패딩 `3px 8px` → `3px 6px`(관전 안에서만). **`.bh-nav .segmented { flex-wrap: nowrap }`** — 전역 `.segmented` 가 `wrap` 이라 좁아지면 세그먼트가 접혀 헤드가 도로 2줄이 됐다. `clock()` 헬퍼는 **로그 줄 타임스탬프**가 계속 쓴다. 사용자 지시 · SCREEN_DESIGN §4-2) · 2026-09-04 (**영웅 초상 풀 14 → 1** — `mock.js:HERO_FACE_MAX`. 옛 `hero_1..14.png` 를 `faces/cartoon/` 과 `faces/example/` 에서 전부 지우고 검투사 한 장(`source_sheet_gradiator.png` 오른쪽 위 타일 · 512² · **누끼 없음 = 배경 불투명**)을 `hero_1.png` 으로. `% 1` 이라 **이름 해시가 죽고 전 영웅이 같은 얼굴**이다 — 스타일 확인용 잠정. 사용자 지시 · SCREEN_DESIGN §5) · 2026-09-04 (**관전 카드 — 이름은 HP 바 바로 위 · 게이지 만충 표시 · 발광 삭제** — 전날의 `margin-bottom: auto`(초상 윗변)를 물려 이름·수치가 한 묶음으로 바닥 정렬 · HP 바 18 → 20(나눔 본문 84 → 86 동반) · `.act-row.full` 발광 삭제 · 행동 이벤트의 틱은 게이지를 **100%** 로 그리고 다음 틱에 전환 없이 스냅 리셋(`battle.js` `u.acted` 플래그 — apply 의 skill/hit/dodge 가 세우고 틱 루프가 눕힌다). 사용자 지시 · SCREEN_DESIGN §4-2) · 2026-09-03 (**관전 카드 — 이름 줄을 초상 윗변에** — `.unit-id` 에 `margin-bottom: auto` 한 줄. 열이 바닥 정렬(flex-end)이라 남는 세로가 이름 위에 쌓이던 것을 이름 아래로 몬다 — 이름은 초상 윗변 · 수치 묶음은 초상 아랫변 · 스킬 칸 크기 불변. 사용자 지시) · 2026-09-03 (**관전 카드 — 이름을 줄 오른쪽 끝으로** — 첫 줄을 `.unit-id`(flex) 로 감싸 왼쪽 `.unit-ident` / 오른쪽 `.unit-name`(`margin-left: auto` · 말줄임은 이름 쪽). 사용자 지시) · 2026-09-03 (**관전 카드 — 이름 줄 하나 · 정예 접두 이름 폐기** — `.unit-name` 안에 `.unit-ident` 가 인라인으로 붙어 `발렌 Lv.5 · 전사` 한 줄이 된다(줄 전체 말줄임 — 이름이 앞이라 먼저 남는다) · `battle.js:enemyName` 이 `eliteName` 을 안 부르고 `monsterName` 만 쓴다(⚠ **`data.js:eliteName` 참조 0** — 조립 규칙 `naming.js` 는 산다 · 정리는 `/client`) · `.battle-body.split .unit-body` 100 → **84**(두 줄 → 한 줄). 사용자 지시) · 2026-09-03 (**관전 카드 — 위칸 완전 폐기, 이름·신원이 오른쪽 열로** — `.unit-band` 마크업·규칙 삭제. `.unit-name`(fs-sm)·`.unit-ident`(fs-xs 무채색)가 `.unit-info` 의 첫 두 줄이 된다(왼쪽 정렬 · 각자 말줄임 · 일반 몬스터는 신원 줄 없음). `.battle-body.split .unit-body` 76 → **100**(두 줄 자리 — 카드 총높이는 위칸 시절 옛 값 112 와 같다). 사용자 지시) · 2026-09-03 (**관전 카드 — 띠 박스 폐지 · 등급 색 윗변 복귀 · `ready` 강조 삭제** — `.unit` 에 `border-top: 3px` 복귀 + 등급 색을 `.unit.enemy/.party/.tier-*` 의 **`border-top-color`** 로(정예·보스는 기존 전체 테두리 규칙이 덮는다 — 순서 유지). `.unit-band` 는 배경·구분선·음수 마진을 걷고 **맨 글자 한 줄**(`margin-bottom: 4px` 뿐 — 선이 둘이면 띠 박스와 다를 게 없다). `.cd-slot.ready` 규칙과 `battle.js` 의 토글 삭제 — 마스크가 다 걷힌 것 자체가 준비다. 사용자 지시) · 2026-09-03 (**관전 카드 — 띠 왼쪽 신원 · 게이지 증량/점등** — `battle.js:identOf` 신설(영웅 `Lv.n · 직업` — `clsName` 은 `D.classes` 조회 · 몬스터는 `kind.elite/boss/chapterBoss` 재사용, 일반은 빈 채) → `.unit-band` 에 `.unit-ident`(무채색 `--fs-xs` · 이름이 말줄임되고 이 조각은 안 줄어든다). HP 바 15→18 · `.act-row` 5→8. **행동 게이지 점등은 85% 문턱**(`.act-row.full` — 테두리 warning + 채움 brightness): 가득의 순간은 다음 이벤트가 바로 리셋해 틱 하나짜리라, 「곧 행동」을 읽히려면 막바지부터 켜야 한다. 문턱 0.85 는 연출값(재생기의 자유 · ui_conventions §9). 새 i18n 키 0. 사용자 지시) · 2026-09-03 (**영웅 띠의 위칸이 초상 밑에 깔려 있었다 — `.hs-band` 에 `z-index: 1`** — 이름과 「하는 일」이 화면에서 **통째로 안 보이던** 버그다(SCREEN_DESIGN §5 는 그 둘을 위칸에 두라고 적고 있었다). `.hs-band` 가 DOM 에서 초상보다 앞인데 `.hero-face` 도 위치 지정 요소라 **뒤에 오는 쪽이 이겼다**. 초상 여백을 걷기 전에는 그림 위쪽이 투명해 가려도 티가 안 났고, 여백을 걷으면서 영구히 가려질 뻔했다) · 2026-09-03 (**관전 카드가 꽉 찬다** — ①`.buff-row` 를 `.unit-slot` 안에서 **`position:absolute`** 로 빼(파티 `top:100%` · 적 `bottom:100%`) 창이 없을 때 26px 를 잡던 것을 없앴다 — 칸 높이 = 카드 높이가 되고, 창이 켜지고 꺼져도 카드가 안 움직인다(옛 「높이를 그대로 둔다」의 목적을 자리를 안 잡는 쪽으로 지킨다). `.unit-slot` 에 `position:relative` · `gap` 삭제 ②`.unit-info` 에 `align-self: stretch` + `justify-content: flex-end` — 가운데 정렬이라 스킬 칸 아랫변이 초상 아랫변보다 위에 떠 있던 것을 바닥에 붙였다 ③⚠ **CSS 가 아니라 자산을 고친 것 하나** — `hero_*.png` 5장의 투명 여백이 커서(가로 57~88%) `object-fit: contain` 이 여백까지 칸에 맞췄다. bbox 로 자르고 정사각 패딩(가로 가운데·세로 아래) 후 512 로 되돌려 가로 77~100%·세로 86~100% 로 맞췄다. 몬스터는 도감 원형 마스크 때문에 제외. 사용자 지시) · 2026-09-03 (**관전 이름 띠 재도입 · 영웅 죄종 색 폐지** — `battle.js` 가 `.unit-band > .unit-name` 을 **양 진영** 카드에 다시 싣고(같은 날 아침 폐기의 되돌림 — 든 것은 이름 하나뿐이라 옛 뒤엉킴은 안 돌아온다), 카드 윗변의 `border-top: 3px` 를 걷어 그 색을 **띠의 아랫변**이 든다. 색은 CSS 클래스가 든다(인라인 없음) — `.unit.party/.enemy .unit-band` 기본 위에 `.unit.tier-rare|tier-unique`(영웅 등급 · `battle.js` 가 `tier-${u.hero.tier}` 를 붙인다) → `.unit.elite` → `.unit.boss` 순으로 덮는다. **`app.js:tierColor(h)` 신설** — `sinColor(h.sin)` 이 앉아 있던 영웅 자리 다섯(후보 카드 윗변 · 영웅 띠 카드 윗변 · 능력치 막대 2곳 · 스킬 창 `accent`)을 전부 등급 색으로 바꾸고, `.ng-name` 의 죄종 칩은 인라인 색을 잃고 `.sin-chip` 기본값(`--text-secondary`)으로 떨어진다. ⚠ `sinColor` 는 **남는다** — 챕터 줄 · 정예 몬스터 칩 · 장비 접사 태그 · 도감은 다른 축이라 그대로다. `.cd-slot.empty` 테두리를 `--border-dark`(#18182a — 카드 배경 #1a1a2a 와 거의 같아 **안 보였다**) → `--border-primary` 로 올렸다: 몬스터는 칸 셋이 전부 비어 그 줄이 통째로 사라져 보였다. 사용자 지시) · 2026-09-03 (**스킬 아이콘 = 그림** — `mock.js:skillIcon`(+`SKILL_ICON_DIR`·`SKILL_ICON_FILES`) 신설. 이모지를 그리던 칸 여섯이 `<img>` 를 받는다 — `app.js:skillImg` · `battle.js:skillImg`(쿨 칸 · 창 칩) · `tip.js:skillImg`(툴팁). 세 파일이 서로를 import 하지 않아 한 줄짜리 헬퍼를 각자 들지만 **규칙은 `mock.skillIcon` 한 곳**이다. CSS 는 각 칸에 `img { object-fit: contain; image-rendering: var(--face-render) }` 를 더하고 `.act-slot .ico`(28)·`.sk-card .ico`(40)·`.tip-sk-ico`(22)에 크기를 줬다(이모지 때는 글씨 크기가 칸을 정했다). 자산이 4종뿐이라 제 그림이 없는 스킬은 id 해시로 넷 중 하나를 쓴다. 사용자 지시) · 2026-09-03 (**전투 헤드 3줄 → 2줄** — `.bh-left`/`.bh-right` 두 칸을 걷고 `.battle-head` 를 세로 flex 로: 첫 줄 `.bh-top`(`.bh-nav` · `.bh-title` · `.round-track` · `.bh-meta`는 `margin-left:auto` 로 오른쪽 끝), 둘째 줄 `.battle-ctrl`. `.round-track` 의 `margin-top: 6px` 삭제(이제 가로줄 안이다) · 헤드 패딩·여백도 한 단 줄였다. 사용자 지시) · 2026-09-03 (**원정 세그먼트가 패널 안으로 · 전투 헤드 두 칸 · 넓게 화면비 2.33** — `renderExpedition` 이 `.sub-bar` 를 안 만들고 `expNav()` 를 세 화면에 나눠 준다(`mountBattle` 의 `opts.nav` → 재생기가 `.bh-nav` 에 꽂는다 · `renderExpIdle(main, nav)` 는 `.panel-nav` 에 제목과 나란히 · `renderExpReport(main, nav)` 는 판정 줄 위). `.battle-head` 를 `.bh-left`(전환·이름·트랙) / `.bh-right`(컨트롤·`.bh-meta` 라운드·시계) 두 칸으로. `.arena` 의 `min-height: 420px` → `aspect-ratio: 2.33; min-height: 350px`, `.battle-body.split .arena` 는 `aspect-ratio: auto` 로 되돌린다. `.panel-nav` 신설. 새 i18n 키 0개. 사용자 지시) · 2026-09-03 (**관전 배치 둘 — 버튼 하나로 오간다** — `.battle-body` 를 되살려 기본은 넓게(전폭 + 로그 창), `.split` 이면 옛 2단 격자(아레나 + `.battle-side` 딜미터 열 300). `.b-layout` 버튼 · `paintLayout()` 이 **판 DOM 을 새로 만들지 않고 집만 옮긴다**(`.bw-box` ↔ `.battle-side` — 쌓인 로그·스크롤 보존) · `paintWin()` 은 `split || state.win` 으로 판을 켠다. `.split` 에서 `.unit-slot`/`.unit-body`/`.sprite`/`.pop` 을 **옛 값(200/76/44)으로 되돌린다** — 226 짜리 카드 3장이 좁은 아레나에서 두 줄로 접히기 때문(사용자 지시: 열은 줄이지 말 것). `.battle-side` 는 `max-height` 를 버리고 격자 stretch 로 아레나와 등고 · ⚠ `display:flex` 라 `.battle-side[hidden]` 규칙이 없으면 넓게 배치에서도 빈 열이 자리를 먹는다. 배치는 app.js `state.btLayout` 이 들고 `opts.layout`/`opts.onLayout` 으로 오간다(세이브 아님) · `bt.layout.toSplit`/`toWide` 2키 · `?dev=play&lay=split`. 사용자 지시) · 2026-09-03 (**관전 유닛 카드 — 위칸 폐기 · 몬스터 = 영웅** — `.unit-band`/`.unit-name`/`.tags`/`.elite-tag`/`.boss-tag`/`.trait-line` 규칙과 마크업을 전부 걷었다(카드에 남는 것은 초상 + 수치 열뿐). `.unit.elite` 테두리를 갈색 → `--color-warning`, 보스는 그대로 빨강 · `battle.js` 의 인라인 `borderTopColor = 죄종색`과 `.sprite.disc`(죄종 색 원판) 삭제. 적 유닛에 **`skills: []` + `buffs: new Map()`** 을 실어 쿨 칸(빈 칸)과 창 뱃지 줄이 몬스터에도 뜬다 — ⚠ `buffs` 가 없어 `buff` 이벤트가 `u.buffs?.set` 에서 조용히 흘려지던 버그가 여기서 닫혔다. `bt.traitsTitle` 키 삭제 · `?dev=play&bt=` 의 모르는 값은 `log` 로 접는다(빈 창 방지) · 도감 도움말의 「죄종 색 원판」 문구 정정. 사용자 지시) · 2026-09-03 (**액티브 칸 = 출처 자리** — `activeCells` 가 배정 순서가 아니라 **`ACTIVE_SOURCES`(고유·무기군·전직)** 로 칸을 배치하고, 무기군은 `SYS.game.weaponGroupOf(G, h)` 를 넘겨 받는다(로직 R35). 칸 이름이 번호에서 **출처**로 바뀌고 빈 칸은 사유를 찍는다(`sk.src.*`·`sk.emptyWeapon`·`sk.emptyAdvance` 4키 신설 · 「고유」는 기존 `sk.innate` 재사용). `.act-slot` 의 이름 열을 `16px` → `max-content` 로 — 영어 출처 이름(Innate/Weapon/Advance)이 아이콘 위로 접히던 것을 고쳤다) · *마지막 업데이트: 2026-09-03 (**관전 유닛 카드 — 초상이 카드 높이를 통째로 쓴다** — `.unit-band` 를 `position:absolute` 로 초상 위에 올리고(⚠ `top/left/right` 는 `.unit` 패딩 `5px 6px` 와 **같은 값**이어야 띠 윗변이 초상 윗변에 맞는다) `.unit-body` 76 → **101px** · `.unit .sprite` 76 → **101px** · `.unit-slot` 200 → **226px** · `.unit-info` 에 `padding-top: 22px` · `.pop` 의 `left` 44 → 57. 카드 전체 높이는 불변. 사용자 지시) · 2026-09-03 (**선술집 후보 카드 축소 + 도박장 줄** — `.tv-cands` 에 **크기 override 만** 다시 얹었다(카드 패딩 · 초상 `clamp(64px,6vw,96px)` · 이름 `--fs-md` · 고유 스킬 칸 34px · 능력치 줄간격·글씨 한 단 · `.ng-line` 압축). 09-01 에 「완전히 같은 카드」로 override 넷을 걷었던 것을 **크기 축만** 되돌린 것이라, 칸 순서·찍는 값은 `.ng-row` 와 그대로다. 세로를 짠 이유는 **명단 + 도구 줄 + 도박장 줄이 세로 예산 700 안에 같이 서야** 해서다(ko/en 둘 다 확인). `.tv-gamble` 신설 — 이름 + `todo-badge` 뿐이고 클릭 핸들러가 **없다**(`todo.lead` 도 안 붙인다 — 그 문구는 「기획은 확정됐고」라고 말하는데 도박장은 기획부터 미확정이다). 새 i18n 키 1개(`tv.gamble.h`). 사용자 지시) · 2026-09-03 (**글씨 크기 토큰 통일 + 고유 스킬 칸 확대** — 「글씨 크기」 절 신설. `--fs-2xs`(9px) 신설로 9·10px 리터럴 열 군데를 한 단으로 모으고(`.cs-a`·`.sk-n`·`.sk-r`·`.sk-v`·`.sk-meta .lv`·`.pd-label`·`.trait-line`·`.round-track .rt`·`.buff-chip`), **`.tier-chip` 을 `.sin-chip` 규격에 맞췄다**(9px/15 → `--fs-xs`/16 · `vertical-align` 제거 — 사용자 지적 「레어랑 분노 글씨 크기」). `.sk-n` 의 en 전용 크기 override 는 크기가 같아져 자간만 남았다. 후보 카드 고유 스킬 칸 30 → **44px**(나중에 들어올 스킬 이미지 자리 · `img` 규칙 미리 깔아 둠 · 아이콘 `--fs-xl`)이고, 그 크기를 감당하려고 **`.ng-role` 이 늘 두 줄 자리를 잡는다** — 영어에서만 접히던 줄이 카드 높이를 흔들어 세 장이 어긋나던 것을 함께 닫았다. 죽은 글리프 크기(`.ng-head .hero-face` 의 `clamp(50px,5vw,80px)`)도 삭제. 사용자 지시) · *마지막 업데이트: 2026-09-03 (**직업 글리프 폐지** — `mock.js:CLASS_GLYPH`·`classGlyph` 삭제. `heroFace` 가 초상 밑에 이모지를 깔지 않고(`app.js`), 관전 유닛 카드도 영웅이면 `underMark` 를 안 깐다(`battle.js` — 몬스터 이니셜은 유지). CSS 에서 글리프용 `font-size`(64px 칸 34px · `.hs-card` 의 `50cqw`)와 그것 때문에 있던 `container-type: inline-size` 를 걷었다. 이유는 **그림이 배경 투명 PNG + `object-fit: contain`** 이라 아트가 있어도 여백으로 이모지가 비쳤기 때문이다. 사용자 지시) · *마지막 업데이트: 2026-09-03 (**관전 아레나 전폭 + 로그 창** — `.battle-body` 2단 격자와 `.battle-side` 우측 열을 걷고 `.arena` 가 메인 칸 전폭 · `min-height: 420px`(옛 clamp 의 뷰포트 비례 삭제. ⚠ `aspect-ratio: 2/1` 을 먼저 넣었다가 걷었다 — 비는 정확했지만 폭 +46% 가 높이도 +46% 끌고 온다). 로그·누적 데미지는 컨트롤 줄의 `.b-win` 버튼 둘이 여는 창(`.battle-win`/`.bw-box`)이 든다 — 규격은 셸의 `.modal-layer`/`.modal-box` 를 그대로 쓰고 크기만 `.bw-box` 가 정한다. **레이어는 셸의 `#modal` 이 아니라 재생기 자기 DOM 안**이고, 열림 여부·판은 `resume.win`/`resume.tab` 으로 재렌더를 건넌다. `paintTab` → `paintWin` · Esc 리스너는 정리 함수가 뗀다. 새 i18n 키 0개. 세로 예산 예외 1건 등재. 사용자 지시) · 2026-09-03 (**후보 카드에 고유 스킬 칸** — `candidateCard` 가 `activeCells(h)` 에서 `source === 'innate'` 인 칸 하나를 골라 네모 아이콘 + 이름을 싣고(`.ng-skill`), 툴팁은 `skillTipCard` 를 재사용한다(⚠ 행동 주기는 안 넘긴다 — 후보는 무기가 없고 시작 화면엔 `G` 도 없다). `.ng-head` 를 `align-items: stretch` + `.ng-id` 세로 flex 로 바꿔 **이름은 초상 윗변 · 칸은 아랫변**에 붙인다 — 칸을 글 흐름에 두면 역할 줄이 2줄인 카드(영어 「Bow / Crossbow」)만 커져 세 장이 어긋난다. 칸 30px · 여백 6px 는 **오른쪽 열이 초상 높이를 안 넘게** 잡은 값이다. 선술집 명단도 같은 카드라 함께 바뀐다. 사용자 지시) · *마지막 업데이트: 2026-09-10 (**초상은 창이 아니라 카드를 기준으로** [사용자 지시 · ADR-0076] — 「화면 폭 정책」의 선술집 예외 줄에 **실측 한 줄**이 붙었다. 같은 `.ng-card` 인데 그릇이 달라 **카드 폭이 343 vs 378px** 이고(시작은 `.ng-wrap` 1060px 에서 잘린다), 초상만 `9.5vw`(창 기준)라 둘 다 137px 이었다 — 카드는 10% 넓은데 얼굴은 그대로. `clamp(96px, 44cqw, 152px)` + `.ng-card { container-type: inline-size }` 로 **비율이 두 화면 같아졌다**(44%). ⚠ 폭 35px 차이는 남았다 — 맞춘 것은 크기가 아니라 비율이다 (SCREEN_DESIGN §3 · §8-1)) · 2026-09-03 (**영웅 초상 풀 5 → 14** — `mock.js:HERO_FACE_MAX` (다인종 흉상 9종이 `faces/cartoon/hero_6..14.png` 로 들어왔다). ⚠ 해시 나머지가 달라져 **기존 영웅 전원의 얼굴이 재배정된다** — 이름·능력치는 그대로다. 사용자 지시) · 2026-09-03 (**아이템 아이콘 = 그림 (임시)** — `mock.js:itemArt`(+`ITEM_ART_GROUPS`·`ITEM_ART_BY_SLOT`) 신설 · `app.js:itemImg` 가 이모지를 그리던 칸 넷(착용·가방·제련소 목록/머리)에 `<img>` 를 꽂는다. `.pd-icon`·`.inv-icon` 에 34px 박스를 주고 `.fg-ic` 는 목록 22 / 머리 30. 그림이 없는 부위는 이모지로 폴백한다. 새 i18n 키 0개. 사용자 지시) · 2026-09-03 (**미장착 칸 = 부위 실루엣** — `mock.js:slotArt`(+`SLOT_ART_DIR`·`SLOT_ART_PARTS`) 신설. `paperdoll()` 의 빈 칸이 이모지 대신 `<img class="pd-art">` 를 들고, `.pd-cell.art` 는 라벨을 칸 아래로 내려 (`justify-content: flex-end` + 그림 `inset` 아래값) 실루엣과 안 겹치게 한다. 그림이 없는 투구·목걸이만 이모지로 남는다. 새 i18n 키 0개. 사용자 지시) · 2026-09-01 (**연구 섹션 목업** — `researchPanel()` 이 파티전술 **위에** 선다(세그먼트 아님) · 값은 `mock.js:RESEARCH`(노드 4 · ⚠ 전부 지어낸 값 · CSV 로 안 간다) · `rs.rs.*` 라벨 6키 · 칸 생김새는 `.rs-cell` 재사용(한 탭 두 섹션이 다른 카드 문법을 쓰면 같은 화면으로 안 읽힌다) · 누르면 `todo.lead` 플래시. 사용자 지시) · 2026-09-01 (**약어를 막대에 붙인다** — `.attr-n` 을 `display:flex; justify-content:space-between` 으로(칸을 꽉 쓰고 양끝으로 민다) · `.cs-a` 에 `flex:0 0 auto`. 사용자 지시) · 2026-09-01 (**§6 막대 정렬 · 등고 · 세로 압축** — `.attr-list` 가 격자를 들고 `.attr-row` 는 `display:contents`(줄마다 자기 격자면 막대 시작점이 흔들린다) · `.cols > .panel + .panel { margin-top:0 }`(세로 쌓기용 여백이 격자로 새어 첫 칸만 높이 서던 것) · `.panel`·h2·`.cs-row`·`.sub-h` 세로 압축 · `itemsPanel` 의 h2 삭제 → 수치는 `.items-meta` 로 도구 줄 오른쪽 끝. **`hero_attribute.csv:abbr` AGI → DEX** (⚠ `attr_en` 은 Agility 그대로라 영어에서 「Agility DEX」로 보인다 — 통일은 기획 결정). 사용자 지시) · 2026-09-01 (**`dispatch` → `town`** — 탭 id·i18n(`nav.town` 마을/Town)·렌더 함수(`renderTown`) 개명 · `POSTS` 순서 선술집·제련소·광산·채집·탐험·상단. ⚠ **`dp.*` 키와 `.dp-*` 클래스는 그대로** — 탭은 장소(마을)이고 그 안의 칸은 여전히 파견처다. 사용자 지시) · 2026-09-01 (**수색 칸** — `searchCell()` 이 `.tv-cands` 셋째 칸을 채운다(`.ng-card.tv-search` — 후보 카드와 같은 크기 · 버튼은 `margin-top:auto` 로 고용 버튼과 같은 자리). 동작 없음 — 누르면 `todo.lead` 플래시(새 문구 안 씀) · `tv.search.*` 3키. 사용자 지시) · 2026-09-01 (**초상 축소 보간을 스타일이 정한다** — 초상 `<img>` 셋 전부에 `image-rendering: pixelated` 가 무조건 걸려 있어, 08-31 기본 스타일이 `cartoon`(512px 손그림)으로 바뀐 뒤 축소된 얼굴이 깨져 보였다(26px 칸이면 20픽셀 중 1픽셀만 남는다). 토큰 `--face-render` + `mock.js:applyDocumentFace` 가 다는 `<html data-face>` 로 갈랐다(`app.js render()` 에서 언어와 나란히 호출). 배경 아트의 `pixelated` 는 건드리지 않았다. 사용자 지시) · 2026-09-01 (**후보 카드 한 벌 · 파견 탭 영웅 띠 삭제** — `.tv-cands` 의 축소 override 넷(카드 패딩 · 초상 40px · 이름 글씨 · 능력치 줄간격)을 걷어 선술집 명단이 `.ng-row` 와 **같은 카드**가 된다 · `candidateCard` 에서 **시작 특성 줄 삭제**(`ng.trait` 는 부르는 곳 0 이 됐고 키는 남긴다)와 **죄종 칩을 `.ng-name` 안 등급 칩 오른쪽으로**(`.ng-chips` 규칙 삭제 — 참조 0) · `renderDispatch` 가 `heroStrip` 을 안 부른다. 사용자 지시) · 2026-09-01 (**창 닫기 = 정사각 X · 마스터리 판 아래로 꽉** — `.modal-x`(28×28 · 글리프 `×` · 이름은 `title`/`aria-label` 이 `ui.close` 로 든다)를 **모든 창 공통 규격**으로 · `.cols.c-skill` 의 `align-items: start` 를 걷어 판 셋의 아랫변을 왼쪽 칸에 맞춘다(칸 높이 62px 고정은 유지 — 남는 자리는 판 안 여백). 사용자 지시) · 2026-09-01 (**탭 10 → 7** — `TABS` 가 `expedition·dispatch·commission·character·research·codex·help` · `POSTS` 6칸 신설(담당 능력치는 `hero_attribute.csv:dispatch` 를 `postAttr()` 이 거꾸로 읽는다 — 화면이 배정표를 갖지 않는다) · `renderSkill` → **`skillTreeBody()`**(창 본문, 영웅 띠 한 벌 삭제) · `renderTavern` → `renderDispatch` + `tavernPanel` · `renderTodo` → `todoPanel` 반환형 · **창 레이어**(`#modal` · `renderModal`/`openModal`/`closeModal` · 닫는 길 셋) · `?dev=tree`. ⚠ `.modal-layer` 는 `display:flex` 라 `[hidden]` 을 **이긴다** — `.modal-layer[hidden]{display:none}` 이 없으면 닫혀도 화면을 덮는다. 사용자 지시) · 2026-08-31 (**창 뱃지 줄이 카드 밖으로** — `.unit` 안의 마지막 줄이던 `.buff-row` 를 카드를 감싼 칸(`.unit-slot`)의 둘째 줄로 내렸다. 카드는 뱃지 줄 이전 크기로 롤백 · 폭 고정(200px)도 카드에서 칸으로 옮겼다 · 재생기는 `u.buffRow` 로 그 줄을 든다. 사용자 지시) · 2026-08-31 (**정리** — `ELEMENT_LABELS` 삭제(`ELEMENT_IDS` 가 유일한 소비자였고 그것이 `hero.js:ELEMENTS` 로 통합되며 참조가 0이 됐다) · `AFFIX_LABELS` ↔ `combat_stat.csv` 중복 12건을 ⚠로 명시 · `TOWN_BG` 는 자산이 실재해 남기되 미연결 표시) · 2026-08-31 (**M7 mock→CSV 이관** — `CLASSES`·`SLOTS`/`EQUIP_SLOTS`·`ITEM_BASES`·`AFFIX_DEFS`·`HERO_NAME_POOL`·`HERO_TRAIT_POOL` 이 CSV 6종으로 · `ELEMENT_IDS` 삭제(SSOT 는 `hero.js:ELEMENTS`) · `nm`/`eliteName` 조립 규칙이 `game_logic/naming.js` 로 · `app.js`·`tip.js` 는 `D.` 에서 읽는다. **화면 결과 불변**) · 2026-08-30 (**영웅 초상 연결** — `heroFace(hero)` 가 이름 해시로 `hero_<n>.png` 를 고른다(인자가 uid → 영웅 객체로 바뀌었다 · `battle.js` 호출부도) · 글리프를 밑에 깔고 `onerror` 폴백 · `.hero-face` 에 `position:relative`) · 2026-08-30 (**연구 탭 신설** — `renderResearch`/`tacticCell` · `rs.*` i18n(조건 문장 9종) · `mock.js` 에 `SKILL_TAGS`·`DAMAGE_KINDS` 표시 사전 · 칸 격자는 `minmax()` 라 CSV 행 수에 맞춰 접힌다) · 2026-08-28 (**스킬 탭 3차** — 단계를 세로로(`.sk-row`) · 화면·도움말에서 티어 번호 제거(잠금은 칸의 필요 레벨이 대신 말한다) · 칸 2/3(높이 고정 62px · `c-skill` 판 폭에 minmax 상한) · 판 머리 높이를 맞춰 첫 줄 정렬) · 2026-08-28 (**스킬 탭 마스터리 실동작** — 가지×깊이 트리 목업(`SKILL_TREES`·`SKILL_GRID`·`SKILL_POINTS`) 삭제 · 티어 열 판(`masteryBox`)이 `masteryState` 를 그린다 · 축 이름은 `stat` 에서 파생(`mock.statLabel`/`statValue`)) · 2026-08-28 (CSV 형태 최적화 — mock 의 이름·얼굴·능력치·도감 사전이 CSV 로 이관 · 표시 헬퍼는 `data.js` · 캐릭터 시트는 `combat_stat.csv:impl=1` 19행만 그린다) · 2026-08-28 (스킬 탭 배치 — 트리 판 셋 나란히 · 판 안 가지는 세로(`.sk-col`) · 스크롤 없이 1280×700 에 전부) · 2026-08-28 (`tip.js` 신설 — 툴팁 기계장치를 app.js 에서 분리해 관전 재생기와 공유 · 영웅 툴팁(기본 능력치 7) · 스킬 툴팁(표기/실효 쿨 · 설명)) · 2026-08-27 (원정 편성 재편 — 영웅 띠가 `formPanel` 안으로, **지역을 눌렀을 때만** 열리고 출발은 패널이 든다 · 관전 유닛 카드 위칸(`.unit-band`)은 왼쪽 태그 / 오른쪽 이름 · **카드 크기 고정**(몬스터=영웅=보스) · 초상 76px · 쿨은 **가로 3칸 아이콘**(32px · 덮개가 걷히며 쿨 표시 · 발동 시 칸이 튀고 스킬 이름 팝업 · 이름·% 는 툴팁으로) · 화면 폭 정책 예외 1건 등재) · 2026-08-27 (원정 편성이 파티·벤치 패널 대신 영웅 띠 — 클릭=파티 토글 · 관전/리포트에는 띠 없음) · 2026-08-27 (관전 배치 상하 대치 복귀) · 2026-08-27 (관전 — 로그/누적 데미지 탭 · 아레나 아래 가방 · 재렌더에도 재생 이어짐(`state.battle.resume`)) · 2026-08-27 (관전 유닛 카드 가로형 — 좌우 대치 · 스킬 쿨 3줄 목업(부채 #13) · HP 수치 바 가운데 · 영웅 띠 카드 — 초상이 카드 전체 · 위칸 `.hs-band` 에 상태 + 이름 · 영웅 초상 네모 박스 `.hero-face` · 글리프 출처 `mock.js classGlyph`) · 2026-08-26 (도움말 탭 신설 — 인게임 패널의 설명 문장 전면 이관) · 2026-08-26 (CLAUDE.md 에서 분리 · 전투 능력치 25행)*
