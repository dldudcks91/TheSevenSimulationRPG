# src/ui — DOM 렌더러

**Phase 2(엔진 이식)에서 버려질 레이어.** 계산·난수 없음 — 상태(G)를 읽고 `game_logic` 시스템(SYS)을 부르고 `save()` 한다.

## 파일별 역할

| 파일 | 역할 |
|---|---|
| `app.js` | 화면 렌더링. 계산·난수 없음 |
| `battle.js` | 전투 관전 = `game_logic/battle.js` 타임라인 **재생기** (계산하지 않는다) |
| `data.js` | CSV fetch → `game_logic` 시스템 조립(주입). 수치 미러 없음 |
| `storage.js` | localStorage 어댑터 — 저장소를 만지는 **유일한** 파일 |
| `tip.js` | 툴팁 — **기계장치 한 곳**(표시 · 따라다니기 · 넘침 보정 · 겹친 툴팁 복귀). 두 렌더러가 함께 쓰는 **영웅 카드 · 스킬 카드**도 여기. 아이템 비교 카드는 `app.js` (app 헬퍼를 많이 탄다) |
| `i18n.js` | 한/영 사전 `STRINGS` + 언어 상태 |
| `mock.js` | 화면 전용 사전 — 죄종·희귀도 색 · 직업·슬롯 · 아이템 베이스 · 접사 정의 · 마스터리 판 프레임(`MASTERY_GRID` — 티어 3 × 노드 3) · 전투 능력치 **카테고리 라벨**(`COMBAT_CATS`) · 자산 경로(`FACE_DIR`·`BG_DIR`·`stageBg`) · 관전 쿨 게이지 목업(`MOCK_ACTIVES`, DEV_PLAN 부채 #13). **이름 ko/en·얼굴 유무·기본 능력치 7·전투 능력치 25·도감 보정·상태는 2026-08-28 로 CSV 로 나갔다** — 헬퍼는 `data.js` (`monsterName`·`monsterFace`·`monsterSin`·`stageName`·`stageBgOf`·`chapterOf`·`eliteName`) |
| `style.css` | 스타일 |

- **공통 영웅 띠** (`app.js heroStrip`) — 캐릭터·스킬·선술집 탭 상단, 원정은 **편성 패널 안**(`formPanel` — 스테이지를 골라야 열린다, `{flat:true}` 로 패널 껍데기를 벗는다)에 같은 띠: 초상(`.hero-face` — 관전 유닛 카드와 같은 네모 박스 · 직업 글리프 `mock.js classGlyph`, [SCREEN_DESIGN §5](../../docs/client/SCREEN_DESIGN.md))이 카드 전체, 그 위 위칸(`.hs-band`)에 지금 하는 일(치료 중 / 전투 파티 / 대기) + 이름. 직업·레벨·죄종·등급은 툴팁

## 다국어 (한/영) — 필수

화면에 나가는 모든 문자열은 한 곳에 ko/en 이 **나란히** 있어야 한다.

| 종류 | 위치 | 사용 |
|---|---|---|
| UI 문구 | `i18n.js` 의 `STRINGS` — 키 하나에 `{ko, en}` | `t('key')` |
| 데이터 문자열 | CSV 의 `_kr`/`_en` 컬럼 쌍 → `{ko, en}` (로더·헬퍼가 조립) · 아직 CSV 로 안 간 것은 `mock.js` | `L(value)` |

- **렌더러(`app.js` / `battle.js` / `tip.js`)에 한국어 리터럴 금지** — 주석 제외. 이게 누락 검증 기준이다
- 이름 조립 규칙(어순·조사)은 렌더러가 아니라 데이터 층에 둔다 (`nm()`, `eliteName()`)
- **설명 문구는 도움말 탭 전용** — 인게임 패널에 안내 문장 금지 (2026-08-26 사용자 지시). 패널에는 숫자·상태·오류·버튼·툴팁만 남기고, 규칙 문구는 `app.js` 의 `helpSections()` 에 **기존 키를 재사용해** 넣는다 ([SCREEN_DESIGN §12](../../docs/client/SCREEN_DESIGN.md))

## 화면 폭 정책

- 상한 **1600px** / 하한 **1280px**, 세로 예산 **700px** (1366×768 노트북 기준)
- 해외 배율 125% 환경에서 CSS 뷰포트가 1536/1280 으로 잡힌다. 영어는 같은 내용이 1.3~1.5배 길다
- 라벨이 들어가는 칸은 **고정 px 금지** — `minmax()` / `clamp()` 로 최소만 보장한다
  - **예외 1건 — 관전 유닛 카드**(`.unit`, 2026-08-27 사용자 지시): 몬스터·영웅·보스가 전부 같은 고정 크기다. 한 진영 최대 3장(`balance.csv:party_size_max` · `wave_monster_max`)이라 하한 폭에서 접히지 않는다. 긴 이름은 위칸에서 말줄임 ([SCREEN_DESIGN §4-2](../../docs/client/SCREEN_DESIGN.md))

---
*마지막 업데이트: 2026-08-28 (**스킬 탭 3차** — 단계를 세로로(`.sk-row`) · 화면·도움말에서 티어 번호 제거(잠금은 칸의 필요 레벨이 대신 말한다) · 칸 2/3(높이 고정 62px · `c-skill` 판 폭에 minmax 상한) · 판 머리 높이를 맞춰 첫 줄 정렬) · 2026-08-28 (**스킬 탭 마스터리 실동작** — 가지×깊이 트리 목업(`SKILL_TREES`·`SKILL_GRID`·`SKILL_POINTS`) 삭제 · 티어 열 판(`masteryBox`)이 `masteryState` 를 그린다 · 축 이름은 `stat` 에서 파생(`mock.statLabel`/`statValue`)) · 2026-08-28 (CSV 형태 최적화 — mock 의 이름·얼굴·능력치·도감 사전이 CSV 로 이관 · 표시 헬퍼는 `data.js` · 캐릭터 시트는 `combat_stat.csv:impl=1` 19행만 그린다) · 2026-08-28 (스킬 탭 배치 — 트리 판 셋 나란히 · 판 안 가지는 세로(`.sk-col`) · 스크롤 없이 1280×700 에 전부) · 2026-08-28 (`tip.js` 신설 — 툴팁 기계장치를 app.js 에서 분리해 관전 재생기와 공유 · 영웅 툴팁(기본 능력치 7) · 스킬 툴팁(표기/실효 쿨 · 설명)) · 2026-08-27 (원정 편성 재편 — 영웅 띠가 `formPanel` 안으로, **지역을 눌렀을 때만** 열리고 출발은 패널이 든다 · 관전 유닛 카드 위칸(`.unit-band`)은 왼쪽 태그 / 오른쪽 이름 · **카드 크기 고정**(몬스터=영웅=보스) · 초상 76px · 쿨은 **가로 3칸 아이콘**(32px · 덮개가 걷히며 쿨 표시 · 발동 시 칸이 튀고 스킬 이름 팝업 · 이름·% 는 툴팁으로) · 화면 폭 정책 예외 1건 등재) · 2026-08-27 (원정 편성이 파티·벤치 패널 대신 영웅 띠 — 클릭=파티 토글 · 관전/리포트에는 띠 없음) · 2026-08-27 (관전 배치 상하 대치 복귀) · 2026-08-27 (관전 — 로그/누적 데미지 탭 · 아레나 아래 가방 · 재렌더에도 재생 이어짐(`state.battle.resume`)) · 2026-08-27 (관전 유닛 카드 가로형 — 좌우 대치 · 스킬 쿨 3줄 목업(부채 #13) · HP 수치 바 가운데 · 영웅 띠 카드 — 초상이 카드 전체 · 위칸 `.hs-band` 에 상태 + 이름 · 영웅 초상 네모 박스 `.hero-face` · 글리프 출처 `mock.js classGlyph`) · 2026-08-26 (도움말 탭 신설 — 인게임 패널의 설명 문장 전면 이관) · 2026-08-26 (CLAUDE.md 에서 분리 · 전투 능력치 25행)*
